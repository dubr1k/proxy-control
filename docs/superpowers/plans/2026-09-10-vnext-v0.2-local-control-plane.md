# Proxy Control vNext v0.2 — локальный control plane: план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Довести Proxy Control от v0.1.0 до v0.2.0 — локальный control plane с зарезервированной local Node identity, доменной моделью `Client`/`AccessGrant`, шифрованным хранилищем секретов, журналируемым созданием доступов и стабильным revocable subscription URL, не расширяя Fleet v1 и не трогая production-хосты.

**Architecture:** Сначала под существующую панель подводится один слой БД (миграции, транзакции, транзакционный аудит, AES-GCM secret versions), затем текущий `FleetStore` прячется за фасад lifecycle узлов и получает зарезервированный узел `local`. Поверх этого появляются `Client`/`AccessGrant` с read-only импортом существующих пользователей Telemt/Naive/Mieru, единый `ProtocolAdapter` c идемпотентными `operation_id` в managers, durable saga создания доступов и подписка как проекция текущих доступов с ETag от эффективного манифеста. Старые protocol endpoints переводятся на доменный сервис под feature flag. Fleet v2, routing и Xray-router — отдельные планы после отдельных gate.

**Tech Stack:** Python 3.12/3.13, FastAPI 0.116, Pydantic 2, SQLite WAL, httpx, `cryptography` (AES-256-GCM), vanilla ES-modules JS, Nginx (панельный vhost), Docker Compose project `mtproxy`, pytest + unittest, ShellCheck, Chromium DevTools (mobile gate), изолированный стенд `ams-test`.

**Spec:** `docs/superpowers/specs/2026-09-10-proxy-control-vnext-design.md` — копия исходного документа «Proxy Control vNext (v0.2–v0.5)»; этот план реализует его разделы 1–3, Phase 0–4 (Tasks 1–19A) и критерии v0.2 из раздела 5. Phase 5–8 (v0.3–v0.5) описаны в конце как roadmap с условиями входа и получают собственные планы.

## Global Constraints

- Целевая база: `main` @ `8c787c52d10fa7f07269bc5972eea63c77f5a392`, `VERSION` = `0.1.0`. Граф `graphify-out` пересобирается после каждой задачи, меняющей структуру кода. Последний прогон 2026-09-11 после Task 19A: **6476 узлов / 18316 рёбер / 244 сообщества** (после Task 6 было 5672/15866/231, до Tasks 4–6 — 5502/15490/233), циклов импорта нет. Аудит раздела 0 делался по более раннему прогону (4624/13519/204), spec ссылается на свой (4638/13543/194) — расхождение объясняется только моментом пересборки и попаданием в граф самих vNext-документов (253 узла), выводы не меняются. Текущие числа воспроизводятся `graphify update .` и заново фиксируются в Task 19A Step 7. Каталог `graphify-out/` держать чистым от Syncthing-конфликтов (`*.sync-conflict-*` удалены 2026-09-10): при конфликте «граф» перестаёт быть однозначным.
- **Все проверки выполняются на `ssh ams-test`** (root, Ubuntu 24.04, Docker 29, Python 3.12, Chromium, shellcheck, systemd-analyze). Локальный macOS не является авторитетным хостом: там всегда падают ~19 тестов (AF_UNIX, `nft`, panel entrypoint). Процедура — раздел 1.
- Production-хосты `ams-server`, `AMS_R`, `AMS_P`, `GER` не трогаются ни одной задачей. `ams-test` — одноразовый; разрушительные сценарии (`guest-runner.sh host`, `LAB_RESET=1`) допустимы только там и только явно.
- Fleet v1 остаётся Telemt-only и байт-совместимым: `OPERATIONS`, `TypedCommand`, sequence/outbox, mTLS, пути `GET /agent/v1/...` не меняются. Все существующие тесты `panel/tests/test_fleet.py`, `test_agent_transport.py` проходят без правок.
- Замороженные идентификаторы (`docs/COMPATIBILITY.md`) не переименовываются: Compose project `mtproxy`, том `mtproxy_panel-data`, `/opt/mtproxy-shared443`, `/var/lib/mtproxy-panel`, `/etc/mtproxy-agent`, unit-файлы, `urn:mtproxy-panel:node:`. Новые таблицы добавляются в тот же `panel.sqlite3`; существующие не переименовываются.
- Секреты никогда не попадают в `audit_log.detail_json`, логи, `repr`, сообщения исключений, Fleet payload/result, API-ответы кроме one-time reveal и самого subscription-ответа. Каждая задача с секретами содержит негативный canary-тест.
- Правило `AGENTS.md`: success-маркер только в успешной ветке `if`; «установлено/протестировано» — только с выводом команды. Каждое изменение кода начинается с падающего теста.
- Ни одна задача не даёт UI обещаний, которых адаптер не выполняет: Mieru credentials не пере-раскрываются без ротации, Telegram не опрашивает подписку, MTProxy не является routing target.
- Коммиты только по явной просьбе владельца; сообщения — на русском в стиле репозитория (`feat:`, `refactor:`, `test:`, `docs:`). Untracked `.hermes/` не коммитить.
- После задач, меняющих структуру кода: `graphify update .` (AST-only, без API).

---

## 0. Что показал аудит кода, графа и стенда (отклонения от spec)

Изучены: spec, `graphify-out` (обновлён), `panel/*`, `naive_manager/*`, `mieru_manager/*`, `installer/*`, `scripts/*`, `compose*.yaml`, `deploy/*`, `.github/workflows/*`, `docs/*`, состояние `ams-test`. Ниже — факты, которые меняют или уточняют задачи spec. Каждая задача ниже уже учитывает их.

1. **Путей `naive_manager/tests/*` и `mieru_manager/tests/*` не существует.** Тесты managers живут в `tests/test_naive_manager.py` (56 узлов графа, фикстура `manager`, `Hooks`) и `tests/test_mieru_manager.py` (фикстуры `manager`, `FakeMita`, `RecoveryMita`). Task 12 правит эти файлы.
2. **Панельный vhost рендерится в двух местах:** релизный установщик `installer/adapters/core.py::_panel_vhost_text` (`/etc/nginx/conf.d/proxy-control-panel.conf`, `listen 127.0.0.1:8443 ssl`) и legacy-путь `scripts/proxyctl.py::_panel_site_content` + `scripts/mtproxy-deploy`. Ни один не задаёт `access_log`, значит запрос `/s/<token>` попадёт в `/var/log/nginx/access.log`. Task 17 правит оба шаблона и оба набора тестов (`tests/test_installer_core.py`, `tests/test_deploy.py`).
3. **uvicorn пишет access log** в stdout контейнера (`panel/entrypoint.sh` запускает `uvicorn ... --proxy-headers` без `--no-access-log`), то есть токен подписки попал бы в `docker logs`. Task 17 отключает access log uvicorn и добавляет canary.
4. **Схему БД инициализируют два процесса:** `Store._init` и `FleetStore._init` (`executescript` + `CREATE TABLE IF NOT EXISTS`) вызываются и в контейнере `panel`, и в `fleet-ingress` (тот же том `panel-data`, `panel/agent_ingress.py`). Миграции (Task 4) обязаны быть межпроцессно безопасными (`BEGIN IMMEDIATE`, checksum, fail-closed при более новой схеме) и не тянуть FastAPI в `panel.agent_ingress` (образ `deploy/Dockerfile.ingress` копирует `panel/` целиком; `panel/fleet.py` сейчас без FastAPI-импортов — сохранить).
5. **One-time reveal живёт только в памяти процесса** (`app.state.reveals` в `web_context.py`), никаких таблиц секретов нет; `Store.audit` вырезает ключи `secret/password/token/link` из `detail`. Task 6 вводит durable `secret_versions`; Task 5 переносит скраббинг в `panel/audit.py` и делает его рекурсивным (как `_walk_secret_free` в `fleet.py`).
6. **Пере-раскрываемость credentials различается по протоколам:** MTProxy — `POST /api/users/{u}/access` читает `links.tls` из `telemt.list_users()`; Naive — `POST /api/naive/users/{u}/access` → `manager.reveal()` (пароль хранится открытым в state managers); **Mieru — нет endpoint `/access`**, mita хранит только `hashedPassword`, пароль виден один раз при `create`/`rotate` (`share_url`). Следствие для Task 11/12d/16 (решение 5 раздела 5): импортированные MTProxy/Naive доступы «захватываются» (capture) в secret store без ротации; Mieru принимается явным действием «Принять доступ» = ротация, в том числе массовая, а до принятия grant не выдаётся подпиской как рабочий.
7. **Managers генерируют пароли сами** (`naive_manager/service.py::create` — `secrets.token_urlsafe(18)`, `mieru_manager/service.py::create_user` — `token_urlsafe(24)`), `operation_id` нет. Naive хранит state в одном атомарном JSON рядом с Caddyfile (`_apply` с backup/transaction файлом); Mieru — журнал `journal.json`+`journal.key` (CAS). Обе точки годятся для записи результата операции по `operation_id` (Task 12).
8. **Telemt** (upstream) генерирует секрет сам, но `list_users` возвращает текущие ссылки, то есть после потери ответа текущий credential восстанавливается чтением, ротация не обязательна (Task 12c).
9. **Контейнер `panel` — `read_only`, все capability сброшены**, секреты приходят как Docker secrets из `./secrets/*` и стейджатся entrypoint'ом в `/run/panel/` (`stage_secret.py` для Mieru). Мастер-ключ (Task 6) идёт тем же путём: `secrets/panel-master-key` → `/run/panel/master-key`, `PANEL_MASTER_KEY_FILE`. Файл создают оба рендерера (`installer/adapters/core.py::_write_generation`, `_PRESERVED_CREDENTIALS`; `scripts/mtproxy-deploy`) автоматически и на установке, и на апгрейде (решение 4 раздела 5), поэтому ручного шага у владельца нет; `master-key-init` остаётся только для установок мимо установщика, а панель стартует без ключа, пока в `secret_versions` нет ни одной строки, и fail-closed отказывается стартовать, если строки есть, а ключ пропал.
10. **UI — ES-модули без бандлера:** `panel/static/app.js` → `js/main.js` (`TITLES`, `RENDERERS`, `bindPanel`, делегирование кликов по `handle*Click`), состояние в `js/state.js`, диалоги — `<dialog class="sheet">` в `index.html`, CSP `script-src 'self'` (inline JS запрещён). Контракт UI проверяется строковыми тестами в `tests/test_deploy.py` и Chromium-гейтом `panel/tests/test_mobile_layout.py` (390 px). Новые модули: `js/nodes.js`, `js/clients.js`, `js/subscriptions.js`.
11. **Fleet-таблицы:** `fleet_nodes(node_id PK, display_name, auth_state, inventory_json, …, last_seen_at)`, `fleet_commands`, `fleet_certificates`; `NODE_RE` допускает `local`; у `auth_state` нет CHECK. Зарезервированный узел `local` вставляется миграцией, а `poll_next`/`authenticate_certificate`/`enqueue` получают guard по `kind='local'` и флагу `disabled` (Task 7/9).
12. **WARP-coupling подтверждён:** `installer/adapters/naive.py:806` и `mieru.py:724`: `egress = 'proxy' if three_xui.warp and not three_xui.warp_domains else 'direct'`; константы `_WARP_EGRESS` = 40000, а парсер selection по умолчанию 45000 (`naive.py:1301`, `mieru.py:1273`). Это материал Task 28 (v0.4), не v0.2.
13. **Release workflow строит только теги `v[0-9]+.[0-9]+.[0-9]+`** и требует `VERSION == version`; `release/build.py::_VERSION_RE` допускает суффикс `-alpha.1`. Пред-релизные теги из spec (`v0.2.0-alpha.1`) сейчас не собираются CI. Решение — в Task 19A (расширить tag-паттерн или собирать alpha через `workflow_dispatch`).
14. **`ams-test` сейчас:** установщик в состоянии `uninstalled` (`/var/lib/proxy-control/installer/state.json`), контейнеров нет, тома `mtproxy_panel-data`, `mtproxy_telemt-config`, `mtproxy_naive-manager-run`, `mtproxy_mieru-manager-run` остались; `/opt/mtproxy-shared443` содержит только `.mtproxy-owned` и `secrets/`; в `/etc/letsencrypt/live` только лабораторные `*.lab.test` сертификаты, реальных для `*.sky.dubr1kkk.uk` нет (потребуется выпуск ACME — учитывать лимиты Let's Encrypt); DNS всех девяти доменов указывает на `89.124.96.226`; UFW inactive; в `/usr/local/x-ui` лежит 28-байтный stub `x-ui` + `/etc/x-ui/x-ui.db` (остаток фикстуры), из-за которого `scripts/lab/managed-xui-acceptance.sh` откажется запускаться, пока их не удалить; `/root/install.toml` (fresh/full/managed-new) и `/root/install.credentials` сохранены; образы `mtproxy-panel:latest`, `mtproxy-mieru-manager:latest`, `proxy-control-acceptance:ubuntu-24.04`, `proxy-control-caddy-naive:v2.11.4` уже есть.
16. **`MemoryMieru` хранит `users`, а не `config`** (`panel/mieru.py:103-119`: `self.users = {}`, `self.revision = "rev-1"`, `self.broken`). В тестах Task 2/11 сравнивать `mieru.users`; сниппеты с `mieru.config` из первой редакции плана исправлены (проверено 2026-09-10, Task 2).
17. **`POST /api/naive/users/{u}/access` возвращает сам payload раскрытия, а не `reveal_token`** (`panel/naive_routes.py:300-309` → `naive_reveal`): `{service, username, clients:{native, nekobox, karing, shadowrocket}}`, то есть Naive-пароль читается из state менеджера в любой момент (подтверждает capture без ротации, п. 6). Попутно: `naive_reveal` уже строит sing-box outbound `type: "naive"` и ссылку `naive+https://`, а `panel/reveals.py::karing_client` — Karing-обёртку; рендереры Task 16 переиспользуют именно эти builders, а не пишут свои.
15. **Baseline на `ams-test`:** результат прогона полного набора на `8c787c5` записан в разделе 1.3 — он и есть точка отсчёта «зелёного» состояния для всех задач.

---

**WAL под конкуренцией (найдено 2026-09-10 в ходе Task 12b).** `Database.__init__`
выполнял `PRAGMA journal_mode=WAL` без повтора. SQLite не применяет busy-handler к
смене journal-mode: pragma отказывает сразу, пока файл держит другое соединение, и
может отказать молча, вернув `delete`. Панель и fleet-ingress открывают один
`panel.sqlite3`, поэтому одновременный холодный старт ронял один из процессов с
`sqlite3.OperationalError: database is locked`. Стресс-прогон: 15 падений из 300 до
правки, 0 из 300 после. Исправлено в `Database._enable_wal` (повтор до `timeout` +
проверка возвращённого режима, `DatabaseError` при исчерпании), закрыто двумя
детерминированными тестами в `panel/tests/test_migrations.py`.

## 1. Стенд `ams-test`: как проводятся все проверки

### 1.1 Уровни проверок

| Уровень | Когда | Команда (с Mac) | Разрушает хост |
|---|---|---|---|
| A. Быстрый | после каждого шага TDD | `scripts/dev/remote-gate.sh quick panel/tests/test_migrations.py` | нет |
| B. Полный репозиторный | перед коммитом задачи | `scripts/dev/remote-gate.sh full` (ruff, весь pytest под root, unittest `tests/test_deploy.py`, shellcheck, `systemd-analyze verify`, doc links, `node --check`, `git diff --check`, сборка образов) | нет |
| C. Compose/образы | задачи, трогающие compose/Dockerfile/entrypoint | `scripts/dev/remote-gate.sh compose` (все `compose config -q` с синтетическими значениями, сборка panel/managers/agent/ingress, проверка uid ingress) | нет |
| D. Контейнерная лаборатория | задачи, трогающие installer/nginx/backup | `scripts/dev/remote-gate.sh lab-container` (`release/build.py` → `scripts/lab/docker_lab.py`) | нет |
| E. Живой стенд | release gate 19A, задачи 6/17 | `scripts/dev/remote-gate.sh lab-host` = `LAB_RESET=1 bash scripts/lab/guest-runner.sh host <sha256>` | **да**, переустанавливает хост целиком |
| F. Реальная установка | release gate 19A | установка релиза профилем `full` по `/root/install.toml`, проверка настоящими клиентами | да |

Уровень A–C запускается из `/root/dev/proxy-control` (rsync рабочего дерева, `.venv`); D–F — из собранного релизного архива, как в CI.

### 1.2 Правила

- Рабочее дерево синхронизируется `rsync --delete` в `/root/dev/proxy-control` (root-owned, `safe.directory` добавлен). Никаких правок на стенде руками — только через rsync из Mac.
- pytest на стенде запускается от root напрямую (`.venv/bin/python -m pytest -q`), как `sudo` в CI: root-only тесты (`nft`, права файлов, AF_UNIX) там реальные.
- Перед прогоном уровня B пересобираются `mtproxy-panel:latest` и `mtproxy-mieru-manager:latest` из текущего дерева (тесты прав внутри образов используют эти теги).
- Chromium-гейт (`test_mobile_layout.py`) на стенде идёт через `/usr/bin/google-chrome`: пакет `chromium-browser` там — snap-заглушка, удалён в Task 0 Step 3a. После пересоздания стенда (`LAB_RESET=1`) удалить его снова, иначе `_browser()` снова выберет заглушку.
- Уровень E/F: сначала `docker volume ls`, backup `/root/install.*`, затем запуск. Перед managed-new установкой удалить stub `/usr/local/x-ui`, `/etc/x-ui`.
- Каждый отчёт задачи содержит хвост вывода `remote-gate.sh` (без секретов) и exit-код.

### 1.3 Baseline (main @ 8c787c5, 2026-09-10, `/root/dev/proxy-control`, `.venv` Python 3.12.3)

Прогон: rsync дерева → `.venv` → сборка `mtproxy-panel:latest` и `mtproxy-mieru-manager:latest` → `ruff check .` → `pytest -q` от root.

| Проверка | Результат |
|---|---|
| `ruff check .` | `All checks passed!` |
| `pytest -q` (весь репозиторий) | **1277 passed, 2 failed, 2 skipped**, 3 мин 18 с |
| Падающие тесты | `panel/tests/test_mobile_layout.py::test_access_cards_and_navigation_do_not_collide_on_phone`, `::test_host_resource_card_stacks_and_stays_inside_the_phone_viewport` |
| Причина | `RuntimeError: Chromium exited before DevTools was ready: 1` — `_browser()` выбирает `/usr/bin/chromium-browser` (stub, завершается с кодом 1), хотя рядом установлен рабочий `/usr/bin/google-chrome`. Это дефект стенда, не кода; устраняется в Task 0 Step 3a. |

Дефект стенда устранён 2026-09-10 (Task 0 Step 3a, уже выполнен): `apt-get remove chromium-browser` → `panel/tests/test_mobile_layout.py`: `2 passed in 1.76s`.

**Измеренный baseline (прогон `remote-gate.sh full` 2026-09-10):**

| Проверка | Результат |
|---|---|
| `ruff check .` | `All checks passed!` |
| `pytest -q` (от root) | **1279 passed, 2 skipped** за 187.30 s |
| `unittest tests/test_deploy.py` | `Ran 28 tests … OK` |
| `scripts/check-doc-links.py` | `Markdown relative links: OK` |
| проверка синтаксиса UI | упала на `node --check` — см. Step 3b, исправлено |

Итог baseline: **1279 passed, 0 failed, 2 skipped**. Все последующие задачи сравниваются с этим: новых падений быть не должно.

---

## 2. Планируемая структура файлов (v0.2)

| Путь | Ответственность |
|---|---|
| `scripts/dev/remote-gate.sh` | Синхронизация дерева на `ams-test` и запуск уровней A–E. |
| `docs/adr/001…007-*.md`, `docs/VNEXT_ARCHITECTURE.md`, `docs/VNEXT_CAPABILITIES.md` | Зафиксированные решения, ownership-термины, матрица возможностей. |
| `tests/fixtures/vnext-capabilities.json` + `tests/test_vnext_capabilities.py` | Машиночитаемая матрица; тест, что все ячейки явные. |
| `panel/tests/test_vnext_characterization.py` | Характеризационные тесты текущих границ. |
| `panel/database.py` | Фабрика соединений (WAL, FK), `transaction()` с `BEGIN IMMEDIATE`. |
| `panel/migrations.py` | Версионированные миграции, `schema_migrations`, checksum, legacy-апгрейд. |
| `panel/audit.py` | `record(db, …)` — аудит внутри транзакции, рекурсивный скраббинг. |
| `panel/keyring.py` | Формат keyring-файла (stdlib only): load/generate/save/rotate. |
| `panel/secrets_store.py` | AES-256-GCM `secret_versions`, AAD-binding, rewrap, verify. |
| `panel/nodes/{models,registry,certificates,read_model,service}.py` | Lifecycle узлов поверх `FleetStore`, узел `local`. |
| `panel/node_routes.py`, `panel/static/js/nodes.js` | API и UI lifecycle узлов; raw fleet — в Advanced. |
| `panel/clients/{models,store,service,importer,provisioning}.py` | Client/AccessGrant, импорт, saga. |
| `panel/protocols/{base,telemt,naive,mieru}.py` | `ProtocolAdapter` и три адаптера. |
| `panel/client_routes.py`, `panel/static/js/clients.js` | API и UI клиентов/доступов. |
| `panel/subscriptions/{models,store,service}.py`, `panel/subscriptions/renderers/{base,manifest,raw,singbox,clash,html}.py` | Токены, поколения, эффективный манифест, ETag, рендереры. |
| `panel/subscription_routes.py`, `panel/static/js/subscriptions.js` | Публичный `/s/{token}` и окно подписки. |
| `panel/events.py` | События подписки → аудит/UI. |
| `naive_manager/service.py`, `naive_manager/server.py`, `mieru_manager/service.py`, `mieru_manager/server.py` | `operation_id` + caller-supplied credential, replay. |
| `panel/telemt_routes.py`, `panel/naive_routes.py`, `panel/mieru_routes.py` | Compatibility façade под флагом `PANEL_VNEXT_WRITER`. |
| `panel/entrypoint.sh`, `compose.yaml`, `installer/adapters/core.py`, `scripts/mtproxy-deploy`, `scripts/proxyctl.py` | Мастер-ключ, `/s/` location, access-log. |
| `docs/BACKUP_RESTORE.*.md`, `docs/UPGRADING.md`, `docs/OPERATIONS.*.md`, `PANEL.*.md` | Ключ, подписки, апгрейд с v0.1.0. |

---

## 3. Задачи

### Task 0: Стенд `ams-test` и baseline

**Files:**
- Create: `scripts/dev/remote-gate.sh`
- Create: `docs/superpowers/plans/2026-09-10-vnext-v0.2-local-control-plane.md` (раздел 1.3 заполнить)

**Interfaces:**
- Produces: `scripts/dev/remote-gate.sh {quick <pytest-args…>|full|compose|lab-container|lab-host}`; переменные `LAB_HOST` (default `ams-test`), `LAB_DIR` (default `/root/dev/proxy-control`).

- [x] **Step 1: Написать скрипт** (выполнено 2026-09-10: `scripts/dev/remote-gate.sh`, режим 0755)

```bash
#!/usr/bin/env bash
# Sync the working tree to the disposable lab host and run one gate level there.
# Never points at a production host: the default is the throwaway ams-test.
set -Eeuo pipefail

LEVEL=${1:-}
shift || true
HOST=${LAB_HOST:-ams-test}
REMOTE=${LAB_DIR:-/root/dev/proxy-control}
ROOT=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd)
SSH=(ssh -o BatchMode=yes -o ConnectTimeout=10 "$HOST")

case $HOST in
  ams-server|AMS_R|AMS_P|GER) echo "refusing production host $HOST" >&2; exit 2 ;;
esac

sync_tree() {
  "${SSH[@]}" "mkdir -p $REMOTE && git config --global --add safe.directory $REMOTE >/dev/null 2>&1 || true"
  rsync -a --delete --no-owner --no-group -e "ssh -o BatchMode=yes" \
    --exclude .venv --exclude .lab-state --exclude lab-results --exclude lab-results-container \
    --exclude dist --exclude dist-again --exclude __pycache__ --exclude .pytest_cache \
    --exclude .ruff_cache --exclude node_modules --exclude .DS_Store --exclude .hermes \
    --exclude secrets --exclude .env --exclude graphify-out/cache \
    "$ROOT/" "$HOST:$REMOTE/"
}

remote() {
  "${SSH[@]}" "cd $REMOTE && $1"
}

ensure_venv='test -x .venv/bin/python || python3 -m venv .venv; .venv/bin/pip install -q -r panel/requirements-dev.txt'
build_images='docker build -q -f panel/Dockerfile -t mtproxy-panel:latest panel >/dev/null && docker build -q -f mieru_manager/Dockerfile -t mtproxy-mieru-manager:latest . >/dev/null'

case $LEVEL in
  quick)
    sync_tree
    remote "$ensure_venv && .venv/bin/ruff check . && .venv/bin/python -m pytest -q -p no:cacheprovider $*"
    ;;
  full)
    sync_tree
    remote "$ensure_venv && $build_images && .venv/bin/ruff check . \
      && .venv/bin/python -m pytest -q -p no:cacheprovider \
      && .venv/bin/python -m unittest -q tests/test_deploy.py \
      && python3 scripts/check-doc-links.py \
      && bash scripts/dev/check-js-syntax.sh \
      && git ls-files -z '*.sh' | xargs -0 -r -n1 bash -n \
      && git ls-files -z '*.sh' | xargs -0 -r shellcheck \
      && shellcheck install-bootstrap \
      && for unit in deploy/*.service; do systemd-analyze verify \"\$unit\"; done \
      && git diff --check && echo REMOTE_GATE_FULL_OK"
    ;;
  compose)
    sync_tree
    remote "mkdir -p /tmp/cover /tmp/letsencrypt secrets \
      && printf 'ci=0123456789abcdef0123456789abcdef\n' > secrets/users.conf \
      && printf 'Bearer ci-token-0123456789abcdef0123456789abcdef\n' > secrets/telemt-api-token \
      && printf 'ci-naive-manager-token-0123456789abcdef0123456789abcdef\n' > secrets/naive-manager-token \
      && printf 'ci-mieru-manager-token-0123456789abcdef0123456789abcdef\n' > secrets/mieru-manager-token \
      && printf '{\"schema\":1,\"active_key_id\":\"k-ci000000\",\"keys\":[{\"key_id\":\"k-ci000000\",\"state\":\"active\",\"created_at\":1,\"key_material\":\"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=\"}]}\n' > secrets/panel-master-key \
      && export MTPROXY_DOMAIN=proxy.example.com MTPROXY_BACKEND_PORT=18445 MTPROXY_COVER_ROOT=/tmp/cover MTPROXY_LETSENCRYPT_ROOT=/tmp/letsencrypt MIERU_MANAGER_TOKEN_FILE=\$PWD/secrets/mieru-manager-token \
      && docker compose -f compose.yaml config -q \
      && NAIVE_PUBLIC_HOST=naive.example.com docker compose -f compose.yaml -f compose.naive.yaml config -q \
      && MIERU_PUBLIC_HOST=mieru.example.com MIERU_MITA_GID=321 MIERU_MITA_BIN=/bin/true MIERU_MITA_SHA256=4aa03abde846548692dc479359fd9d6c378c0b0e3ab22f94b2c22b1e54dcdb31 docker compose -f compose.yaml -f compose.mieru.yaml config -q \
      && FLEET_NODE_ID=node-ci FLEET_CENTRAL_URL=https://fleet.example.com:8790 FLEET_CLIENT_CERT=/tmp/client.crt FLEET_CLIENT_KEY=/tmp/client.key docker compose -f compose.yaml -f compose.agent.yaml config -q \
      && FLEET_SERVER_CERT=/tmp/server.crt FLEET_SERVER_KEY=/tmp/server.key FLEET_CLIENT_CA=/tmp/client-ca.crt docker compose -f compose.yaml -f compose.fleet-central.yaml config -q \
      && docker build -q -f deploy/Dockerfile.agent -t proxy-control-agent:test . >/dev/null \
      && docker build -q -f deploy/Dockerfile.ingress -t proxy-control-ingress:test . >/dev/null \
      && test \"\$(docker run --rm --entrypoint id proxy-control-ingress:test -u)\" = 10001 \
      && echo REMOTE_GATE_COMPOSE_OK"
    ;;
  lab-container)
    sync_tree
    remote "python3 release/build.py --source . --output dist --version \"\$(cat VERSION)\" --allow-dirty \
      && sha=\$(awk '/proxy-control-v.*\.tar\.gz\$/ {print \$1}' dist/SHA256SUMS) \
      && python3 scripts/lab/docker_lab.py --release-archive dist/proxy-control-v\$(cat VERSION).tar.gz --release-sha256 \$sha --output lab-results-container \
      && echo REMOTE_GATE_LAB_CONTAINER_OK"
    ;;
  lab-host)
    test "${LAB_RESET:-0}" = 1 || { echo "lab-host reinstalls $HOST entirely; rerun with LAB_RESET=1" >&2; exit 2; }
    sync_tree
    remote "python3 release/build.py --source . --output dist --version \"\$(cat VERSION)\" --allow-dirty \
      && sha=\$(awk '/proxy-control-v.*\.tar\.gz\$/ {print \$1}' dist/SHA256SUMS) \
      && LAB_RESET=1 bash scripts/lab/guest-runner.sh host \"\$sha\""
    ;;
  *)
    echo "usage: $0 {quick <pytest args…>|full|compose|lab-container|lab-host}" >&2
    exit 2
    ;;
esac
```

- [x] **Step 2: Проверить скрипт статически** (выполнено 2026-09-10)

Run: `bash -n scripts/dev/remote-gate.sh && shellcheck scripts/dev/remote-gate.sh`
Expected: без вывода, exit 0.
Факт: `bash -n` локально чисто; shellcheck на macOS не установлен, поэтому прогнан на стенде — `SHELLCHECK_REMOTE_GATE_OK` + `BASH_N_OK`. Важно: уровень `full` гоняет shellcheck только по `git ls-files '*.sh'`, то есть неотслеживаемый `remote-gate.sh` в него не попадает до первого коммита — до этого его надо проверять явно.

- [x] **Step 3: Прогнать полный уровень и записать baseline** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh full 2>&1 | tail -60`
Expected: `REMOTE_GATE_FULL_OK` либо список падающих тестов. Записать в раздел 1.3 этого плана: `N passed, M failed, K skipped`, имена падающих тестов и причину (до любых изменений).

- [x] **Step 3b: Дефект гейта — `node --check` не умеет ES-модули (выполнено 2026-09-10)**

Первый же прогон `full` остановился после зелёного pytest на шаге `node --check panel/static/app.js`:
`SyntaxError: Cannot use import statement outside a module` (Node v18.19.1). Причина — `panel/static/app.js` давно ES-модуль (`import { boot } from "/static/js/main.js"`), а `node --check` разбирает `.js` как CommonJS. Та же сломанная команда была прописана в `AGENTS.md` (обе языковые секции) — то есть обязательный репозиторный гейт был невыполним ни для кого.
Исправлено: добавлен `scripts/dev/check-js-syntax.sh` — копирует `panel/static/app.js` и все `panel/static/js/*.js` во временные `.mjs` и проверяет `node --check` по ним (теперь проверяются все 14 модулей UI, а не один entry point). `remote-gate.sh` и обе секции `AGENTS.md` теперь вызывают его.

- [x] **Step 3a: Дефект стенда — Chromium (выполнено 2026-09-10)**

`_browser()` в `panel/tests/test_mobile_layout.py` перебирает `chromium`, `chromium-browser`, `google-chrome`; на `ams-test` `/usr/bin/chromium-browser` был deb-заглушкой snap и завершался до старта DevTools. Исправление на стенде: `ssh ams-test 'DEBIAN_FRONTEND=noninteractive apt-get remove -y chromium-browser'` → тест выбирает `/usr/bin/google-chrome` → `2 passed`. Код не менялся. Если стенд пересоздаётся (`LAB_RESET=1`), повторить. Не использовать `pkill -f <строка>` по SSH — шаблон совпадает с командной строкой самой удалённой оболочки и обрывает сессию (exit 255); только `pkill -x chrome`.

- [ ] **Step 4: Коммит (по разрешению владельца)**

```bash
git add scripts/dev/remote-gate.sh docs/superpowers/plans/2026-09-10-vnext-v0.2-local-control-plane.md docs/superpowers/specs/2026-09-10-proxy-control-vnext-design.md
git commit -m "chore: добавить прогон проверок на стенде ams-test и план vNext v0.2"
```

---

### Task 1: ADR и архитектурная запись v0.2

**Files:**
- Create: `docs/adr/001-pull-only-node-transport.md`
- Create: `docs/adr/002-declarative-generations.md`
- Create: `docs/adr/003-one-writer-per-resource.md`
- Create: `docs/adr/004-client-access-grant-subscription.md`
- Create: `docs/adr/005-secret-references.md`
- Create: `docs/adr/006-routing-policy-ir.md`
- Create: `docs/adr/007-routing-enforcement-ownership.md`
- Create: `docs/VNEXT_ARCHITECTURE.md`
- Modify: `docs/README.md` (обе языковые секции)

**Interfaces:**
- Produces: термины `managed | adopted | foreign | drifted | tombstoned`, определения `Client`, `AccessGrant`, `ClientSubscription`, `Node(kind=local|remote)`, `SecretVersion`; на них ссылаются docstring'и Tasks 6–19.

- [x] **Step 1: Написать ADR по единому шаблону** (выполнено 2026-09-10: `docs/adr/001…007-*.md`, язык — английский, как у `ACCOUNTING.md`/`VALIDATION.md`)

Каждый файл: заголовок, `Status: accepted (v0.2)` / `proposed (v0.3+)`, `Context`, `Decision`, `Consequences`, `Non-goals`. Содержание — прямая запись executive decisions spec (раздел 1) и раздела 3:

- 001: pull-only outbound mTLS, exact cert→`node_id` binding, Fleet v1 = Telemt-only compatibility boundary; никакого generic RPC. Status accepted.
- 002: immutable `DesiredGeneration`/`ObservedGeneration`, «rollback = новое более высокое поколение», same generation + другой digest = security conflict. Status proposed (v0.3).
- 003: один писатель на ресурс; после adoption пишет только vNext domain service; legacy endpoints — façade, затем read-only. Определить `managed | adopted | foreign | drifted | tombstoned`. Status accepted.
- 004: `Client ≠ username`; `AccessGrant` = конкретный доступ протокола на узле; subscription = pull-проекция, generation + ETag от эффективного вывода; «auto-refresh зависит от клиента». Status accepted.
- 005: секреты только по ссылкам; `SecretVersion` AES-256-GCM с AAD-binding; threat boundary (защита от кражи БД/backup, не от компрометации процесса панели). Status accepted.
- 006: нейтральный routing IR (UUID, first-match, explicit backend, fail-closed). Status proposed (v0.4).
- 007: enforcement ownership — 3x-ui-owned Xray не общий router; выделенный `xray-router` только как изолированный backend. Status proposed (v0.5).

- [x] **Step 2: Написать `docs/VNEXT_ARCHITECTURE.md`** (выполнено 2026-09-10; ссылка на `VNEXT_CAPABILITIES.md` не ставится до Task 3 — `check-doc-links.py` проверяет относительные ссылки и упал бы на несуществующем файле)

Разделы: «Цель v0.2», «Целевая модель» (блок `Client/AccessGrant/ClientSubscription/Node/SecretVersion` из spec раздела 3 дословно), «Что не меняется в v0.2» (Fleet v1, пути, тома), «Границы секретов», «Совместимость клиентов подписки» (spec «Важное ограничение»), «Релизный поезд» (spec Task 41, с оговоркой про tag-паттерн CI), ссылки на ADR.

- [x] **Step 3: Добавить ссылки в индекс** (выполнено 2026-09-10: подразделы «Архитектура vNext» / «vNext architecture» в обеих секциях `docs/README.md`)

В `docs/README.md` в обеих секциях (`### Эксплуатация`/`### Operations` → новый подраздел «Архитектура vNext»/«vNext architecture»): ссылка с текстом «Архитектура vNext» на файл `VNEXT_ARCHITECTURE.md` и ссылки «ADR 001…007» на файлы `adr/001-pull-only-node-transport.md` … `adr/007-routing-enforcement-ownership.md` (относительные пути от `docs/`).

- [ ] **Step 4: Проверить ссылки**

Run: `scripts/dev/remote-gate.sh quick --co -q panel/tests/test_store.py && ssh ams-test 'cd /root/dev/proxy-control && python3 scripts/check-doc-links.py'`
Expected: `Markdown relative links: OK`.

- [ ] **Step 5: Коммит**

```bash
git add docs/adr docs/VNEXT_ARCHITECTURE.md docs/README.md
git commit -m "docs: зафиксировать архитектуру Proxy Control vNext"
```

---

### Task 2: Характеризационные тесты текущих границ

**Files:**
- Create: `panel/tests/test_vnext_characterization.py`

**Interfaces:**
- Consumes: фикстуры `client`, `login_user`, `telemt`, `naive`, `mieru` из `panel/tests/conftest.py`; `panel.fleet.OPERATIONS`, `validate_payload`, `validate_result`, `ProtocolError`.

- [x] **Step 1: Написать тесты (они должны пройти на текущем коде — это фиксация поведения)** (выполнено 2026-09-10 с двумя поправками по факту, см. ниже)

```python
from __future__ import annotations

import pytest

from panel.fleet import OPERATIONS, ProtocolError, validate_payload, validate_result

pytestmark = pytest.mark.anyio


async def _csrf(client, login_user):
    await login_user(client)
    return client.cookies["panel_csrf"]


def test_fleet_v1_allowlist_is_exactly_the_five_telemt_operations():
    assert OPERATIONS == {
        "telemt.inventory.refresh",
        "telemt.user.enable",
        "telemt.user.disable",
        "telemt.user.update_limits",
        "telemt.user.reset_quota",
    }


@pytest.mark.parametrize("payload", [
    {"username": "alice", "token": "x"},
    {"username": "alice", "proxy_url": "tg://proxy"},
    {"username": "alice", "nested": {"api_token": "x"}},
])
def test_fleet_v1_rejects_secret_bearing_payload(payload):
    with pytest.raises(ProtocolError):
        validate_payload("telemt.user.update_limits", payload)


def test_fleet_v1_rejects_secret_bearing_result():
    with pytest.raises(ProtocolError, match="secret-bearing"):
        validate_result({"message": "ok", "link": "tg://proxy?secret=00"}, "telemt.user.enable", "succeeded")


async def test_same_username_in_three_managers_is_three_independent_accounts(client, login_user, telemt, naive, mieru):
    csrf = await _csrf(client, login_user)
    headers = {"X-CSRF-Token": csrf}
    assert (await client.post("/api/users", json={"username": "alice"}, headers=headers)).status_code == 201
    assert (await client.post("/api/naive/users", json={"username": "alice"}, headers=headers)).status_code == 201
    created = await client.post(
        "/api/mieru/users",
        json={"username": "alice", "quotas": [], "expected_revision": mieru.revision},
        headers=headers,
    )
    assert created.status_code == 201
    assert (await client.post("/api/naive/users/alice/disable", headers=headers)).status_code == 200
    assert telemt.users["alice"]["enabled"] is True
    assert naive.users["alice"]["enabled"] is False
    assert any(row["name"] == "alice" for row in mieru.config["users"])


async def test_mtproxy_and_naive_access_are_re_revealable_but_mieru_is_not(client, login_user, mieru):
    csrf = await _csrf(client, login_user)
    headers = {"X-CSRF-Token": csrf}
    await client.post("/api/users", json={"username": "phone"}, headers=headers)
    await client.post("/api/naive/users", json={"username": "phone"}, headers=headers)
    await client.post(
        "/api/mieru/users",
        json={"username": "phone", "quotas": [], "expected_revision": mieru.revision},
        headers=headers,
    )
    mtproxy = await client.post("/api/users/phone/access", headers=headers)
    assert mtproxy.status_code == 200 and mtproxy.json()["link"].startswith("tg://proxy")
    naive_access = await client.post("/api/naive/users/phone/access", headers=headers)
    assert naive_access.status_code == 200 and "reveal_token" in naive_access.json()
    # Mieru keeps only hashedPassword: there is no re-reveal route at all.
    mieru_access = await client.post("/api/mieru/users/phone/access", headers=headers)
    assert mieru_access.status_code in {404, 405, 422}


async def test_one_time_reveal_is_consumed_once_and_lives_only_in_memory(client, login_user):
    csrf = await _csrf(client, login_user)
    created = await client.post("/api/naive/users", json={"username": "laptop"}, headers={"X-CSRF-Token": csrf})
    token = created.json()["reveal_token"]
    assert (await client.get(f"/api/reveal/{token}")).status_code == 200
    assert (await client.get(f"/api/reveal/{token}")).status_code == 410
    schema = " ".join(client._transport.app.state.store.dump_schema()).lower()
    assert "reveal" not in schema and "secret" not in schema
```

Перед написанием проверить точные имена атрибутов `MemoryMieru` (`revision`, `config`) и `MemoryNaive.users` в `panel/mieru.py:103-173`, `panel/naive.py:77-201`; если поле называется иначе — использовать реальное имя, а не менять Memory-классы.

- [x] **Step 2: Прогнать только новый файл** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_vnext_characterization.py -v`
Expected: все PASS. Если какой-то тест падает — это не повод править код: уточнить ожидание под фактическое поведение и записать факт в раздел 0 плана.
Факт: `ruff` чист, `8 passed in 2.04s`. Две ожидаемые величины уточнены по факту и записаны в раздел 0 как пункты 16–17: `mieru.users` вместо `mieru.config` и Naive-`/access`, отдающий payload раскрытия вместо `reveal_token`.

- [ ] **Step 3: Коммит**

```bash
git add panel/tests/test_vnext_characterization.py
git commit -m "test: зафиксировать текущие контракты fleet и клиентов"
```

---

### Task 3: Матрица возможностей и её фикстура

**Files:**
- Create: `docs/VNEXT_CAPABILITIES.md`
- Create: `tests/fixtures/vnext-capabilities.json`
- Create: `tests/test_vnext_capabilities.py`

**Interfaces:**
- Produces: JSON `{"schema": 1, "columns": [...], "rows": {"<protocol>": {"<capability>": {"status": "supported|unsupported|unproven|out_of_scope", "note": "…"}}}, "clients": {"<client>": {"<protocol>": {"status": …, "note": "…"}}}}`; секция `clients` — именованная матрица auto-refresh из решения 2 раздела 5 (`karing`, `mihomo`, `singbox`, `mieru_cli`, `telegram`, `shadowrocket`, `nekobox`, `v2rayn`, `hiddify`), каждый `note` называет первоисточник. На фикстуру ссылаются Task 11 (capture vs rotate), Task 12d (adopt) и Task 16 (renderers, `compatibility.MATRIX`).

- [x] **Step 1: Написать тест фикстуры** (выполнено 2026-09-10: `tests/test_vnext_capabilities.py`, два теста — матрица протоколов и именованная матрица клиентов)

```python
import json
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
FIXTURE = ROOT / "tests/fixtures/vnext-capabilities.json"
CAPABILITIES = (
    "create", "enable", "disable", "rotate", "delete", "quota", "expiry", "accounting",
    "stable_access_artifact", "credential_capture_without_rotation", "whole_service_socks_upstream",
    "cidr_routing", "domain_routing", "per_client_routing", "hot_reload", "rollback",
    "dns_ownership", "socks_hostname_propagation", "tcp_udp_upstream", "identity_propagation",
    "fail_closed", "binary_hot_upgrade", "exact_geodata_version",
)
STATUSES = {"supported", "unsupported", "unproven", "out_of_scope"}


def test_every_cell_is_explicit_and_required_conclusions_hold():
    data = json.loads(FIXTURE.read_text())
    assert data["schema"] == 1
    assert tuple(data["columns"]) == CAPABILITIES
    assert set(data["rows"]) == {"fleet_v1", "mtproxy", "naive", "mieru"}
    for protocol, cells in data["rows"].items():
        assert set(cells) == set(CAPABILITIES), protocol
        for name, cell in cells.items():
            assert cell["status"] in STATUSES, (protocol, name)
            assert cell["note"].strip(), (protocol, name)
    rows = data["rows"]
    assert rows["fleet_v1"]["create"]["status"] == "unsupported"
    assert rows["naive"]["whole_service_socks_upstream"]["status"] == "supported"
    assert rows["naive"]["per_client_routing"]["status"] == "unproven"
    assert rows["mieru"]["domain_routing"]["status"] == "unproven"
    assert rows["mieru"]["credential_capture_without_rotation"]["status"] == "unsupported"
    assert rows["mtproxy"]["credential_capture_without_rotation"]["status"] == "supported"
    for routing in ("cidr_routing", "domain_routing", "per_client_routing", "whole_service_socks_upstream"):
        assert rows["mtproxy"][routing]["status"] == "out_of_scope"


def test_named_client_matrix_is_explicit_and_matches_the_owner_decision():
    clients = json.loads(FIXTURE.read_text())["clients"]
    assert set(clients) == {
        "karing", "mihomo", "singbox", "mieru_cli", "telegram",
        "shadowrocket", "nekobox", "v2rayn", "hiddify",
    }
    for name, cells in clients.items():
        assert set(cells) == {"mtproxy", "naive", "mieru"}, name
        for protocol, cell in cells.items():
            assert cell["status"] in STATUSES and cell["note"].strip(), (name, protocol)
    assert clients["karing"]["naive"]["status"] == "supported"
    assert clients["karing"]["mieru"]["status"] == "supported"
    assert clients["singbox"]["naive"]["status"] == "supported"      # sing-box 1.13.0, outbound naive
    assert clients["singbox"]["mieru"]["status"] == "unsupported"     # нет outbound mieru
    assert clients["mihomo"]["mieru"]["status"] == "supported"        # type: mieru
    assert clients["mihomo"]["naive"]["status"] == "unsupported"      # MetaCubeX/mihomo#273
    assert clients["mieru_cli"]["mieru"]["status"] == "unsupported"   # клиент не умеет подписок
    assert all(cells["mtproxy"]["status"] == "unsupported" for cells in clients.values())
```

- [x] **Step 2: Запустить — должен упасть на отсутствующем файле** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick tests/test_vnext_capabilities.py`
Expected: FAIL `FileNotFoundError`.
Факт: фикстура была написана раньше первого прогона, поэтому падение доказано явно и воспроизводимо: на стенде фикстура временно убрана → `2 failed in 0.14s` с `pathlib.py:1015: FileNotFoundError`, после возврата → `2 passed in 0.03s`.

- [x] **Step 3: Заполнить фикстуру и документ** (выполнено 2026-09-10: 4×23 ячейки + 9×3 клиентских, `docs/VNEXT_CAPABILITIES.md` со спайк-планом и правилом «unsupported — явная ячейка»; ссылки добавлены в `docs/README.md` и `VNEXT_ARCHITECTURE.md`)

Найденные при заполнении факты, которые влияют на следующие задачи: `accounting` для Mieru — `unsupported` (`panel/mieru.py:58-68` требует ответа ровно `capability=unavailable`), `expiry` — `unsupported` у всех трёх протоколов (значит `valid_until` из Task 15 исполняется только отключением), `tcp_udp_upstream` — `supported` у Mieru и `unsupported` у Naive (HTTP/2 CONNECT по TCP).

Фикстура — все 4 строки × 23 столбца плюс секция `clients` (9 клиентов × 3 протокола; `note` — ссылка на первоисточник: `sing-box 1.13.0 outbound naive`, `MetaCubeX/mihomo#273`, релиз-ноты Karing про Mieru, `enfein/mieru docs/client-install.md`), каждая ячейка с `status` и `note` (источник note — код: например `naive.whole_service_socks_upstream: supported — installer/adapters/naive.py renders 'upstream socks5://127.0.0.1:<warp_port>'`). `docs/VNEXT_CAPABILITIES.md` — та же таблица в Markdown + spike-план (spec Task 30 кратко) + правило «unsupported — явная ячейка, не пробел». Добавить ссылку в `docs/README.md` рядом с `VNEXT_ARCHITECTURE.md`.

- [ ] **Step 4: Прогнать тест и проверку ссылок**

Run: `scripts/dev/remote-gate.sh quick tests/test_vnext_capabilities.py && ssh ams-test 'cd /root/dev/proxy-control && python3 scripts/check-doc-links.py'`
Expected: PASS, `Markdown relative links: OK`.

- [ ] **Step 5: Коммит**

```bash
git add docs/VNEXT_CAPABILITIES.md tests/fixtures/vnext-capabilities.json tests/test_vnext_capabilities.py docs/README.md
git commit -m "docs: добавить матрицу возможностей vNext"
```

---

### Task 4: Единый слой БД и миграции

**Files:**
- Create: `panel/database.py`
- Create: `panel/migrations.py`
- Create: `panel/tests/test_migrations.py`
- Modify: `panel/store.py:33-72`
- Modify: `panel/fleet.py:247-313`
- Modify: `panel/app.py:47-49`
- Modify: `panel/cli.py` (команды `db-migrate`, `db-status`)

**Interfaces:**
- Produces: `panel.database.Database(path)` с `connect()`, `transaction()` (context manager, `BEGIN IMMEDIATE`, commit/rollback, close); `panel.migrations.apply_migrations(database) -> list[int]`, `migration_status(database) -> list[dict]`, `MigrationError`, `MIGRATIONS: tuple[Migration, ...]`, `Migration(version, name, statements)` с `checksum`.
- Consumes: ничего нового.

- [x] **Step 1: Снять фикстуру старой схемы** (выполнено 2026-09-10: снимок со стенда вшит в `PRE_V02_SCHEMA_SQL`. Два отличия от черновика плана: в боевой схеме `login_attempts.reservation_id TEXT NOT NULL` без `DEFAULT ''`, а `fleet_nodes.last_seen_at` и четыре колонки `fleet_commands` пришли через `ALTER TABLE` и стоят в конце определения)

На стенде: `ssh ams-test 'cd /root/dev/proxy-control && .venv/bin/python - <<"PY"
from pathlib import Path
from panel.store import Store
from panel.fleet import FleetStore
p = Path("/tmp/pre-v0.2.sqlite3"); p.unlink(missing_ok=True)
Store(p).create_admin("owner", "correct horse battery staple", "owner")
FleetStore(p).register_node("edge-01", "edge", {"telemt_version": "3.4.25"})
print("\n".join(Store(p).dump_schema()))
PY'` — сохранить вывод `dump_schema` как константу `PRE_V02_SCHEMA` в тесте (это точный текст CREATE-выражений сегодняшнего кода).

- [x] **Step 2: Написать падающие тесты** (выполнено 2026-09-10: `panel/tests/test_migrations.py`, 7 тестов)

```python
from __future__ import annotations

import sqlite3
import threading
from pathlib import Path

import pytest

from panel.database import Database
from panel.fleet import FleetStore
from panel.migrations import MIGRATIONS, MigrationError, apply_migrations, migration_status
from panel.store import Store


def _legacy_database(path: Path) -> None:
    """Build a database exactly the way v0.1.0 code did: two executescript initialisers."""
    db = sqlite3.connect(path)
    db.executescript(PRE_V02_SCHEMA_SQL)  # snapshot from Task 4 Step 1, joined with ';\n'
    db.execute("INSERT INTO admins(username,password_hash,role,created_at) VALUES('owner','$argon2id$stub',
'owner',1)")
    db.execute("INSERT INTO audit_log(happened_at,actor_username,action,target,ip) VALUES(1,'owner','login','-','127.0.0.1')")
    db.execute("INSERT INTO fleet_nodes(node_id,display_name,auth_state,inventory_json,created_at,updated_at) VALUES('edge-01','edge','unenrolled','{}',1,1)")
    db.commit()
    db.close()


def test_migrates_pre_v02_database_and_keeps_rows_readable(tmp_path):
    path = tmp_path / "panel.sqlite3"
    _legacy_database(path)
    applied = apply_migrations(Database(path))
    assert applied == [migration.version for migration in MIGRATIONS]
    assert [row["username"] for row in Store(path).admins()] == ["owner"]
    assert Store(path).audits()[0]["action"] == "login"
    assert FleetStore(path).node("edge-01")["display_name"] == "edge"
    status = migration_status(Database(path))
    assert all(row["applied"] for row in status)


def test_fresh_database_gets_the_same_schema_as_a_migrated_one(tmp_path):
    fresh, legacy = tmp_path / "fresh.sqlite3", tmp_path / "legacy.sqlite3"
    _legacy_database(legacy)
    apply_migrations(Database(fresh))
    apply_migrations(Database(legacy))
    def columns(path):
        db = sqlite3.connect(path)
        return {
            table: [row[1] for row in db.execute(f"PRAGMA table_info({table})")]
            for (table,) in db.execute("SELECT name FROM sqlite_master WHERE type='table' ORDER BY name")
        }
    assert columns(fresh) == columns(legacy)


def test_interrupted_migration_rolls_back_and_records_nothing(tmp_path, monkeypatch):
    from panel import migrations
    broken = migrations.Migration(
        version=9999, name="broken", statements=("CREATE TABLE probe(x)", "CREATE TABLE probe(x)"),
    )
    monkeypatch.setattr(migrations, "MIGRATIONS", migrations.MIGRATIONS + (broken,))
    path = tmp_path / "panel.sqlite3"
    with pytest.raises(sqlite3.OperationalError):
        apply_migrations(Database(path))
    db = sqlite3.connect(path)
    assert db.execute("SELECT count(*) FROM sqlite_master WHERE name='probe'").fetchone()[0] == 0
    assert db.execute("SELECT count(*) FROM schema_migrations WHERE version=9999").fetchone()[0] == 0


def test_checksum_mismatch_fails_closed(tmp_path):
    path = tmp_path / "panel.sqlite3"
    apply_migrations(Database(path))
    db = sqlite3.connect(path)
    db.execute("UPDATE schema_migrations SET checksum='tampered' WHERE version=1")
    db.commit()
    with pytest.raises(MigrationError, match="checksum"):
        apply_migrations(Database(path))


def test_newer_database_than_code_fails_closed(tmp_path):
    path = tmp_path / "panel.sqlite3"
    apply_migrations(Database(path))
    db = sqlite3.connect(path)
    db.execute("INSERT INTO schema_migrations VALUES(9999,'future','x',1)")
    db.commit()
    with pytest.raises(MigrationError, match="newer"):
        apply_migrations(Database(path))


def test_two_processes_initialising_concurrently_are_safe(tmp_path):
    path = tmp_path / "panel.sqlite3"
    errors = []
    def run():
        try:
            apply_migrations(Database(path))
        except Exception as exc:  # noqa: BLE001 - the test records any failure
            errors.append(exc)
    threads = [threading.Thread(target=run) for _ in range(4)]
    [t.start() for t in threads]
    [t.join() for t in threads]
    assert errors == []
    assert len(migration_status(Database(path))) == len(MIGRATIONS)


def test_transaction_helper_commits_or_rolls_back_everything(tmp_path):
    database = Database(tmp_path / "panel.sqlite3")
    apply_migrations(database)
    with pytest.raises(RuntimeError):
        with database.transaction() as db:
            db.execute("INSERT INTO fleet_nodes(node_id,display_name,auth_state,inventory_json,created_at,updated_at) VALUES('a','a','unenrolled','{}',1,1)")
            raise RuntimeError("injected")
    with database.connect() as db:
        assert db.execute("SELECT count(*) FROM fleet_nodes").fetchone()[0] == 0
```

- [x] **Step 3: Запустить — падает на импорте** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_migrations.py`
Expected: FAIL `ModuleNotFoundError: panel.database`.
Факт: тесты и реализация написаны подряд до первого прогона, поэтому падение доказано явно: на стенде `panel/database.py` временно убран → `E ModuleNotFoundError: No module named 'panel.database'` (через `panel/store.py:13`), после возврата — зелёно.

- [x] **Step 4: Реализовать `panel/database.py`** (выполнено 2026-09-10, без отклонений от плана)

```python
"""One SQLite boundary for the panel: connections, pragmas and explicit transactions."""
from __future__ import annotations

import sqlite3
from contextlib import contextmanager
from pathlib import Path


class Database:
    def __init__(self, path: Path, *, timeout: float = 10.0):
        self.path = Path(path)
        self.timeout = timeout
        self.path.parent.mkdir(parents=True, exist_ok=True)
        with self.connect() as db:
            # WAL cannot be switched on inside a transaction, so it is set here once.
            db.execute("PRAGMA journal_mode=WAL")

    def connect(self) -> sqlite3.Connection:
        db = sqlite3.connect(self.path, timeout=self.timeout)
        db.row_factory = sqlite3.Row
        db.execute("PRAGMA foreign_keys=ON")
        return db

    @contextmanager
    def transaction(self):
        """BEGIN IMMEDIATE … COMMIT, or ROLLBACK on any exception; the connection is closed after."""
        db = self.connect()
        try:
            db.execute("BEGIN IMMEDIATE")
            yield db
            db.execute("COMMIT")
        except BaseException:
            if db.in_transaction:
                db.execute("ROLLBACK")
            raise
        finally:
            db.close()
```

- [x] **Step 5: Реализовать `panel/migrations.py`** (выполнено 2026-09-10; единственное отличие от черновика — `import hashlib` на уровне модуля и константа `_FLEET_COMMANDS_STATEMENT` вместо `BASELINE.statements[6]` в теле функции)

```python
"""Versioned, checksummed, cross-process-safe schema migrations for panel.sqlite3."""
from __future__ import annotations

import hashlib
import time
from dataclasses import dataclass

from .database import Database


class MigrationError(RuntimeError):
    pass


@dataclass(frozen=True)
class Migration:
    version: int
    name: str
    statements: tuple[str, ...]

    @property
    def checksum(self) -> str:
        canonical = "\n".join(" ".join(statement.split()) for statement in self.statements)
        return hashlib.sha256(f"{self.version}:{self.name}\n{canonical}".encode()).hexdigest()


# Baseline = the exact tables v0.1.0 created in Store._init and FleetStore._init, with the
# columns FleetStore added incrementally already present. Older databases are aligned by
# _legacy_upgrade() first, so the baseline is a no-op for them.
BASELINE = Migration(1, "baseline-v0.1.0", (
    """CREATE TABLE IF NOT EXISTS admins (
      id INTEGER PRIMARY KEY, username TEXT NOT NULL UNIQUE COLLATE NOCASE,
      password_hash TEXT NOT NULL, role TEXT NOT NULL CHECK(role IN ('owner','admin','viewer')),
      active INTEGER NOT NULL DEFAULT 1, created_at INTEGER NOT NULL)""",
    """CREATE TABLE IF NOT EXISTS sessions (
      token_hash TEXT PRIMARY KEY, admin_id INTEGER NOT NULL REFERENCES admins(id) ON DELETE CASCADE,
      csrf_hash TEXT NOT NULL, created_at INTEGER NOT NULL, expires_at INTEGER NOT NULL,
      last_seen_at INTEGER NOT NULL)""",
    """CREATE TABLE IF NOT EXISTS login_attempts (
      scope TEXT NOT NULL, happened_at INTEGER NOT NULL, reservation_id TEXT NOT NULL DEFAULT '')""",
    "CREATE INDEX IF NOT EXISTS login_attempts_scope_time ON login_attempts(scope,happened_at)",
    """CREATE TABLE IF NOT EXISTS audit_log (
      id INTEGER PRIMARY KEY, happened_at INTEGER NOT NULL, actor_id INTEGER,
      actor_username TEXT NOT NULL, action TEXT NOT NULL, target TEXT NOT NULL,
      detail_json TEXT NOT NULL DEFAULT '{}', ip TEXT NOT NULL)""",
    """CREATE TABLE IF NOT EXISTS fleet_nodes (
      node_id TEXT PRIMARY KEY, display_name TEXT NOT NULL, auth_state TEXT NOT NULL,
      inventory_json TEXT NOT NULL, created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL,
      next_sequence INTEGER NOT NULL DEFAULT 1, last_result_sequence INTEGER NOT NULL DEFAULT 0,
      last_seen_at INTEGER)""",
    """CREATE TABLE IF NOT EXISTS fleet_commands (
      command_id TEXT PRIMARY KEY, node_id TEXT NOT NULL REFERENCES fleet_nodes(node_id) ON DELETE RESTRICT,
      sequence INTEGER NOT NULL, idempotency_key TEXT NOT NULL, protocol_version INTEGER NOT NULL,
      operation TEXT NOT NULL, expected_revision TEXT NOT NULL, payload_json TEXT NOT NULL,
      status TEXT NOT NULL CHECK(status IN ('queued','dispatched','succeeded','failed','indeterminate')),
      result_json TEXT, created_at INTEGER NOT NULL, completed_at INTEGER,
      actor TEXT NOT NULL DEFAULT 'system', expires_at INTEGER NOT NULL DEFAULT 0,
      payload_sha256 TEXT NOT NULL DEFAULT '', dispatched_at INTEGER,
      UNIQUE(node_id,sequence), UNIQUE(node_id,idempotency_key))""",
    "CREATE INDEX IF NOT EXISTS fleet_commands_node_sequence ON fleet_commands(node_id,sequence)",
    """CREATE TABLE IF NOT EXISTS fleet_certificates (
      serial TEXT PRIMARY KEY, node_id TEXT NOT NULL REFERENCES fleet_nodes(node_id) ON DELETE RESTRICT,
      fingerprint_sha256 TEXT NOT NULL UNIQUE, not_before INTEGER NOT NULL, not_after INTEGER NOT NULL,
      state TEXT NOT NULL CHECK(state IN ('active','revoked')), issued_at INTEGER NOT NULL, revoked_at INTEGER)""",
))

MIGRATIONS: tuple[Migration, ...] = (BASELINE,)


def _columns(db, table: str) -> set[str]:
    return {row[1] for row in db.execute(f"PRAGMA table_info({table})")}


def _legacy_upgrade(db) -> None:
    """Exactly what Store._init / FleetStore._init did incrementally before the runner existed."""
    if _columns(db, "login_attempts") and "reservation_id" not in _columns(db, "login_attempts"):
        db.execute("ALTER TABLE login_attempts ADD COLUMN reservation_id TEXT NOT NULL DEFAULT ''")
    if not _columns(db, "fleet_commands"):
        return
    for column, definition in (
        ("last_seen_at", "INTEGER"),
    ):
        if column not in _columns(db, "fleet_nodes"):
            db.execute(f"ALTER TABLE fleet_nodes ADD COLUMN {column} {definition}")
    for column, definition in (
        ("actor", "TEXT NOT NULL DEFAULT 'system'"),
        ("expires_at", "INTEGER NOT NULL DEFAULT 0"),
        ("payload_sha256", "TEXT NOT NULL DEFAULT ''"),
        ("dispatched_at", "INTEGER"),
    ):
        if column not in _columns(db, "fleet_commands"):
            db.execute(f"ALTER TABLE fleet_commands ADD COLUMN {column} {definition}")
    import hashlib as _hashlib
    for row in db.execute("SELECT command_id,payload_json,created_at FROM fleet_commands WHERE payload_sha256='' OR expires_at=0").fetchall():
        db.execute("UPDATE fleet_commands SET payload_sha256=?,expires_at=? WHERE command_id=?",
                   (_hashlib.sha256(row["payload_json"].encode()).hexdigest(), row["created_at"] + 300, row["command_id"]))
    sql = db.execute("SELECT sql FROM sqlite_master WHERE type='table' AND name='fleet_commands'").fetchone()[0]
    if "'dispatched'" not in sql:
        # Verbatim FleetStore._migrate_command_status_constraint, now inside the runner's transaction.
        db.execute("DROP INDEX IF EXISTS fleet_commands_node_sequence")
        db.execute("ALTER TABLE fleet_commands RENAME TO fleet_commands_old")
        db.execute(BASELINE.statements[6])
        db.execute("""INSERT INTO fleet_commands SELECT command_id,node_id,sequence,idempotency_key,protocol_version,
            operation,expected_revision,payload_json,status,result_json,created_at,completed_at,actor,expires_at,
            payload_sha256,dispatched_at FROM fleet_commands_old""")
        db.execute("DROP TABLE fleet_commands_old")
        db.execute("CREATE INDEX fleet_commands_node_sequence ON fleet_commands(node_id,sequence)")


def apply_migrations(database: Database) -> list[int]:
    with database.connect() as db:
        db.execute("""CREATE TABLE IF NOT EXISTS schema_migrations (
            version INTEGER PRIMARY KEY, name TEXT NOT NULL, checksum TEXT NOT NULL, applied_at INTEGER NOT NULL)""")
    applied: list[int] = []
    with database.transaction() as db:
        newest = db.execute("SELECT max(version) FROM schema_migrations").fetchone()[0] or 0
        if newest > MIGRATIONS[-1].version:
            raise MigrationError(f"database schema {newest} is newer than this code ({MIGRATIONS[-1].version})")
        for migration in MIGRATIONS:
            row = db.execute("SELECT checksum FROM schema_migrations WHERE version=?", (migration.version,)).fetchone()
            if row is not None:
                if row["checksum"] != migration.checksum:
                    raise MigrationError(f"migration {migration.version} checksum mismatch")
                continue
            if migration is BASELINE:
                _legacy_upgrade(db)
            for statement in migration.statements:
                db.execute(statement)
            db.execute("INSERT INTO schema_migrations VALUES(?,?,?,?)",
                       (migration.version, migration.name, migration.checksum, int(time.time())))
            applied.append(migration.version)
    return applied


def migration_status(database: Database) -> list[dict]:
    with database.connect() as db:
        rows = {row["version"]: dict(row) for row in db.execute("SELECT * FROM schema_migrations")}
    return [{"version": m.version, "name": m.name, "applied": m.version in rows,
             "checksum_ok": rows.get(m.version, {}).get("checksum") == m.checksum} for m in MIGRATIONS]
```

- [x] **Step 6: Подключить runner в `Store`, `FleetStore`, `create_app`, CLI** (выполнено 2026-09-10: удалены `Store._init`, `FleetStore._init`, `_add_column`, `_migrate_command_status_constraint` и больше ненужный `import sqlite3` в обоих модулях; добавлены `db-migrate`/`db-status` в CLI и `app.state.database`)

В `panel/store.py`: `Store.__init__` → `self.database = Database(path)`; `connect()` делегирует `self.database.connect()`; `_init()` заменить на `apply_migrations(self.database)`. В `panel/fleet.py`: то же для `FleetStore` (удалить `_init`, `_add_column`, `_migrate_command_status_constraint` — их логика теперь в `_legacy_upgrade`). `panel/fleet.py` не должен импортировать FastAPI (проверка: `python -c "import panel.agent_ingress"` в образе ingress). В `panel/app.py`: `app.state.database = Database(settings.database_path)` перед `Store`/`FleetStore`. В `panel/cli.py`: `db-migrate` (печатает applied versions) и `db-status` (JSON `migration_status`).

- [x] **Step 7: Прогнать целевые и соседние тесты** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_migrations.py panel/tests/test_store.py panel/tests/test_fleet.py panel/tests/test_agent_transport.py panel/tests/test_security.py`
Expected: все PASS; `test_fleet.py` без правок.
Факт: `ruff` чист, `51 passed in 27.42s`; ни `test_fleet.py`, ни `test_agent_transport.py` не правились.

- [x] **Step 8: Полный уровень и compose** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh full && scripts/dev/remote-gate.sh compose`
Expected: `REMOTE_GATE_FULL_OK`, `REMOTE_GATE_COMPOSE_OK`.
Факт: оба маркера получены. Отдельно проверено ограничение ingress: `docker run --rm --entrypoint python proxy-control-ingress:test -c "import panel.agent_ingress…"` → `agent_ingress imports OK`, `fastapi loaded: False` — новый слой БД не затащил FastAPI в образ ingress.

- [ ] **Step 9: Коммит**

```bash
git add panel/database.py panel/migrations.py panel/tests/test_migrations.py panel/store.py panel/fleet.py panel/app.py panel/cli.py
git commit -m "refactor: ввести единый слой миграций панели"
```

---

### Task 5: Транзакционный структурированный аудит

**Files:**
- Create: `panel/audit.py`
- Create: `panel/tests/test_audit_transactions.py`
- Modify: `panel/migrations.py` (миграция 2)
- Modify: `panel/store.py:224-263`
- Modify: `panel/web_context.py:54-69,95-124`
- Modify: `panel/auth_routes.py:224-243` (фильтр по `request_id`, поле в ответе)

**Interfaces:**
- Produces: `panel.audit.record(db, *, actor: dict, action: str, target: str, ip: str, detail: dict | None = None, request_id: str | None = None, correlation_id: str | None = None, reason_code: str | None = None, generation: int | None = None, before_digest: str | None = None, after_digest: str | None = None) -> int`; `panel.audit.scrub(detail: dict) -> dict`; `panel.audit.digest(value) -> str` (sha256 канонического JSON).
- Consumes: `Database.transaction()`.

- [x] **Step 1: Падающие тесты** (выполнено 2026-09-10 с одной поправкой)

Дефект черновика: в `test_scrub_removes_secret_bearing_keys_recursively` секреты были однобуквенные (`"p"`, `"t"`), а проверка `assert "p" not in str(scrubbed)` ловит букву в уцелевшем ключе `keep` — тест падал не из-за кода (`1 failed, 30 passed`). Значения заменены на `CANARY-1..4`, добавлена проверка, что несекретный сосед `{"keep": 1}` выживает. Добавлен тест `test_scrubbed_detail_is_written_without_the_secret`: канарейка отсутствует и в `panel.sqlite3`, и в `-wal` (без чтения WAL проверка прошла бы вхолостую).

```python
from __future__ import annotations

import pytest

from panel import audit
from panel.database import Database
from panel.migrations import apply_migrations

pytestmark = pytest.mark.anyio
ACTOR = {"id": 1, "username": "owner"}


def _database(tmp_path):
    database = Database(tmp_path / "panel.sqlite3")
    apply_migrations(database)
    return database


def test_domain_write_and_audit_commit_or_roll_back_together(tmp_path):
    database = _database(tmp_path)
    with pytest.raises(RuntimeError):
        with database.transaction() as db:
            db.execute("INSERT INTO fleet_nodes(node_id,display_name,auth_state,inventory_json,created_at,updated_at) VALUES('n1','n','unenrolled','{}',1,1)")
            audit.record(db, actor=ACTOR, action="fleet.node.create", target="n1", ip="127.0.0.1")
            raise RuntimeError("injected after domain write and audit")
    with database.connect() as db:
        assert db.execute("SELECT count(*) FROM fleet_nodes").fetchone()[0] == 0
        assert db.execute("SELECT count(*) FROM audit_log").fetchone()[0] == 0
    with database.transaction() as db:
        db.execute("INSERT INTO fleet_nodes(node_id,display_name,auth_state,inventory_json,created_at,updated_at) VALUES('n1','n','unenrolled','{}',1,1)")
        audit.record(db, actor=ACTOR, action="fleet.node.create", target="n1", ip="127.0.0.1",
                     request_id="req-1", reason_code="operator", after_digest=audit.digest({"node_id": "n1"}))
    with database.connect() as db:
        assert db.execute("SELECT count(*) FROM fleet_nodes").fetchone()[0] == 1
        row = db.execute("SELECT * FROM audit_log").fetchone()
        assert (row["request_id"], row["reason_code"]) == ("req-1", "operator")
        assert len(row["after_digest"]) == 64


@pytest.mark.parametrize("detail", [
    {"password": "p", "quota_bytes": 5},
    {"nested": {"share_url": "mierus://u:p@h", "keep": 1}, "quota_bytes": 5},
    {"list": [{"token": "t"}], "quota_bytes": 5},
    {"proxy-url": "x", "quota_bytes": 5},
])
def test_scrub_removes_secret_bearing_keys_recursively(detail):
    scrubbed = audit.scrub(detail)
    assert scrubbed["quota_bytes"] == 5
    assert "p" not in str(scrubbed) and "mierus" not in str(scrubbed) and "t" != str(scrubbed)
    for forbidden in ("password", "share_url", "token", "proxy-url"):
        assert forbidden not in str(scrubbed)


async def test_every_mutation_response_and_audit_row_carry_the_same_request_id(client, login_user):
    await login_user(client)
    csrf = client.cookies["panel_csrf"]
    response = await client.post("/api/users", json={"username": "alice"}, headers={"X-CSRF-Token": csrf})
    request_id = response.headers["x-request-id"]
    assert len(request_id) == 16
    log = await client.get("/api/audit", params={"action": "user.create"})
    assert log.json()["items"][0]["request_id"] == request_id
```

- [ ] **Step 2: Запустить — падает**

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_audit_transactions.py`
Expected: FAIL `ModuleNotFoundError: panel.audit`.

- [x] **Step 3: Миграция 2 и `panel/audit.py`** (выполнено 2026-09-10: `AUDIT_V2`, `panel/audit.py`, `Store.audit`/`Store.audits` через `audit.record`, `X-Request-Id` в middleware, фильтр `request_id` в `GET /api/audit`)

В `panel/migrations.py` добавить:

```python
AUDIT_V2 = Migration(2, "audit-structured", (
    "ALTER TABLE audit_log ADD COLUMN request_id TEXT",
    "ALTER TABLE audit_log ADD COLUMN correlation_id TEXT",
    "ALTER TABLE audit_log ADD COLUMN reason_code TEXT",
    "ALTER TABLE audit_log ADD COLUMN generation INTEGER",
    "ALTER TABLE audit_log ADD COLUMN before_digest TEXT",
    "ALTER TABLE audit_log ADD COLUMN after_digest TEXT",
    "CREATE INDEX IF NOT EXISTS audit_log_request ON audit_log(request_id)",
))
MIGRATIONS = (BASELINE, AUDIT_V2)
```

`panel/audit.py`:

```python
"""Audit rows written inside the caller's transaction, never with secret values."""
from __future__ import annotations

import hashlib
import json
import time

FORBIDDEN_FRAGMENTS = ("secret", "password", "token", "credential", "link", "url", "authorization")


def _forbidden(key: str) -> bool:
    normalized = key.casefold().replace("-", "_")
    return any(fragment in normalized for fragment in FORBIDDEN_FRAGMENTS)


def scrub(value):
    if isinstance(value, dict):
        return {key: scrub(child) for key, child in value.items() if isinstance(key, str) and not _forbidden(key)}
    if isinstance(value, list):
        return [scrub(child) for child in value[:100]]
    if isinstance(value, str):
        return value[:512]
    return value if value is None or isinstance(value, (int, float, bool)) else str(type(value).__name__)


def digest(value) -> str:
    return hashlib.sha256(json.dumps(value, sort_keys=True, separators=(",", ":"), ensure_ascii=True).encode()).hexdigest()


def record(db, *, actor: dict, action: str, target: str, ip: str, detail: dict | None = None,
           request_id: str | None = None, correlation_id: str | None = None, reason_code: str | None = None,
           generation: int | None = None, before_digest: str | None = None, after_digest: str | None = None) -> int:
    cursor = db.execute(
        """INSERT INTO audit_log(happened_at,actor_id,actor_username,action,target,detail_json,ip,
           request_id,correlation_id,reason_code,generation,before_digest,after_digest)
           VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)""",
        (int(time.time()), actor.get("admin_id") or actor.get("id"), actor["username"], action, target,
         json.dumps(scrub(detail or {}), ensure_ascii=True), ip, request_id, correlation_id, reason_code,
         generation, before_digest, after_digest),
    )
    return cursor.lastrowid
```

`Store.audit(...)` становится: `with self.connect() as db: return audit.record(db, actor=actor, action=action, target=target, ip=ip, detail=detail, request_id=request_id)` (новый kwarg `request_id=None`). `Store.audits` — добавить фильтр `request_id`.

В `web_context.install_security_middleware`: до `call_next` — `request.state.request_id = secrets.token_hex(8)`; после — `response.headers["X-Request-Id"] = request.state.request_id`. В `RequestContext.audit` передавать `request_id=getattr(request.state, "request_id", None)`. В `auth_routes.audit_log` — query-параметр `request_id: str | None`.

- [x] **Step 4: Прогнать** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_audit_transactions.py panel/tests/test_rbac_audit.py panel/tests/test_security.py panel/tests/test_migrations.py`
Expected: PASS.
Факт: `ruff` чист, `31 passed in 8.23s` (первый прогон — `1 failed, 30 passed` из-за дефекта черновика в Step 1, исправлено в тесте, не в коде).

- [x] **Step 5: Полный уровень** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh full`
Expected: `REMOTE_GATE_FULL_OK`.
Факт: получен вместе с Task 6 (общий прогон, `1310+ passed`). Первый прогон после Task 6 дал два падения в характеризационных тестах — они запрещали слово `secret` в схеме БД, а Task 6 намеренно добавил `secret_versions`; ожидания уточнены в тестах, код не менялся.

- [ ] **Step 6: Коммит**

```bash
git add panel/audit.py panel/migrations.py panel/store.py panel/web_context.py panel/auth_routes.py panel/tests/test_audit_transactions.py
git commit -m "feat: связать аудит с бизнес-транзакциями"
```

---

### Task 6: Шифрованное хранилище версий секретов и мастер-ключ

**Files:**
- Create: `panel/keyring.py` (stdlib only)
- Create: `panel/secrets_store.py`
- Create: `panel/tests/test_secrets_store.py`
- Modify: `panel/migrations.py` (миграция 3)
- Modify: `panel/settings.py` (`master_key_file`)
- Modify: `panel/app.py` (загрузка keyring, fail-closed)
- Modify: `panel/cli.py` (`master-key-init`, `master-key-rotate`, `master-key-verify`)
- Modify: `panel/requirements.txt`, `panel/requirements-dev.txt` (pin `cryptography`)
- Modify: `panel/entrypoint.sh` (staging `/run/panel/master-key`)
- Modify: `compose.yaml` (secret `panel-master-key`)
- Modify: `installer/adapters/core.py:70-74,1728-1804` (`secrets/panel-master-key`)
- Modify: `scripts/mtproxy-deploy` (рендер ключа)
- Modify: `tests/test_deploy.py:352-392,463-475`, `tests/test_installer_core.py` (ключ в рендере, 0600, идемпотентность), `tests/test_panel_entrypoint.py` (контракт entrypoint)
- Modify: `docs/BACKUP_RESTORE.en.md`, `docs/BACKUP_RESTORE.ru.md`, `docs/UPGRADING.md`, `docs/UPGRADING.ru.md`, `PANEL.en.md`, `PANEL.ru.md`

**Interfaces:**
- Produces: `panel.keyring.Keyring` (`load(path)`, `generate()`, `save(path)`, `rotate()`, `active`, `get(key_id)`, `key_ids`), `KeyringError`; файл JSON `{"schema":1,"active_key_id":"k-<hex8>","keys":[{"key_id","state":"active|retiring","created_at","key_material":"<base64 32B>"}]}`, режим 0600.
- Produces: `panel.secrets_store.SecretStore(keyring | None)` с `enabled`, `store(db, *, secret_id, version, purpose, grant_id, permitted_node_id, plaintext: bytes, state="pending") -> SecretRef`, `reveal(db, ref, *, purpose, grant_id, permitted_node_id) -> bytes`, `transition(db, ref, state)`, `rewrap(db, batch_size=200) -> int`, `verify_all(db) -> dict[str, int]`, `SecretRef(secret_id, version)`, `SecretError`. Состояния `pending|active|retiring|revoked`.
- Consumes: `Database`, `apply_migrations`.

- [x] **Step 1: Зафиксировать версию `cryptography`** (выполнено 2026-09-10: `cryptography==50.0.1` — последняя стабильная по `pip index versions`; `cffi` уже был в списке)

Run: `ssh ams-test 'cd /root/dev/proxy-control && .venv/bin/pip index versions cryptography | head -1'` — взять последнюю стабильную версию и записать её в `panel/requirements.txt` (`cryptography==X.Y.Z` плюс её зависимость `cffi` уже есть). Проверить установку в образе: `docker build -f panel/Dockerfile -t mtproxy-panel:latest panel`.

- [x] **Step 2: Падающие тесты** (выполнено 2026-09-10: `panel/tests/test_secrets_store.py`, 9 тестов; `import sqlite3` не понадобился, `monkeypatch` в тесте ротации тоже — прерывание делается исключением внутри транзакции)

```python
from __future__ import annotations

import base64
import json
import sqlite3
import stat

import pytest

from panel.database import Database
from panel.keyring import Keyring, KeyringError
from panel.migrations import apply_migrations
from panel.secrets_store import SecretError, SecretRef, SecretStore


@pytest.fixture
def database(tmp_path):
    database = Database(tmp_path / "panel.sqlite3")
    apply_migrations(database)
    return database


@pytest.fixture
def keyring_path(tmp_path):
    path = tmp_path / "master-key"
    Keyring.generate().save(path)
    return path


def test_keyring_file_is_private_json_with_one_active_key(keyring_path):
    assert stat.S_IMODE(keyring_path.stat().st_mode) == 0o600
    data = json.loads(keyring_path.read_text())
    assert data["schema"] == 1 and len(data["keys"]) == 1
    assert len(base64.b64decode(data["keys"][0]["key_material"])) == 32
    keyring = Keyring.load(keyring_path)
    assert keyring.active.key_id == data["active_key_id"]


def test_keyring_refuses_world_readable_or_malformed_files(keyring_path):
    keyring_path.chmod(0o644)
    with pytest.raises(KeyringError, match="mode"):
        Keyring.load(keyring_path)
    keyring_path.chmod(0o600)
    keyring_path.write_text('{"schema": 1, "active_key_id": "missing", "keys": []}')
    with pytest.raises(KeyringError):
        Keyring.load(keyring_path)


def test_store_and_reveal_round_trip_binds_identity(database, keyring_path):
    secrets_store = SecretStore(Keyring.load(keyring_path))
    with database.transaction() as db:
        ref = secrets_store.store(db, secret_id="s1", version=1, purpose="grant.credential",
                                  grant_id="g1", permitted_node_id="local", plaintext=b"hunter2-hunter2")
    assert ref == SecretRef("s1", 1)
    with database.connect() as db:
        assert secrets_store.reveal(db, ref, purpose="grant.credential", grant_id="g1", permitted_node_id="local") == b"hunter2-hunter2"
        with pytest.raises(SecretError):
            secrets_store.reveal(db, ref, purpose="grant.credential", grant_id="g2", permitted_node_id="local")
        with pytest.raises(SecretError):
            secrets_store.reveal(db, ref, purpose="grant.credential", grant_id="g1", permitted_node_id="edge-01")


def test_copied_ciphertext_under_another_row_fails(database, keyring_path):
    secrets_store = SecretStore(Keyring.load(keyring_path))
    with database.transaction() as db:
        secrets_store.store(db, secret_id="s1", version=1, purpose="p", grant_id="g1", permitted_node_id="local", plaintext=b"a")
        secrets_store.store(db, secret_id="s2", version=1, purpose="p", grant_id="g2", permitted_node_id="local", plaintext=b"b")
        row = db.execute("SELECT nonce,ciphertext FROM secret_versions WHERE secret_id='s1'").fetchone()
        db.execute("UPDATE secret_versions SET nonce=?,ciphertext=? WHERE secret_id='s2'", (row["nonce"], row["ciphertext"]))
    with database.connect() as db:
        with pytest.raises(SecretError):
            secrets_store.reveal(db, SecretRef("s2", 1), purpose="p", grant_id="g2", permitted_node_id="local")


def test_database_without_key_holds_no_recognisable_secret(database, keyring_path):
    secrets_store = SecretStore(Keyring.load(keyring_path))
    canary = b"CANARY-9f3c1c0a-do-not-leak"
    with database.transaction() as db:
        secrets_store.store(db, secret_id="s1", version=1, purpose="p", grant_id="g1", permitted_node_id="local", plaintext=canary)
    raw = database.path.read_bytes() + (database.path.with_name(database.path.name + "-wal").read_bytes() if database.path.with_name(database.path.name + "-wal").exists() else b"")
    assert canary not in raw and base64.b64encode(canary) not in raw


def test_unknown_or_retired_key_ids_fail_closed(database, keyring_path):
    keyring = Keyring.load(keyring_path)
    secrets_store = SecretStore(keyring)
    with database.transaction() as db:
        secrets_store.store(db, secret_id="s1", version=1, purpose="p", grant_id="g1", permitted_node_id="local", plaintext=b"x")
    fresh = SecretStore(Keyring.generate())  # a keyring that never had this key
    with database.connect() as db:
        with pytest.raises(SecretError, match="key"):
            fresh.reveal(db, SecretRef("s1", 1), purpose="p", grant_id="g1", permitted_node_id="local")


def test_rotation_is_overlap_first_and_interrupted_rewrap_stays_decryptable(database, keyring_path, monkeypatch):
    old = Keyring.load(keyring_path)
    old_store = SecretStore(old)
    with database.transaction() as db:
        for index in range(5):
            old_store.store(db, secret_id=f"s{index}", version=1, purpose="p", grant_id=f"g{index}", permitted_node_id="local", plaintext=b"v")
    rotated = old.rotate()
    assert {key.state for key in rotated.keys} == {"active", "retiring"}
    new_store = SecretStore(rotated)
    # Interrupt after the first batch of two rows.
    with pytest.raises(RuntimeError):
        with database.transaction() as db:
            new_store.rewrap(db, batch_size=2)
            raise RuntimeError("crash between batches")
    with database.connect() as db:
        counts = new_store.verify_all(db)
    assert sum(counts.values()) == 5  # every row decrypts under the overlap keyring
    with database.transaction() as db:
        while new_store.rewrap(db, batch_size=2):
            pass
    with database.connect() as db:
        assert new_store.verify_all(db) == {rotated.active.key_id: 5}


def test_repr_and_errors_never_contain_plaintext(database, keyring_path):
    secrets_store = SecretStore(Keyring.load(keyring_path))
    with database.transaction() as db:
        ref = secrets_store.store(db, secret_id="s1", version=1, purpose="p", grant_id="g1", permitted_node_id="local", plaintext=b"PLAINTEXT-CANARY")
    assert "PLAINTEXT" not in repr(ref) and "PLAINTEXT" not in repr(secrets_store)
    with database.connect() as db:
        try:
            secrets_store.reveal(db, ref, purpose="wrong", grant_id="g1", permitted_node_id="local")
        except SecretError as exc:
            assert "PLAINTEXT" not in str(exc)


def test_missing_key_with_encrypted_rows_fails_closed_at_app_start(database, keyring_path, tmp_path):
    from panel.app import Settings, create_app
    secrets_store = SecretStore(Keyring.load(keyring_path))
    with database.transaction() as db:
        secrets_store.store(db, secret_id="s1", version=1, purpose="p", grant_id="g1", permitted_node_id="local", plaintext=b"x")
    with pytest.raises(ValueError, match="PANEL_MASTER_KEY_FILE"):
        create_app(Settings(database_path=database.path, master_key_file=None, allowed_hosts=("testserver",)))
```

- [ ] **Step 3: Запустить — падает на импорте**

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_secrets_store.py`
Expected: FAIL `ModuleNotFoundError: panel.keyring`.

- [x] **Step 4: Миграция 3** (выполнено 2026-09-10)

```python
SECRETS_V3 = Migration(3, "secret-versions", (
    """CREATE TABLE IF NOT EXISTS secret_versions (
      secret_id TEXT NOT NULL, version INTEGER NOT NULL, purpose TEXT NOT NULL,
      grant_id TEXT, permitted_node_id TEXT, key_id TEXT NOT NULL,
      nonce BLOB NOT NULL, ciphertext BLOB NOT NULL,
      state TEXT NOT NULL CHECK(state IN ('pending','active','retiring','revoked')),
      created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL,
      PRIMARY KEY(secret_id, version))""",
    "CREATE INDEX IF NOT EXISTS secret_versions_key ON secret_versions(key_id)",
    "CREATE INDEX IF NOT EXISTS secret_versions_grant ON secret_versions(grant_id, state)",
))
MIGRATIONS = (BASELINE, AUDIT_V2, SECRETS_V3)
```

- [x] **Step 5: `panel/keyring.py`** (выполнено 2026-09-10, без отклонений)

```python
"""Versioned master keyring file: stdlib only so installers can render it without cryptography."""
from __future__ import annotations

import base64
import json
import os
import secrets
import stat
import time
from dataclasses import dataclass
from pathlib import Path

SCHEMA = 1
STATES = {"active", "retiring"}


class KeyringError(RuntimeError):
    pass


@dataclass(frozen=True)
class Key:
    key_id: str
    state: str
    created_at: int
    material: bytes

    def __repr__(self) -> str:
        return f"Key(key_id={self.key_id!r}, state={self.state!r})"


def new_key(state: str = "active") -> Key:
    return Key(f"k-{secrets.token_hex(4)}", state, int(time.time()), secrets.token_bytes(32))


@dataclass(frozen=True)
class Keyring:
    keys: tuple[Key, ...]
    active_key_id: str

    def __repr__(self) -> str:
        return f"Keyring(active={self.active_key_id!r}, keys={len(self.keys)})"

    @property
    def active(self) -> Key:
        return self.get(self.active_key_id)

    @property
    def key_ids(self) -> set[str]:
        return {key.key_id for key in self.keys}

    def get(self, key_id: str) -> Key:
        for key in self.keys:
            if key.key_id == key_id:
                return key
        raise KeyringError("unknown key id")

    @classmethod
    def generate(cls) -> "Keyring":
        key = new_key()
        return cls((key,), key.key_id)

    def rotate(self) -> "Keyring":
        """Overlap-first: the new key becomes active, every previous key stays readable as retiring."""
        key = new_key()
        retiring = tuple(Key(k.key_id, "retiring", k.created_at, k.material) for k in self.keys)
        return Keyring((key, *retiring), key.key_id)

    def retire_all_but_active(self) -> "Keyring":
        return Keyring((self.active,), self.active_key_id)

    def to_json(self) -> str:
        return json.dumps({
            "schema": SCHEMA,
            "active_key_id": self.active_key_id,
            "keys": [{"key_id": k.key_id, "state": k.state, "created_at": k.created_at,
                      "key_material": base64.b64encode(k.material).decode()} for k in self.keys],
        }, indent=2) + "\n"

    def save(self, path: Path) -> None:
        path = Path(path)
        temporary = path.with_name(path.name + ".tmp")
        fd = os.open(temporary, os.O_WRONLY | os.O_CREAT | os.O_TRUNC | os.O_NOFOLLOW, 0o600)
        with os.fdopen(fd, "w") as handle:
            handle.write(self.to_json())
            handle.flush()
            os.fsync(handle.fileno())
        os.replace(temporary, path)
        os.chmod(path, 0o600)

    @classmethod
    def load(cls, path: Path) -> "Keyring":
        path = Path(path)
        try:
            info = path.lstat()
        except FileNotFoundError as exc:
            raise KeyringError("master key file does not exist") from exc
        if not stat.S_ISREG(info.st_mode) or stat.S_IMODE(info.st_mode) & 0o077:
            raise KeyringError("master key file must be a regular file with mode 0600 or 0400")
        try:
            data = json.loads(path.read_text())
            if data["schema"] != SCHEMA:
                raise KeyringError("unsupported keyring schema")
            keys = tuple(
                Key(str(item["key_id"]), str(item["state"]), int(item["created_at"]), base64.b64decode(item["key_material"], validate=True))
                for item in data["keys"]
            )
            active_key_id = str(data["active_key_id"])
        except (KeyError, TypeError, ValueError, json.JSONDecodeError) as exc:
            raise KeyringError("master key file is malformed") from exc
        if not keys or any(len(k.material) != 32 or k.state not in STATES for k in keys):
            raise KeyringError("master key file contains an invalid key")
        if len({k.key_id for k in keys}) != len(keys):
            raise KeyringError("duplicate key id")
        keyring = cls(keys, active_key_id)
        if keyring.active.state != "active":
            raise KeyringError("active key is not in state active")
        return keyring
```

- [x] **Step 6: `panel/secrets_store.py`** (выполнено 2026-09-10, без отклонений)

```python
"""AES-256-GCM secret versions bound to their identity through AAD; plaintext lives one call."""
from __future__ import annotations

import json
import os
import time
from dataclasses import dataclass

from cryptography.exceptions import InvalidTag
from cryptography.hazmat.primitives.ciphers.aead import AESGCM

from .keyring import Keyring, KeyringError

STATES = ("pending", "active", "retiring", "revoked")


class SecretError(RuntimeError):
    """Messages are fixed strings: never interpolate plaintext or ciphertext."""


@dataclass(frozen=True)
class SecretRef:
    secret_id: str
    version: int


def _aad(secret_id: str, version: int, purpose: str, grant_id: str | None, permitted_node_id: str | None) -> bytes:
    return json.dumps({"secret_id": secret_id, "version": version, "purpose": purpose,
                       "grant_id": grant_id, "permitted_node_id": permitted_node_id},
                      sort_keys=True, separators=(",", ":")).encode()


class SecretStore:
    def __init__(self, keyring: Keyring | None):
        self._keyring = keyring

    def __repr__(self) -> str:
        return f"SecretStore(enabled={self.enabled})"

    @property
    def enabled(self) -> bool:
        return self._keyring is not None

    def _require(self) -> Keyring:
        if self._keyring is None:
            raise SecretError("secret store is disabled: PANEL_MASTER_KEY_FILE is not configured")
        return self._keyring

    def store(self, db, *, secret_id: str, version: int, purpose: str, grant_id: str | None,
              permitted_node_id: str | None, plaintext: bytes, state: str = "pending") -> SecretRef:
        if state not in STATES:
            raise SecretError("invalid secret state")
        key = self._require().active
        nonce = os.urandom(12)
        ciphertext = AESGCM(key.material).encrypt(nonce, plaintext, _aad(secret_id, version, purpose, grant_id, permitted_node_id))
        now = int(time.time())
        db.execute("""INSERT INTO secret_versions(secret_id,version,purpose,grant_id,permitted_node_id,key_id,nonce,ciphertext,state,created_at,updated_at)
                      VALUES(?,?,?,?,?,?,?,?,?,?,?)""",
                   (secret_id, version, purpose, grant_id, permitted_node_id, key.key_id, nonce, ciphertext, state, now, now))
        return SecretRef(secret_id, version)

    def reveal(self, db, ref: SecretRef, *, purpose: str, grant_id: str | None, permitted_node_id: str | None) -> bytes:
        keyring = self._require()
        row = db.execute("SELECT * FROM secret_versions WHERE secret_id=? AND version=?", (ref.secret_id, ref.version)).fetchone()
        if row is None or row["state"] == "revoked":
            raise SecretError("secret version unavailable")
        try:
            key = keyring.get(row["key_id"])
        except KeyringError as exc:
            raise SecretError("secret version was encrypted under an unknown key") from exc
        try:
            return AESGCM(key.material).decrypt(row["nonce"], row["ciphertext"], _aad(ref.secret_id, ref.version, purpose, grant_id, permitted_node_id))
        except InvalidTag as exc:
            raise SecretError("secret version failed authentication for this identity") from exc

    def transition(self, db, ref: SecretRef, state: str) -> None:
        if state not in STATES:
            raise SecretError("invalid secret state")
        changed = db.execute("UPDATE secret_versions SET state=?,updated_at=? WHERE secret_id=? AND version=?",
                             (state, int(time.time()), ref.secret_id, ref.version)).rowcount
        if changed != 1:
            raise SecretError("secret version unavailable")

    def rewrap(self, db, *, batch_size: int = 200) -> int:
        """Re-encrypt up to batch_size rows that are not under the active key; returns how many."""
        keyring = self._require()
        active = keyring.active
        rows = db.execute("SELECT * FROM secret_versions WHERE key_id<>? LIMIT ?", (active.key_id, batch_size)).fetchall()
        for row in rows:
            aad = _aad(row["secret_id"], row["version"], row["purpose"], row["grant_id"], row["permitted_node_id"])
            try:
                plaintext = AESGCM(keyring.get(row["key_id"]).material).decrypt(row["nonce"], row["ciphertext"], aad)
            except (KeyringError, InvalidTag) as exc:
                raise SecretError("rewrap cannot decrypt a row under the overlap keyring") from exc
            nonce = os.urandom(12)
            db.execute("UPDATE secret_versions SET key_id=?,nonce=?,ciphertext=?,updated_at=? WHERE secret_id=? AND version=?",
                       (active.key_id, nonce, AESGCM(active.material).encrypt(nonce, plaintext, aad), int(time.time()),
                        row["secret_id"], row["version"]))
        return len(rows)

    def verify_all(self, db) -> dict[str, int]:
        """Decrypt every row (result discarded) and count rows per key id; raises on the first failure."""
        keyring = self._require()
        counts: dict[str, int] = {}
        for row in db.execute("SELECT * FROM secret_versions"):
            aad = _aad(row["secret_id"], row["version"], row["purpose"], row["grant_id"], row["permitted_node_id"])
            try:
                AESGCM(keyring.get(row["key_id"]).material).decrypt(row["nonce"], row["ciphertext"], aad)
            except (KeyringError, InvalidTag) as exc:
                raise SecretError("a secret version does not decrypt under the current keyring") from exc
            counts[row["key_id"]] = counts.get(row["key_id"], 0) + 1
        return counts
```

- [x] **Step 7: Settings, `create_app`, CLI** (выполнено 2026-09-10: `master_key_file` в `Settings`, fail-closed в `create_app`, команды `master-key-init|rotate|verify`)

`panel/settings.py`: `master_key_file: Path | None = field(default_factory=lambda: Path(os.environ["PANEL_MASTER_KEY_FILE"]) if os.getenv("PANEL_MASTER_KEY_FILE") else None)`.

`panel/app.py` после `Store`/`FleetStore`:

```python
    keyring = Keyring.load(settings.master_key_file) if settings.master_key_file else None
    if keyring is None:
        with app.state.database.connect() as db:
            if db.execute("SELECT count(*) FROM secret_versions").fetchone()[0]:
                raise ValueError("PANEL_MASTER_KEY_FILE is required: the database holds encrypted secrets")
    app.state.secrets = SecretStore(keyring)
```

`panel/cli.py`: `master-key-init --path P` (отказ, если файл существует; `Keyring.generate().save(P)`), `master-key-rotate --path P` (load → rotate → save; затем `rewrap` батчами в транзакциях; `verify_all`; только после успеха `retire_all_but_active().save(P)`), `master-key-verify --path P` (`verify_all` → JSON счётчиков без значений).

- [x] **Step 8: Прогнать тесты хранилища** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_secrets_store.py panel/tests/test_migrations.py`
Expected: PASS.
Факт: `16 passed in 1.10s`.

- [x] **Step 9: Контейнер, compose, установщики, документация** (выполнено 2026-09-10)

Отклонение от черновика, найденное тестом: нельзя просто дописать `secrets/panel-master-key` в `_PRESERVED_CREDENTIALS` — этот же кортеж используется в `_validate_existing_credentials(require_all=True)`, и тогда установщик отказывается принимать проект v0.1.0, где ключа ещё нет (`test_core_tolerates_the_naive_compose_secret_in_the_shared_project` упал с `pre-existing project credentials are unsafe`). Введён отдельный `_REQUIRED_CREDENTIALS` (три старых файла) — ключ сохраняется и валидируется, но не требуется при adopt, а создаётся в `_write_generation`.
Ещё две поправки: в `scripts/dev/remote-gate.sh` уровень `compose` теперь рендерит синтетический `secrets/panel-master-key` (иначе `docker compose config` падает на отсутствующем файле секрета), а в `tests/test_panel_entrypoint.py` фейковый `setpriv` логирует четвёртую переменную `PANEL_MASTER_KEY_FILE`.

`panel/entrypoint.sh` после блока Naive:

```sh
MASTER_KEY_SOURCE=${PANEL_MASTER_KEY_SOURCE:-/run/secrets/panel-master-key}
MASTER_KEY_TARGET=/run/panel/master-key
if [ -r "$MASTER_KEY_SOURCE" ]; then
  install -m 0400 -o panel -g panel "$MASTER_KEY_SOURCE" "$MASTER_KEY_TARGET"
  export PANEL_MASTER_KEY_FILE="$MASTER_KEY_TARGET"
fi
```

`compose.yaml`: в `secrets:` добавить `panel-master-key: {file: ./secrets/panel-master-key}`; в `services.panel.secrets` — `panel-master-key`.

`installer/adapters/core.py`: `_PRESERVED_CREDENTIALS += ("secrets/panel-master-key",)`; в `render()` режим `"secrets/panel-master-key": 0o600`; в `_write_generation` после `bootstrap_file`:

```python
        master_key = credential_dir / "panel-master-key"
        if not master_key.exists():
            key_id = "k-" + secrets.token_hex(4)
            payload = json.dumps({"schema": 1, "active_key_id": key_id, "keys": [{
                "key_id": key_id, "state": "active", "created_at": int(time.time()),
                "key_material": base64.b64encode(secrets.token_bytes(32)).decode()}]}, indent=2) + "\n"
            self._atomic(master_key, payload.encode(), 0o600)
```

и в `_validate_existing_credentials` — проверка, что существующий файл парсится как JSON со `schema == 1` (без чтения материала в диагностику). То же в `scripts/mtproxy-deploy` рядом с `telemt-api-token`. Тесты: `tests/test_deploy.py::test_render_creates_secret_safe_parameterized_stack` — `secrets/panel-master-key` существует, mode 0600, `json.loads(...)['schema'] == 1`, материал не встречается в `state.json`; `test_render_is_idempotent_and_preserves_existing_secrets` — файл не меняется при повторном рендере; `tests/test_installer_core.py` — аналогичная пара для `CoreAdapter.render/_write_generation` (шаблон — существующие тесты `test_core_runs_through_transaction_apply_repair_and_uninstall`); `tests/test_panel_entrypoint.py` — строка `PANEL_MASTER_KEY_FILE` присутствует и стейджинг стоит после `install -d … /run/panel`. Общий контракт формата: тест в `panel/tests/test_secrets_store.py`, который рендерит ключ функцией установщика во временный каталог и загружает `Keyring.load` без ошибок.

Документы: `docs/BACKUP_RESTORE.*.md` — ключ в инвентаре backup отдельно от тома (`secrets/panel-master-key`, «БД без ключа бесполезна, ключ без БД — тоже»), процедура restore drill с `master-key-verify`; `docs/UPGRADING*.md` — «перед `compose up` v0.2 создать `secrets/panel-master-key`: `docker run --rm --entrypoint python … -m panel.cli master-key-init --path /out/panel-master-key` либо `installer repair`»; `PANEL.*.md` — раздел «Мастер-ключ и ротация».

- [x] **Step 10: Полный уровень, compose, контейнерная лаборатория** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh full && scripts/dev/remote-gate.sh compose && scripts/dev/remote-gate.sh lab-container`
Expected: три `REMOTE_GATE_*_OK`; отчёт `lab-results-container/report.json` без failed. Дополнительно на стенде: `docker run --rm mtproxy-panel:latest python -c "import cryptography, panel.secrets_store"` → exit 0.
Факт: `REMOTE_GATE_FULL_OK`, `REMOTE_GATE_COMPOSE_OK`, `REMOTE_GATE_LAB_CONTAINER_OK`; `lab-results-container/report.json` — 9 сценариев, ни одного failed (включая `secrets-scan`, который теперь проходит и по новому мастер-ключу). Образ панели: `panel image has cryptography 50.0.1` (импорт `panel.secrets_store` в том же вызове).

- [ ] **Step 11: Коммит**

```bash
git add panel/keyring.py panel/secrets_store.py panel/tests/test_secrets_store.py panel/migrations.py panel/settings.py panel/app.py panel/cli.py panel/requirements.txt panel/requirements-dev.txt panel/entrypoint.sh compose.yaml installer/adapters/core.py scripts/mtproxy-deploy tests/test_deploy.py tests/test_installer_core.py tests/test_panel_entrypoint.py docs/BACKUP_RESTORE.en.md docs/BACKUP_RESTORE.ru.md docs/UPGRADING.md docs/UPGRADING.ru.md PANEL.en.md PANEL.ru.md
git commit -m "feat: добавить версионируемое хранилище секретов"
```

---

### Task 7: Разделить lifecycle узлов и fleet transport (фасад над `FleetStore`)

**Files:**
- Create: `panel/nodes/__init__.py`, `panel/nodes/models.py`, `panel/nodes/registry.py`, `panel/nodes/certificates.py`, `panel/nodes/read_model.py`, `panel/nodes/service.py`
- Create: `panel/tests/test_node_lifecycle.py`
- Modify: `panel/migrations.py` (миграция 4: `fleet_nodes.kind`, `fleet_nodes.disabled`)
- Modify: `panel/fleet.py:339-363` (`_node` отдаёт `kind`, `disabled`), `panel/fleet.py:455-467` (`authenticate_certificate` учитывает `disabled`), `panel/fleet.py:365-403,469-518` (`enqueue`/`poll_next` отклоняют `kind='local'`)
- Modify: `panel/app.py` (`app.state.nodes = NodeLifecycleService(...)`)
- Modify: `panel/cli.py` (`node-list`, `node-disable`, `node-enable`)

**Interfaces:**
- Produces: `panel.nodes.models.NodeView(node_id, display_name, kind, disabled, enrollment_state, connectivity_state, daemon_health, inventory, capabilities, last_seen_at, last_checked_at, certificates: list[CertificateInfo], pending_commands: int)`; `CertificateInfo(serial, fingerprint_sha256, not_before, not_after, state, issued_at, revoked_at)`.
- Produces: `panel.nodes.read_model.derive(row: dict, certificates: list[CertificateInfo], pending_commands: int, now: int, stale_after: int = 90) -> NodeView`; состояния: `enrollment_state ∈ {local, unenrolled, enrolled, revoked}`, `connectivity_state ∈ {not_applicable, never, online, stale}`, `daemon_health ∈ {unknown, reported}`.
- Produces: `panel.nodes.service.NodeLifecycleService(database, fleet, clock=time)` c `list() -> list[NodeView]`, `get(node_id) -> NodeView`, `register(node_id, display_name, *, actor, ip, request_id) -> NodeView`, `rename(node_id, display_name, *, actor, ip, request_id)`, `set_disabled(node_id, disabled, *, actor, ip, request_id)`, `revoke_all_certificates(node_id, *, confirm, actor, ip, request_id) -> int`; исключения `NodeConflict(RuntimeError)`, `KeyError` для неизвестного узла.
- Consumes: `FleetStore` (без изменения публичных методов), `Database.transaction()`, `panel.audit.record`.

- [x] **Step 1: Падающие тесты** (выполнено 2026-09-10 с четырьмя поправками против черновика)

Факт. Черновик снова разошёлся с кодом, и правильным было поправить ожидание:

1. Гварды local в транспорте появились ещё в Task 7: `enqueue`, `poll_next` и
   `bind_certificate` уже отказывают, `authenticate_certificate` уже возвращает `False`,
   а `set_disabled` уже поднимает `NodeConflict`. Task 9 добавляет к ним только запрет
   `revoke_all_certificates` и саму строку в базе.
2. Метод листинга у `FleetStore` называется `nodes()`, а не `list_nodes()`.
3. Строка `local` ломает три чужих утверждения, считавших `count(*) FROM fleet_nodes`
   как «сколько узлов создал тест»: в `test_audit_transactions.py` (две штуки),
   `test_migrations.py::test_transaction_helper…` и
   `test_node_lifecycle.py::test_a_failed_mutation…`. Каждое сужено до своей строки
   (`WHERE node_id='n1'`, `WHERE kind='remote'`), а не ослаблено до `>= 1`.
4. Прежний `test_local_node_has_no_transport_and_cannot_be_disabled` сам вставлял строку
   `local` через `nodes.insert(...)`; после миграции 5 это дубликат первичного ключа,
   поэтому тест переписан от `service.local()`.

Добавлено: `test_migration_creates_exactly_one_local_node_idempotently`,
`test_local_node_is_listed_first_and_can_be_renamed`,
`test_the_local_node_id_cannot_be_registered_again`,
`test_local_node_is_listed_first_with_manager_health`,
`test_local_node_reports_unavailable_managers`,
`test_local_node_marks_switched_off_protocols_disabled`,
`test_local_node_refuses_transport_and_lifecycle_actions`, локальная карточка в
`test_mobile_layout.py` со своей проверкой стекинга и контракт UI в `tests/test_deploy.py`.


```python
from __future__ import annotations

import time

import pytest

from panel.audit import record
from panel.database import Database
from panel.fleet import CommandConflict, FleetStore
from panel.migrations import apply_migrations
from panel.nodes.models import CertificateInfo
from panel.nodes.read_model import derive
from panel.nodes.service import NodeConflict, NodeLifecycleService

ACTOR = {"id": 1, "username": "owner"}
CTX = {"actor": ACTOR, "ip": "127.0.0.1", "request_id": "req-1"}


def _cert(state="active", not_after=None):
    now = int(time.time())
    return CertificateInfo("AA01", "f" * 64, now - 10, not_after or now + 86400 * 30, state, now - 10, None)


@pytest.fixture
def service(tmp_path):
    database = Database(tmp_path / "panel.sqlite3")
    apply_migrations(database)
    return NodeLifecycleService(database, FleetStore(database.path))


def test_read_model_derives_enrollment_and_connectivity():
    now = int(time.time())
    row = {"node_id": "edge-01", "display_name": "edge", "kind": "remote", "disabled": 0,
           "inventory": {"telemt_version": "3.4.25", "capabilities": ["telemt.inventory.refresh"]},
           "last_seen_at": None, "updated_at": now}
    view = derive(row, [], 0, now)
    assert (view.enrollment_state, view.connectivity_state, view.daemon_health) == ("unenrolled", "never", "reported")
    view = derive({**row, "last_seen_at": now - 10}, [_cert()], 2, now)
    assert (view.enrollment_state, view.connectivity_state, view.pending_commands) == ("enrolled", "online", 2)
    view = derive({**row, "last_seen_at": now - 600}, [_cert()], 0, now)
    assert view.connectivity_state == "stale"
    view = derive({**row, "last_seen_at": now - 10}, [_cert(state="revoked")], 0, now)
    assert view.enrollment_state == "revoked"
    assert derive({**row, "kind": "local"}, [], 0, now).connectivity_state == "not_applicable"


def test_register_writes_node_and_audit_in_one_transaction(service):
    view = service.register("edge-01", "Edge", **CTX)
    assert view.enrollment_state == "unenrolled"
    with service.database.connect() as db:
        row = db.execute("SELECT action,target,request_id FROM audit_log").fetchone()
    assert tuple(row) == ("node.register", "edge-01", "req-1")
    with pytest.raises(NodeConflict):
        service.register("edge-01", "Edge again", **CTX)


def test_disabled_node_is_rejected_by_transport_authentication(service):
    service.register("edge-01", "Edge", **CTX)
    service.fleet.bind_certificate("edge-01", {"serial": "AA01", "fingerprint_sha256": "f" * 64,
                                               "not_before": 1, "not_after": 2**31 - 1})
    assert service.fleet.authenticate_certificate("edge-01", "AA01", "f" * 64, "edge-01") is True
    service.set_disabled("edge-01", True, **CTX)
    assert service.fleet.authenticate_certificate("edge-01", "AA01", "f" * 64, "edge-01") is False
    service.set_disabled("edge-01", False, **CTX)
    assert service.fleet.authenticate_certificate("edge-01", "AA01", "f" * 64, "edge-01") is True


def test_disable_refuses_while_commands_are_pending(service):
    service.register("edge-01", "Edge", **CTX)
    service.fleet.enqueue("edge-01", "disable-alice-1", "telemt.user.disable", {"username": "alice"}, "rev-1")
    with pytest.raises(NodeConflict, match="pending"):
        service.set_disabled("edge-01", True, **CTX)


def test_revoke_all_requires_typed_confirmation(service):
    service.register("edge-01", "Edge", **CTX)
    service.fleet.bind_certificate("edge-01", {"serial": "AA01", "fingerprint_sha256": "f" * 64, "not_before": 1, "not_after": 2**31 - 1})
    with pytest.raises(NodeConflict, match="confirm"):
        service.revoke_all_certificates("edge-01", confirm="wrong", **CTX)
    assert service.revoke_all_certificates("edge-01", confirm="edge-01", **CTX) == 1
    assert service.get("edge-01").enrollment_state == "revoked"
```

Перед написанием проверить формат `metadata` для `FleetStore.bind_certificate` в `panel/fleet.py:430-442` и подставить реальные ключи.

- [x] **Step 2: Запустить — падает** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_node_lifecycle.py`
Expected: FAIL `ModuleNotFoundError: panel.nodes`.

- [x] **Step 3: Миграция 4 и изменения `FleetStore`** (выполнено 2026-09-10: `kind`/`disabled`, JOIN в `authenticate_certificate`, отказы для `kind='local'` в `enqueue`/`poll_next`/`bind_certificate`; публичные сигнатуры не менялись)

```python
NODES_V4 = Migration(4, "node-lifecycle", (
    "ALTER TABLE fleet_nodes ADD COLUMN kind TEXT NOT NULL DEFAULT 'remote' CHECK(kind IN ('local','remote'))",
    "ALTER TABLE fleet_nodes ADD COLUMN disabled INTEGER NOT NULL DEFAULT 0",
))
MIGRATIONS = (BASELINE, AUDIT_V2, SECRETS_V3, NODES_V4)
```

В `FleetStore`: `_node()` — `value["disabled"] = bool(value["disabled"])`; `authenticate_certificate` — запрос дополнить `JOIN fleet_nodes n ON n.node_id=c.node_id … AND n.disabled=0 AND n.kind='remote'`; в `enqueue` и `poll_next` — если `kind='local'`: `raise CommandConflict("local node has no fleet transport")`; `bind_certificate` для `kind='local'` — `ProtocolError("local node has no certificates")`. Публичные сигнатуры не меняются; `test_fleet.py` и `test_agent_transport.py` проходят без правок.

- [x] **Step 4: Пакет `panel/nodes`** (выполнено 2026-09-10: `models/read_model/certificates/registry/service`; `register` идёт через `NodeRegistry.insert` в одной транзакции с аудитом, `FleetStore.register_node` остаётся для CLI/API v1)

`models.py` — два frozen dataclass из Interfaces. `read_model.py`:

```python
def derive(row: dict, certificates: list[CertificateInfo], pending_commands: int, now: int, stale_after: int = 90) -> NodeView:
    active = [c for c in certificates if c.state == "active"]
    if row.get("kind") == "local":
        enrollment, connectivity = "local", "not_applicable"
    elif active:
        enrollment = "enrolled"
        seen = row.get("last_seen_at")
        connectivity = "never" if not seen else ("online" if now - seen <= stale_after else "stale")
    else:
        enrollment = "revoked" if certificates else "unenrolled"
        connectivity = "never" if not row.get("last_seen_at") else "stale"
    inventory = row.get("inventory") or {}
    return NodeView(
        node_id=row["node_id"], display_name=row["display_name"], kind=row.get("kind", "remote"),
        disabled=bool(row.get("disabled")), enrollment_state=enrollment, connectivity_state=connectivity,
        daemon_health="reported" if inventory.get("telemt_version") else "unknown",
        inventory={k: v for k, v in inventory.items() if k != "capabilities"},
        capabilities=list(inventory.get("capabilities", [])), last_seen_at=row.get("last_seen_at"),
        last_checked_at=row.get("updated_at"), certificates=certificates, pending_commands=pending_commands,
    )
```

`certificates.py` — `CertificateRegistry(database)`: `list(db, node_id)` (SELECT из `fleet_certificates` ORDER BY issued_at DESC), `revoke_all(db, node_id) -> int` (UPDATE … SET state='revoked', revoked_at=? WHERE node_id=? AND state='active'). `registry.py` — `NodeRegistry(database)`: `rows(db)`, `row(db, node_id)`, `rename(db, node_id, display_name)`, `set_disabled(db, node_id, disabled)`, `pending_commands(db, node_id)` (COUNT status IN ('queued','dispatched')). `service.py` — оркестрация: каждая мутация = `with self.database.transaction() as db:` → изменение → `record(db, …)`; `register` вызывает `self.fleet.register_node` (он сам открывает соединение — это допустимо, потому что регистрация не требует атомарности с аудитом… **нет**: требует. Поэтому `register` делает INSERT напрямую через `NodeRegistry.insert(db, node_id, display_name)` c той же валидацией `NODE_RE`/`validate_inventory({})`, а `FleetStore.register_node` остаётся для CLI/API v1). Ошибки: дубликат → `NodeConflict("node already exists")`, pending → `NodeConflict("node has pending commands")`, confirm → `NodeConflict("confirmation must equal node_id")`.

- [x] **Step 5: Прогнать** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_node_lifecycle.py panel/tests/test_fleet.py panel/tests/test_agent_transport.py panel/tests/test_migrations.py`
Expected: PASS, `test_fleet.py` без правок.
Факт: `44 passed in 20.84s`, оба fleet-файла не правились. Добавлены два теста сверх черновика: у узла `kind='local'` нет ни очереди, ни poll, ни сертификатов и его нельзя отключить; неудачная мутация не оставляет ни строки узла, ни строки аудита.

- [x] **Step 6: Полный уровень** (выполнено 2026-09-10; коммит — по разрешению владельца)

Run: `scripts/dev/remote-gate.sh full` → `REMOTE_GATE_FULL_OK`.
Факт: маркер получен. Дополнительно к плану подключены `app.state.nodes = NodeLifecycleService(...)` и CLI-команды `node-list`/`node-disable`/`node-enable` (последние две отказывают на `local` и на узле с незавершёнными командами).

```bash
git add panel/nodes panel/tests/test_node_lifecycle.py panel/migrations.py panel/fleet.py panel/app.py panel/cli.py
git commit -m "refactor: разделить lifecycle узлов и fleet transport"
```

---

### Task 8: UI и API lifecycle узлов вместо raw Fleet-формы

**Files:**
- Create: `panel/node_routes.py`
- Create: `panel/static/js/nodes.js`
- Create: `panel/tests/test_node_routes.py`
- Modify: `panel/static/js/main.js:20-35,37-46,116-150` (renderer вида `fleet` → `renderNodes`, обработчики `handleNodesClick`, `openNodeModal`)
- Modify: `panel/static/js/fleet.js` (экспорт `commandForm`, `nodeDetail`, `refreshCommands` для Advanced-drawer; удалить самостоятельный рендер списка)
- Modify: `panel/static/index.html` (диалоги `node-modal`, `node-rename-modal`; подпись nav «Узлы» остаётся)
- Modify: `panel/static/style.css` (`.node-card`, `.advanced-drawer`, `.enrollment-checklist`)
- Modify: `panel/app.py` (`register_node_routes`)
- Modify: `tests/test_deploy.py` (контрактные строки), `panel/tests/test_mobile_layout.py:226-254` (вариант карточки узла)
- Modify: `PANEL.ru.md`, `PANEL.en.md`, `FLEET.ru.md`, `FLEET.en.md` (раздел «Экран узлов»)

**Interfaces:**
- Produces API: `GET /api/nodes` → `{"items":[NodeView…]}` (все роли); `GET /api/nodes/{node_id}`; `POST /api/nodes` (owner) body `{node_id, display_name}` → 201 `{node, enrollment_checklist:[str…]}`; `POST /api/nodes/{node_id}/rename` (owner) `{display_name}`; `POST /api/nodes/{node_id}/disable|enable` (owner); `POST /api/nodes/{node_id}/certificates/revoke-all` (owner) `{confirm}` → `{revoked:int}`. Ошибки: 404 unknown, 409 `NodeConflict`, 422 валидация.
- Audit actions: `node.register`, `node.rename`, `node.disable`, `node.enable`, `node.certificates.revoke_all`.
- Consumes: `NodeLifecycleService`, `RequestContext.roles`, `Pydantic` схемы `NodeCreate(node_id: constr(pattern=NODE_RE), display_name: constr(max_length=128))`, `NodeRename`, `NodeRevokeAll(confirm: str)` в `panel/schemas.py`.

- [ ] **Step 1: Падающие API-тесты**

```python
from __future__ import annotations

import pytest

pytestmark = pytest.mark.anyio


async def test_viewer_reads_nodes_but_cannot_register(client, login_user):
    app = client._transport.app
    app.state.store.create_admin("viewer", "viewer-password-123", "viewer")
    await login_user(client, "viewer", "viewer-password-123")
    csrf = client.cookies["panel_csrf"]
    assert (await client.get("/api/nodes")).status_code == 200
    denied = await client.post("/api/nodes", json={"node_id": "edge-01", "display_name": "Edge"}, headers={"X-CSRF-Token": csrf})
    assert denied.status_code == 403


async def test_owner_registers_node_and_gets_enrollment_checklist(client, login_user):
    await login_user(client)
    csrf = client.cookies["panel_csrf"]
    created = await client.post("/api/nodes", json={"node_id": "edge-01", "display_name": "Edge"}, headers={"X-CSRF-Token": csrf})
    assert created.status_code == 201
    body = created.json()
    assert body["node"]["enrollment_state"] == "unenrolled"
    assert any("fleet-sign-csr" in step for step in body["enrollment_checklist"])
    audit = await client.get("/api/audit", params={"action": "node.register"})
    assert audit.json()["items"][0]["target"] == "edge-01"
    listed = await client.get("/api/fleet/nodes")
    assert [n["node_id"] for n in listed.json()["items"] if n["node_id"] == "edge-01"]


async def test_disable_enable_and_revoke_all_are_owner_actions_with_confirmation(client, login_user):
    await login_user(client)
    csrf = client.cookies["panel_csrf"]
    await client.post("/api/nodes", json={"node_id": "edge-01", "display_name": "Edge"}, headers={"X-CSRF-Token": csrf})
    assert (await client.post("/api/nodes/edge-01/disable", headers={"X-CSRF-Token": csrf})).status_code == 200
    assert (await client.get("/api/nodes/edge-01")).json()["disabled"] is True
    assert (await client.post("/api/nodes/edge-01/enable", headers={"X-CSRF-Token": csrf})).status_code == 200
    wrong = await client.post("/api/nodes/edge-01/certificates/revoke-all", json={"confirm": "nope"}, headers={"X-CSRF-Token": csrf})
    assert wrong.status_code == 409
    ok = await client.post("/api/nodes/edge-01/certificates/revoke-all", json={"confirm": "edge-01"}, headers={"X-CSRF-Token": csrf})
    assert ok.status_code == 200 and ok.json() == {"revoked": 0}


async def test_node_view_never_leaks_unknown_inventory_keys(client, login_user):
    await login_user(client)
    app = client._transport.app
    with app.state.database.connect() as db:
        db.execute("INSERT INTO fleet_nodes(node_id,display_name,auth_state,inventory_json,created_at,updated_at) VALUES('edge-02','x','unenrolled','{\"telemt_version\":\"3.4.25\",\"api_token\":\"leak\"}',1,1)")
    body = (await client.get("/api/nodes/edge-02")).json()
    assert "leak" not in str(body) and body["daemon_health"] == "reported"
```

- [x] **Step 2: Запустить — падает (404 на `/api/nodes`)** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_node_routes.py`
Expected: FAIL.

- [x] **Step 3: Реализовать `panel/node_routes.py`** (выполнено 2026-09-10: 6 эндпоинтов, `ENROLLMENT_CHECKLIST` с явным «copy only the CSR, never the key»; схемы `NodeCreate/NodeRename/NodeRevokeAll` в `panel/schemas.py`)

По образцу `panel/fleet_routes.py`: `register_node_routes(app, context)`; чтение — `asyncio.to_thread(app.state.nodes.list)`; мутации — `context.roles("owner")`, передавать `actor=user, ip=context.client_ip(request), request_id=request.state.request_id`; `NodeConflict` → `HTTPException(409, str(exc))`; `KeyError` → 404. `enrollment_checklist` — список строк из `FLEET.ru.md` «Enroll … without exporting its private key»: `fleet-ca-init`, генерация CSR на ноде (`openssl req -new -newkey ec …`), `python -m panel.cli fleet-sign-csr <node_id> --ca-dir … --csr … --out …`, `fleet-bind-cert`, заполнение `FLEET_NODE_ID/FLEET_CENTRAL_URL/FLEET_CLIENT_CERT/FLEET_CLIENT_KEY` в `.env` и `compose.agent.yaml`. `NodeView` сериализуется через `dataclasses.asdict`.

- [x] **Step 4: UI** (выполнено 2026-09-10 с тремя поправками против черновика)

1. `<details>/<summary>` для Advanced не годится: делегирование кликов в `main.js` ловит только `button`, так что дравер — `div.advanced-drawer` с кнопкой и `aria-expanded`.
2. Подтверждение отзыва — отдельный диалог `#node-revoke-modal` с полем `#node-revoke-confirm` (а не `window.prompt`).
3. Старый экран списка удалён целиком: из `fleet.js` убраны `renderFleet`, `nodeCard`, `openFleetModal`, `bindFleet`, а из `index.html` — `#fleet-modal`; остались только части, которые рендерит дравер. Обновлён старый контрактный тест `test_users_adapter_ui.py`, который требовал `#fleet-modal`.

`nodes.js`: `renderNodes(context, generation)` — `GET /api/nodes`, карточки (`.data-row.node-card`): identity (display_name, node_id, kind badge «Этот сервер»/«Удалённый»), enrollment pill, transport (`last_seen_at` через `date()` из common.js + connectivity), daemon (`telemt_version`, `agent_version`), certificates (serial · до `not_after`, предупреждение при < 14 дней), capabilities; действия (owner): «Переименовать», «Отключить/Включить», «Отозвать все сертификаты» (через `ui.confirmed(...)` + ввод node_id в `#node-revoke-confirm`), `<details class="advanced-drawer"><summary>Advanced: transport v1</summary>…</details>` с `commandForm`/`nodeDetail` из fleet.js. `openNodeModal` — форма `node_id`/`display_name`, после 201 показывает checklist в том же диалоге. В `main.js`: `RENDERERS.fleet = renderNodes`, `TITLES.fleet = ["Узлы", "Enrollment, транспорт и состояние демонов; raw-команды Telemt v1 — в Advanced"]`, `#add` для `fleet` открывает `openNodeModal`, клики — `handleNodesClick` перед `handleFleetClick`. `state.js`: `nodes: []`.

Контрактные тесты (добавить в `tests/test_deploy.py::DeployCliTests`):

```python
    def test_nodes_view_exposes_lifecycle_actions_and_hides_raw_transport_in_advanced(self):
        javascript = (ROOT / "panel/static/js/nodes.js").read_text()
        html = (ROOT / "panel/static/index.html").read_text()
        main = (ROOT / "panel/static/js/main.js").read_text()
        self.assertIn('data-node-action="revoke-all"', javascript)
        self.assertIn('class="advanced-drawer"', javascript)
        self.assertIn("/api/nodes/${encodeURIComponent(nodeId)}/certificates/revoke-all", javascript)
        self.assertIn('id="node-modal"', html)
        self.assertIn("fleet: renderNodes", main)
```

В `test_mobile_layout.py::test_access_cards_and_navigation_do_not_collide_on_phone` добавить четвёртую карточку `card("data-row node-card", "ND", "edge-node-with-a-very-long-name", "Узел · enrolled · online", "Переименовать")`.

- [x] **Step 5: Прогнать** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_node_routes.py panel/tests/test_mobile_layout.py panel/tests/test_rbac_audit.py` (проверка синтаксиса UI — через `scripts/dev/check-js-syntax.sh`, см. Task 0 Step 3b)
Expected: PASS.
Факт: API узлов — `10 passed`; UI-контракты + мобильный гейт — `35 passed`; после добавления карточки узла в `test_mobile_layout.py` — `2 passed`. Дефект тест-жгута: его скрипт раскладывал каждую `.data-row` как карточку пользователя (`[identity, status, traffic, actions]`), поэтому карточка узла роняла скрипт молча — тест зависал на `RuntimeError: Mobile layout assertions did not finish`. Пользовательский цикл сужен до `.data-row:not(.node-card)`, карточке узла добавлена своя проверка стекинга и выхода за границы.

- [x] **Step 6: Документация и полный уровень** (выполнено 2026-09-10; коммит — по разрешению владельца)

`PANEL.*.md`: раздел «Узлы» (что показывает экран, что делает каждая кнопка, где Advanced). `FLEET.*.md`: ссылка на экран узлов из раздела enrollment. `scripts/dev/remote-gate.sh full` → OK.
Факт: разделы «Узлы»/«The Nodes screen» добавлены в `PANEL.ru.md`/`PANEL.en.md`, разделы «Экран „Узлы“ в панели»/«The panel's node screen» — в `FLEET.ru.md`/`FLEET.en.md`. `REMOTE_GATE_FULL_OK` получен.

```bash
git add panel/node_routes.py panel/static/js/nodes.js panel/static/js/fleet.js panel/static/js/main.js panel/static/js/state.js panel/static/index.html panel/static/style.css panel/app.py panel/schemas.py panel/tests/test_node_routes.py panel/tests/test_mobile_layout.py tests/test_deploy.py PANEL.ru.md PANEL.en.md FLEET.ru.md FLEET.en.md
git commit -m "feat: упростить управление узлами в панели"
```

---

### Task 9: Зарезервированная local Node identity

**Files:**
- Modify: `panel/migrations.py` (миграция 5: строка `local`)
- Modify: `panel/nodes/service.py` (`local()`, guards), `panel/nodes/registry.py`
- Modify: `panel/node_routes.py` (в `NodeView` local — `services: {telemt, naive, mieru}` из health managers)
- Modify: `panel/static/js/nodes.js` (карточка local первой, без enrollment-действий)
- Modify: `panel/tests/test_node_lifecycle.py`, `panel/tests/test_node_routes.py`
- Modify: `panel/cli.py` (`node-list` показывает kind)

**Interfaces:**
- Produces: константа `panel.nodes.LOCAL_NODE_ID = "local"`; `NodeLifecycleService.local() -> NodeView`; в `GET /api/nodes` для local — дополнительное поле `services: {"telemt": "ok|unavailable", "naive": "ok|unavailable|disabled", "mieru": "ok|unavailable|disabled"}`.
- Consumes (Task 10/11): каждый импортированный grant ссылается на `node_id='local'`.

- [ ] **Step 1: Падающие тесты**

```python
def test_migration_creates_exactly_one_local_node_idempotently(tmp_path):
    database = Database(tmp_path / "panel.sqlite3")
    apply_migrations(database)
    apply_migrations(database)
    with database.connect() as db:
        rows = db.execute("SELECT node_id,kind,display_name FROM fleet_nodes WHERE kind='local'").fetchall()
    assert [tuple(r) for r in rows] == [("local", "local", "Этот сервер")]


def test_local_node_has_no_transport_certificates_or_disable(service):
    local = service.local()
    assert (local.enrollment_state, local.connectivity_state) == ("local", "not_applicable")
    with pytest.raises(CommandConflict):
        service.fleet.enqueue("local", "k-1", "telemt.user.disable", {"username": "alice"}, "rev-1")
    with pytest.raises(ProtocolError):
        service.fleet.bind_certificate("local", {"serial": "AA01", "fingerprint_sha256": "f" * 64, "not_before": 1, "not_after": 2**31 - 1})
    assert service.fleet.authenticate_certificate("local", "AA01", "f" * 64, "local") is False
    with pytest.raises(NodeConflict, match="local"):
        service.set_disabled("local", True, **CTX)
    service.rename("local", "Амстердам", **CTX)
    assert service.local().display_name == "Амстердам"
    assert [n.node_id for n in service.list()][0] == "local"
```

И в `test_node_routes.py`:

```python
async def test_local_node_is_listed_first_with_manager_health(client, login_user):
    await login_user(client)
    body = (await client.get("/api/nodes")).json()
    assert body["items"][0]["node_id"] == "local"
    assert body["items"][0]["services"] == {"telemt": "ok", "naive": "ok", "mieru": "ok"}
    csrf = client.cookies["panel_csrf"]
    queued = await client.post("/api/fleet/nodes/local/commands", json={
        "idempotency_key": "refresh-local-01", "operation": "telemt.inventory.refresh",
        "payload": {}, "expected_telemt_revision": "rev-1"}, headers={"X-CSRF-Token": csrf})
    assert queued.status_code == 409
```

- [x] **Step 2: Запустить — падает** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_node_lifecycle.py panel/tests/test_node_routes.py` → FAIL.
Факт: `ImportError: cannot import name 'LOCAL_NODE_ID' from 'panel.nodes'` — сборка тестов
падает до запуска, то есть по ожидаемой причине.

- [x] **Step 3: Миграция 5 и сервис** (выполнено 2026-09-10 с тремя уточнениями)

Факт:

1. `INSERT OR IGNORE` оставлен намеренно: если в базе с v0.1.0 уже есть узел с
   `node_id='local'`, миграция его не трогает и не превращает чужой удалённый узел в
   локальный. Чтобы новое столкновение не возникло, `NodeRegistry.insert` теперь
   отказывает на зарезервированном id (`ProtocolError`, в API — 422).
2. Порядок «local первым» реализован в SQL `NodeRegistry.rows`
   (`ORDER BY CASE WHEN kind='local' THEN 0 ELSE 1 END, node_id`), а не сортировкой в
   сервисе: так один и тот же порядок получают и `list()`, и всё, что читает строки.
3. Здоровье менеджера определяется фактом ответа, а не полем `ready`: каждый клиент
   (`TelemtClient`, `NaiveClient`, `MieruClient`) уже поднимает свою ошибку на любом
   не-2xx и на обрыве, а форма тела `/v1/health/ready` у Telemt не гарантирована.
   Поэтому `ok` = ответил, `unavailable` = поднял `TelemtError/NaiveError/MieruError`,
   `disabled` = протокол выключен в `Settings` и менеджер вообще не опрашивается
   (в тесте это подтверждено клиентом, который падает с `AssertionError`, если его
   всё-таки спросят).

`LOCAL_NODE_ID` живёт в `panel/nodes/models.py` (переэкспорт из `panel/nodes/__init__.py`):
`panel/migrations.py` не должен импортировать `panel.nodes`, иначе `panel.agent_ingress`
потянет за собой лишнее в образ ingress.


```python
LOCAL_NODE_V5 = Migration(5, "local-node", (
    """INSERT OR IGNORE INTO fleet_nodes(node_id,display_name,auth_state,inventory_json,created_at,updated_at,kind)
       VALUES('local','Этот сервер','local','{}',CAST(strftime('%s','now') AS INTEGER),CAST(strftime('%s','now') AS INTEGER),'local')""",
))
MIGRATIONS = (BASELINE, AUDIT_V2, SECRETS_V3, NODES_V4, LOCAL_NODE_V5)
```

`NodeLifecycleService.list()` сортирует `kind='local'` первым; `set_disabled('local', …)` → `NodeConflict("local node cannot be disabled")`; `revoke_all_certificates('local', …)` → `NodeConflict("local node has no certificates")`. В `node_routes`: для local — `services` собираются через `asyncio.gather` из `app.state.telemt.health()`, `naive.health()` (если `settings.naive_enabled`, иначе `disabled`), `mieru.health()`; исключения `TelemtError/NaiveError/MieruError` → `unavailable`. В `nodes.js` карточка local без кнопок enrollment/disable/revoke, с блоком «Сервисы: Telemt · Naive · Mieru».

- [x] **Step 4: Прогнать целевые + fleet + полный уровень** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_node_lifecycle.py panel/tests/test_node_routes.py panel/tests/test_fleet.py panel/tests/test_agent_transport.py panel/tests/test_migrations.py && scripts/dev/remote-gate.sh full`
Expected: PASS, `REMOTE_GATE_FULL_OK`.
Факт: целевые + fleet + agent-transport + миграции + аудит — `63 passed`; мобильный
гейт — `20 passed` (и отдельная проверка канарейкой: карточка local с добавленным
`.node-actions` роняет жгут с `['local node card offers enrollment controls']`, то есть
новые утверждения действительно исполняются, а не молчат).

Первый полный прогон нашёл ещё одно чужое ожидание: `test_users_adapter_ui.py::
test_sidebar_counters_are_filled_by_the_overview_not_by_visiting_a_section` требовал
`GET /api/fleet/nodes` → `items == []` на свежей базе. Теперь там всегда есть строка
`local`, поэтому утверждение сужено до `[node["node_id"] …] == ["local"]`. Побочный
эффект, который нужно помнить: счётчик `#fleet-count` на свежей установке показывает
`1`, а не `0` — узел действительно один, этот сервер.

Второй полный прогон: `REMOTE_GATE_FULL_OK`, весь набор — `1334 passed, 2 skipped`
(база до Tasks 0–8 была `1279 passed, 2 skipped`; новых падений нет).

CLI на стенде:

```
$ .venv/bin/python -m panel.cli --database /tmp/task9-cli.sqlite3 db-migrate
{"applied": [1, 2, 3, 4, 5], "database": "/tmp/task9-cli.sqlite3"}
$ .venv/bin/python -m panel.cli --database /tmp/task9-cli.sqlite3 node-list
[{… "display_name": "Этот сервер", "enrollment_state": "local", "kind": "local",
  "connectivity_state": "not_applicable", "certificates": [], "pending_commands": 0 …}]
$ .venv/bin/python -m panel.cli --database /tmp/task9-cli.sqlite3 node-disable local
cli.py: error: the local node cannot be disabled     # exit=2
```

- [ ] **Step 5: Коммит** (ждёт разрешения владельца)

`panel/cli.py` править не пришлось: `node-list` печатает `dataclasses.asdict(NodeView)`,
а `kind` — поле `NodeView` с Task 7, поэтому оно уже в выводе (см. факт Step 4).
Затронутые файлы шире черновика — добавились документация, контракт UI и три чужих
теста, чьи ожидания пришлось сузить:

```bash
git add panel/migrations.py panel/nodes panel/node_routes.py panel/static/js/nodes.js \
        panel/static/style.css panel/tests/test_node_lifecycle.py panel/tests/test_node_routes.py \
        panel/tests/test_migrations.py panel/tests/test_audit_transactions.py \
        panel/tests/test_mobile_layout.py panel/tests/test_users_adapter_ui.py \
        tests/test_deploy.py PANEL.ru.md PANEL.en.md
git commit -m "feat: добавить зарезервированный локальный узел и безопасный lifecycle"
```

---

### Task 10: Таблицы и доменная модель `Client` / `AccessGrant`

**Files:**
- Create: `panel/clients/__init__.py`, `panel/clients/models.py`, `panel/clients/store.py`, `panel/clients/service.py`
- Create: `panel/tests/test_clients_domain.py`
- Modify: `panel/migrations.py` (миграция 6)
- Modify: `panel/app.py` (`app.state.clients = ClientService(...)`)

**Interfaces:**
- Produces (`panel.clients.models`, Pydantic v2): `PROTOCOLS = ("mtproxy","naive","mieru")`; `USERNAME_RE = r"^[A-Za-z0-9_.-]{1,64}$"`; `Client(id, display_name, state: Literal["active","suspended","archived"], metadata: dict, created_at, updated_at)`; `MtproxyOptions(data_quota_bytes, rate_limit_up_bps, rate_limit_down_bps, max_tcp_conns, max_unique_ips, expiration: int|None)` (границы как `panel.fleet.LIMIT_FIELDS`); `NaiveOptions(quota_bytes: int|None)`; `MieruOptions(quotas: list[MieruQuota], share_template: str|None)` (`share_template` — `mierus://{username}:{password}@host?…` без значений); `GrantIntent(protocol, node_id="local", endpoint_id="default", runtime_username, options, valid_from: int|None, valid_until: int|None)`; `AccessGrant(id, client_id, protocol, node_id, endpoint_id, runtime_username, secret_ref: SecretRef|None, desired_state: Literal["enabled","disabled","deleted"], observed_state: Literal["unknown","pending","enabled","disabled","missing"], valid_from, valid_until, options, origin: Literal["imported","provisioned"], created_at, updated_at)`; `effective_enabled(grant, client, now) -> bool`.
- Produces (`panel.clients.store.ClientStore(database)`, все методы принимают `db`): `insert_client(db, client)`, `client(db, id)`, `clients(db, *, state=None)`, `set_client_state(db, id, state)`, `insert_grant(db, grant)`, `grant(db, id)`, `grants(db, *, client_id=None, protocol=None, node_id=None, include_deleted=False)`, `update_grant(db, id, **fields)`, `find_grant(db, protocol, node_id, endpoint_id, runtime_username)`; `ClientConflict(RuntimeError)`.
- Produces (`panel.clients.service.ClientService(database, secrets, clock=time)`): `create_client(display_name, *, actor, ip, request_id) -> Client`, `set_state(client_id, state, *, actor, ip, request_id)`, `client_with_grants(client_id) -> tuple[Client, list[AccessGrant]]`, `list_clients()`; `on_change: list[Callable[[db, client_id], None]]` — хук, который Task 15 использует для инкремента generation подписки в той же транзакции.
- Consumes: `Database`, `SecretRef`, `panel.audit.record`, `panel.fleet.LIMIT_FIELDS`.

- [x] **Step 1: Миграция 6** (выполнено 2026-09-10, ровно как в черновике)

```python
CLIENTS_V6 = Migration(6, "clients-and-grants", (
    """CREATE TABLE IF NOT EXISTS clients (
      id TEXT PRIMARY KEY, display_name TEXT NOT NULL,
      state TEXT NOT NULL CHECK(state IN ('active','suspended','archived')),
      metadata_json TEXT NOT NULL DEFAULT '{}',
      created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL)""",
    """CREATE TABLE IF NOT EXISTS access_grants (
      id TEXT PRIMARY KEY,
      client_id TEXT NOT NULL REFERENCES clients(id) ON DELETE RESTRICT,
      protocol TEXT NOT NULL CHECK(protocol IN ('mtproxy','naive','mieru')),
      node_id TEXT NOT NULL REFERENCES fleet_nodes(node_id) ON DELETE RESTRICT,
      endpoint_id TEXT NOT NULL DEFAULT 'default',
      runtime_username TEXT NOT NULL,
      secret_id TEXT, secret_version INTEGER,
      desired_state TEXT NOT NULL CHECK(desired_state IN ('enabled','disabled','deleted')),
      observed_state TEXT NOT NULL DEFAULT 'unknown' CHECK(observed_state IN ('unknown','pending','enabled','disabled','missing')),
      valid_from INTEGER, valid_until INTEGER,
      protocol_options_json TEXT NOT NULL DEFAULT '{}',
      origin TEXT NOT NULL CHECK(origin IN ('imported','provisioned')),
      created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL,
      UNIQUE(protocol,node_id,endpoint_id,runtime_username))""",
    "CREATE INDEX IF NOT EXISTS access_grants_client ON access_grants(client_id)",
))
MIGRATIONS = (BASELINE, AUDIT_V2, SECRETS_V3, NODES_V4, LOCAL_NODE_V5, CLIENTS_V6)
```

- [x] **Step 2: Падающие тесты** (выполнено 2026-09-10 с поправками против черновика)

Факт. `pytest.raises(Exception)` в черновике заменён на конкретный
`sqlite3.IntegrityError` (иначе утверждение поймало бы что угодно, включая опечатку в
самом тесте). `_grant(...)` строит options по протоколу, а не всегда `NaiveOptions` —
иначе `AccessGrant(protocol="mtproxy", options=NaiveOptions(...))` не прошёл бы
собственную валидацию модели. Добавлены четыре теста сверх черновика: round-trip
gran-а через базу с типизированными options, отказ `MtproxyOptions(max_tcp_conns=0)`
по границам `LIMIT_FIELDS`, срабатывание `on_change` внутри транзакции и успешный
`archived` после того, как единственный grant переведён в `deleted`.


```python
from __future__ import annotations

import time
import uuid

import pytest
from pydantic import ValidationError

from panel.clients.models import AccessGrant, Client, GrantIntent, MieruOptions, NaiveOptions, effective_enabled
from panel.clients.service import ClientService
from panel.clients.store import ClientConflict, ClientStore
from panel.database import Database
from panel.migrations import apply_migrations
from panel.secrets_store import SecretStore

ACTOR = {"id": 1, "username": "owner"}
CTX = {"actor": ACTOR, "ip": "127.0.0.1", "request_id": "req-1"}


@pytest.fixture
def service(tmp_path):
    database = Database(tmp_path / "panel.sqlite3")
    apply_migrations(database)
    return ClientService(database, SecretStore(None))


def _grant(client_id, protocol="naive", username="alice", node_id="local"):
    now = int(time.time())
    return AccessGrant(id=str(uuid.uuid4()), client_id=client_id, protocol=protocol, node_id=node_id,
                       endpoint_id="default", runtime_username=username, secret_ref=None,
                       desired_state="enabled", observed_state="unknown", valid_from=None, valid_until=None,
                       options=NaiveOptions(quota_bytes=None), origin="imported", created_at=now, updated_at=now)


def test_client_ids_are_uuids_and_grants_belong_to_one_client_and_node(service):
    client = service.create_client("Sergey", **CTX)
    uuid.UUID(client.id)
    store = ClientStore(service.database)
    with service.database.transaction() as db:
        store.insert_grant(db, _grant(client.id))
        with pytest.raises(ClientConflict, match="runtime_username"):
            store.insert_grant(db, _grant(client.id))          # same protocol+node+endpoint+username
        store.insert_grant(db, _grant(client.id, protocol="mtproxy"))  # same username, other protocol: fine
    with service.database.connect() as db:
        with pytest.raises(Exception):
            store.insert_grant(db, _grant(client.id, node_id="edge-missing"))  # FK to fleet_nodes


def test_options_are_typed_and_secret_free():
    with pytest.raises(ValidationError):
        GrantIntent(protocol="mieru", runtime_username="a", options=NaiveOptions(quota_bytes=1))
    with pytest.raises(ValidationError):
        GrantIntent(protocol="naive", runtime_username="a b", options=NaiveOptions(quota_bytes=1))
    with pytest.raises(ValidationError):
        MieruOptions(quotas=[], share_template="mierus://u:realpassword@h?port=1")


def test_suspended_client_compiles_every_grant_as_disabled(service):
    client = service.create_client("Sergey", **CTX)
    now = int(time.time())
    grant = _grant(client.id)
    assert effective_enabled(grant, client, now) is True
    service.set_state(client.id, "suspended", **CTX)
    client, _ = service.client_with_grants(client.id)
    assert effective_enabled(grant, client, now) is False
    expired = grant.model_copy(update={"valid_until": now - 1})
    assert effective_enabled(expired, client.model_copy(update={"state": "active"}), now) is False


def test_archive_refuses_while_grants_are_not_deleted(service):
    client = service.create_client("Sergey", **CTX)
    with service.database.transaction() as db:
        ClientStore(service.database).insert_grant(db, _grant(client.id))
    with pytest.raises(ClientConflict, match="grants"):
        service.set_state(client.id, "archived", **CTX)


def test_every_mutation_leaves_an_audit_row_in_the_same_transaction(service, monkeypatch):
    from panel.clients import service as module
    def boom(*_args, **_kwargs):
        raise RuntimeError("audit failed")
    monkeypatch.setattr(module, "record", boom)
    with pytest.raises(RuntimeError):
        service.create_client("Sergey", **CTX)
    assert service.list_clients() == []
```

- [x] **Step 3: Запустить — падает на импорте** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_clients_domain.py` → FAIL `ModuleNotFoundError: panel.clients`.
Факт: получен ровно этот `ModuleNotFoundError: No module named 'panel.clients'`.

- [x] **Step 4: Реализовать models/store/service** (выполнено 2026-09-10 с пятью уточнениями)

Факт:

1. **Валидатор соответствия `protocol` ↔ `options` обязателен, и это проверено
   канарейкой.** Одного `extra="forbid"` мало: с отключённым валидатором
   `GrantIntent(protocol="mieru", options=NaiveOptions(quota_bytes=1))` проходит —
   smart-union pydantic принимает чужой объект. Поэтому `protocol` и `options` подняты
   в общую базу `_ProtocolOptions` с `model_validator(mode="after")`, а не описаны
   дважды. Канарейка (валидатор заменён на `return self`) роняет
   `test_options_are_typed_and_secret_free` — утверждение действительно кусается.
2. **Правило `share_template` сформулировано точно**, а не «не содержать `@` перед ними
   с другим текстом»: шаблон обязан содержать литералы `{username}` и `{password}`, и
   если в URI есть сегмент учётных данных перед `@`, он должен быть ровно
   `{username}:{password}`. Черновиковый пример `mierus://u:realpassword@h?port=1`
   отвергается по первому же условию.
3. **`insert_grant` — метод экземпляра, а не `@staticmethod`,** потому что вызывает
   `find_grant`; проверка перед INSERT нужна не для красоты. Канарейка (проверка убрана)
   показывает, что UNIQUE-индекс поднимает `sqlite3.IntegrityError` и обрывает всю
   транзакцию вызывающего — а тест как раз продолжает работать в ней после конфликта.
4. `set_client_state(db, id, state, *, updated_at)` принимает время: часы принадлежат
   сервису (`clock`), store их не выдумывает. `update_grant` пишет только колонки из
   белого списка `UPDATABLE` — иначе `**fields` был бы дырой в SQL-строке.
5. `on_change` вызывается и на `create_client`, не только на `set_state`: подписка
   Task 15 должна увидеть появление клиента так же, как его блокировку.

`SecretRef` — обычный frozen dataclass; pydantic валидирует его как есть, в базе он
лежит двумя колонками (`secret_id`, `secret_version`), а `_grant` собирает его обратно
только когда обе не `NULL`.


`models.py`: `GrantIntent` — `model_validator(mode="after")`, проверяющий соответствие `protocol` ↔ тип `options` (`mtproxy→MtproxyOptions`, `naive→NaiveOptions`, `mieru→MieruOptions`) и `USERNAME_RE`; `MieruOptions.share_template` — валидатор: строка должна содержать буквально `{username}` и `{password}` и не содержать `@` перед ними с другим текстом (шаблон, не ссылка). `effective_enabled` = `client.state == "active" and grant.desired_state == "enabled" and (valid_from is None or valid_from <= now) and (valid_until is None or now < valid_until)`. `ClientStore.insert_grant` — перед INSERT проверяет `find_grant` и бросает `ClientConflict("runtime_username already granted on this endpoint")`; `secret_ref` хранится как две колонки. `ClientService.create_client`: `with self.database.transaction() as db: store.insert_client(...); record(db, actor=..., action="client.create", target=client.id, ip=..., request_id=..., after_digest=digest(client.model_dump()))`; `set_state`: `archived` требует, чтобы все grants были `deleted`; после изменения вызывает `for hook in self.on_change: hook(db, client_id)`.

- [x] **Step 5: Прогнать** (выполнено 2026-09-10; коммит — по разрешению владельца)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_clients_domain.py panel/tests/test_migrations.py && scripts/dev/remote-gate.sh full`
Факт: целевые — `15 passed`. Первый полный прогон дал `2 failed, 1339 passed, 2 skipped`:
страж схемы в `test_users_adapter_ui.py` и `test_vnext_characterization.py` разрешал
только `{secret_versions, secret_versions_key, secret_versions_grant, secret_id}`, а
миграция 6 добавила колонку-ссылку `secret_version` в `access_grants`. Это ровно тот же
класс, что уже разрешённый `secret_id` — ссылка, а не значение, — поэтому allowlist
расширен именно на неё, без ослабления самого стража. Второй прогон:
`REMOTE_GATE_FULL_OK`, весь набор — `1341 passed, 2 skipped`.

```bash
git add panel/clients panel/tests/test_clients_domain.py panel/migrations.py panel/app.py \
        panel/tests/test_users_adapter_ui.py panel/tests/test_vnext_characterization.py
git commit -m "feat: добавить доменную модель клиентов и доступов"
```

---

### Task 11: Read-only импорт существующих пользователей и явная привязка

**Files:**
- Create: `panel/clients/importer.py`
- Create: `panel/client_routes.py`
- Create: `panel/static/js/clients.js`
- Create: `panel/tests/test_client_import.py`
- Modify: `panel/app.py`, `panel/static/index.html` (nav «Клиенты» в sidebar и mobile-nav, диалоги `client-modal`, `client-import-modal`), `panel/static/js/main.js`, `panel/static/js/state.js`
- Modify: `panel/nodes/service.py` (decommission guard: узел с активными grants нельзя отключить)
- Modify: `tests/test_deploy.py`, `panel/tests/test_mobile_layout.py:231` (кнопка «Клиенты» в списке nav)

**Interfaces:**
- Produces (`panel.clients.importer`): `InventoryItem(protocol, runtime_username, enabled: bool, options: dict, imported_grant_id: str|None)`; `async inventory(telemt, naive, mieru, *, naive_enabled, mieru_enabled, store, database) -> list[InventoryItem]` (только чтение managers); `propose(items) -> list[ProposedClient(display_name, items: list[InventoryItem], same_username_hint: bool)]` — группирует **только как подсказку**, по умолчанию каждая (protocol, username) — отдельный клиент; `ImportDecision(display_name: str, client_id: str|None, items: list[tuple[protocol, runtime_username]])`; `confirm(service, decisions, *, actor, ip, request_id) -> ImportResult(created_clients: int, created_grants: int, skipped: list[str])`.
- Produces API: `GET /api/clients` (все роли) → `{"items":[{client, grants:[…]}]}`; `POST /api/clients` (owner/admin) `{display_name}` → 201; `GET /api/clients/{id}`; `POST /api/clients/{id}/state` `{state}`; `GET /api/clients/import/inventory` (owner/admin) → `{"proposals":[…], "already_imported": n}`; `POST /api/clients/import` `{decisions:[…]}` → `ImportResult`.
- Audit: `client.create`, `client.state`, `client.import` (detail: counts and runtime usernames only).

- [x] **Step 1: Падающие тесты** (выполнено 2026-09-10, черновик расширен четырьмя тестами)

Факт. К четырём тестам черновика добавлены: импорт запоминает фактическое состояние
рантайма (выключенный naive-пользователь приходит как `desired_state=disabled`) и не
пишет пароль в аудит; импорт в существующего клиента (`client_id` задан); отказ на
неизвестной учётной записи не оставляет ни клиента, ни гранта (весь импорт — одна
транзакция); узел снова можно отключить после перевода гранта в `deleted`. Проверка
`created_clients == 0` на повторном импорте добавлена отдельно: без неё повтор мог бы
создавать пустых клиентов-двойников, и тест этого не заметил бы.

```python
from __future__ import annotations

import copy

import pytest

pytestmark = pytest.mark.anyio


async def _seed(client, login_user, telemt, naive, mieru):
    await login_user(client)
    csrf = client.cookies["panel_csrf"]
    headers = {"X-CSRF-Token": csrf}
    await client.post("/api/users", json={"username": "alice"}, headers=headers)
    await client.post("/api/naive/users", json={"username": "alice"}, headers=headers)
    await client.post("/api/naive/users", json={"username": "bob"}, headers=headers)
    await client.post("/api/mieru/users", json={"username": "alice", "quotas": [], "expected_revision": mieru.revision}, headers=headers)
    return headers


async def test_inventory_proposes_same_username_only_as_a_hint(client, login_user, telemt, naive, mieru):
    await _seed(client, login_user, telemt, naive, mieru)
    inventory = await client.get("/api/clients/import/inventory")
    assert inventory.status_code == 200
    proposals = inventory.json()["proposals"]
    alice = [p for p in proposals if p["display_name"] == "alice"]
    assert len(alice) == 3 and all(p["same_username_hint"] for p in alice)
    assert [p for p in proposals if p["display_name"] == "bob"][0]["same_username_hint"] is False


async def test_import_is_idempotent_and_makes_no_manager_mutation(client, login_user, telemt, naive, mieru):
    headers = await _seed(client, login_user, telemt, naive, mieru)
    before = (copy.deepcopy(telemt.users), copy.deepcopy(naive.users), copy.deepcopy(mieru.users))
    decisions = [
        {"display_name": "Alice phone", "client_id": None, "items": [["mtproxy", "alice"], ["naive", "alice"]]},
        {"display_name": "Alice mieru", "client_id": None, "items": [["mieru", "alice"]]},
        {"display_name": "bob", "client_id": None, "items": [["naive", "bob"]]},
    ]
    first = await client.post("/api/clients/import", json={"decisions": decisions}, headers=headers)
    assert first.status_code == 200 and first.json()["created_grants"] == 4
    second = await client.post("/api/clients/import", json={"decisions": decisions}, headers=headers)
    assert second.json()["created_grants"] == 0 and len(second.json()["skipped"]) == 4
    assert (telemt.users, naive.users, mieru.users) == before
    listed = (await client.get("/api/clients")).json()["items"]
    assert {item["client"]["display_name"] for item in listed} == {"Alice phone", "Alice mieru", "bob"}
    grant = [g for item in listed for g in item["grants"] if g["protocol"] == "mieru"][0]
    assert grant["node_id"] == "local" and grant["origin"] == "imported" and grant["secret_ref"] is None


async def test_import_rejects_unknown_runtime_user_and_needs_admin(client, login_user, telemt, naive, mieru):
    headers = await _seed(client, login_user, telemt, naive, mieru)
    bad = await client.post("/api/clients/import", json={"decisions": [{"display_name": "x", "client_id": None, "items": [["naive", "ghost"]]}]}, headers=headers)
    assert bad.status_code == 422
    app = client._transport.app
    app.state.store.create_admin("viewer", "viewer-password-123", "viewer")
    await login_user(client, "viewer", "viewer-password-123")
    assert (await client.get("/api/clients/import/inventory")).status_code == 403


async def test_node_with_active_grants_cannot_be_disabled(client, login_user, telemt, naive, mieru):
    headers = await _seed(client, login_user, telemt, naive, mieru)
    await client.post("/api/nodes", json={"node_id": "edge-01", "display_name": "Edge"}, headers=headers)
    app = client._transport.app
    with app.state.database.transaction() as db:
        db.execute("INSERT INTO clients VALUES('c1','c','active','{}',1,1)")
        db.execute("""INSERT INTO access_grants(id,client_id,protocol,node_id,endpoint_id,runtime_username,desired_state,origin,created_at,updated_at)
                      VALUES('g1','c1','naive','edge-01','default','remote-user','enabled','imported',1,1)""")
    assert (await client.post("/api/nodes/edge-01/disable", headers=headers)).status_code == 409
```

- [x] **Step 2: Запустить — падает** (выполнено 2026-09-10)

Факт: `6 failed` — все шесть по ожидаемым причинам (404 на новых маршрутах и `200`
вместо `409` на отключении узла с грантами).

- [x] **Step 3: Реализовать importer, routes, guard** (выполнено 2026-09-10 с шестью уточнениями)

Факт:

1. **`context.roles()` не годится для GET.** Он зависит от `context.mutation`, то есть
   требует `X-CSRF-Token`, которого у чтения нет: `GET /api/clients/import/inventory`
   возвращал `403` даже владельцу. Добавлен `RequestContext.read_roles(*allowed)` —
   ролевой гейт поверх `current`, без CSRF. CSRF защищает изменения; инвентаризация
   ничего не меняет.
2. **Telemt не отдаёт `expiration` числом.** В его выдаче `expiration_rfc3339` — строка,
   а `MtproxyOptions.expiration` — эпоха. Импортированный grant остаётся без
   expiration, пока оператор не задаст его сам; выдумывать разбор даты здесь нечего.
3. `ClientService` получил `new_client(db, display_name, *, now)` и публичный
   `notify(db, client_id)`: импорт складывает несколько клиентов и грантов в **одну**
   транзакцию, а сервисный `create_client` открыл бы свою на каждого.
4. **Повторный импорт не создаёт пустых клиентов.** Если все элементы решения уже
   импортированы, клиент не создаётся вовсе (`created_clients == 0`), иначе каждый
   повторный запуск плодил бы двойников с нулём доступов.
5. Guard вынесен в `NodeRegistry.active_grants(db, node_id)` обычным SQL, чтобы
   `panel/nodes` не начал зависеть от `panel/clients`: слои остаются как в ADR 001.
6. Менеджер, который не отвечает, не блокирует импорт: `_read` ловит
   `TelemtError/NaiveError/MieruError` и даёт пустой список — иначе один упавший
   менеджер запретил бы принять пользователей двух остальных.

Мобильный жгут: карточка клиента исключена из пользовательского цикла
(`.data-row:not(.node-card):not(.client-card)`) и получила свою проверку стекинга.
Канарейка здесь потребовала третьей попытки и дала отдельный урок: `display:block` на
списке чипов и отрицательный `margin-top` на действиях **не** ломают раскладку (grid
пересчитывает строку), а `min-width:900px` на чипе роняет тест раньше — на ширине
вьюпорта. Настоящую проверку перекрытия ловит только сдвиг вне потока
(`position:relative; top:-80px`) → `['client actions overlap grants']`.


`inventory()`: `telemt.list_users()` → `mtproxy` items (`enabled`, options из `safe_user`-полей: квота/лимиты); `naive.list_users()` → `quota_bytes`, `enabled`; `mieru.list_users()` → `quotas`; `imported_grant_id` через `ClientStore.find_grant(db, protocol, "local", "default", username)`. Никаких вызовов `create/rotate/delete/set_*`. `confirm()` — одна транзакция на всё решение: для каждого `ImportDecision` создать/взять клиента, для каждого item — если уже импортирован → в `skipped`, иначе `insert_grant(origin="imported", node_id="local", desired_state = "enabled" if enabled else "disabled", observed_state = "enabled"/"disabled", options из inventory, `share_template` для Mieru — пусто до ротации)`; неизвестный item → `ValueError` → 422; audit `client.import`. Decommission guard в `NodeLifecycleService.set_disabled`: `SELECT count(*) FROM access_grants WHERE node_id=? AND desired_state<>'deleted'` > 0 → `NodeConflict("node has active grants")`.

UI: `clients.js` — список карточек клиентов (identity: display_name/state, chips grants `protocol · username · состояние`, кнопки «Приостановить/Возобновить», «Архивировать»); диалог `#client-modal` (display_name); диалог `#client-import-modal` — таблица предложений с чекбоксами и селектором «в нового клиента / в существующего», подсказка «одинаковое имя — только подсказка, объединения по умолчанию нет». Grant без `secret_ref` получает пометку «нет сохранённого секрета — не попадает в подписку» и кнопку «Принять доступ»; для Mieru кнопка открывает подтверждение «старый `mierus://` перестанет работать» и поддерживает массовое «Принять все Mieru-доступы» (решение 5 раздела 5). Сами вызовы идут в `ClientService.adopt_credential` из Task 12d: до его появления кнопка рендерится `disabled` с подсказкой «доступно после Task 12d», а не врёт. В `main.js`: `TITLES.clients = ["Клиенты", "Абоненты и их доступы MTProxy · Naive · Mieru"]`, `RENDERERS.clients = renderClients`, `#add` → `openClientModal`, кнопка «Импорт» внутри вида. Nav: `<button class="nav-item" data-view="clients">…Клиенты…</button>` после «Обзор» в sidebar и mobile-nav; в `test_mobile_layout.py:231` добавить `"Клиенты"` в список кнопок.

Контракт в `tests/test_deploy.py`: `clients.js` содержит `'/api/clients/import/inventory'`, `'same_username_hint'`, текст «только подсказка»; `index.html` содержит `id="client-import-modal"` и `data-view="clients"` дважды (sidebar + mobile).

- [x] **Step 4: Прогнать и полный уровень** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_client_import.py panel/tests/test_clients_domain.py panel/tests/test_node_routes.py panel/tests/test_mobile_layout.py && scripts/dev/remote-gate.sh full`
Факт: целевые — `6 passed` по импорту, `8 passed` вместе с мобильным гейтом; новый
контракт `tests/test_deploy.py::…test_clients_view_imports_read_only…` — `OK`.
Полный уровень с первого прогона: `REMOTE_GATE_FULL_OK`, весь набор —
`1348 passed, 2 skipped` (после Task 10 было `1341 passed, 2 skipped`).

- [ ] **Step 5: Коммит** (ждёт разрешения владельца)

Файлов больше, чем в черновике: добавились схемы, `read_roles`, стили, регистр
`nodes/registry.py` и документация экрана.

```bash
git add panel/clients/importer.py panel/clients/service.py panel/client_routes.py panel/schemas.py \
        panel/web_context.py panel/nodes/registry.py panel/nodes/service.py panel/app.py \
        panel/static/js/clients.js panel/static/js/main.js panel/static/js/state.js \
        panel/static/index.html panel/static/style.css \
        panel/tests/test_client_import.py panel/tests/test_mobile_layout.py tests/test_deploy.py \
        PANEL.ru.md PANEL.en.md
git commit -m "feat: добавить безопасный импорт существующих клиентов"
```

---

### Task 12a: Naive manager — `operation_id` и caller-supplied credential

**Files:**
- Modify: `naive_manager/service.py:345-373,561-633` (`create`, `rotate`, `_read_state`)
- Modify: `naive_manager/server.py:153-178` (`password`, `operation_id` в body)
- Modify: `panel/naive.py:62-67,153-163` (`NaiveClient.create/rotate`, `MemoryNaive` replay и `faults`)
- Modify: `tests/test_naive_manager.py` (новые тесты, фикстура `manager`)
- Modify: `panel/tests/test_naive_management.py` (Memory-контракт)

**Interfaces:**
- Produces (manager HTTP): `POST /v1/users` body `{username, quota_bytes?, password?, operation_id?}`; `POST /v1/users/{u}/rotate` body `{password?, operation_id?}`; ответ как раньше (`reveal`-dict) плюс `"replayed": true` при повторе; `409 {"code":"operation_conflict"}` если `operation_id` уже использован с другим `username`/операцией.
- Produces (`NaiveCredentialManager`): `create(username, quota_bytes=None, *, password=None, operation_id=None)`, `rotate(username, *, password=None, operation_id=None)`; state gains `"operations": {operation_id: {"operation","username","completed_at","result"}}` (хранится вместе с users в том же атомарном `users.json`, обрезка записей старше 7 дней при чтении).
- Produces (`panel.naive.NaiveClient`): `create(username, quota_bytes=None, *, password=None, operation_id=None)`, `rotate(username, *, password=None, operation_id=None)`; `MemoryNaive.faults: dict[str, str]` — `faults["create"] = "lose_response"` выполняет мутацию, затем бросает `NaiveError` (эмуляция потери ответа после commit).
- Правило пароля: `^[A-Za-z0-9_.~-]{16,128}$`.

- [x] **Step 1: Падающие тесты manager'а (`tests/test_naive_manager.py`)** (выполнено 2026-09-10 с двумя поправками)

Факт:

1. **`manager` — не фикстура.** В `tests/test_naive_manager.py` это обычная
   функция-хелпер `manager(tmp_path, hooks)`; каждый тест сам делает
   `hooks = Hooks()` и `service.bootstrap()`. Тесты черновика с параметром `manager`
   падали на `fixture 'manager' not found`. Добавлен локальный хелпер `_bootstrapped`.
2. **`bootstrap()` импортирует двух пользователей** из образцового `CADDY`
   (`old-user`, `second`), поэтому `list_users()` после `create("phone")` — это
   `["old-user", "second", "phone"]`, а не `["phone"]`.

Сверх черновика добавлены: запись операции пишется тем же атомарным файлом, что и
состояние, которое она описывает; записи старше окна хранения вычищаются при чтении;
`replayed` отсутствует в ответе на первый вызов; `code == "operation_conflict"`.


```python
def test_create_with_operation_id_replays_the_same_credential(manager):
    first = manager.create("phone", None, password="caller-supplied-password-01", operation_id="op-1")
    second = manager.create("phone", None, password="caller-supplied-password-01", operation_id="op-1")
    assert first["proxy_url"] == second["proxy_url"] and second["replayed"] is True
    assert [u["username"] for u in manager.list_users()] == ["phone"]


def test_operation_id_reused_for_another_username_conflicts(manager):
    manager.create("phone", None, password="caller-supplied-password-01", operation_id="op-1")
    with pytest.raises(ManagerConflict, match="operation"):
        manager.create("laptop", None, password="caller-supplied-password-02", operation_id="op-1")


def test_caller_supplied_password_is_used_verbatim_and_validated(manager):
    revealed = manager.create("phone", None, password="caller-supplied-password-01")
    assert "caller-supplied-password-01" in revealed["proxy_url"]
    with pytest.raises(ValueError):
        manager.create("laptop", None, password="short")


def test_rotate_with_operation_id_is_replayable(manager):
    manager.create("phone", None)
    first = manager.rotate("phone", password="rotated-password-0123456", operation_id="op-2")
    second = manager.rotate("phone", password="rotated-password-0123456", operation_id="op-2")
    assert first["proxy_url"] == second["proxy_url"]


def test_legacy_state_without_operations_map_is_upgraded_on_read(manager):
    state = manager._read_state()
    state.pop("operations", None)
    manager._apply(state)
    assert manager._read_state()["operations"] == {}
```

Проверить в начале файла точный способ конструирования фикстуры `manager` (`Hooks`, Caddy-заглушки) и имена импортов `ManagerConflict`; тесты используют только публичные методы плюс `_read_state/_apply`, как существующие.

- [x] **Step 2: Запустить — падает** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick tests/test_naive_manager.py` → FAIL
`TypeError: NaiveCredentialManager.create() got an unexpected keyword argument 'password'` (7 тестов).
Факт: `-k "a or b"` через `remote-gate.sh quick` не работает — скрипт передаёт `$*` в
удалённую оболочку, кавычки теряются, и pytest получает `or` как путь. Передавайте
файл целиком или однословный `-k`.

- [x] **Step 3: Реализовать** (выполнено 2026-09-10 с тремя уточнениями)

Факт:

1. `reveal()` помечен `@synchronized` и читает состояние из файла, поэтому результат
   операции им не построить до `_apply`. Выделен чистый `_reveal_row(row)`; публичный
   `reveal()` теперь просто зовёт его. Так результат и состояние ложатся одним
   атомарным файлом, как и требовалось.
2. `_read_state` отвергает неизвестные ключи верхнего уровня
   (`set(state) - {...}`), поэтому `operations` пришлось внести в этот набор явно —
   иначе первая же запись сделала бы состояние «unsupported».
3. Обрезка вынесена в `_pruned_operations`, которая заодно валидирует форму записи:
   чужой ключ или битый `completed_at` — это `ManagerConflict("invalid operation
   state")`, а не молчаливое игнорирование.

Канарейка: `_replay`, возвращающий `None` всегда, роняет три теста
(`KeyError: 'replayed'` и «user already exists») — проверка кусается.

`MemoryNaive` получил `operations`, `faults` и тот же replay: без этого панельные
тесты доказывали бы безопасность повтора на фейке, который не умеет терять ответ.


В `create`/`rotate`: после `state = self._read_state()` — `replay = self._replay(state, operation_id, "create", username)`; если не `None` → вернуть `{**replay, "replayed": True}`. `_replay` бросает `ManagerConflict("operation id already used for another request", code="operation_conflict")`, если запись есть, но `operation`/`username` отличаются. Пароль: `password or secrets.token_urlsafe(18)`; при `password` — `re.fullmatch(PASSWORD_RE, password)` иначе `ValueError`. После построения `desired` и до `_apply`: `desired["operations"][operation_id] = {"operation": "create", "username": username, "completed_at": _now(), "result": <reveal for desired state>}` — reveal считается из `desired`, а не после `_apply`, чтобы результат и state записались одним атомарным файлом. `_read_state`: `state.setdefault("operations", {})` + обрезка по `completed_at` старше 7 дней; `_assert_consistent` допускает ключ. `server.py`: передать `body.get("password")`, `body.get("operation_id")` (валидация типов: str или None, `operation_id` ≤ 128 символов `[A-Za-z0-9_:.-]`). `panel/naive.py`: `NaiveClient.create(...)` → `{"username", "quota_bytes", "password", "operation_id"}` (пропуская None); `MemoryNaive` — тот же replay/faults.

- [x] **Step 4: Прогнать manager и панельные тесты** (выполнено 2026-09-10)

Run: `scripts/dev/remote-gate.sh quick tests/test_naive_manager.py panel/tests/test_naive_management.py panel/tests/test_users_adapter_ui.py`
Факт: `117 passed, 1 skipped`.

- [x] **Step 5: Собрать образ manager'а и compose-уровень** (выполнено 2026-09-10; коммит — по разрешению владельца)

Run: `scripts/dev/remote-gate.sh compose && scripts/dev/remote-gate.sh full`
Факт: `REMOTE_GATE_COMPOSE_OK`, затем `REMOTE_GATE_FULL_OK`; весь набор —
`1356 passed, 2 skipped` (после Task 11 было `1348 passed, 2 skipped`).

```bash
git add naive_manager/service.py naive_manager/server.py panel/naive.py \
        tests/test_naive_manager.py panel/tests/test_naive_management.py
git commit -m "feat: сделать создание и ротацию Naive идемпотентными по operation_id"
```

---

### Task 12b: Mieru manager — `operation_id` и caller-supplied credential

**Files:**
- Modify: `mieru_manager/service.py:1098-1200,745-771` (`create_user`, `rotate_user`, `_state`)
- Modify: `mieru_manager/server.py:112-129,161-173`
- Modify: `panel/mieru.py:75-100,132-162` (`MieruClient.create/rotate`, `MemoryMieru`)
- Modify: `tests/test_mieru_manager.py`, `panel/tests/test_mieru_management.py`

**Interfaces:**
- Produces (manager HTTP): `POST /v1/users` optional `password`, `operation_id`; `POST /v1/users/{u}/rotate` body `{expected_revision, password?, operation_id?}`. Повтор с тем же `operation_id`: если пароль был caller-supplied → `200 {"username","revision":<исходная>,"replayed":true}` **без** `share_url` (журнал не хранит секретов; панель восстанавливает ссылку из своего escrow); если пароль генерировал manager → `409 {"code":"result_unrecoverable"}` — панель обязана ротировать.
- Produces (`MieruManager`): `create_user(..., password=None, operation_id=None)`, `rotate_user(..., password=None, operation_id=None)`; journal state gains `"operations": {operation_id: {"operation","username","revision","completed_at","credential_origin":"caller|manager"}}` (без паролей и ссылок).
- Produces (`panel.mieru.MieruClient`): `create(payload)` принимает `password`/`operation_id` в payload; новый `rotate(username, revision, *, password=None, operation_id=None)`; `MemoryMieru.faults` как у Naive.
- Правило пароля: как в 12a.

- [x] **Step 1: Падающие тесты** (выполнено 2026-09-10 с тремя поправками)

Факт. Та же ошибка черновика, что и в 12a, плюс две своих:

1. `manager(tmp_path, mita)` — функция-хелпер, не фикстура; ревизия берётся из
   `service.bootstrap()["revision"]`.
2. `list_users()` отдаёт ключ `username`, а не `name`.
3. В образцовом `BASE` уже есть `alice`, поэтому после создания `phone` список —
   `["alice", "phone"]`.

Сверх черновика: конфликт `operation_id` для другого запроса, валидация
caller-пароля и обрезка записей старше окна хранения; проверка, что
`credential_origin` и `revision` действительно записаны.


```python
def test_create_with_caller_password_and_operation_id_replays_without_secret(manager):
    revision = manager.inspect()["revision"]
    first = manager.create_user("phone", [], expected_revision=revision, password="caller-supplied-password-01", operation_id="op-1")
    assert "caller-supplied-password-01" in first["share_url"]
    second = manager.create_user("phone", [], expected_revision="stale", password="caller-supplied-password-01", operation_id="op-1")
    assert second == {"username": "phone", "revision": first["revision"], "replayed": True}
    assert [u["name"] for u in manager.list_users()] == ["phone"]
    journal = manager._state()
    assert "caller-supplied-password-01" not in json.dumps(journal) and "share_url" not in json.dumps(journal)


def test_manager_generated_credential_cannot_be_replayed(manager):
    revision = manager.inspect()["revision"]
    manager.create_user("phone", [], expected_revision=revision, operation_id="op-2")
    with pytest.raises(ConfigConflict, match="unrecoverable"):
        manager.create_user("phone", [], expected_revision="stale", operation_id="op-2")


def test_rotate_with_operation_id_is_replayable(manager):
    revision = manager.inspect()["revision"]
    created = manager.create_user("phone", [], expected_revision=revision)
    first = manager.rotate_user("phone", expected_revision=created["revision"], password="rotated-password-0123456", operation_id="op-3")
    second = manager.rotate_user("phone", expected_revision="stale", password="rotated-password-0123456", operation_id="op-3")
    assert second["revision"] == first["revision"] and second["replayed"] is True
```

Перед написанием проверить, как `inspect()` отдаёт revision и как тесты файла получают `manager` (строки 1–120 `tests/test_mieru_manager.py`).

- [x] **Step 2: Запустить — падает** (выполнено 2026-09-10)

Факт: `5 failed` — `TypeError: MieruManager.create_user() got an unexpected keyword argument 'password'`.

- [x] **Step 3: Реализовать** (выполнено 2026-09-10 с тремя уточнениями)

Факт:

1. **Replay обязан идти до `_check_revision`.** Повтор приходит со *старой* ревизией —
   именно в этом смысл потерянного ответа, — поэтому compare-and-swap отверг бы его
   раньше, чем журнал успел бы ответить. В тестах это зафиксировано явным
   `expected_revision="stale"`.
2. **Запись операции пишет `_transaction`, а не вызывающий.** Ревизия появляется
   только внутри транзакции, и запись должна лечь тем же атомарным `_atomic(state)`.
   Иначе падение между двумя записями оставило бы replay, указывающий на ревизию,
   которой не было. Реализовано параметром `operation_record=(operation_id, entry)`.
3. **`_state()` сверял набор ключей на точное равенство.** Заменено на
   «обязательные ⊆ set(state) ⊆ обязательные ∪ {operations}», поэтому состояние,
   записанное прежним manager'ом, читается без миграции.

Журнал намеренно не хранит ни пароля, ни `share_url`: для manager-generated
credential честный ответ на повтор — `409 result_unrecoverable`, панель обязана
ротировать. Канарейка (убрана проверка `credential_origin`) роняет
`test_manager_generated_credential_cannot_be_replayed`.

`MieruError` получил поле `code`; `MieruClient.rotate(username, revision, *, password,
operation_id)` — новый явный метод (прежний `operation(...)` остаётся для
enable/disable). `MemoryMieru` получил `operations`, `faults` и ту же семантику.


В `create_user`/`rotate_user` внутри `with self._writer()`: `state = self._state()`; `entry = state.get("operations", {}).get(operation_id)`; если есть и совпадает `(operation, username)`: `credential_origin == "caller"` → вернуть `{"username", "revision": entry["revision"], "replayed": True}`, иначе `raise ConfigConflict("operation result is unrecoverable; rotate the credential")`; если есть, но не совпадает → `ConfigConflict("operation id already used")`. Иначе — обычный путь с `raw = password or secrets.token_urlsafe(24)`; перед `_transaction` положить в `state["operations"][operation_id] = {..., "revision": None}` и после получения `revision` дописать — **нет**: `_transaction` записывает journal атомарно вместе с config, значит запись должна быть в `state` до вызова; `_transaction` возвращает новый revision, который известен только после. Решение: `_transaction` принимает `operation_record=(operation_id, entry)` и сам вписывает `entry["revision"] = new_revision` в journal перед атомарной записью (`mieru_manager/service.py:878-961`). `_state()` — `setdefault("operations", {})`, обрезка > 7 дней. `server.py`: optional поля в `_exact`. `panel/mieru.py`: `rotate(...)` → `POST /v1/users/{u}/rotate` с `{expected_revision, password?, operation_id?}`; `MemoryMieru` — replay/faults.

- [x] **Step 4: Прогнать** (выполнено 2026-09-10; коммит — по разрешению владельца)

Run: `scripts/dev/remote-gate.sh quick tests/test_mieru_manager.py panel/tests/test_mieru_management.py tests/test_mieru_deployment.py && scripts/dev/remote-gate.sh compose && scripts/dev/remote-gate.sh full`
Факт: целевые — `116 passed`; `REMOTE_GATE_COMPOSE_OK`; после правки WAL —
`REMOTE_GATE_FULL_OK`, весь набор `1365 passed, 2 skipped` (после Task 12a было
`1356 passed, 2 skipped`).

**Полный прогон нашёл настоящий дефект, не относящийся к 12b** (см. раздел 0, пункт
«WAL под конкуренцией»): `panel/tests/test_migrations.py::
test_two_processes_initialising_concurrently_are_safe` упал с
`sqlite3.OperationalError: database is locked`. Отдельно тест проходил 8/8, но
стресс-прогон 300 попыток дал **15 падений (5%)**, все — на
`PRAGMA journal_mode=WAL` в `Database.__init__`. Причина: SQLite не применяет
busy-handler к смене journal-mode, и pragma отказывает сразу, пока файл держит другое
соединение. Панель и fleet-ingress открывают один `panel.sqlite3`, поэтому
одновременный холодный старт (например, после перезагрузки) убивал один из процессов.
Исправлено в `Database._enable_wal`: повтор с ожиданием до `timeout` и проверка
**возвращённого** режима — молчаливый отказ, оставляющий rollback-режим, тоже ловится.
Добавлены два детерминированных теста (удержание `BEGIN IMMEDIATE` из соседнего
потока) и `DatabaseError`. После правки стресс: **0 падений из 300**.

```bash
git add mieru_manager/service.py mieru_manager/server.py panel/mieru.py \
        tests/test_mieru_manager.py panel/tests/test_mieru_management.py \
        panel/database.py panel/tests/test_migrations.py
git commit -m "feat: сделать создание и ротацию Mieru идемпотентными по operation_id"
```

---

### Task 12c: Telemt — восстановление после потери ответа

**Files:**
- Modify: `panel/telemt.py:14-49,52-101` (`TelemtClient` — типизированная `TelemtIndeterminate(TelemtError)` для timeout/connection после отправки; `MemoryTelemt.faults`)
- Create: `panel/protocols/__init__.py`, `panel/protocols/base.py` (типы из Task 12d, нужные здесь: `CredentialPlan`, `AppliedGrant`)
- Modify: `panel/tests/test_users_adapter_ui.py` (или новый `panel/tests/test_telemt_recovery.py`)

**Interfaces:**
- Produces: `panel.telemt.TelemtIndeterminate(TelemtError)` — поднимается для `httpx.TimeoutException`/`httpx.TransportError` после отправки запроса; `TelemtClient.current_access(username) -> dict | None` (из `list_users`, возвращает `{"link","secret"}` через `secret_reveal` из `telemt_routes`); `MemoryTelemt.faults`.
- Правило: после `TelemtIndeterminate` на `create_user`/`rotate` адаптер читает `current_access(username)`: пользователь есть → его текущая ссылка и есть credential (escrow); нет → повторный `create_user` безопасен.

- [x] **Step 1: Падающие тесты** (выполнено 2026-09-10, новый файл `panel/tests/test_telemt_recovery.py`)

Факт. Утверждение черновика `access["secret"] != created["secret"]` проходило бы по
неверной причине: `secret_reveal` для строки из `list_users` вернул бы `secret=None`,
а `None != "<hex>"` истинно всегда. Заменено на проверку по факту: секрет берётся из
самой ссылки (`?secret=`), и после потерянной ротации ссылка обязана отличаться от
выданной при создании. Добавлены два теста сверх черновика: соединение, которое не
установилось, — **не** indeterminate; `current_access` читает живую ссылку из
`/v1/users` (проверено на `httpx.MockTransport`, а не на фейке).

```python
async def test_lost_create_response_is_recovered_by_reading_the_current_link(telemt):
    telemt.faults["create_user"] = "lose_response"
    with pytest.raises(TelemtIndeterminate):
        await telemt.create_user("alice")
    access = await telemt.current_access("alice")
    assert access is not None and access["link"].startswith("tg://proxy")
    assert await telemt.current_access("nobody") is None


async def test_lost_rotate_response_yields_the_new_secret_not_the_old(telemt):
    created = await telemt.create_user("alice")
    telemt.faults["rotate"] = "lose_response"
    with pytest.raises(TelemtIndeterminate):
        await telemt.rotate("alice")
    access = await telemt.current_access("alice")
    assert access["secret"] != created["secret"]
```

- [x] **Step 2: Запустить — падает**; **Step 3: реализовать** (выполнено 2026-09-10 с тремя уточнениями)

Факт:

1. **Не всякий транспортный сбой — indeterminate.** `httpx.ConnectError` и
   `ConnectTimeout` означают, что соединение не установилось и запрос не ушёл:
   это обычная ошибка, повтор безопасен. `TelemtIndeterminate` поднимается только для
   остальных `TimeoutException`/`TransportError`, то есть когда запрос уже отправлен.
   Слепое правило черновика заставляло бы делать восстановительное чтение там, где
   ничего не произошло.
2. **`secret_reveal` для восстановления не годится.** Он живёт в `telemt_routes`,
   тянет FastAPI и считает QR. В `panel/telemt.py` добавлена чистая
   `access_from_user(row)`: ссылка из `links.tls[0]` и секрет из её query. Что
   реальный Telemt отдаёт `links` в `/v1/users`, подтверждается работающим маршрутом
   `/api/users/{username}/access` (`telemt_routes.py:405`), а не предположением.
3. **Найден дефект фейка.** `MemoryTelemt.create_user` возвращал живую ссылку на
   внутренний dict, поэтому позднейшая ротация задним числом переписывала уже
   выданный ответ — тест поймал это на своём же утверждении. Реальный HTTP-ответ —
   снимок; `create_user`, `rotate` и `set_enabled` теперь отдают `deepcopy`.

`panel/protocols/base.py` здесь **не** создаётся: `CredentialPlan`/`AppliedGrant`
нужны Task 12d, и вводить их заранее — пустая абстракция. Канарейка (убрана
классификация indeterminate) роняет `test_a_reply_lost_after_the_request_was_sent…`.
 (`_request`: `except (httpx.TimeoutException, httpx.TransportError) as exc: raise TelemtIndeterminate(...)`; `current_access` — `next((secret_reveal(u) for u in await self.list_users() if u.get("username")==username), None)`; `MemoryTelemt.faults` — выполнить операцию, затем `raise TelemtIndeterminate("simulated loss")`).

- [x] **Step 4: Прогнать** (выполнено 2026-09-10; коммит — по разрешению владельца)

Факт: целевые — `5 passed`; вместе с `test_users_adapter_ui.py` и
`test_vnext_characterization.py` — `41 passed`; `REMOTE_GATE_FULL_OK`, весь набор —
`1370 passed, 2 skipped` (после Task 12b было `1365 passed, 2 skipped`).

```bash
git add panel/telemt.py panel/tests/test_telemt_recovery.py
git commit -m "feat: различать потерянный ответ Telemt и восстанавливать доступ чтением"
```

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_telemt_recovery.py panel/tests/test_users_adapter_ui.py`

```bash
git add panel/telemt.py panel/protocols panel/tests/test_telemt_recovery.py
git commit -m "feat: восстанавливать доступ Telemt после потери ответа"
```

---

### Task 12d: Единый `ProtocolAdapter` и три адаптера

**Files:**
- Modify: `panel/protocols/base.py`
- Create: `panel/protocols/telemt.py`, `panel/protocols/naive.py`, `panel/protocols/mieru.py`
- Create: `panel/tests/test_protocol_adapter_contract.py`
- Modify: `panel/app.py` (`app.state.adapters = {"mtproxy": TelemtAdapter(...), "naive": NaiveAdapter(...), "mieru": MieruAdapter(...)}`)
- Modify: `panel/clients/service.py` (`capture_credential(grant_id, …)`, `adopt_credential(grant_id, *, allow_rotation, …)`)

**Interfaces (`panel.protocols.base`):**

```python
class ProtocolAdapter(Protocol):
    protocol: str
    credential_origin: Literal["caller", "manager"]     # naive/mieru: caller; mtproxy: manager
    capture_supported: bool                             # mtproxy True, naive True, mieru False
    async def discover(self) -> ObservedInventory: ...
    async def preflight(self, intent: GrantIntent) -> Preflight: ...
    async def create(self, operation_id: str, intent: GrantIntent, credential: CredentialPlan) -> AppliedGrant: ...
    async def enable(self, grant: GrantRef) -> AppliedGrant: ...
    async def disable(self, grant: GrantRef) -> AppliedGrant: ...
    async def rotate(self, operation_id: str, grant: GrantRef, credential: CredentialPlan) -> AppliedGrant: ...
    async def delete(self, grant: GrantRef) -> None: ...
    async def capture(self, grant: GrantRef) -> bytes | None: ...   # текущий credential без ротации, если протокол умеет
    def render_artifacts(self, grant: AccessGrant, credential: bytes, *, public_host: str) -> list[AccessArtifact]: ...

@dataclass(frozen=True)
class ObservedGrant: runtime_username: str; enabled: bool; options: dict; revision: str | None
@dataclass(frozen=True)
class ObservedInventory: items: tuple[ObservedGrant, ...]
@dataclass(frozen=True)
class Preflight: ok: bool; reason: str | None
@dataclass(frozen=True)
class CredentialPlan: origin: Literal["caller", "manager"]; plaintext: bytes | None   # caller → plaintext обязателен
@dataclass(frozen=True)
class AppliedGrant: runtime_username: str; enabled: bool; credential: bytes | None; revision: str | None; artifact_template: dict; recovered: bool = False
@dataclass(frozen=True)
class GrantRef: protocol: str; runtime_username: str; revision: str | None
@dataclass(frozen=True)
class AccessArtifact: kind: str; label: str; media_type: str; value: str; auto_refresh: Literal["supported", "unsupported"]
class AdapterError(RuntimeError): ...
class ManualInterventionRequired(AdapterError): ...
```

- `render_artifacts` переиспользует существующие builders: `telemt_routes.secret_reveal/proxy_link`, `naive_routes.naive_reveal`-логику (вынести в `panel/protocols/naive.py::artifacts(username, password, host)`), `mieru_routes.mieru_access` (для `mierus://` по `share_template`).
- `create` для `credential_origin="manager"` (Telemt): при `TelemtIndeterminate` → `current_access` → `AppliedGrant(recovered=True, credential=secret)`; для caller-протоколов: при `NaiveError/MieruError` после отправки → повтор с тем же `operation_id` (replay); если replay отвечает `result_unrecoverable` → `ManualInterventionRequired`.

- [x] **Step 1: Падающие контрактные тесты** (выполнено 2026-09-10 с тремя поправками)

Факт:

1. `MemoryTelemt` не имел `faults`; ключ приведён к общему `"create"` во всех трёх
   фейках, как и требовал план.
2. Фикстура черновика строила адаптеры от фикстур `telemt/naive/mieru` из общего
   `conftest`; здесь нужен свой набор бэкендов на тест, иначе параметризация делит
   состояние между тремя прогонами.
3. `"lose_response_and_forget"` не понадобился: для manager-generated пароля Mieru и
   так отвечает `result_unrecoverable`, поэтому обычного `"lose_response"` достаточно,
   и лишний режим отказа в фейке не заводится.


```python
@pytest.fixture(params=["mtproxy", "naive", "mieru"])
async def adapter(request, telemt, naive, mieru):
    from panel.protocols.mieru import MieruAdapter
    from panel.protocols.naive import NaiveAdapter
    from panel.protocols.telemt import TelemtAdapter
    return {"mtproxy": TelemtAdapter(telemt, public_host="proxy.example.com"),
            "naive": NaiveAdapter(naive, public_host="naive.example.com"),
            "mieru": MieruAdapter(mieru, public_host="mieru.example.com")}[request.param]


def _intent(protocol, username="alice"):
    options = {"mtproxy": MtproxyOptions(), "naive": NaiveOptions(quota_bytes=None), "mieru": MieruOptions(quotas=[], share_template=None)}[protocol]
    return GrantIntent(protocol=protocol, runtime_username=username, options=options)


def _credential(adapter):
    return CredentialPlan("manager", None) if adapter.credential_origin == "manager" else CredentialPlan("caller", b"caller-supplied-password-01")


async def test_full_lifecycle_matches_the_capability_matrix(adapter):
    assert (await adapter.preflight(_intent(adapter.protocol))).ok
    applied = await adapter.create("op-1", _intent(adapter.protocol), _credential(adapter))
    assert applied.enabled and applied.credential
    ref = GrantRef(adapter.protocol, "alice", applied.revision)
    disabled = await adapter.disable(ref)
    assert disabled.enabled is False
    enabled = await adapter.enable(GrantRef(adapter.protocol, "alice", disabled.revision))
    rotated = await adapter.rotate("op-2", GrantRef(adapter.protocol, "alice", enabled.revision), _credential(adapter) if adapter.credential_origin == "manager" else CredentialPlan("caller", b"rotated-password-0123456"))
    assert rotated.credential != applied.credential
    captured = await adapter.capture(GrantRef(adapter.protocol, "alice", rotated.revision))
    assert (captured == rotated.credential) if adapter.capture_supported else (captured is None)
    artifacts = adapter.render_artifacts(_grant_row(adapter.protocol, applied), rotated.credential, public_host="x.example.com")
    assert artifacts and all(rotated.credential.decode() in a.value or a.kind == "instructions" for a in artifacts)
    await adapter.delete(GrantRef(adapter.protocol, "alice", rotated.revision))
    assert all(item.runtime_username != "alice" for item in (await adapter.discover()).items)


async def test_lost_response_after_manager_commit_never_creates_a_second_user(adapter, telemt, naive, mieru):
    backend = {"mtproxy": telemt, "naive": naive, "mieru": mieru}[adapter.protocol]
    backend.faults["create"] = "lose_response"
    applied = await adapter.create("op-1", _intent(adapter.protocol), _credential(adapter))
    assert applied.recovered is True and applied.credential
    inventory = await adapter.discover()
    assert [i.runtime_username for i in inventory.items].count("alice") == 1


async def test_manager_generated_mieru_credential_lost_twice_requires_manual_intervention(mieru):
    from panel.protocols.mieru import MieruAdapter
    adapter = MieruAdapter(mieru, public_host="mieru.example.com")
    mieru.faults["create"] = "lose_response_and_forget"   # replay answers result_unrecoverable
    with pytest.raises(ManualInterventionRequired):
        await adapter.create("op-1", _intent("mieru"), CredentialPlan("manager", None))
```

`_grant_row(protocol, applied)` — helper, собирающий `AccessGrant` с `options` из `applied.artifact_template` (для Mieru — `share_template`). Имена fault-ключей (`"create"`, `"rotate"`) зафиксировать одинаковыми во всех трёх Memory-классах (в 12a/12b/12c использовать `faults["create"]`, а не `faults["create_user"]`).

- [x] **Step 2: Запустить — падает**; **Step 3: реализовать адаптеры** (выполнено 2026-09-10 с тремя уточнениями)

Факт:

1. **Контрактный тест вскрыл неоднозначность credential у MTProxy.** Telemt отдаёт
   `secret` без префикса, а Fake-TLS-ссылка несёт `ee<secret>`. Если положить в escrow
   «сырое» поле, восстановить ссылку из него будет нельзя, и каждый отрендеренный
   артефакт окажется неверным. Адаптер всегда берёт секрет **из самой ссылки**
   (`_link_secret`), поэтому `create`, `rotate` и `capture` дают одно и то же значение.
2. **`public_host` для Telemt и Mieru не является конфигурацией панели.** В `Settings`
   есть только `naive_public_host`; хост MTProxy приходит внутри ссылки, хост Mieru —
   внутри `share_url`. Параметр сделан необязательным, а `create_app` передаёт хост
   только Naive-адаптеру.
3. `AdapterError`/`ManualInterventionRequired` разделены по смыслу: второй означает,
   что рантайм изменился, а credential невосстановим, — только тогда нужен человек.

Ещё раз про порядок: `MieruAdapter.capture_supported = False` — это не заглушка, а
факт: mita хранит хеш. Честный `None` лучше выдуманной ссылки.
 (тонкие обёртки над `TelemtClient/NaiveClient/MieruClient` с маппингом ошибок в `AdapterError`; Mieru хранит `revision` в `GrantRef`, `discover()` возвращает текущий revision в каждом item; `artifact_template` для Mieru — `share_template` из `share_url` с заменой имени/пароля на `{username}`/`{password}`).

- [x] **Step 4: `ClientService.capture_credential(...)`** (выполнено 2026-09-10)

Для grant без `secret_ref` и адаптера с `capture_supported`: `credential = await adapter.capture(ref)`; в одной транзакции — `secrets.store(db, secret_id=f"grant:{grant_id}", version=1, purpose="grant.credential", grant_id=grant_id, permitted_node_id=grant.node_id, plaintext=credential, state="active")`, `update_grant(secret_ref=…)`, audit `grant.credential.capture`. Для `capture_supported=False` → `ClientConflict("credential requires rotation")`. Тест в `test_protocol_adapter_contract.py`: capture для импортированного Naive/MTProxy grant заполняет `secret_ref` и не меняет менеджер; для Mieru → 409.

- [x] **Step 4a: `ClientService.adopt_credential(...)`** (выполнено 2026-09-10 с двумя уточнениями)

Факт:

1. **Аудит различает capture и adopt по существу.** Принятие Naive- или MTProxy-доступа
   — это `grant.credential.capture` с `rotated: false`, принятие Mieru —
   `grant.credential.adopt` с `rotated: true`. Тест сначала ожидал `.adopt` для Naive и
   был неправ: у капчурящегося протокола ссылка клиента не меняется, и запись об этом
   не должна выглядеть как ротация.
2. **Тестовое приложение получило keyring.** Без мастер-ключа escrow отключён, и
   принятие падало с `SecretError`. Ключ добавлен в общий `conftest`, а маршрут
   отображает `SecretError` в `409`: ротировать ссылку клиента и потом выбросить
   результат — худший из исходов.

`POST /api/clients/grants/{id}/adopt` и `POST /api/clients/grants/adopt-batch`
(owner/admin). Пакетное принятие не прячет отказы: они возвращаются в `refused`
вместе с уже принятыми. Канарейка (`allow_rotation` игнорируется) роняет
`test_mieru_cannot_be_captured_and_says_so`.


Единая точка «сделать импортированный доступ пригодным для подписки»: `capture_supported` → `capture_credential`; иначе, при `allow_rotation=True` → `adapter.rotate(ref)` под `operation_id`, сохранение нового credential тем же путём, `update_grant(secret_ref=…, share_template=…)`, audit `grant.credential.adopt` c полем `rotated: true` (без значения секрета). При `allow_rotation=False` и `capture_supported=False` → `ClientConflict("credential requires rotation")` — UI обязан сначала показать предупреждение. API: `POST /api/clients/grants/{id}/adopt {"allow_rotation": bool}` (owner/admin) и `POST /api/clients/grants/adopt-batch {"protocol": "mieru", "allow_rotation": true}` для массового принятия. Тесты: Mieru с `allow_rotation=False` → 409 и менеджер не тронут; c `True` → в mita ровно одна ротация, `secret_ref` заполнен, старый `share_template` заменён; повторный вызов на grant с `secret_ref` → 409 `already adopted`; в ответе и в аудите нет пароля (canary).

- [x] **Step 5: Прогнать** (выполнено 2026-09-10; коммит — по разрешению владельца)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_protocol_adapter_contract.py panel/tests/test_users_adapter_ui.py panel/tests/test_naive_management.py panel/tests/test_mieru_management.py && scripts/dev/remote-gate.sh full`
Факт: контракт адаптеров — `18 passed`; клиентские наборы вместе с мобильным гейтом —
`35 passed`; `REMOTE_GATE_FULL_OK`, весь набор — `1390 passed, 2 skipped` (после
Task 12c было `1370 passed, 2 skipped`).

```bash
git add panel/protocols panel/app.py panel/clients/service.py panel/client_routes.py \
        panel/schemas.py panel/static/js/clients.js panel/tests/conftest.py \
        panel/tests/test_protocol_adapter_contract.py panel/tests/test_client_import.py \
        tests/test_deploy.py PANEL.ru.md PANEL.en.md
git commit -m "refactor: унифицировать контракт protocol adapters"
```

---

### Task 13: Durable saga создания нескольких доступов

**Files:**
- Create: `panel/clients/provisioning.py`
- Create: `panel/tests/test_provisioning_saga.py`
- Modify: `panel/migrations.py` (миграция 7: `provisioning_operations`)
- Modify: `panel/client_routes.py` (`POST /api/clients/{id}/grants`, `GET /api/operations/{id}`, `POST /api/operations/{id}/resume`, `POST /api/operations/{id}/bundle`)
- Modify: `panel/static/js/clients.js`, `panel/static/index.html` (диалог `grant-modal`: чекбоксы протоколов, имя, квоты, срок; после успеха — one-time bundle через существующие access-диалоги)
- Modify: `panel/app.py` (`app.state.provisioning`), `panel/cli.py` (`operations-resume`)

**Interfaces:**
- Produces: `ProvisioningService(database, secrets, adapters, clients: ClientService, clock=time)` c `start(client_id, intents: list[GrantIntent], *, actor, ip, request_id) -> str` (валидирует все intents, `preflight` каждого адаптера, резервирует строки `access_grants` с `observed_state='pending'`, для caller-протоколов генерирует пароль `secrets.token_urlsafe(24)` и кладёт `SecretVersion(state='pending')`, пишет операцию `status='pending'` — всё одной транзакцией), `async run(operation_id) -> OperationResult(status: Literal["succeeded","compensated","manual_intervention_required"], steps: list[StepResult])` — resumable по журналу шагов, `status(operation_id)`, `bundle(operation_id, owner_session) -> reveal_token` (артефакты всех grants операции одним one-time reveal).
- Таблица: `provisioning_operations(operation_id TEXT PK, client_id TEXT NOT NULL REFERENCES clients(id), status TEXT CHECK(status IN ('pending','applying','succeeded','compensating','compensated','manual_intervention_required')), steps_json TEXT NOT NULL, created_at, updated_at)`; `steps_json = [{"grant_id","protocol","status": "pending|applied|escrowed|active|compensating|compensated|manual","error": str|null}]`.
- Инварианты: adapter `operation_id` шага = `f"{operation_id}:{grant_id}"`; после каждого перехода шага — отдельная транзакция (durable checkpoint); компенсация удаляет **только** grants, созданные этой операцией (`applied|escrowed|active` в её `steps_json`); pre-existing ресурсы никогда не удаляются; любой исход ∈ {succeeded, compensated, manual_intervention_required}.

- [x] **Step 1: Падающие fault-тесты** (выполнено 2026-09-10 с четырьмя поправками)

Факт:

1. **`start` пришлось сделать async.** Черновик объявил её синхронной, но preflight
   спрашивает рантайм, свободно ли имя, — а это await. Отказать до записи стоит
   ничего; отказать на середине означало бы компенсировать уже созданное. Тест
   `test_a_refused_preflight_reserves_nothing` фиксирует, что при отказе не
   появляется ни grant, ни строка операции, ни pending-секрет.
2. **`MemoryMieru` хранит `users`, а не `config["users"]`** — утверждение черновика
   `all(r["name"] != "alice" for r in mieru.config["users"])` не выполнимо.
3. `after_response_before_db_commit:mtproxy` из черновика требовал monkeypatch
   приватного `_mark_step`; вместо подмены внутренностей взят честный набор отказов
   через `self.faults` и `faults["create"]` у самого бэкенда.
4. Добавлены сверх черновика: чистый прогон, компенсация не трогает
   pre-existing-учётку, повторный `run` завершённой операции ничего не меняет,
   читаемый `status` во время работы.


```python
FAULTS = ["before_first_apply", "after_apply:naive", "after_apply:mieru", "during_compensation",
          "after_manager_commit_before_response:naive", "after_response_before_db_commit:mtproxy"]


@pytest.fixture
def injector(monkeypatch):
    from panel.clients import provisioning
    state = {"fault": None, "fired": False}
    original_mark = provisioning.ProvisioningService._mark_step
    def mark(self, db, operation_id, grant_id, status, error=None):
        if state["fault"] == "after_response_before_db_commit:mtproxy" and status == "applied" and not state["fired"]:
            state["fired"] = True
            raise RuntimeError("injected before db commit")
        return original_mark(self, db, operation_id, grant_id, status, error)
    monkeypatch.setattr(provisioning.ProvisioningService, "_mark_step", mark)
    return state


@pytest.mark.parametrize("fault", FAULTS)
async def test_every_fault_ends_in_a_declared_outcome_and_resumes_by_operation_id(fault, injector, provisioning, naive, mieru, telemt):
    naive.seed("keep", "pre-existing-password-0001")            # must survive any compensation
    intents = [_intent("mtproxy"), _intent("naive"), _intent("mieru")]
    injector["fault"] = fault
    if fault.startswith("after_manager_commit_before_response:"):
        naive.faults["create"] = "lose_response"
    if fault.startswith("after_apply:"):
        provisioning.faults["after_apply"] = fault.split(":")[1]
    if fault == "before_first_apply":
        provisioning.faults["before_first_apply"] = True
    if fault == "during_compensation":
        provisioning.faults["after_apply"] = "mieru"
        provisioning.faults["during_compensation"] = True
    operation_id = provisioning.start(CLIENT_ID, intents, **CTX)
    try:
        first = await provisioning.run(operation_id)
    except RuntimeError:
        first = None                                            # crashed: restart below
    resumed = await ProvisioningService(provisioning.database, provisioning.secrets, provisioning.adapters, provisioning.clients).run(operation_id)
    assert resumed.status in {"succeeded", "compensated", "manual_intervention_required"}
    assert "keep" in naive.users
    if resumed.status == "succeeded":
        assert [u["username"] for u in await naive.list_users()].count("alice") == 1
        grants = provisioning.clients.client_with_grants(CLIENT_ID)[1]
        assert all(g.secret_ref is not None and g.observed_state == "enabled" for g in grants)
    if resumed.status == "compensated":
        assert "alice" not in naive.users and all(r["name"] != "alice" for r in mieru.config["users"])
        assert all(g.desired_state == "deleted" for g in provisioning.clients.client_with_grants(CLIENT_ID)[1])


async def test_bundle_is_one_time_and_contains_every_artifact(provisioning, client, login_user):
    operation_id = provisioning.start(CLIENT_ID, [_intent("mtproxy"), _intent("naive")], **CTX)
    assert (await provisioning.run(operation_id)).status == "succeeded"
    await login_user(client)
    csrf = client.cookies["panel_csrf"]
    token = (await client.post(f"/api/operations/{operation_id}/bundle", headers={"X-CSRF-Token": csrf})).json()["reveal_token"]
    revealed = await client.get(f"/api/reveal/{token}")
    assert revealed.status_code == 200 and {a["protocol"] for a in revealed.json()["grants"]} == {"mtproxy", "naive"}
    assert (await client.get(f"/api/reveal/{token}")).status_code == 410
```

`provisioning.faults` — тестовый хук в самом сервисе (`self.faults: dict = {}`, проверяется в трёх точках; в production пуст). Фикстура `provisioning` собирает сервис из `Database` в `tmp_path`, `SecretStore(Keyring.generate())`, адаптеров над Memory-бэкендами и `ClientService`, создаёт клиента `CLIENT_ID`.

- [x] **Step 2: Запустить — падает**; **Step 3: реализовать** (выполнено 2026-09-10)

Факт: миграция 7 добавлена; `panel/clients/provisioning.py` реализует журнал шагов
(`pending → applied → escrowed → active`), где **каждый переход — отдельная
транзакция**, поэтому падение между шагами оставляет журнал в состоянии, с которого
следующий запуск продолжает. Компенсация проходит только по шагам этой операции со
статусом из `CREATED`, поэтому чужая учётка не может быть удалена в принципе.
Ошибка удаления переводит шаг в `manual`, а операцию — в
`manual_intervention_required`: повторное «удалить вслепую» опаснее, чем позвать
человека.

Канарейка: убрана проверка `status in TERMINAL` в начале `run` — три fault-теста
падают, потому что повторный запуск завершённой операции начинает создавать заново.
 (`start` → одна транзакция; `run`: цикл по шагам в порядке `steps_json`, для каждого статуса — следующий переход; `_mark_step` пишет `steps_json` и `status` операции в отдельной транзакции; исключение адаптера → `status='compensating'` → для шагов ≥ `applied` вызвать `adapter.delete` → `compensated`; ошибка удаления → шаг `manual`, операция `manual_intervention_required`; `ManualInterventionRequired` от адаптера → сразу `manual`; успех всех → `secrets.transition(active)`, `update_grant(secret_ref, observed_state='enabled')`, `status='succeeded'`; события/аудит `grant.provision.<status>`).

- [x] **Step 4: Routes и UI** (выполнено 2026-09-10)

`POST /api/clients/{id}/grants`, `GET /api/operations/{id}`,
`POST /api/operations/{id}/resume`, `POST /api/operations/{id}/bundle`;
`python -m panel.cli operations-resume <id>` (проверено на стенде:
`cli.py: error: unknown operation ghost-operation`, exit 2). В UI — диалог
`#grant-modal` с чекбоксами протоколов и одноразовый `#bundle-modal`. Тост говорит
исход как есть: `compensated` — это не успех, а `manual_intervention_required`
показывает id операции и команду продолжения.

Прежний вариант плана
 (`POST /api/clients/{id}/grants` → `{operation_id, status}` после `run`; UI-диалог показывает статус и bundle; при `manual_intervention_required` — понятное сообщение и id операции).

- [x] **Step 5: Прогнать** (выполнено 2026-09-10; коммит — по разрешению владельца)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_provisioning_saga.py panel/tests/test_client_import.py panel/tests/test_secrets_store.py && scripts/dev/remote-gate.sh full`
Факт: сага — `11 passed`; вместе с импортом и мобильным гейтом — `23 passed`;
`REMOTE_GATE_FULL_OK`, весь набор — `1403 passed, 2 skipped` (после Task 12d было
`1390 passed, 2 skipped`).

```bash
git add panel/clients/provisioning.py panel/migrations.py panel/client_routes.py \
        panel/schemas.py panel/static/js/clients.js panel/static/js/access.js \
        panel/static/index.html panel/static/style.css panel/app.py panel/cli.py \
        panel/tests/test_provisioning_saga.py panel/tests/test_client_import.py \
        tests/test_deploy.py PANEL.ru.md PANEL.en.md
git commit -m "feat: добавить журналируемое создание нескольких доступов"
```

---

### Task 14: Legacy protocol endpoints через доменный сервис (feature flag)

**Files:**
- Modify: `panel/settings.py` (`vnext_writer: str = os.getenv("PANEL_VNEXT_WRITER", "legacy")`, допустимо `legacy|domain`)
- Modify: `panel/telemt_routes.py:388-475`, `panel/naive_routes.py:276-399`, `panel/mieru_routes.py:206-302`
- Modify: `panel/clients/service.py` (`legacy_intent(protocol, username, options)`, `touch_import(protocol, username)`)
- Modify: `panel/tests/conftest.py` (фикстура `client` параметризована `writer in ("legacy","domain")`)
- Modify: `panel/tests/test_users_adapter_ui.py`, `panel/tests/test_naive_management.py`, `panel/tests/test_mieru_management.py` (ожидания одинаковы в обоих режимах; добавить проверки, что в `domain` появляются строки `clients`/`access_grants` и `reason_code="domain"` в аудите)
- Modify: `compose.yaml` (`PANEL_VNEXT_WRITER: ${PANEL_VNEXT_WRITER:-legacy}`), `PANEL.*.md`, `docs/UPGRADING*.md` (порядок cutover: импорт → флаг → проверка → read-only позже)

**Interfaces:**
- В режиме `domain`: `POST /api/users|/api/naive/users|/api/mieru/users` → `ProvisioningService.start+run` с одним intent (клиент `display_name=username` создаётся автоматически, `origin='provisioned'`), ответ прежней формы (`{"username","reveal_token"}`; Mieru — плюс `revision`); `enable/disable/rotate/delete/limits/quota/reset` → через адаптеры и `ClientService` с обновлением `desired_state/observed_state/secret_ref`; неизвестный панели username, существующий в manager → `touch_import` (одна строка grant с `origin='imported'`, без слияния, без мутации manager), затем операция.
- Прямые вызовы `app.state.telemt/naive/mieru` из route-модулей в режиме `domain` запрещены: тест `test_domain_mode_has_no_direct_manager_writes` подменяет `create/rotate/delete/set_enabled` на Memory-объектах функциями, которые бросают, если `request.state.writer != "domain"`-путь не прошёл через адаптер (проверка через маркер `adapter.calls`).

- [x] **Step 1: Параметризовать фикстуру и получить падение в `domain`** (выполнено 2026-09-10)

Факт: фикстура `client` параметризована по `legacy|domain`, весь API-набор теперь
прогоняется дважды — это и есть доказательство, что флаг не меняет наблюдаемое
поведение. Сама параметризация ничего не роняет (ветки ещё нет), поэтому добавлены два
теста, которые проверяют то, что флаг обязан менять: в `domain` появляются строки
`clients`/`access_grants` с `secret_id`, а в `legacy` их нет; и операция над
неизвестным панели пользователем записывает его как `imported` без пересоздания.


```python
@pytest.fixture(params=["legacy", "domain"])
async def client(request, tmp_path, telemt, naive, mieru):
    settings = Settings(database_path=tmp_path / "panel.sqlite3", session_cookie_secure=False, allowed_hosts=("testserver",),
                        login_attempts=3, login_window_seconds=60, reveal_ttl_seconds=60, naive_public_host="naive.example.com",
                        naive_enabled=True, mieru_enabled=True, master_key_file=_keyring(tmp_path), vnext_writer=request.param)
    ...
```

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_users_adapter_ui.py panel/tests/test_naive_management.py panel/tests/test_mieru_management.py -k domain` → часть тестов падает (в `domain` пока нет ветки).

- [x] **Step 2: Реализовать ветвление** (выполнено 2026-09-10 с пятью уточнениями)

Факт:

1. **Префикс `ee` всплыл ещё раз, теперь на границе API.** Escrow хранит секрет
   ссылки (`ee<hex>`), а `/api/reveal` исторически отдаёт «голый» 32-символьный
   секрет. Добавлена `panel.telemt.api_secret()` — ровно одно место, рядом с разбором
   ссылки, где префикс уже понимается. Канарейка (конверсия убрана) роняет
   `test_user_crud_rotate_and_one_time_reveal[domain]`.
2. **Хост Mieru нельзя выдумывать.** Первая версия строила `mierus://` из
   `adapter.public_host`, который в проде пуст, и панель получала
   `409 Mieru connection link unavailable`. Правильный источник — сам `share_url`
   менеджера: `template_from()` заменяет в нём credential на `{username}:{password}`,
   сага сохраняет шаблон в options гранта, и ссылка потом строится из того, что mita
   реально отдала.
3. **`domain_context` переехал в `RequestContext`.** Импорт хелпера из
   `telemt_routes` в `naive_routes` создавал цикл (`telemt_routes` уже импортирует
   `safe_naive_traffic` из `naive_routes`).
4. **Импорт-тесты сеяли пользователей через API** — а в `domain` они уже
   записываются, и импортировать становится нечего. Сев перенесён прямо в бэкенды:
   импорт существует для учёток, которые появились **до** панели.
5. **Элевированные Mieru-флаги остаются на прямом вызове** и в `domain`: это
   осознанно ручной путь, протаскивать его через общий intent — подмена смысла.
 (в каждом route: `if settings.vnext_writer == "domain": return await app.state.domain_facade.<op>(…)`; `panel/clients/facade.py` собирает старую форму ответа из результата saga/адаптера, включая `reveal_token` через `context.create_reveal`).

- [x] **Step 3: Прогнать всё в обоих режимах, полный уровень** (выполнено 2026-09-10; коммит — по разрешению владельца)

Run: `scripts/dev/remote-gate.sh compose && scripts/dev/remote-gate.sh full`
Факт: `REMOTE_GATE_COMPOSE_OK`. Первый полный прогон дал `1 failed, 1493 passed`:
`test_every_mutation_response_and_audit_row_carry_the_same_request_id[domain]` требовал
**ровно одну** строку аудита на мутацию — свойство legacy-писателя. Домен честно пишет
рядом строку саги (`grant.provision.start`) с тем же `request_id`, и инвариант здесь —
корреляция, а не количество; утверждение уточнено. Второй прогон:
`REMOTE_GATE_FULL_OK`, весь набор — `1494 passed, 2 skipped` (после Task 13 было
`1403 passed, 2 skipped`; рост в основном за счёт того, что API-набор идёт дважды).

```bash
git add panel/settings.py panel/telemt_routes.py panel/naive_routes.py panel/mieru_routes.py panel/clients panel/tests/conftest.py panel/tests/test_users_adapter_ui.py panel/tests/test_naive_management.py panel/tests/test_mieru_management.py compose.yaml PANEL.ru.md PANEL.en.md docs/UPGRADING.md docs/UPGRADING.ru.md
git commit -m "refactor: направить старые API клиентов через единый сервис"
```

---

### Task 15: Lifecycle токена подписки и generation

**Files:**
- Create: `panel/subscriptions/__init__.py`, `panel/subscriptions/models.py`, `panel/subscriptions/store.py`, `panel/subscriptions/service.py`
- Create: `panel/tests/test_subscription_lifecycle.py`
- Modify: `panel/migrations.py` (миграция 8)
- Modify: `panel/clients/service.py` (регистрация `on_change` → `bump_generation`), `panel/clients/provisioning.py` (bump после `succeeded`), `panel/settings.py` (`public_url: str = os.getenv("PANEL_PUBLIC_URL", "")`), `panel/app.py`, `compose.yaml` (`PANEL_PUBLIC_URL: ${PANEL_PUBLIC_URL:-}`), `installer/adapters/core.py` (в `.env`: `PANEL_PUBLIC_URL=https://<panel_domain>`)

**Interfaces:**
- Миграция 8:

```python
SUBSCRIPTIONS_V8 = Migration(8, "client-subscriptions", (
    """CREATE TABLE IF NOT EXISTS client_subscriptions (
      id TEXT PRIMARY KEY, client_id TEXT NOT NULL REFERENCES clients(id) ON DELETE RESTRICT,
      public_token_hash TEXT NOT NULL UNIQUE, generation INTEGER NOT NULL DEFAULT 1,
      state TEXT NOT NULL CHECK(state IN ('active','revoked')),
      update_interval_hours INTEGER NOT NULL DEFAULT 12, last_fetched_at INTEGER,
      created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL, revoked_at INTEGER)""",
    "CREATE UNIQUE INDEX IF NOT EXISTS client_subscriptions_one_active ON client_subscriptions(client_id) WHERE state='active'",
))
```

- Produces (`panel.subscriptions.models`): `Subscription(id, client_id, generation, state, update_interval_hours, last_fetched_at, created_at, updated_at, revoked_at)` (без токена и хэша); `ManifestGrant(grant_id, protocol, node_id, endpoint_id, runtime_username, secret_version: int, options: dict, enabled: bool, valid_from, valid_until)`; `Manifest(version: int = 1, client_id, client_name, generation, grants: list[ManifestGrant])`.
- Produces (`panel.subscriptions.service.SubscriptionService(database, clients, secrets, clock=time)`): `create(client_id, *, actor, ip, request_id) -> tuple[Subscription, str]` (второй элемент — plaintext-токен, показывается один раз; отказ `ClientConflict` с перечнем grants без `secret_ref`), `rotate(client_id, …) -> tuple[Subscription, str]`, `revoke(client_id, …)`, `get(client_id) -> Subscription | None`, `bump_generation(db, client_id)` (в транзакции вызывающего; no-op без активной подписки), `resolve(token) -> Subscription | None` (`sha256`, `secrets.compare_digest`, только `active`), `effective_manifest(subscription, now) -> Manifest`, `etag(manifest, renderer_version: int) -> str` (sha256 канонического JSON `{"grants": [только enabled=true поля grant_id, secret_version, options, valid_from, valid_until, runtime_username, endpoint_id, node_id, protocol], "renderer": version}` — **generation и last_fetched_at не входят**), `record_fetch(subscription_id, now)`, `public_url(token) -> str` (`f"{settings.public_url.rstrip('/')}/s/{token}"`).
- Consumes: `ClientService.on_change`, `effective_enabled`, `SecretRef`.

- [x] **Step 1: Падающие тесты** (выполнено 2026-09-10)

Факт: пять тестов из черновика плюс три сверх него — `public_url` строится от базы,
expiry меняет ETag, и **тест на подключение хука в `create_app`**: он поднимает
приложение в режиме `domain`, отключает пользователя через старый
`POST /api/naive/users/{u}/disable` и ждёт `generation == 2`. Именно он падал, пока
сервис не был подключён (канарейка: без `on_change.append` — `AssertionError`).

```python
async def test_token_is_shown_once_and_only_its_hash_is_stored(subscriptions, client_id):
    subscription, token = subscriptions.create(client_id, **CTX)
    assert len(token) >= 43 and subscription.generation == 1
    with subscriptions.database.connect() as db:
        raw = db.execute("SELECT public_token_hash FROM client_subscriptions").fetchone()[0]
    assert raw != token and token not in subscriptions.database.path.read_bytes().decode("latin-1")
    assert subscriptions.resolve(token).id == subscription.id
    assert subscriptions.resolve("x" * 43) is None


async def test_rotate_revokes_the_old_url(subscriptions, client_id):
    _, old = subscriptions.create(client_id, **CTX)
    _, new = subscriptions.rotate(client_id, **CTX)
    assert subscriptions.resolve(old) is None and subscriptions.resolve(new) is not None
    subscriptions.revoke(client_id, **CTX)
    assert subscriptions.resolve(new) is None


async def test_generation_increments_with_the_mutation_or_not_at_all(subscriptions, clients, client_id, monkeypatch):
    subscription, _ = subscriptions.create(client_id, **CTX)
    clients.set_state(client_id, "suspended", **CTX)
    assert subscriptions.get(client_id).generation == 2
    from panel.clients import service as module
    monkeypatch.setattr(module, "record", lambda *a, **k: (_ for _ in ()).throw(RuntimeError("audit down")))
    with pytest.raises(RuntimeError):
        clients.set_state(client_id, "active", **CTX)
    assert subscriptions.get(client_id).generation == 2


async def test_create_refuses_grants_without_escrowed_credentials(subscriptions, client_id, grant_without_secret):
    with pytest.raises(ClientConflict, match=grant_without_secret.runtime_username):
        subscriptions.create(client_id, **CTX)


async def test_etag_follows_the_effective_set_not_the_fetch_time(subscriptions, client_id, clock):
    subscription, _ = subscriptions.create(client_id, **CTX)
    before = subscriptions.etag(subscriptions.effective_manifest(subscription, clock.time()), 1)
    subscriptions.record_fetch(subscription.id, clock.time())
    assert subscriptions.etag(subscriptions.effective_manifest(subscription, clock.time()), 1) == before
    clock.advance(30 * 86400)   # past the grant's valid_until set by the fixture
    after = subscriptions.etag(subscriptions.effective_manifest(subscription, clock.time()), 1)
    assert after != before
    assert subscriptions.effective_manifest(subscription, clock.time()).grants[0].enabled is False
```

Фикстуры: `clients`/`subscriptions` над `Database` в `tmp_path` с `SecretStore(Keyring.generate())`; `client_id` — клиент с двумя grants (`secret_ref` заполнен через `secrets.store`), у одного `valid_until = now + 7d`; `grant_without_secret` — импортированный Mieru-grant без `secret_ref`; `clock` — объект с `time()`/`advance()`.

- [x] **Step 2: Запустить — падает**; **Step 3: реализовать** (выполнено 2026-09-11 с тремя уточнениями)

Факт:

1. **Не `PANEL_PUBLIC_URL`, а `PANEL_SUBSCRIPTION_URL`.** Черновик задачи писал базу
   ссылки как `https://<panel_domain>`, но решение владельца 1 (раздел 5) выделяет под
   подписку отдельный домен, чтобы из ссылки нельзя было вывести адрес панели.
   Поэтому поле в `Settings` называется `subscription_url`, переменная —
   `PANEL_SUBSCRIPTION_URL` (в `compose.yaml` — `${PANEL_SUBSCRIPTION_URL:-}`), а
   запись в `.env` установщиком **отложена до Task 17**, где появляется ключ
   `subscription_domain`: писать туда домен панели было бы ошибкой по существу.
2. **Bump живёт в одном месте.** Все пути изменения grant — сага (`_activate`),
   `capture_credential`/`adopt_credential` (`_escrow`), фасад Task 14
   (`set_enabled`/`forget`/`rotate`/`touch_import`) и `set_state` клиента — уже
   зовут `ClientService.notify(db, client_id)` внутри своей транзакции, поэтому в
   `create_app` достаточно одного `on_change.append(subscriptions.bump_generation)`.
   Компенсация саги хук не зовёт: по плану generation привязан к активации. Grant «в
   полёте» (`observed_state='pending'`, без `secret_ref`) в манифесте виден только как
   строка без credential, и его появление/исчезновение отражает ETag (он считается от
   эффективного набора), а не generation. **К Task 16:** рендереры обязаны отдавать такой
   grant как `unsupported … no stored credential`, а не пытаться строить ссылку.
3. `create` отказывает не по `desired_state`, а по `store.grants()` — тот же фильтр
   `desired_state<>'deleted'`, что и везде.

- [x] **Step 4: Прогнать и коммит** (выполнено 2026-09-11; коммит — по разрешению владельца)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_subscription_lifecycle.py panel/tests/test_clients_domain.py panel/tests/test_provisioning_saga.py && scripts/dev/remote-gate.sh full`
Факт: quick — `27 passed`; full — `REMOTE_GATE_FULL_OK`, **1503 passed, 2 skipped**
(после Task 14 было 1494; +9 — весь `test_subscription_lifecycle.py`).

```bash
git add panel/subscriptions panel/migrations.py panel/clients panel/settings.py panel/app.py compose.yaml installer/adapters/core.py panel/tests/test_subscription_lifecycle.py
git commit -m "feat: добавить lifecycle клиентских подписок"
```

---

### Task 16: Канонический манифест и рендереры

**Files:**
- Create: `panel/subscriptions/renderers/__init__.py`, `base.py`, `manifest.py`, `raw.py`, `singbox.py`, `clash.py`, `html.py`
- Create: `panel/subscriptions/compatibility.py` (матрица «протокол × клиент → auto_refresh: supported|unsupported|unproven» из `tests/fixtures/vnext-capabilities.json` и spec «Важное ограничение»)
- Create: `panel/tests/test_subscription_renderers.py`

**Interfaces:**
- `MEDIA_TYPE_MANIFEST = "application/vnd.proxy-control.subscription+json;version=1"`.
- `Renderer` (Protocol): `name: str`, `media_type: str`, `version: int`, `render(manifest: Manifest, artifacts: dict[str, list[AccessArtifact]]) -> bytes`; `RENDERERS: dict[str, Renderer] = {"manifest", "singbox", "clash", "raw", "html"}` (решение 2 раздела 5).
- `resolve_artifacts(manifest, secrets, adapters, db, *, public_hosts: dict[str, str]) -> dict[grant_id, list[AccessArtifact]]` — раскрывает `SecretVersion` в памяти и вызывает `adapter.render_artifacts`; plaintext нигде не сохраняется.
- Формат `raw`: `text/plain; charset=utf-8`, по одной ссылке на строку (`tg://proxy?…`, `naive+https://…`, `mierus://…`), **без base64-обёртки** (решение 6 раздела 5), для комбинаций без проверенной ссылки или без `secret_ref` — строка `# unsupported <protocol> <runtime_username>: <reason>`.
- Формат `singbox`: `application/json`, `{"outbounds": […], "proxy_control": {"unsupported": […], "generation": n}}`; в `outbounds` только `type: "naive"` (server/server_port/username/password/tls.server_name) и `type: "mieru"` (server/port или port_range/transport/username/password/multiplexing) — первый разбирает и официальный sing-box ≥ 1.13, оба — Karing; MTProxy всегда в `unsupported`.
- Формат `clash`: `text/yaml; charset=utf-8`, `proxies:` только `type: mieru` (`server`, `port`/`port-range`, `transport`, `username`, `password`, `multiplexing`, `udp: true`) плюс `proxy-groups`/`proxy-control.unsupported` с причинами для naive и MTProxy. **Новой зависимости не появляется**: PyYAML в репозитории нет, YAML пишется вручную ограниченным подмножеством (только скаляры в одинарных кавычках с удвоением `'`), а тест проверяет строки и отсутствие управляющих символов в подставляемых значениях; значение с `\n`, `'`, `:` → `RenderError`.
- Формат `manifest`: канонический JSON `Manifest` + `artifacts` (значения ссылок) + `compatibility`; `html`: страница без JS с ссылками, QR (`panel.reveals.qr_data`) и матрицей совместимости.
- Границы: не более 64 grants в ответе (иначе `RenderError("too many grants")`), суммарный размер ≤ 512 KiB.

- [x] **Step 1: Падающие тесты** (выполнено 2026-09-11)

Факт: `panel/tests/test_subscription_renderers.py` — 21 тест. Фикстура сеет шесть grants
прямо в базу: MTProxy/Naive/Mieru с секретом, импортированный Mieru без `secret_ref`,
отключённый Naive с секретом и Mieru с диапазоном портов. Отличия от черновика:
`Manifest` — dataclass, поэтому `dataclasses.replace`, а не `model_copy`; канарейка читает
и `-wal`; проверка лимита размера идёт через `monkeypatch(base.MAX_BYTES)` — 512 KiB шестью
грантами не набрать честно.

```python
@pytest.mark.parametrize("name", ["manifest", "singbox", "clash", "raw", "html"])
def test_every_renderer_emits_its_media_type_and_no_malformed_links(name, manifest, artifacts):
    renderer = RENDERERS[name]
    body = renderer.render(manifest, artifacts)
    assert renderer.media_type and body
    if name == "raw":
        lines = body.decode().splitlines()
        assert any(line.startswith("tg://proxy?") for line in lines)
        assert any(line.startswith("naive+https://") for line in lines)
        assert any(line.startswith("# unsupported mieru") for line in lines)   # fixture grant without share_template
    if name == "singbox":
        data = json.loads(body)
        assert {o["type"] for o in data["outbounds"]} <= {"naive", "mieru"} and data["proxy_control"]["unsupported"]
    if name == "clash":
        text = body.decode()
        assert "type: mieru" in text and "udp: true" in text
        assert "naive" not in text.split("proxy-control:")[0]     # naive — только в unsupported
        assert "\n\t" not in text and "\r" not in text


def test_rendering_leaves_no_secret_in_database_logs_or_errors(database, manifest_with_canary, secrets, adapters, caplog):
    artifacts = resolve_artifacts(manifest_with_canary, secrets, adapters, database.connect(), public_hosts=HOSTS)
    RENDERERS["raw"].render(manifest_with_canary, artifacts)
    assert b"CANARY-PASSWORD" not in database.path.read_bytes()
    assert "CANARY-PASSWORD" not in caplog.text
    broken = manifest_with_canary.model_copy(update={"grants": manifest_with_canary.grants * 65})
    with pytest.raises(RenderError) as exc:
        RENDERERS["raw"].render(broken, artifacts)
    assert "CANARY-PASSWORD" not in str(exc.value)


def test_compatibility_matrix_follows_the_fixture_and_never_claims_telegram_auto_refresh():
    from panel.subscriptions.compatibility import MATRIX
    assert set(MATRIX) == {"mtproxy", "naive", "mieru"}
    assert all(value in {"supported", "unsupported", "unproven"} for row in MATRIX.values() for value in row.values())
    assert MATRIX["mtproxy"]["telegram"] == "unsupported"
    assert MATRIX["naive"]["karing"] == MATRIX["mieru"]["karing"] == "supported"
    assert MATRIX["naive"]["singbox"] == "supported" and MATRIX["mieru"]["singbox"] == "unsupported"
    assert MATRIX["mieru"]["mihomo"] == "supported" and MATRIX["naive"]["mihomo"] == "unsupported"
    fixture = json.loads((ROOT / "tests/fixtures/vnext-capabilities.json").read_text())["clients"]
    assert {protocol: {client: cell["status"] for client, cells in fixture.items() for p, cell in cells.items() if p == protocol}
            for protocol in MATRIX} == MATRIX      # единственный источник — фикстура Task 3
```

- [x] **Step 2: Запустить — падает**; **Step 3: реализовать** (выполнено 2026-09-11 с пятью уточнениями)

Факт:

1. **Разбор `mierus://` вынесен в одно место.** Строгий разбор ссылки жил внутри
   `mieru_routes.mieru_access` вперемешку с `HTTPException`. Теперь это
   `panel.protocols.mieru.parse_share_url() -> MieruShare` (`ValueError` на любую
   странность) и `singbox_outbounds(share, tag=…)`; роут зовёт их и переводит `ValueError`
   в тот же 409. Поведение роута не менялось — `test_mieru_management.py` прошёл без правок.
2. **`naive+https://` строится рендерером, а не адаптером.** Артефакт `NaiveAdapter` —
   родной `https://user:pass@host` NaiveProxy (его же показывает bundle в UI); `raw` и `html`
   превращают его в `naive+https://user:pass@host:443#Naive · user` (`base.naive_share_url`)
   — то, что импортируют NekoBox-подобные клиенты и QR. Добавлять второй артефакт в
   адаптер значило бы менять выдачу bundle — не задача этого шага.
3. **`resolve_artifacts` берёт строку grant из базы** (`ClientStore.grant` — staticmethod),
   а не собирает `AccessGrant` из манифеста: адаптеры рендерят от типизированных
   options строки, а выдумывать `origin`/`created_at` для синтетической строки — ложь.
   Раскрываются только `enabled` grants с `secret_version ≥ 1`: отключённый grant не раскрывается
   вообще, и ни один рендерер не может сделать из него ссылку даже по ошибке (канарейка:
   убран фильтр `enabled` в singbox — grant попал лишь в `unsupported`, не в `outbounds`).
4. **Mieru с диапазоном портов — `unsupported` в singbox, но `port-range` в clash.** Форма
   outbound Karing (`server_port`) поля для диапазона не имеет (так же, как и v0.1.0-профиль),
   а mihomo диапазон понимает. `multiplexing` не пишется: значения мы не знаем, а у mihomo
   есть дефолт.
5. **YAML-писатель допускает `:`.** Внутри одинарных кавычек двоеточие безопасно, а запрет
   сломал бы причины из матрицы («… mieru apply config: …»). Отказ (`RenderError`) — на `'`,
   управляющие символы и любой непечатный (в том числе U+2028); ошибка роняет весь
   ответ, а не один grant: значения идут из валидированных полей панели, и непредставимое
   значение — дефект, а не случай для тихой деградации.

Также: `compatibility.MATRIX`/`NOTES` — копия секции `clients` фикстуры Task 3, потому что
образ панели не содержит `tests/`; тест сравнивает оба словаря с фикстурой целиком. `raw`
пишет `# disabled <protocol> <user>` для неэффективного grant — человек видит, почему строка
исчезла; singbox/clash такой grant просто не содержат. В singbox пока идут и naive, и mieru
(профиль Karing); вариант «только naive» для официального sing-box — вопрос вариантов ссылки
под клиента (Task 18).

- [x] **Step 4: Прогнать и коммит** (выполнено 2026-09-11; коммит — по разрешению владельца)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_subscription_renderers.py panel/tests/test_protocol_adapter_contract.py`
Факт: quick — `21 passed` (рендереры), контракт адаптеров и `test_mieru_management.py` без правок;
full — `REMOTE_GATE_FULL_OK`, **1524 passed, 2 skipped** (после Task 15 было 1503).

```bash
git add panel/subscriptions/renderers panel/subscriptions/compatibility.py panel/tests/test_subscription_renderers.py
git commit -m "feat: добавить форматы подписки MTProxy Naive и Mieru"
```

---

### Task 17: Публичный endpoint `/s/{token}` с корректной HTTP-семантикой и без токенов в логах

**Files:**
- Create: `panel/subscription_routes.py`
- Create: `panel/tests/test_subscription_http.py`
- Modify: `panel/app.py` (регистрация; `PANEL_ALLOWED_HOSTS` = домен панели + домен подписки; `/s/` обслуживается **только** при `Host == PANEL_SUBSCRIPTION_HOST`, на домене панели — 404 той же веткой, что и неизвестный токен — решение 1 раздела 5)
- Modify: `panel/web_context.py`, `panel/static/js/subscriptions.js` (база ссылки и QR — `PANEL_SUBSCRIPTION_URL`, не `PANEL_PUBLIC_URL`)
- Modify: `installer/models.py`, `installer/adapters/core.py`, `docs/INSTALLER_REFERENCE.*.md` (ключ `subscription_domain` в `install.toml` — на стенде `eclipse.sky.dubr1kkk.uk`, выпуск ACME на него, `PANEL_SUBSCRIPTION_HOST`/`PANEL_SUBSCRIPTION_URL` в env панели)
- Modify: `panel/web_context.py:95-124` (для путей `/s/` не переопределять `Cache-Control` на `no-store`, оставить `private, no-cache`; `X-Request-Id` сохраняется)
- Modify: `panel/entrypoint.sh` (`uvicorn … --no-access-log`)
- Modify: `installer/adapters/core.py:2524-2544` (`_panel_vhost_text` остаётся без `/s/`; рядом новый `_subscription_vhost_text` — отдельный `server` на `subscription_domain` с `access_log off;`, единственным `location ^~ /s/` и `location / { return 404; }`), `scripts/proxyctl.py:928-944` (`_panel_site_content`: то же разделение), `scripts/mtproxy-deploy` (если рендерит панельный vhost — то же)
- Modify: `tests/test_installer_core.py`, `tests/test_deploy.py`, `tests/test_panel_entrypoint.py`
- Modify: `docs/OPERATIONS.*.md` (раздел «Подписки: логи и ротация»)

**Contract:**
- `GET|HEAD /s/{token}`; токен `^[A-Za-z0-9_-]{43,64}$`, иначе 404; неизвестный/отозванный → `404 {"detail":"not found"}` (одинаковое тело и ветка кода; сравнение хэшей через `secrets.compare_digest`).
- `format=manifest|singbox|clash|raw|html` или negotiation по `Accept` (`text/html` → html; `application/vnd.proxy-control.subscription+json` → manifest; `text/yaml`/`application/yaml` → clash; `application/json` → singbox; иначе raw). Неизвестный `format` → 404 той же веткой, что и неизвестный токен (не раскрывать существование подписки).
- `Host`, отличный от `PANEL_SUBSCRIPTION_HOST` (в том числе домен панели) → `404`, без `record_fetch` и без разницы во времени ответа.
- Ответ: `ETag: "<sha256>"` = `SubscriptionService.etag(effective_manifest(now), renderer.version)`; `If-None-Match` совпал → `304` c тем же `ETag`; `Cache-Control: private, no-cache`; `Vary: Accept`; `Profile-Update-Interval: <hours>`; `X-Robots-Tag: noindex`; `Content-Type` рендерера; `Subscription-Userinfo` не отдаётся (учёт не биллинговый).
- Rate limit: `Store.reserve_login_attempt(scopes=[f"subscription:{client_ip}"], attempts=60, window=60)` → `429`; тело запроса > 0 байт → 413 (уже в middleware).
- На 200/304 — `record_fetch` (только `last_fetched_at`).
- Логи: приложение не логирует путь `/s/…`; uvicorn access log выключен; Nginx `access_log off` в `/s/`.

- [ ] **Step 1: Падающие тесты**

```python
async def test_public_subscription_serves_formats_with_etag_and_304(public, token):
    first = await public.get(f"/s/{token}")
    assert first.status_code == 200 and first.headers["cache-control"] == "private, no-cache"
    etag = first.headers["etag"]
    assert first.headers["profile-update-interval"] == "12" and first.headers["x-robots-tag"] == "noindex"
    again = await public.get(f"/s/{token}", headers={"If-None-Match": etag})
    assert again.status_code == 304 and again.headers["etag"] == etag
    html = await public.get(f"/s/{token}", headers={"Accept": "text/html"})
    assert html.headers["content-type"].startswith("text/html")
    singbox = await public.get(f"/s/{token}", params={"format": "singbox"})
    assert singbox.headers["content-type"].startswith("application/json")
    clash = await public.get(f"/s/{token}", params={"format": "clash"})
    assert clash.headers["content-type"].startswith("text/yaml")
    assert (await public.get(f"/s/{token}", headers={"host": PANEL_HOST})).status_code == 404
    head = await public.head(f"/s/{token}")
    assert head.status_code == 200 and head.headers["etag"] == etag and not head.content


async def test_absent_and_revoked_tokens_are_indistinguishable_404(public, token, subscriptions, client_id):
    missing = await public.get("/s/" + "A" * 43)
    subscriptions.revoke(client_id, **CTX)
    revoked = await public.get(f"/s/{token}")
    assert missing.status_code == revoked.status_code == 404 and missing.content == revoked.content
    assert (await public.get("/s/short")).status_code == 404


async def test_expiry_boundary_invalidates_the_old_etag(public, token, clock):
    etag = (await public.get(f"/s/{token}")).headers["etag"]
    clock.advance(30 * 86400)
    after = await public.get(f"/s/{token}", headers={"If-None-Match": etag})
    assert after.status_code == 200 and after.headers["etag"] != etag
    assert "# unsupported" not in after.text or "expired" in after.text


async def test_token_never_reaches_application_logs_or_audit(public, token, caplog, app):
    caplog.set_level("DEBUG")
    await public.get(f"/s/{token}")
    assert token not in caplog.text
    with app.state.database.connect() as db:
        rows = db.execute("SELECT detail_json, target FROM audit_log").fetchall()
    assert all(token not in row[0] and token not in row[1] for row in rows)


async def test_rate_limit_returns_429(public, token):
    for _ in range(60):
        await public.get(f"/s/{token}", headers={"If-None-Match": '"none"'})
    assert (await public.get(f"/s/{token}")).status_code == 429
```

Плюс контрактные тесты:

```python
# tests/test_panel_entrypoint.py
def test_uvicorn_access_log_is_disabled_so_subscription_tokens_never_reach_docker_logs():
    text = (ROOT / "panel/entrypoint.sh").read_text()
    assert "--no-access-log" in text

# tests/test_installer_core.py и tests/test_deploy.py
def test_subscription_lives_on_its_own_vhost_without_access_log(...):
    panel_vhost = <рендер панельного vhost>
    subscription_vhost = <рендер vhost подписки>
    assert "/s/" not in panel_vhost                                  # решение 1 раздела 5
    assert "access_log off;" in subscription_vhost
    assert "location ^~ /s/" in subscription_vhost
    assert "location / { return 404; }" in subscription_vhost
    assert "server_name sub.example.com;" in subscription_vhost
```

- [x] **Step 1–3** (выполнено 2026-09-11 с шестью уточнениями)

Факт:

1. **Хост MTProxy панель не знает — его надо учить из ссылки.** `MTPROXY_DOMAIN` в env панели
   не попадает, а bundle Task 13 подставлял в `tg://proxy?server=` домен панели — ссылка
   работала только потому, что оба имени резолвятся в один IP. Теперь `access_from_user`
   возвращает ещё `server`/`port` из ссылки Telemt, `TelemtAdapter._applied` кладёт их в
   `artifact_template`, `MtproxyOptions` получил поля `host`/`port`, а `_remember_template` саги
   обобщён: сохраняет любой ключ шаблона, для которого у options-модели есть поле (Mieru —
   `share_template`, Telemt — `host`/`port`). `render_artifacts` берёт выученный хост, а старый
   fallback (домен панели) остаётся только для grants, созданных до этой правки.
2. **`PANEL_SUBSCRIPTION_HOST` выводится из URL.** `Settings.__post_init__` берёт hostname из
   `subscription_url`, если хост не задан явно; пустой хост = endpoint выключен, любой `/s/` — 404.
3. **Rate limit стоит до поиска токена** (после проверок Host/формы/формата): перебор токенов
   тратит тот же бюджет 60/мин на адрес, что и честные запросы (тест считает 30 попаданий + 30
   промахов → 429). Счётчик — `Store.reserve_login_attempt` со scope `subscription:<ip>`.
4. **CSP для `/s/` своя.** Общая `style-src 'self'` запрещает inline-стиль html-рендерера;
   middleware на `/s/` ставит `default-src 'none'; style-src 'unsafe-inline'; img-src data:` (скриптов
   на странице нет) и не перезаписывает `Cache-Control`.
5. **httpx логирует URL, который сам запросил.** Тест «токен не в логах» падал на строке
   тестового клиента (`httpx` INFO). Проверяется только то, что логирует приложение (записи
   не из `httpx`/`httpcore`); строка аудита и `public_token_hash` проверяются отдельно.
6. **Установщик: один сертификат, один слушатель.** `domains.subscription` (необязательный,
   есть в мастере как пустой по умолчанию вопрос) попадает SAN'ом в lineage `proxy-control`, SNI-маршрутом
   на тот же `127.0.0.1:8443`, вторым `server`-блоком в том же файле `_PANEL_VHOST` и в `.env`
   (`PANEL_ALLOWED_HOSTS=panel,sub`, `PANEL_SUBSCRIPTION_HOST/URL`). Мутация `subscription-domain=`
   в Core action обязательна и может быть пустой — иначе `_action_from_selection` старых
   checkpoint'ов ронял `Core action is invalid` (54 теста). `scripts/proxyctl.py` и
   `scripts/mtproxy-deploy` не тронуты: первый домена подписки не знает (endpoint выключен, а
   `/s/` на домене панели — 404 самим приложением), второй панельный vhost не рендерит.
   Отдельный текстовый тест на `--no-access-log` не нужен: `test_panel_entrypoint` сравнивает argv
   uvicorn целиком.

- [x] **Step 4: Прогнать, compose и контейнерная лаборатория (nginx-шаблоны изменились)** (выполнено 2026-09-11)

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_subscription_http.py panel/tests/test_security.py tests/test_installer_core.py tests/test_panel_entrypoint.py && ssh ams-test 'cd /root/dev/proxy-control && .venv/bin/python -m unittest -q tests/test_deploy.py' && scripts/dev/remote-gate.sh full && scripts/dev/remote-gate.sh lab-container`
Факт: full — `REMOTE_GATE_FULL_OK`, **1538 passed, 2 skipped** (после Task 16 было 1524;
`test_deploy.py` внутри full — `Ran 30 tests … OK`); compose — `REMOTE_GATE_COMPOSE_OK`.
`lab-container` — выполняется в составе Task 19A Step 2 (тот же прогон, чтобы не жечь стенд дважды).

- [ ] **Step 5: Коммит** (по разрешению владельца)

```bash
git add panel/subscription_routes.py panel/app.py panel/web_context.py panel/entrypoint.sh installer/adapters/core.py scripts/proxyctl.py scripts/mtproxy-deploy tests/test_installer_core.py tests/test_deploy.py tests/test_panel_entrypoint.py panel/tests/test_subscription_http.py docs/OPERATIONS.en.md docs/OPERATIONS.ru.md
git commit -m "feat: опубликовать обновляемый subscription endpoint"
```

---

### Task 18: Единое окно подписки

**Files:**
- Create: `panel/static/js/subscriptions.js`
- Create: `panel/tests/test_subscription_ui_contract.py`
- Modify: `panel/client_routes.py` (`GET /api/clients/{id}/subscription`, `POST …/subscription` → `{reveal_token}` с `{url, qr}`, `POST …/subscription/rotate`, `POST …/subscription/revoke`, `GET /api/subscriptions/compatibility`)
- Modify: `panel/static/js/clients.js` (кнопка «Подписка» на карточке клиента), `panel/static/index.html` (диалог `subscription-modal`), `panel/static/style.css`, `panel/tests/test_mobile_layout.py`

**UI (диалог):** текущее поколение и `last_fetched_at`; список включённых grants c пометкой auto-refresh из матрицы; выбор варианта ссылки под клиента (`?format=singbox` — Karing и sing-box, `?format=clash` — mihomo и Clash-совместимые, `?format=raw` — ручной импорт и QR) с подписью, что именно попадёт в каждый вариант и что останется за бортом (решение 2 раздела 5); кнопки «Создать URL», «Скопировать», QR (через `qr_data`), «Ротировать» и «Отозвать» (оба через `ui.confirmed`); URL показывается **один раз** после создания/ротации (reveal), потом только «URL выдан <дата>, поколение N»; постоянное предупреждение: «Telegram и часть native-клиентов не обновляют подписку автоматически — после изменений отправьте клиенту ссылку заново».

- [ ] **Step 1: Контрактные тесты (`panel/tests/test_subscription_ui_contract.py`)**

```python
ROOT = Path(__file__).resolve().parents[1]

def test_subscription_dialog_shows_url_once_and_warns_about_clients_without_auto_refresh():
    javascript = (ROOT / "static/js/subscriptions.js").read_text()
    html = (ROOT / "static/index.html").read_text()
    assert 'id="subscription-modal"' in html
    assert "не обновляют подписку автоматически" in html
    assert "/api/reveal/" in javascript and "subscription/rotate" in javascript and "subscription/revoke" in javascript
    assert "ui.confirmed(" in javascript
    assert "auto_refresh" in javascript
```

плюс API-тесты в `test_subscription_http.py`-стиле: создание отдаёт `reveal_token`, reveal содержит `url` c `/s/` и `qr`, повторный GET reveal → 410; `GET /api/clients/{id}/subscription` не содержит токена.

- [x] **Step 1–3** (выполнено 2026-09-11 с четырьмя уточнениями)

Факт:

1. **Админ-роуты лежат в `panel/subscription_routes.py`** (`register_subscription_admin_routes`), а не в
   `client_routes.py`: всё про подписку — публичный `/s/`, управление и `/api/events` — в одном файле.
   Reveal-пейлоад: `{subscription_id, generation, url, qr, variants: {raw|singbox|clash: {url, qr}}}` —
   QR для каждого варианта считается на сервере (`qr_data`), потому что в UI нет QR-библиотеки, а
   reveal одноразовый. Создание при пустом `public_base` — 409 с упоминанием `PANEL_SUBSCRIPTION_URL`.
2. **Кнопка «Подписка» видна и viewer'у** — диалог для него read-only (состояние, поколение,
   `last_fetched_at`, матрица), а кнопки создать/ротировать/отозвать рендерятся по роли; API отвечает 403.
3. **Копирование в bundle-диалоге Task 13 не работало**: кнопки `data-copy` не имели обработчика.
   В `access.bind()` добавлен делегированный обработчик `button[data-copy]` — попутная правка.
4. **Жгут для самого диалога.** Мобильный тест проверял только карточки; добавлен
   `test_subscription_dialog_fits_the_phone_viewport_when_open` — берёт настоящий `<dialog>` из
   `index.html`, открывает `showModal()` с вариантами, QR и кнопками и мерит выход за границы.
   Канарейка (QR 600px) роняет его четырьмя ошибками. Карточка клиента в фикстуре получила
   четыре кнопки, как в жизни. Предостережение: `git checkout <файл>` для отката канарейки сносит и
   незакоммиченные правки того же файла — откатывать только точечной обратной правкой.

Run: `scripts/dev/remote-gate.sh quick panel/tests/test_subscription_ui_contract.py panel/tests/test_subscription_http.py panel/tests/test_mobile_layout.py` — `3 + 11 + 3 passed`; full — см. Task 19.

- [ ] **Step 4: Коммит** (по разрешению владельца)

```bash
git add panel/static/js/subscriptions.js panel/static/js/clients.js panel/static/index.html panel/static/style.css panel/client_routes.py panel/tests/test_subscription_ui_contract.py panel/tests/test_mobile_layout.py
git commit -m "feat: добавить единое окно подписки"
```

---

### Task 19: События изменений подписки (без обещания push)

**Files:**
- Create: `panel/events.py`
- Create: `panel/tests/test_subscription_events.py`
- Modify: `panel/subscriptions/service.py`, `panel/subscription_routes.py`, `panel/client_routes.py` (`GET /api/events?after=<id>&limit=100`)

**Interfaces:**
- `EventBus(database)`: `emit(db, name, payload: dict) -> int` — пишет строку в `audit_log` (`actor_username='system'`, `action=name`, `target=payload["subscription_id"]`, `detail_json=scrub(payload)`) и в кольцевой буфер `app.state.events` (deque maxlen 500); имена: `subscription.generation.changed`, `subscription.fetched`, `subscription.revoked`. Ни одно событие не содержит `applied`/`delivered`.

- [x] **Step 1: Тесты** (выполнено 2026-09-11) — `panel/tests/test_subscription_events.py`, шесть тестов;
  фикстуры импортируются из `test_subscription_http.py` (`# ruff: noqa: F811`).
- [x] **Step 2: Реализовать**; **Step 3: прогнать** (выполнено 2026-09-11)

  Факт: `panel/events.py` — `EventBus(database, capacity=500)`: `emit(db, name, payload)` отказывает
  на имени вне трёх разрешённых (так `applied`/`delivered` не появятся даже случайно), пишет строку
  `audit_log` через `audit.record` (актор `system`, `ip='local'`, payload через `scrub`) и зеркалит в
  `recent`; `since(after, limit)` читает из `audit_log`, а не из кольца — пагинация по id обязана быть
  долговечной. `SubscriptionService(events=…)`: `bump_generation` эмитит только при `rowcount > 0`
  (без активной подписки — ничего), `rotate` эмитит `revoked` для старой строки с `reason=rotated`,
  `record_fetch(subscription, now, status=, format=)` пишет отметку и `fetched` в одной транзакции.
  `GET /api/events?after&limit` (limit 1..500, 422 вне) — для всех ролей, только три имени событий
  (операторские `subscription.create/rotate/revoke` из аудита — не события). Тест
  `test_rotate_and_revoke_move_the_public_url` считал последние три строки аудита — события
  встали между ними; утверждение сужено до операторских действий (инвариант — порядок, не соседство).

  Run: `scripts/dev/remote-gate.sh quick panel/tests/test_subscription_events.py panel/tests/test_subscription_http.py panel/tests/test_subscription_lifecycle.py panel/tests/test_clients_domain.py` — `33 passed`; full — см. ниже.
- [ ] **Step 4: Коммит** — `feat: добавить события изменений подписки` (по разрешению владельца).

---

### Task 19A: Release gate v0.2 (только на `ams-test`)

**Scope:** Tasks 0–19. Fleet v2 и routing отсутствуют и не анонсируются.

- [x] **Step 1: Репозиторные и compose-гейты** (выполнено 2026-09-11)

Run: `scripts/dev/remote-gate.sh full && scripts/dev/remote-gate.sh compose`
Expected: `REMOTE_GATE_FULL_OK`, `REMOTE_GATE_COMPOSE_OK`, ноль падений (Task 0 Step 3a устранил Chromium).
Факт: на рабочем дереве с Tasks 15–19 — `REMOTE_GATE_FULL_OK`, **1551 passed, 2 skipped**
(4:37), `REMOTE_GATE_COMPOSE_OK`. Рост с 1494 (Task 14): +9 lifecycle, +21 рендереры,
+14 HTTP/UI-контракт/мобильный жгут, +6 события, +7 установщик/мастер/конфиг.

- [x] **Step 2: Контейнерная лаборатория** (выполнено 2026-09-11)

Run: `scripts/dev/remote-gate.sh lab-container` → `lab-results-container/report.json` без `failed`.
Факт: `REMOTE_GATE_LAB_CONTAINER_OK`; архив `proxy-control-v0.1.0.tar.gz`
(`4119b5ef…d58f9f`, `--allow-dirty`), сценарии `environment-preflight`, `release-artifact-integrity`,
`audit`, `plan`, `nginx-multi-map`, `coexist-existing-xui`, `uninstall-foreign-identity`,
`dns-tls-preflight`, `secrets-scan` — все `passed`. Лабораторный `install.toml` без
`domains.subscription` (endpoint выключен) — сценарии установщика с ключом покрываются Step 5.

- [x] **Step 3: Апгрейд-drill с данных v0.1.0** (выполнено 2026-09-11)

На стенде лежит том `mtproxy_panel-data` от v0.1.0. Скопировать его в изолированный каталог (`docker run --rm -v mtproxy_panel-data:/src -v /root/upgrade-drill:/dst alpine cp -a /src/. /dst/`), запустить `panel.cli db-migrate --database /root/upgrade-drill/panel.sqlite3` через образ `mtproxy-panel:latest`, затем `db-status` — все миграции applied, `admins/audit_log/fleet_*` читаются; без ключа `master-key-init` панель стартует (секретов нет); после `master-key-init` и создания одного доступа — без ключа стартовать отказывается (fail-closed).

Факт: **drill вскрыл дефект** — `db-status` на базе v0.1.0 (ещё без `schema_migrations`)
падал трассой `no such table: schema_migrations`. Исправлено: `migration_status` теперь
отвечает «ничего не применено», не создавая таблицу (тест в `test_migrations.py`, `10 passed`).
Дальше по порядку, все команды — через `docker run --entrypoint python … mtproxy-panel:latest`:
`db-status` до — 8 × `applied: false`; `db-migrate` — `applied: [1..8]`; `db-status` после —
8 × `applied: true, checksum_ok: true`; `Store.admins()` → `['owner']`, 54 строки аудита,
`FleetStore.nodes()` → `['local']`. Без ключа и без секретов `create_app()` стартует
(`secrets enabled = False`); `master-key-init` → файл 0600; один escrow-секрет
(`DRILL-CANARY-SECRET`, симуляция доступа) → с ключом стартует, **без ключа —
`ValueError: PANEL_MASTER_KEY_FILE is required: the database holds encrypted secrets`**;
`master-key-verify` → `{"k-…": 1}`; `grep` канарейки по `panel.sqlite3` — 0 вхождений.

- [x] **Step 4: Живой стенд (разрушительно)** (выполнено 2026-09-11, три дефекта гейта по пути)

`ssh ams-test 'rm -rf /usr/local/x-ui /etc/x-ui'` (stub фикстуры), затем `LAB_RESET=1 scripts/dev/remote-gate.sh lab-host` — полный сценарий `guest-runner.sh host` (install → docker-build → repair → idempotence → reboot-recovery → crash-every-phase → report → secrets-scan → uninstall → coexistence). Все `LAB_RESULT … passed`.

Факт: `REMOTE_GATE_LAB_HOST_OK`, 18 × `passed` (`install` 93 s, `repair` 37 s,
`reboot-recovery` 61 s, `uninstall` 27 s), `LAB_PLAN_DIGEST 56002c6e…`. Три вещи пришлось
починить, прежде чем этот результат стал возможен:

1. **`remote-gate.sh lab-host` запускал раннер из dev-чекаута.** `guest-runner.sh host`
   берёт корень от собственного местоположения и требует `release/release.json` и
   отсутствие `.git` — т.е. должен быть копией внутри распакованного релиза, как его
   ставит `docker_lab.py`. Теперь гейт кладёт архив в `/tmp/proxy-control-release.tar.gz`,
   sha в `.sha256`, распаковывает и зовёт раннер оттуда. Тир Task 0 «доступен» — но
   ни разу не был прогнан до конца; отсюда и дефект.
2. **Релиз собирается из `git ls-files`.** Первый настоящий прогон упал на
   `ModuleNotFoundError: No module named 'panel.events'`: новые файлы Tasks 15–19 не были
   в индексе, и архив их не содержал (`--allow-dirty` разрешает правки, но не берёт
   неотслеживаемое). Исправлено `git add -N` новых файлов — коммита это не требует, но
   **любой новый файл должен попасть в индекс до сборки релиза**.
3. **Установка обрывает SSH-сессию** (правила nft/mss-clamp сбрасывают established
   соединения; `Connection closed by remote host` ровно на `install`). Раннер при этом
   доживает до конца, но сводка терялась. Гейт теперь запускает раннер отсоединённо
   (`setsid nohup … > /root/lab-host.log`) и опрашивает лог до `LAB_HOST_EXIT=`.

Дополнительно написан `scripts/lab/host-teardown.sh` (`LAB_RESET=1`): хост-сценарий
намеренно оставляет coexistence-топологию (чужой роутер, fake certbot, lab-резолвер,
чужой 3x-ui), а `host_mode = "fresh"` живой установки против неё отказывает — скрипт
возвращает хост в исходное состояние (stock `nginx.conf`, stub-resolver, без `*.lab.test`).

- [x] **Step 5: Реальная установка и живые протокольные проверки** (выполнено 2026-09-11, включая финальную переустановку итоговым архивом)

Факт по порядку:

- `LAB_RESET=1 scripts/lab/host-teardown.sh` → `LAB_HOST_TEARDOWN_OK`; в `/root/install.toml`
  добавлен `subscription = "eclipse.sky.dubr1kkk.uk"`; релиз собран `--allow-dirty` из
  индексированного дерева, распакован в `/tmp/proxy-control-release`, mita/mieru `.deb`
  застейджены через `stage_mita_package` из раннера.
- `plan` показал ожидаемое: SNI-маршрут `eclipse → 127.0.0.1:8443`, `core.runtime
  subscription-domain=eclipse…`, lineage `proxy-control` с тремя именами. Первый `install`
  был `BLOCKED: accepted plan digest does not match` — первый `plan` после teardown увидел
  `nginx.stream_enabled=False`, второй `True`; digest честно пересчитан, установка прошла:
  `status: active`, 12 действий `verified`, 5 контейнеров healthy. Сертификат
  `/etc/letsencrypt/live/nebula…` несёт SAN `aurora`, `eclipse`, `nebula`; в
  `proxy-control-panel.conf` два `server`, у второго `access_log off` и только `location ^~ /s/`;
  `.env`: `PANEL_ALLOWED_HOSTS=aurora,eclipse`, `PANEL_SUBSCRIPTION_HOST/URL`.
- **Дефект установщика профиля `full` (v0.1.0)**: панель после установки отвечала
  `feature unavailable` на NaiveProxy — `docker inspect` показал
  `config_files=compose.yaml,compose.mieru.yaml`, без naive-overlay и без `NAIVE_ENABLED`.
  Адаптеры Naive и Mieru поднимали Compose только со своим overlay, и тот, кто применялся
  последним (Mieru), пересоздавал `panel` без соседа. Исправлено: `_compose_argv` в обоих
  адаптерах добавляет overlay и `.env.*` соседа, если тот существует на хосте (тесты
  `test_mieru_compose_carries_the_naive_overlay…`/`…names_only_its_own_overlay…`).
- **Два дефекта `repair` для managed-new 3x-ui**: (1) verify `three_xui.routes` требовал, чтобы
  каждый маршрут был Xray-inbound'ом, а панель 3x-ui (`8451`) и её sub-сервер (`2096`) —
  слушатели самого 3x-ui → `a routed 3x-ui backend is not an audited loopback inbound`;
  для них теперь доказательство — открытый loopback-порт (тест
  `test_route_verify_counts_the_panel_as_a_listener_not_an_inbound`); (2) `repair` делал
  `systemctl restart x-ui` и сразу шёл в verify — гонка, которую provision уже обходил
  через `_await_panel`; теперь и repair ждёт порт. После правок `repair` → `REPAIR_EXIT=0`,
  `status: active`, панель с обоими overlay (`NAIVE_ENABLED=true`, `MIERU_ENABLED=true`).
- `scripts/lab/subscription-acceptance.py` (новый, весь Step 5 одним прогоном, через API
  панели и `/s/`): `SUBSCRIPTION_ACCEPTANCE_OK` — provisioning `succeeded` (клиент с
  MTProxy+Naive+Mieru), reveal одноразовый (повтор 410), URL на домене подписки; `raw` 200 с
  `ETag`, `Cache-Control: private, no-cache`, `Profile-Update-Interval: 12`; строки `tg://`,
  `naive+https://`, `mierus://` — по одной, комментариев 0; `If-None-Match` → 304; тот же
  токен на домене панели → 404, тело байт-в-байт равно телу для неизвестного токена;
  `singbox`, `singbox-official`, `clash`, `manifest`, `html` — 200 с верными media type;
  suspend клиента → ETag сменился и в raw появилось `# disabled naive …`, resume → прежний
  ETag; `generation ≥ 3`; события `subscription.fetched` и `generation.changed` записаны;
  revoke → 404. **Ядра (решение 2)**: официальный `sing-box 1.14.0`
  (`ghcr.io/sagernet/sing-box@sha256:4bed9332…`) — `check` фида `client=singbox` ok, `run` +
  `curl --proxy socks5h://…` через naive-outbound → внешний IP стенда `89.124.96.226`;
  `mihomo v1.19.30` (`metacubex/mihomo@sha256:baf38d28…`) — `-t` ok, TCP через mieru-proxy →
  тот же IP, **UDP** — DNS-запрос к 1.1.1.1 через SOCKS5 UDP ASSOCIATE получил ответ.
  Матрица подтверждена живьём: `naive/singbox=supported`, `mieru/mihomo=supported`.
  Karing — ручная проверка владельца на устройстве (не выполнена в этом прогоне).
- Naive через `curl --proxy https://user:pass@comet…` → внешний IP стенда. MTProxy: installer
  acceptance `respq_verified: 2` (из `report.json`).
- Canary-скан: `docker logs proxy-control-panel` — 0 строк `/s/`, 0 вхождений пароля Mieru;
  `journalctl -u nginx` — 0; `sqlite3 .dump` без ключа — 0 вхождений пароля; в БД два
  `public_token_hash`, токенов нет. В `/var/log/nginx/access.log` настоящий токен встретился
  ровно в трёх моих же запросах к **домену панели** (`aurora…/s/<token>` → 404) — панельный
  vhost логировал чужой для него путь. Закрыто: в `_panel_vhost_text` добавлен
  `location ^~ /s/ { access_log off; return 404; }` (тест в `test_installer_core`). Остальные
  совпадения `/s/…` в логе — интернет-сканеры (`/s/<мусор>/_/;/META-INF/...`), не наш токен.
- Backup/restore drill по `docs/BACKUP_RESTORE.*.md`: online `sqlite3.backup` из контейнера
  (`integrity_check: ok`), ключ скопирован отдельно, `master-key-verify` на восстановленной
  паре → `{"k-a13e6492": 8}`, чужой ключ → ненулевой выход, подписка клиента `acceptance`
  отрендерена из восстановленных данных без менеджеров (3 строки: `tg`, `naive+https`,
  `mierus`); значения на экран не печатались.
- Замечание: `install` поверх активной транзакции с тем же плановым digest ничего не
  перерендеривает (vhost с новым `location` не обновился), `repair` проверяет владение по
  записанным хэшам — обновление шаблона на уже установленном хосте требует
  `uninstall` → `install` новым релизом.
- **Финальная переустановка итоговым архивом** (`/root/final-reinstall.sh` на стенде, детаченно):
  `uninstall --json` прежним релизом → `uninstalled` за 30 s, compose/vhost/контейнеры/x-ui сняты,
  секреты, `mtproxy_panel-data` и сертификаты сохранены. **Дефект транзакции**: `install` после
  завершённого `uninstall` — `BLOCKED: an installer transaction already exists`; guard в
  `Engine.apply` снимал только `rolled_back`, хотя `uninstalled` так же ничем не владеет.
  Исправлено (`installer/transaction.py`, тест
  `test_a_completed_uninstall_does_not_block_the_next_install`; прерванная транзакция
  по-прежнему блокирует). Вторая попытка уперлась в задуманное поведение `managed-new` 3x-ui:
  `managed 3x-ui refuses a pre-existing database: /etc/x-ui/x-ui.db` (базу сохранил сам
  `uninstall` без `--purge-data`), откат чистый (`rolled_back`); на разовом стенде база перенесена
  в сторону, ограничение записано в `docs/INSTALLER_REFERENCE.*.md`. Третья попытка: архив
  `6563217f93b4a8a09bc177619e830e31ce12678fc68604b11b530a05232baa6e` (из дерева с фиксом
  транзакции, `--allow-dirty`), `plan` digest `2696ddaa…`, `install` 143 s → `status: active`;
  `proxy-control-panel.conf`: два `server`, у панельного `location ^~ /s/ { access_log off;
  return 404; }`, у `eclipse` — `access_log off` и только `/s/`; `config_files` = три overlay,
  `NAIVE_ENABLED=true`, `MIERU_ENABLED=true`, `PANEL_ALLOWED_HOSTS=aurora,eclipse`; сертификат
  `nebula` прежний (`notBefore` 08:43 UTC, один `cert*.pem` в archive — перевыпуска не было);
  5 контейнеров healthy, `x-ui` active. `subscription-acceptance.py` заново —
  `SUBSCRIPTION_ACCEPTANCE_OK` (теми же пиннед-образами; проверка «тело 404 на домене панели =
  тело неизвестного токена» переписана: теперь панельный vhost отвечает самим nginx, поэтому
  инварианты — `panel_host_token_blind` (ответ на домене панели не зависит от токена) и
  `unknown_token_404_same_body` (неизвестный = малформ = отозванный на домене подписки)).
  Canary после переустановки: `/var/log/nginx/access.log` — 0 строк `/s/` из 108 с 13:22 (теперь и
  панельный vhost не логирует `/s/`), `docker logs proxy-control-panel` — 0, `journalctl -u nginx` — 0.

Собрать релиз (`python3 release/build.py --source . --output dist --version $(cat VERSION)`), доставить на стенд, установить профилем `full` по `/root/install.toml` (учитывая лимиты Let's Encrypt — сертификаты выпускаются заново). Затем:
- MTProxy: installer acceptance (TDLib `resPQ`) + `probe/`;
- Naive: `curl --proxy https://user:pass@comet.sky.dubr1kkk.uk https://api.ipify.org` → внешний адрес сервера;
- Mieru: контейнер `proxy-control-mieru-client:3.36.0` с `deploy/mieru-client/probe.py` по `mieru-client.json` из подписки (TCP и UDP);
- Через панель: импорт → «Принять доступ» (MTProxy и Naive — capture, Mieru — массовая ротация) → создание подписки → `curl -sI https://eclipse.sky.dubr1kkk.uk/s/<token>` (200, ETag) → повтор с `If-None-Match` (304) → тот же URL на домене панели → 404 → отключить grant → новый ETag → отозвать → 404;
- Проверка рендереров настоящими ядрами (решение 2 раздела 5): `?format=singbox` → `sing-box check`/`sing-box run --config` в одноразовом контейнере и реальный запрос через naive-outbound; `?format=clash` → `mihomo -t -f` и реальный запрос через mieru-proxy (TCP и UDP). Оба ядра — пиннед образы на стенде; падение любого из них означает ячейку `unproven`, а не замалчивание. Karing проверяется вручную на устройстве владельца (импорт URL, автообновление по интервалу приложения, трафик по naive и mieru) с записью результата в фикстуру Task 3;
- Canary-скан токена и credentials: `docker logs proxy-control-panel`, `/var/log/nginx/access.log`, `journalctl -u nginx`, `sqlite3 panel.sqlite3 .dump` (без ключа) — вхождений нет;
- Backup/restore drill по `docs/BACKUP_RESTORE.*.md` в изолированный контейнер: подписка рендерится, `master-key-verify` проходит, значения не печатаются.

- [x] **Step 6: Релизный поезд** (подготовлено 2026-09-11; тег ставит владелец)

Факт: `release.yml` — тег-паттерн расширен на `v[0-9]+.[0-9]+.[0-9]+-*`; job `lab-amd64`
удалён (`attest` теперь `needs: [build-twice-and-compare]`); добавлен вход
`workflow_dispatch.inputs.expected_sha256`, а для тега digest читается из аннотации
(`git tag -a vX.Y.Z -m 'lab-sha256: <digest>'`) — шаг `build-twice-and-compare` отказывает,
если digest не задан или не совпал с собранным. `VERSION` = `0.2.0-beta.1` (alpha.1 не
тегировалась: к моменту релизного гейта были готовы все Tasks 0–19, поэтому первый тег —
beta.1; сборка принимает предрелизный суффикс, `install-bootstrap` его отказывает — в
release notes описана ручная распаковка). `CHANGELOG.md`: секция `[0.2.0-beta.1] -
2026-09-11` (v0.2 + накопленное после 0.1.0), `[Unreleased]` пуст;
`docs/releases/v0.2.0-beta.1.md` — двуязычные release notes, checksum записывается при
тегировании. `tests/test_release_build.py::VERSION` поднят вслед за файлом;
`test_release_workflow_pins_actions_and_separates_privileged_jobs` теперь требует отсутствия
`lab-amd64`, вход `expected_sha256`, чтение `lab-sha256:` и оба тег-паттерна (первый финальный
`full` упал именно на старом ожидании `lab-amd64:`, 1 failed / 1555 passed).
Архив, прошедший финальную переустановку и приёмку (Step 5): sha256
`6563217f93b4a8a09bc177619e830e31ce12678fc68604b11b530a05232baa6e` — собран `--allow-dirty` из
рабочего дерева до правок документации/плана, поэтому **не равен** digest закоммиченного
дерева; для аннотации тега владелец после коммитов собирает чистое дерево на стенде
(`remote-gate.sh lab-host` делает это и печатает sha) и берёт digest из `dist/SHA256SUMS`.
Тег `v0.2.0-beta.1` и `v0.2.0` (после независимого security-review) — действия владельца.

По решению 3 раздела 5 гейт целиком на `ams-test`, а CI только подтверждает и публикует. В `.github/workflows/release.yml`: `on.push.tags` расширить до `"v[0-9]+.[0-9]+.[0-9]+"` и `"v[0-9]+.[0-9]+.[0-9]+-*"`; job `lab-amd64` убрать из цепочки (`attest` теперь `needs: [build-twice-and-compare]`) — его сценарий выполнен на стенде в Step 2 и Step 4; добавить вход `workflow_dispatch.inputs.expected_sha256` и шаг `test "$first" = "$EXPECTED_SHA256"` в `build-twice-and-compare`, чтобы опубликованы были ровно те байты, которые прошли `ams-test` (sha берётся из `dist/SHA256SUMS` на стенде). `VERSION` = `0.2.0-alpha.1` на момент тега; `CHANGELOG.md` `[Unreleased]` → `[0.2.0-alpha.1]`; `docs/releases/v0.2.0-alpha.1.md`. Теги `v0.2.0-alpha.1` (Tasks 0–11), `v0.2.0-beta.1` (Tasks 12–19), `v0.2.0` — после независимого security-review и полного Step 1–5. Каждый тег ставится только после зелёных Step 1–5 на стенде; `publish` по-прежнему ждёт ручного подтверждения в environment `release-publish`.

- [x] **Step 7: Обновить граф и отчёт** (выполнено 2026-09-11)

Факт: `graphify update .` — **6476 узлов / 18316 рёбер / 244 сообщества**, `Import Cycles: None
detected`, `*.sync-conflict-*` в `graphify-out/` нет. Финальный прогон на итоговом дереве (с
фиксом транзакции, правкой теста `release.yml` и новыми проверками acceptance):
`REMOTE_GATE_FULL_OK` — **1557 passed, 1 skipped** (4:47), `REMOTE_GATE_COMPOSE_OK`,
`REMOTE_GATE_LAB_CONTAINER_OK` (9 сценариев `passed`, при живой установке на том же хосте —
контейнерная лаборатория порты не публикует и ей не мешает; после прогона `status: active`,
5 контейнеров healthy). Отчёт владельцу — в ответе сессии и в
`CONTINUE-HERE-vnext-v0.2.md` («Состояние репозитория», «Что осталось владельцу»).
Не запускалось: Karing на устройстве (ручная проверка владельца); тег и коммиты — действия
владельца; `lab-host` на чистом закоммиченном дереве (даёт digest для аннотации тега) — после
коммитов.

**Повторный сквозной прогон по запросу владельца (2026-09-11, итоговое дерево с правками
документации):** `stand-refresh.sh` — `full` **1557 passed, 1 skipped** → `compose` OK →
`host-teardown` → `lab-host` **18/18 passed** (`install` 107 s, `repair` 38 s, `reboot-recovery` 63 s,
`uninstall` 28 s; архив `de22f79d8b09fc29d022be789e5d56dd686018a14884338f8f5c6ef08e68612a`,
`--allow-dirty`) → `host-teardown` → живая установка тем же архивом на чистый хост (первый `plan`
после teardown снова дал другой digest, второй и третий совпали — `5f3376b4…`; `install` 177 s →
`active`, четыре lineage LE выпущены заново 16:42–16:43 UTC — вторая выдача за день на каждый
набор имён, лимит 5/неделя; панельный vhost с `location ^~ /s/ { access_log off; return 404; }`,
три overlay) → `SUBSCRIPTION_ACCEPTANCE_OK` (sing-box через naive и mihomo через mieru TCP+UDP →
`89.124.96.226`) → `lab-container` **9/9 passed** при стоящей живой установке →
`REMOTE_GATE_LAB_CONTAINER_OK`. Canary после установки: 0 строк `/s/` в access.log, логах панели и
журнале nginx. Стенд оставлен с этой живой установкой. Побочное наблюдение: сеть на стороне
оператора дважды рвала SSH — опрос `lab-host` растянулся на часы, а `lab-container` завис без EOF
после завершения на стенде (перезапущен, OK); сам стенд отработал всё за штатное время.

`graphify update .` → `graphify-out` актуален; отчёт по AGENTS.md: файлы/границы, команды и результаты, что не запускалось.

---

## 4. Roadmap после v0.2 (отдельные планы, условия входа)

| Релиз | Что входит (spec) | Условие входа | Что требует отдельного плана/spike |
|---|---|---|---|
| **v0.3** Fleet v2 | Tasks 20–25A: protocol v2 schemas + capability handshake, desired/observed store, Unit of Work, v2 mTLS ingress с secret-results (X25519+HKDF+AES-GCM, второй keyring `PANEL_SECRET_INGRESS_KEYRING_FILE`), node-local journal/reconcile, три node-адаптера, shadow adoption и canary | v0.2.0 отмечен; ingress-контейнер получает свой keyring (`deploy/Dockerfile.ingress`, `compose.fleet-central.yaml`, `deploy/mtproxy-fleet-ingress.service`) | план `2026-xx-xx-vnext-v0.3-fleet-v2.md`; лаборатория с двумя узлами (`ams-test` как central + одноразовый второй хост или второй systemd-контейнер) |
| **v0.4** Routing | Tasks 26–31A: routing IR + preview, UI, **Task 28** — извлечение WARP из `[three_xui]` (подтверждённое coupling в `installer/adapters/naive.py:806`, `mieru.py:724`; дефолт порта 40000 vs 45000), typed egress API managers, spike | v0.3.0; Task 28 может идти раньше как самостоятельный installer-change (не зависит от Fleet v2) | spike `docs/spikes/VNEXT_ROUTING_ENGINE.md` на одноразовом хосте; решение по DNS/UDP |
| **v0.5** Xray-router | Tasks 32–36: отдельный runtime/manager, компилятор, bridges | результат spike Task 30 | отдельный план после spike |
| **Сквозное** | Tasks 37–41: backup/restore, staged rollouts, негативная матрица, release sequence | по мере релизов | частично закрыто в v0.2 (backup ключа, canary-сканы) |

## 5. Решения владельца (закрыты 2026-09-10)

Все семь вопросов закрыты. Раздел 7 spec остаётся историческим: для v0.2 авторитетен этот раздел.

**1. Subscription URL — отдельный, десятый домен `eclipse.sky.dubr1kkk.uk`** (выдан владельцем 2026-09-10, `A` → `89.124.96.226`, проверено `dig`). Домен используется исключительно под подписку; `zenith` остаётся за подпиской 3x-ui, `aurora` — за панелью. `/s/{token}` публикуется только на десятом домене, панельный vhost этот путь не обслуживает вообще (404), поэтому из подписки нельзя вывести имя панели. Реализация — Task 17: переменные `PANEL_SUBSCRIPTION_HOST` (имя) и `PANEL_SUBSCRIPTION_URL` (база для UI и QR), отдельный `server`-блок nginx с `access_log off` и только `location ^~ /s/`, ключ `subscription_domain` в `install.toml`, выпуск ACME на десятый домен, `PANEL_ALLOWED_HOSTS` = панель + подписка. Tasks 0–16 не блокированы: имя приходит из конфигурации, юнит-тесты используют синтетический `sub.example.com`, а живой гейт (Task 19A Step 5) — настоящий `eclipse.sky.dubr1kkk.uk`. К Task 17: добавить `subscription_domain = "eclipse.sky.dubr1kkk.uk"` в `/root/install.toml` на стенде и в примеры `docs/INSTALLER_REFERENCE.*.md`/`docs/OPERATIONS.*.md`; сертификат на десятый домен выпускается вместе с остальными в Step 5 (учитывать лимиты Let's Encrypt).

**2. Именованная матрица клиентов — по проверенным фактам, а не по названиям приложений.** Проверено 2026-09-10 по первоисточникам:

- sing-box: outbound `naive` появился в 1.13.0 и доступен только на Apple/Android/Windows и части сборок Linux; outbound `mieru` в официальном sing-box отсутствует.
- mihomo (Clash.Meta): `type: mieru` поддержан (`server`, `port`/`port-range`, `transport`, `username`, `password`, `multiplexing`, `udp: true`); naive не реализован — открытый feature request `MetaCubeX/mihomo#273`.
- Karing: ядро — собственный форк sing-box; поддержка Mieru заявлена в релиз-нотах приложения («Added support for the Mieru protocol (sing-box, clash)»); принимает подписки Clash, Clash.Meta, sing-box JSON, V2ray base64, Surfboard с автоопределением формата; авто-обновление задаётся интервалом в самом приложении, заголовок `Profile-Update-Interval` его семейством пока не разбирается (открытый запрос в `KaringX/clashmi#324`).
- Официальный клиент mieru (3.33+): подписок нет в принципе — только `mieru apply config <file>` и `mieru import config <mieru://|mierus://>`.
- Telegram: только `tg://proxy`, подписок нет и не будет.
- V2ray-base64-подписки парсят исключительно `vmess/vless/trojan/ss/hysteria/hysteria2/tuic`; ни `naive+https://`, ни `mierus://` в них не распознаются.

Отсюда набор рендереров v0.2 (заменяет `karing` из первой редакции плана):

| Рендерер | Media type | Что несёт | Кто получает auto-refresh |
|---|---|---|---|
| `singbox` | `application/json` | outbounds `naive` + `mieru`; MTProxy — в `unsupported` | Karing (обе связки), sing-box ≥ 1.13 (только naive, mieru пропущен с причиной) |
| `clash` | `text/yaml; charset=utf-8` | `proxies` типа `mieru` c `udp: true`; naive и MTProxy — в `unsupported` | mihomo и Clash-совместимые клиенты, Karing |
| `manifest` | `application/vnd.proxy-control.subscription+json;version=1` | канонический манифест + артефакты + `compatibility` | панель, CLI, будущие интеграции |
| `raw` | `text/plain; charset=utf-8` | `tg://proxy?…`, `naive+https://…`, `mierus://…` построчно | никто — ручной импорт, QR, человек |
| `html` | `text/html` | ссылки, QR, матрица совместимости | человек |

Гарантия v0.2: auto-refresh доказывается на живом стенде для Karing (naive + mieru), mihomo (mieru) и sing-box (naive). Все прочие приложения (Shadowrocket, NekoBox/Throne, v2rayN, Hiddify, Streisand) — явные ячейки `unproven`/`unsupported` с причиной; MTProxy в Telegram — навсегда `unsupported`. Ни один рендерер не выдаёт ссылку, которую целевой клиент не разбирает.

**3. Гейт — только `ams-test`, публикация после зелёного стенда.** GitHub Actions перестаёт быть гейтом: job `lab-amd64` (QEMU-лаборатория, красная с v0.1.0) убирается из обязательной цепочки — её сценарий полностью покрывают `remote-gate.sh lab-container` и `lab-host` на стенде. В `release.yml` остаются `quality` → `build-twice-and-compare` → `attest` → `draft-release` → `publish` (последний уже требует ручного подтверждения в environment `release-publish`), добавляется вход `expected_sha256`: workflow отказывается публиковать, если собранные им байты не совпадают с архивом, прошедшим `ams-test`. Так стенд решает, а CI только подтверждает воспроизводимость, ставит provenance-attestation и публикует. Тег-паттерн расширяется до `v[0-9]+.[0-9]+.[0-9]+` и `v[0-9]+.[0-9]+.[0-9]+-*`, чтобы alpha/beta собирались тем же путём. Альтернатива, если владелец захочет вообще нулевую сборку в CI: `gh release create` прямо с артефактами `ams-test` — но тогда теряется build-provenance attestation, поэтому по умолчанию берём вариант выше.

**4. Мастер-ключ — без ручного шага апгрейда.** Суть вопроса была: v0.2 шифрует пароли ключом из файла `secrets/panel-master-key`, а compose не стартует, если файла нет, — то есть апгрейд v0.1.0 → v0.2.0 мог потребовать ручного действия. Решение: ключ генерирует установщик так же, как остальные учётные данные (`_PRESERVED_CREDENTIALS`, режим `0600`), и на установке, и на апгрейде — владелец не делает ничего. `panel.cli master-key-init` остаётся только для установок, собранных руками мимо установщика, и упоминается одним абзацем в `docs/UPGRADING.md`. Панель стартует без ключа, пока в `secret_versions` нет ни одной записи, и отказывается стартовать (fail-closed), если зашифрованные строки есть, а ключ пропал. Резервная копия ключа хранится отдельно от БД — это уже в `docs/BACKUP_RESTORE.*.md` (Task 6).

**5. Mieru — «работает», а не «принято как UX».** Новые Mieru-доступы: пароль генерирует панель и передаёт менеджеру (Task 12b, caller-supplied credential), секрет ложится в хранилище в момент создания — подписка работает сразу. Импортированные доступы: mita хранит только `hashedPassword`, восстановить пароль нельзя, поэтому вводится явное действие «Принять доступ» — для MTProxy и Naive это capture без ротации, для Mieru это ротация, в том числе массовая («Принять все Mieru-доступы»). Диалог прямо предупреждает, что старый `mierus://` перестаёт работать в момент успешной ротации. Grant без `secret_ref` никогда не притворяется рабочим: в подписке он даёт строку `# unsupported mieru <username>: no stored credential`, а в UI подсвечивается как требующий действия. После принятия Mieru в подписке равноправен с остальными протоколами.

**6. `raw` — plain text, без base64.** Base64 — конвенция v2ray-подписок, парсеры которых принимают только `vmess/vless/trojan/ss/hysteria/hysteria2/tuic`; `naive+https://` и `mierus://` в них не разбираются, значит обёртка не добавляет ни одного клиента, зато ломает `curl`, диагностику, QR и чтение человеком. Auto-refresh обеспечивают `singbox` и `clash`, а `raw` остаётся человеко- и инструмент-читаемым. Если однажды появится клиент, которому нужен base64, добавляется отдельный `format=raw-base64` — новый рендерер с собственной ячейкой матрицы, а не изменение существующего.

**7. `.hermes/` — в `.gitignore`** (внесено в секцию `# Agent`).

### Что из этого меняется в задачах

- Task 3 — фикстура получает секцию `clients` с именованной матрицей из решения 2; каждая ячейка со `status` и `note`, ссылающейся на первоисточник.
- Task 11 — карточка grant показывает «нет сохранённого секрета» и действие «Принять доступ»; массовое принятие Mieru.
- Task 12d — `ClientService.adopt_credential(grant_id, *, allow_rotation)`: capture там, где протокол умеет, ротация там, где нет.
- Task 16 — рендереры `manifest | singbox | clash | raw | html`; `raw` — plain text; `compatibility.MATRIX` строится по решению 2.
- Task 17 — `/s/{token}` только на домене подписки (`eclipse.sky.dubr1kkk.uk`), отдельный vhost, `PANEL_SUBSCRIPTION_HOST`/`PANEL_SUBSCRIPTION_URL`, `subscription_domain` в `install.toml`.
- Task 19A — гейт целиком на `ams-test`; `release.yml` без `lab-amd64`, со входом `expected_sha256` и расширенным тег-паттерном.

## 6. Self-review плана (по writing-plans)

- **Покрытие spec (Phase 0–4):** Task 1→ADR/VNEXT_ARCHITECTURE ✓; Task 2→характеризация ✓; Task 3→матрица ✓; Task 4→БД/миграции ✓ (плюс межпроцессная безопасность, п. 4 раздела 0); Task 5→аудит ✓; Task 6→секреты + ключ в обоих рендерерах ✓; Task 7/8/9→nodes ✓ (decommission guard завершён в Task 11 ✓); Task 10/11 ✓ (capture vs rotate по протоколам — п. 6); Task 12 разбит на 12a–12d ✓; Task 13 saga с 6 точками отказа ✓; Task 14 флаг + import-on-touch (уточнение spec: «legacy request becomes domain intent») ✓; Task 15–19 ✓ (ETag от эффективного набора, expiry-тест, логи uvicorn/nginx — п. 2–3); Task 19A ✓ + релизный поезд (п. 13). Spec Task 16 предлагал править `panel/static/js/access.js` — не требуется: рендереры переиспользуют серверные builders; один-time reveal UI не меняется.
- **Плейсхолдеров нет**: каждая задача содержит тесты, интерфейсы, команды `remote-gate.sh` и коммит. Шаги «реализовать» опираются на код из предыдущих шагов той же задачи.
- **Решения владельца (раздел 5, 2026-09-10) внесены в задачи**: Task 3 (секция `clients` фикстуры), Task 11 («Принять доступ» и массовое принятие Mieru), Task 12d (Step 4a `adopt_credential`), Task 16 (`singbox`/`clash` вместо `karing`, `raw` без base64, `MATRIX` из фикстуры), Task 17 (отдельный vhost домена подписки, 404 на домене панели), Task 18 (варианты ссылки под клиента), Task 19A (проверка рендереров ядрами sing-box/mihomo, релизный поезд без `lab-amd64`). Единственный оставшийся ввод от владельца — имя десятого домена (нужно к Task 17).
- **Согласованность типов**: `Database/transaction`, `apply_migrations/Migration`, `audit.record/scrub/digest`, `Keyring/SecretStore/SecretRef/SecretError`, `NodeView/NodeLifecycleService/NodeConflict`, `Client/AccessGrant/GrantIntent/ClientStore/ClientService/ClientConflict`, `ProtocolAdapter/CredentialPlan/AppliedGrant/GrantRef/AccessArtifact`, `ProvisioningService`, `SubscriptionService/Manifest/RENDERERS` используются с одинаковыми именами во всех задачах; ключи fault-инъекций — `faults["create"|"rotate"]` во всех Memory-классах.
