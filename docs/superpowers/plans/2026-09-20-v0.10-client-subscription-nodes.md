# v0.10 — клиент на нескольких узлах и подписка под рукой: план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Оператор создаёт клиента сразу на нескольких узлах, получает ссылку подписки и QR в том же окне, а потом открывает клиента и видит ссылку снова, правя состав узлов галочками.

**Architecture:** Токен подписки дополнительно кладётся в `secret_versions` под keyring (`purpose="subscription"`), хэш остаётся для `/s/{token}`; новая ручка `reveal` расшифровывает его через одноразовый reveal с аудитом. Подписка выдаётся в транзакции создания клиента. UI: общий модуль `placement.js` рисует матрицу узел × протокол и считает диф, который уходит в существующие ручки `POST /grants` и `grants/{id}/{enable|disable}`; `#subscription-modal` становится окном клиента.

**Tech Stack:** Python 3.12, FastAPI, SQLite (миграции `panel/migrations.py`), pydantic, `cryptography` AES-GCM через `SecretStore`; фронт — ES-модули без сборки; тесты pytest + anyio + httpx ASGITransport, UI-контракты читают исходники, мобильный аудит — headless Chrome.

**Spec:** `docs/superpowers/specs/2026-09-20-v0.10-client-subscription-nodes-design.md`

## Global Constraints

- Все тесты и гейт — на стенде `ams-test` по SSH (`/root/dev/proxy-control`, rsync с Mac, `.venv`), не локально и не на боевых хостах; длинные прогоны отсоединённо (`setsid nohup … &`). Быстрый локальный `pytest` одной группы тестов допустим только для красного/зелёного цикла задачи; финальный прогон — на стенде.
- Сообщения коммитов — на русском, о сути; **без трейлеров** `Co-Authored-By`/`Generated with`; писать через `git commit -F -`.
- Ни одного секрета, токена, URL подписки в выводе, логах, коммитах, тестовых фикстурах, документации.
- Токен подписки никогда не попадает в модель `Subscription`, в ответ `GET /subscription`, в аудит, в события; наружу — только через `context.create_reveal`.
- Строки UI — на русском, в стиле существующих диалогов; `ui.confirmed(` только для деструктивных действий.
- Следующая миграция — **20** (`MIGRATIONS` заканчивается на `EXITS_V19`).
- Владелец подтвердил: подписки в парке ещё никому не выдавались, поэтому «подписка до v0.10 → ротируйте» остаётся в коде и в UPGRADING одной строкой, без отдельной процедуры.

## Структура файлов

| Файл | Ответственность |
|---|---|
| `panel/migrations.py` | миграция 20: `secret_id`, `secret_version` у `client_subscriptions` |
| `panel/subscriptions/models.py` | `Subscription.secret_ref` |
| `panel/subscriptions/store.py` | чтение/запись `secret_ref` |
| `panel/subscriptions/service.py` | escrow при `_issue`, `revoke`/`rotate` гасят версию, `reveal_token`, `create_with_client`; `_renderable` удаляется |
| `panel/subscription_routes.py` | `POST …/subscription/reveal`, `escrowed`/`secret_store`/`node_id` в обзоре |
| `panel/schemas.py` | `ClientCreate.subscription` |
| `panel/client_routes.py` | `POST /api/clients` с подпиской и `subscription_reveal_token` |
| `panel/static/js/placement.js` | **новый**: матрица узел × протокол — рендер, чтение, диф |
| `panel/static/js/subscriptions.js` | окно клиента: показ по кнопке, матрица, «Применить» |
| `panel/static/js/clients.js` | «Новый клиент» с матрицей; клик по карточке открывает окно; диалог «Выдать доступ» убран |
| `panel/static/js/access.js` | блок подписки в «Доступы выданы» |
| `panel/static/js/audit.js` | подпись `subscription.reveal` |
| `panel/static/index.html` | разметка окна клиента, «Новый клиент», бандла; `#grant-modal` удалён |
| `panel/static/style.css` | `.placement-*` |
| `panel/tests/test_subscription_escrow.py` | **новый**: escrow, reveal, ротация/отзыв, без keyring, rewrap |
| `panel/tests/test_subscription_lifecycle.py` | гранты без секрета не блокируют; клиент + подписка одной транзакцией |
| `panel/tests/test_subscription_http.py` | ручка reveal, `POST /clients` с подпиской, роли |
| `panel/tests/test_migrations.py` | миграция 20 на базе v0.9 |
| `panel/tests/test_placement_ui_contract.py` | **новый**: контракт матрицы и дифа (Node-скрипт поверх `placement.js`) |
| `panel/tests/test_subscription_ui_contract.py` | обновлённый контракт окна клиента |
| `panel/tests/test_mobile_layout.py` | окно клиента с матрицей на телефоне |
| `docs/adr/004-…`, `docs/OPERATIONS.{ru,en}.md`, `PANEL.ru.md`, `CHANGELOG.{md,ru.md}`, `docs/UPGRADING.{ru,en}.md`, `VERSION` | документация выпуска |

---

### Task 1: Миграция 20 и `secret_ref` у подписки

**Files:**
- Modify: `panel/migrations.py:445-465`
- Modify: `panel/subscriptions/models.py:13-23`
- Modify: `panel/subscriptions/store.py`
- Test: `panel/tests/test_migrations.py`, `panel/tests/test_subscription_escrow.py` (создать)

**Interfaces:**
- Produces: `Subscription.secret_ref: SecretRef | None` (поле dataclass, по умолчанию `None`); `SubscriptionStore.insert(db, subscription, token_hash)` пишет `secret_ref` из объекта; `SubscriptionStore.set_secret_ref(db, subscription_id, ref)`; таблица `client_subscriptions` получает `secret_id TEXT`, `secret_version INTEGER`.

- [ ] **Step 1: Тест миграции — колонки появляются, старая строка читается**

В `panel/tests/test_migrations.py` после `test_migration_creates_exactly_one_local_node_idempotently` добавить:

```python
def test_migration_20_adds_escrow_columns_and_keeps_old_subscriptions_readable(tmp_path):
    from panel.migrations import MIGRATIONS, apply_migrations
    from panel.subscriptions.store import SubscriptionStore

    database = Database(tmp_path / "panel.sqlite3")
    # Up to v0.9 (migration 19), a subscription row as v0.9 wrote it.
    apply_migrations(database, migrations=[m for m in MIGRATIONS if m.version <= 19])
    with database.transaction() as db:
        db.execute("INSERT INTO clients(id,display_name,state,metadata_json,created_at,updated_at) VALUES('c1','Old','active','{}',1,1)")
        db.execute(
            "INSERT INTO client_subscriptions(id,client_id,public_token_hash,generation,state,update_interval_hours,created_at,updated_at)"
            " VALUES('s1','c1','hash-1',1,'active',12,1,1)"
        )
    apply_migrations(database)
    with database.connect() as db:
        columns = {row[1] for row in db.execute("PRAGMA table_info(client_subscriptions)")}
        assert {"secret_id", "secret_version"} <= columns
        old = SubscriptionStore.active(db, "c1")
    assert old is not None and old.secret_ref is None
```

Если `apply_migrations` не принимает `migrations=`, посмотреть его сигнатуру в `panel/migrations.py` (функция в конце файла) и использовать тот способ, которым `test_migration_12_rebuilds_provisioning_operations_with_existing_rows` применяет часть миграций; имена колонок `clients` взять из `CLIENTS_V6`.

- [ ] **Step 2: Запустить — должен упасть**

Run: `cd panel && ../.venv/bin/pytest tests/test_migrations.py::test_migration_20_adds_escrow_columns_and_keeps_old_subscriptions_readable -q` (локально только для цикла; либо на стенде)
Expected: FAIL — нет колонок `secret_id`/`secret_version` или `Subscription` не имеет `secret_ref`.

- [ ] **Step 3: Миграция и модель**

`panel/migrations.py`, после `EXITS_V19`:

```python
# The subscription token gets an escrowed copy under the keyring (v0.10): the hash keeps
# serving /s/{token}, the copy lets the operator see the URL again. NULL = issued without
# escrow (before v0.10 or with no master key) — such a URL can only be rotated.
SUBSCRIPTION_ESCROW_V20 = Migration(20, "subscription-escrow", (
    "ALTER TABLE client_subscriptions ADD COLUMN secret_id TEXT",
    "ALTER TABLE client_subscriptions ADD COLUMN secret_version INTEGER",
))
```

и добавить `SUBSCRIPTION_ESCROW_V20` в конец `MIGRATIONS`.

`panel/subscriptions/models.py`:

```python
from ..secrets_store import SecretRef

@dataclass(frozen=True)
class Subscription:
    id: str
    client_id: str
    generation: int
    state: str
    update_interval_hours: int
    last_fetched_at: int | None
    created_at: int
    updated_at: int
    revoked_at: int | None = None
    # Where the escrowed token lives (v0.10); None = the URL can only be rotated, not shown.
    secret_ref: SecretRef | None = None
```

Докстринг модуля поправить: «Neither type carries the token: the panel keeps its hash and, with a keyring, an encrypted copy referenced by `secret_ref`; the plaintext never lives in a model.»

`panel/subscriptions/store.py`:

```python
from ..secrets_store import SecretRef
from .models import Subscription

COLUMNS = (
    "id, client_id, generation, state, update_interval_hours, last_fetched_at, "
    "created_at, updated_at, revoked_at, secret_id, secret_version"
)


def to_subscription(row) -> Subscription:
    return Subscription(
        id=row["id"],
        client_id=row["client_id"],
        generation=row["generation"],
        state=row["state"],
        update_interval_hours=row["update_interval_hours"],
        last_fetched_at=row["last_fetched_at"],
        created_at=row["created_at"],
        updated_at=row["updated_at"],
        revoked_at=row["revoked_at"],
        secret_ref=SecretRef(row["secret_id"], row["secret_version"]) if row["secret_id"] else None,
    )
```

В `insert` добавить две колонки и значения:

```python
    @staticmethod
    def insert(db, subscription: Subscription, token_hash: str) -> None:
        ref = subscription.secret_ref
        db.execute(
            """INSERT INTO client_subscriptions(id,client_id,public_token_hash,generation,state,
               update_interval_hours,last_fetched_at,created_at,updated_at,revoked_at,secret_id,secret_version)
               VALUES(?,?,?,?,?,?,?,?,?,?,?,?)""",
            (
                subscription.id, subscription.client_id, token_hash, subscription.generation,
                subscription.state, subscription.update_interval_hours, subscription.last_fetched_at,
                subscription.created_at, subscription.updated_at, subscription.revoked_at,
                ref.secret_id if ref else None, ref.version if ref else None,
            ),
        )
```

Докстринг модуля: «Subscription rows. The token itself is written only as a hash; its escrowed copy lives in `secret_versions` and the row keeps a reference.»

- [ ] **Step 4: Запустить тест миграции и всю группу подписок**

Run: `cd panel && ../.venv/bin/pytest tests/test_migrations.py tests/test_subscription_lifecycle.py tests/test_subscription_http.py -q`
Expected: PASS (существующие тесты не трогают новые колонки).

- [ ] **Step 5: Коммит**

```bash
git add panel/migrations.py panel/subscriptions/models.py panel/subscriptions/store.py panel/tests/test_migrations.py
printf '%s\n' 'подписки: миграция 20 — ссылка на escrow-копию токена (secret_id/secret_version) у client_subscriptions; Subscription.secret_ref' | git commit -q -F -
```

---

### Task 2: Escrow при выдаче, гашение при ротации/отзыве, `reveal_token`

**Files:**
- Modify: `panel/subscriptions/service.py`
- Create: `panel/tests/test_subscription_escrow.py`
- Modify: `panel/tests/test_subscription_lifecycle.py` (тест `test_create_refuses_grants_without_escrowed_credentials` заменить)

**Interfaces:**
- Consumes: `Subscription.secret_ref`, `SubscriptionStore.insert` (Task 1); `SecretStore.store/reveal/transition/enabled`.
- Produces: `SubscriptionService.SUBSCRIPTION_PURPOSE = "subscription"`; `SubscriptionService.reveal_token(client_id, *, actor, ip, request_id=None) -> tuple[Subscription, str]` — бросает `KeyError` (нет клиента/подписки), `ClientConflict("subscription is not escrowed: rotate it to get a URL that can be shown")`, `SecretError` (нет keyring); `SubscriptionService.escrow_enabled -> bool`.

- [ ] **Step 1: Тесты escrow**

Создать `panel/tests/test_subscription_escrow.py`:

```python
"""The subscription token is kept twice: as a hash for /s/{token}, and encrypted under the
keyring so the operator can see the URL again. Showing it is an audited action; a rotated
or revoked URL cannot be shown any more; without a keyring nothing is escrowed."""

from __future__ import annotations

import time
import uuid

import pytest

from panel.clients.models import AccessGrant, NaiveOptions
from panel.clients.service import CREDENTIAL_PURPOSE, ClientService
from panel.clients.store import ClientConflict
from panel.database import Database
from panel.keyring import Keyring
from panel.migrations import apply_migrations
from panel.secrets_store import SecretError, SecretStore
from panel.subscriptions.service import SUBSCRIPTION_PURPOSE, SubscriptionService

CTX = {"actor": {"id": 1, "username": "owner"}, "ip": "127.0.0.1", "request_id": "req-1"}


class Clock:
    def __init__(self):
        self.now = int(time.time())

    def time(self):
        return self.now


def _world(tmp_path, *, keyring):
    database = Database(tmp_path / "panel.sqlite3")
    apply_migrations(database)
    secrets = SecretStore(keyring)
    clients = ClientService(database, secrets, clock=Clock())
    subscriptions = SubscriptionService(database, clients, secrets, clock=clients.clock, public_base="https://sub.example.com")
    clients.on_change.append(subscriptions.bump_generation)
    return clients, subscriptions


def _grant(clients, client_id, *, with_secret=True):
    now = clients.clock.time()
    grant = AccessGrant(
        id=str(uuid.uuid4()), client_id=client_id, protocol="naive", node_id="local",
        endpoint_id="default", runtime_username="alice", desired_state="enabled",
        observed_state="enabled", options=NaiveOptions(), origin="provisioned",
        created_at=now, updated_at=now,
    )
    with clients.database.transaction() as db:
        clients.store.insert_grant(db, grant)
        if with_secret:
            ref = clients.secrets.store(
                db, secret_id=f"grant:{grant.id}", version=1, purpose=CREDENTIAL_PURPOSE,
                grant_id=grant.id, permitted_node_id="local", plaintext=b"pw", state="active",
            )
            clients.store.update_grant(db, grant.id, secret_id=ref.secret_id, secret_version=ref.version)
    return grant


def test_the_token_is_escrowed_and_can_be_shown_again_with_an_audit_row(tmp_path):
    clients, subscriptions = _world(tmp_path, keyring=Keyring.generate())
    client = clients.create_client("Sergey", **CTX)
    _grant(clients, client.id)
    subscription, token = subscriptions.create(client.id, **CTX)
    assert subscription.secret_ref is not None and subscription.secret_ref.secret_id == f"subscription:{subscription.id}"
    with subscriptions.database.connect() as db:
        row = db.execute("SELECT purpose, grant_id, permitted_node_id, state FROM secret_versions WHERE secret_id=?", (subscription.secret_ref.secret_id,)).fetchone()
        stored_hash = db.execute("SELECT public_token_hash FROM client_subscriptions").fetchone()[0]
    assert tuple(row) == (SUBSCRIPTION_PURPOSE, None, None, "active") and stored_hash != token
    # The plaintext is on disk only as ciphertext.
    assert token not in subscriptions.database.path.read_bytes().decode("latin-1")

    shown, again = subscriptions.reveal_token(client.id, **CTX)
    assert again == token and shown.id == subscription.id
    assert subscriptions.resolve(again).id == subscription.id
    with subscriptions.database.connect() as db:
        actions = [r[0] for r in db.execute("SELECT action FROM audit_log ORDER BY id")]
        details = [r[0] or "" for r in db.execute("SELECT detail_json FROM audit_log")]
    assert actions[-1] == "subscription.reveal" and all(token not in d for d in details)


def test_rotate_and_revoke_retire_the_escrowed_copy(tmp_path):
    clients, subscriptions = _world(tmp_path, keyring=Keyring.generate())
    client = clients.create_client("Sergey", **CTX)
    _grant(clients, client.id)
    first, old_token = subscriptions.create(client.id, **CTX)
    second, new_token = subscriptions.rotate(client.id, **CTX)
    with subscriptions.database.connect() as db:
        states = dict(db.execute("SELECT secret_id, state FROM secret_versions WHERE purpose=?", (SUBSCRIPTION_PURPOSE,)).fetchall())
    assert states[first.secret_ref.secret_id] == "revoked" and states[second.secret_ref.secret_id] == "active"
    assert subscriptions.reveal_token(client.id, **CTX)[1] == new_token
    subscriptions.revoke(client.id, **CTX)
    with subscriptions.database.connect() as db:
        states = dict(db.execute("SELECT secret_id, state FROM secret_versions WHERE purpose=?", (SUBSCRIPTION_PURPOSE,)).fetchall())
    assert states[second.secret_ref.secret_id] == "revoked"
    with pytest.raises(KeyError):
        subscriptions.reveal_token(client.id, **CTX)


def test_without_a_keyring_nothing_is_escrowed_and_reveal_refuses(tmp_path):
    clients, subscriptions = _world(tmp_path, keyring=None)
    client = clients.create_client("Sergey", **CTX)
    subscription, token = subscriptions.create(client.id, **CTX)
    assert subscription.secret_ref is None and len(token) >= 43
    assert subscriptions.escrow_enabled is False
    with pytest.raises(SecretError):
        subscriptions.reveal_token(client.id, **CTX)


def test_a_subscription_issued_before_escrow_is_a_conflict_not_a_crash(tmp_path):
    clients, subscriptions = _world(tmp_path, keyring=Keyring.generate())
    client = clients.create_client("Sergey", **CTX)
    subscription, _ = subscriptions.create(client.id, **CTX)
    with subscriptions.database.transaction() as db:
        db.execute("UPDATE client_subscriptions SET secret_id=NULL, secret_version=NULL WHERE id=?", (subscription.id,))
    with pytest.raises(ClientConflict, match="not escrowed"):
        subscriptions.reveal_token(client.id, **CTX)


def test_grants_without_a_credential_no_longer_block_a_subscription(tmp_path):
    clients, subscriptions = _world(tmp_path, keyring=Keyring.generate())
    client = clients.create_client("Imported", **CTX)
    _grant(clients, client.id, with_secret=False)
    subscription, _ = subscriptions.create(client.id, **CTX)
    assert subscription.generation == 1


def test_rewrap_and_verify_cover_subscription_tokens(tmp_path):
    keyring = Keyring.generate()
    clients, subscriptions = _world(tmp_path, keyring=keyring)
    client = clients.create_client("Sergey", **CTX)
    subscription, token = subscriptions.create(client.id, **CTX)
    rotated = keyring.rotate()
    fresh = SecretStore(rotated)
    with subscriptions.database.transaction() as db:
        while fresh.rewrap(db, batch_size=10):
            pass
        counts = fresh.verify_all(db)
    assert sum(counts.values()) >= 1
    subscriptions.secrets = fresh
    assert subscriptions.reveal_token(client.id, **CTX)[1] == token
```

Как `Keyring` ротируется — посмотреть `test_rotation_is_overlap_first_and_interrupted_rewrap_stays_decryptable` в `panel/tests/test_secrets_store.py:108` и повторить тот же вызов (там `keyring.rotate()` или аналог); если имя другое, использовать его.

В `panel/tests/test_subscription_lifecycle.py` **удалить** `test_create_refuses_grants_without_escrowed_credentials` (его смысл теперь обратный и покрыт `test_grants_without_a_credential_no_longer_block_a_subscription`). В `test_token_is_shown_once_and_only_its_hash_is_stored` оставить проверку `stored != token` и `token not in … read_bytes()` (шифртекст не содержит plaintext), название сменить на `test_token_is_stored_only_as_hash_and_ciphertext`; докстринг модуля: «…so the panel keeps its hash — and, under the keyring, an encrypted copy that only an audited reveal opens».

- [ ] **Step 2: Запустить — должно упасть**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_escrow.py -q`
Expected: FAIL — `ImportError: SUBSCRIPTION_PURPOSE`.

- [ ] **Step 3: Реализация в `panel/subscriptions/service.py`**

Импорты: `from ..secrets_store import SecretError, SecretRef`. Константа после `TOKEN_BYTES`:

```python
SUBSCRIPTION_PURPOSE = "subscription"
```

Заменить `_renderable` (удалить целиком) и `_issue`:

```python
    @property
    def escrow_enabled(self) -> bool:
        return self.secrets is not None and self.secrets.enabled

    def _issue(self, db, client_id: str, *, generation: int) -> tuple[Subscription, str]:
        now = int(self.clock.time())
        token = secret_tokens.token_urlsafe(TOKEN_BYTES)
        subscription_id = str(uuid.uuid4())
        secret_ref: SecretRef | None = None
        if self.escrow_enabled:
            # The AAD binds the ciphertext to this subscription id: a row cannot be moved
            # under another client, and the purpose keeps grant reveals from opening it.
            secret_ref = self.secrets.store(
                db, secret_id=f"subscription:{subscription_id}", version=1,
                purpose=SUBSCRIPTION_PURPOSE, grant_id=None, permitted_node_id=None,
                plaintext=token.encode(), state="active",
            )
        subscription = Subscription(
            id=subscription_id,
            client_id=client_id,
            generation=generation,
            state="active",
            update_interval_hours=12,
            last_fetched_at=None,
            created_at=now,
            updated_at=now,
            secret_ref=secret_ref,
        )
        self.store.insert(db, subscription, _hash(token))
        return subscription, token

    def _retire_escrow(self, db, subscription: Subscription) -> None:
        """A rotated or revoked URL must not be showable: the ciphertext row is revoked too."""
        if subscription.secret_ref is not None and self.escrow_enabled:
            self.secrets.transition(db, subscription.secret_ref, "revoked")
```

В `create`: убрать вызов `self._renderable(db, client_id)`. В `rotate`: убрать `self._renderable(...)`, после `self.store.revoke(db, client_id, now=now)` добавить `self._retire_escrow(db, current)`. В `revoke`: после `self.store.revoke(...)` добавить `self._retire_escrow(db, current)`.

Новый метод после `revoke`:

```python
    def reveal_token(self, client_id: str, *, actor: dict, ip: str, request_id: str | None = None):
        """Open the escrowed token for an operator: audited, never logged, never cached."""
        with self.database.transaction() as db:
            self.clients.store.client(db, client_id)  # KeyError when the client is unknown
            current = self.store.active(db, client_id)
            if current is None:
                raise KeyError(client_id)
            if current.secret_ref is None:
                raise ClientConflict("subscription is not escrowed: rotate it to get a URL that can be shown")
            if not self.escrow_enabled:
                raise SecretError("secret store is disabled: PANEL_MASTER_KEY_FILE is not configured")
            token = self.secrets.reveal(
                db, current.secret_ref, purpose=SUBSCRIPTION_PURPOSE, grant_id=None, permitted_node_id=None,
            ).decode()
            record(
                db, actor=actor, action="subscription.reveal", target=current.id,
                ip=ip, request_id=request_id, generation=current.generation,
                detail={"client_id": client_id},
            )
        return current, token
```

Докстринг модуля (первые абзацы) переписать: «The URL is a bearer credential, so its hash is what `/s/{token}` looks up. With a keyring the panel also keeps an encrypted copy so an operator can see the URL again; every such reveal is an audit row, and a rotated or revoked URL retires its copy in the same transaction.»

- [ ] **Step 4: Запустить группу**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_escrow.py tests/test_subscription_lifecycle.py tests/test_subscription_http.py tests/test_subscription_events.py -q`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add panel/subscriptions/service.py panel/tests/test_subscription_escrow.py panel/tests/test_subscription_lifecycle.py
printf '%s\n' 'подписки: токен хранится и в escrow под keyring — reveal_token с аудитом subscription.reveal, ротация и отзыв гасят копию, гранты без секрета больше не блокируют выдачу' | git commit -q -F -
```

---

### Task 3: Ручка `POST /api/clients/{id}/subscription/reveal` и расширенный обзор

**Files:**
- Modify: `panel/subscription_routes.py:64-157`
- Test: `panel/tests/test_subscription_http.py`

**Interfaces:**
- Consumes: `SubscriptionService.reveal_token`, `escrow_enabled` (Task 2).
- Produces: `POST /api/clients/{client_id}/subscription/reveal` → `{"reveal_token": str}` (201 не нужен, 200); `GET …/subscription` → `{"configured", "secret_store": bool, "escrowed": bool, "subscription", "grants": [{grant_id, protocol, node_id, runtime_username, enabled, has_credential}]}`.

- [ ] **Step 1: Тесты HTTP**

В `panel/tests/test_subscription_http.py` после `test_rotate_and_revoke_move_the_public_url` добавить:

```python
async def test_the_url_can_be_shown_again_through_an_audited_reveal(owner, public, client_id, app):
    created = await owner.post(f"/api/clients/{client_id}/subscription")
    first = (await owner.get(f"/api/reveal/{created.json()['reveal_token']}")).json()
    overview = (await owner.get(f"/api/clients/{client_id}/subscription")).json()
    assert overview["escrowed"] is True and overview["secret_store"] is True
    assert {item["node_id"] for item in overview["grants"]} == {"local"}

    shown = await owner.post(f"/api/clients/{client_id}/subscription/reveal")
    assert shown.status_code == 200 and set(shown.json()) == {"reveal_token"}
    payload = (await owner.get(f"/api/reveal/{shown.json()['reveal_token']}")).json()
    assert payload["url"] == first["url"] and set(payload["variants"]) == {"raw", "singbox", "singbox-official", "clash"}
    assert payload["qr"].startswith("data:image/svg+xml;base64,")
    # Consumed on first read, like every reveal.
    assert (await owner.get(f"/api/reveal/{shown.json()['reveal_token']}")).status_code == 410
    with app.state.database.connect() as db:
        actions = [row[0] for row in db.execute("SELECT action FROM audit_log ORDER BY id").fetchall()]
    assert actions.count("subscription.reveal") == 1

    await owner.post(f"/api/clients/{client_id}/subscription/revoke")
    assert (await owner.post(f"/api/clients/{client_id}/subscription/reveal")).status_code == 404


async def test_reveal_refuses_a_subscription_without_an_escrowed_copy(owner, client_id, app):
    await owner.post(f"/api/clients/{client_id}/subscription")
    with app.state.database.transaction() as db:
        db.execute("UPDATE client_subscriptions SET secret_id=NULL, secret_version=NULL")
    refused = await owner.post(f"/api/clients/{client_id}/subscription/reveal")
    assert refused.status_code == 409 and "rotate" in refused.json()["detail"]
    assert (await owner.get(f"/api/clients/{client_id}/subscription")).json()["escrowed"] is False
```

В `test_subscription_management_needs_a_writer_role_and_a_configured_url` внутри блока viewer добавить:

```python
        assert (await viewer.post(f"/api/clients/{client_id}/subscription/reveal")).status_code == 403
```

- [ ] **Step 2: Запустить — 404 на новой ручке**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_http.py -q -k "shown_again or without_an_escrowed or writer_role"`
Expected: FAIL (404/`KeyError: 'escrowed'`).

- [ ] **Step 3: Реализация в `panel/subscription_routes.py`**

Импорт: `from .secrets_store import SecretError`. В `overview`:

```python
        return {
            "configured": bool(service().public_base),
            "secret_store": service().escrow_enabled,
            "escrowed": current is not None and current.secret_ref is not None,
            "subscription": _public(current) if current else None,
            "grants": [
                {
                    "grant_id": grant.id,
                    "protocol": grant.protocol,
                    "node_id": grant.node_id,
                    "runtime_username": grant.runtime_username,
                    "enabled": effective_enabled(grant, client, now),
                    "has_credential": grant.secret_ref is not None,
                }
                for grant in grants
            ],
        }
```

и вспомогательная функция на уровне модуля (ссылка на секрет наружу не нужна, `dataclasses.asdict` её отдал бы):

```python
def _public(subscription) -> dict:
    """The row as the operator sees it: no secret reference, no token."""
    body = dataclasses.asdict(subscription)
    body.pop("secret_ref", None)
    return body
```

Новая ручка после `rotate`:

```python
    @app.post("/api/clients/{client_id}/subscription/reveal")
    async def reveal(client_id: str, request: Request, user=Depends(context.roles("owner", "admin"))):
        """Show the current URL again: the escrowed token is opened for one reveal and audited."""
        try:
            subscription, token = await asyncio.to_thread(
                service().reveal_token, client_id, **context.domain_context(request, user)
            )
        except KeyError as exc:
            raise HTTPException(404, "client or subscription not found") from exc
        except ClientConflict as exc:
            raise HTTPException(409, str(exc)) from exc
        except SecretError as exc:
            raise HTTPException(409, str(exc)) from exc
        return {"reveal_token": context.create_reveal(reveal_payload(subscription, token), user)}
```

Докстринг `register_subscription_admin_routes`: «The operator's side: one subscription per client; its URL is shown at creation and, with a keyring, again on request.»

- [ ] **Step 4: Запустить весь файл**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_http.py -q`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add panel/subscription_routes.py panel/tests/test_subscription_http.py
printf '%s\n' 'подписки: POST /api/clients/{id}/subscription/reveal — показать ссылку снова через одноразовый reveal (owner/admin, аудит); обзор отдаёт escrowed, secret_store и node_id грантов' | git commit -q -F -
```

---

### Task 4: Подписка выдаётся вместе с клиентом

**Files:**
- Modify: `panel/schemas.py:143-144`
- Modify: `panel/subscriptions/service.py` (новый метод `create_with_client`)
- Modify: `panel/client_routes.py:68-73`
- Test: `panel/tests/test_subscription_lifecycle.py`, `panel/tests/test_subscription_http.py`

**Interfaces:**
- Consumes: `ClientService.new_client(db, display_name, *, now)`, `ClientService.notify`, `_issue` (Task 2).
- Produces: `SubscriptionService.create_with_client(display_name, *, actor, ip, request_id=None) -> tuple[Client, Subscription | None, str | None]`; `ClientCreate.subscription: bool = True`; `POST /api/clients` → `{**client, "subscription_reveal_token": str | None}`.

- [ ] **Step 1: Тесты**

В `panel/tests/test_subscription_lifecycle.py` добавить:

```python
async def test_a_client_and_its_subscription_are_born_in_one_transaction(subscriptions, clients, monkeypatch):
    subscriptions.public_base = "https://sub.example.com"
    client, subscription, token = subscriptions.create_with_client("Together", **CTX)
    assert subscription.client_id == client.id and subscription.generation == 1 and len(token) >= 43
    assert subscriptions.resolve(token).id == subscription.id
    with subscriptions.database.connect() as db:
        actions = [r[0] for r in db.execute("SELECT action FROM audit_log ORDER BY id")]
    assert actions[-2:] == ["client.create", "subscription.create"]

    from panel.subscriptions import service as module

    calls = {"n": 0}
    original = module.record

    def fail_on_subscription(db, **kwargs):
        if kwargs.get("action") == "subscription.create":
            raise RuntimeError("audit down")
        return original(db, **kwargs)

    monkeypatch.setattr(module, "record", fail_on_subscription)
    with pytest.raises(RuntimeError):
        subscriptions.create_with_client("Half", **CTX)
    # Neither the client nor the subscription survived: one transaction.
    assert all(c.display_name != "Half" for c in clients.list_clients())


async def test_create_with_client_without_a_subscription_domain_makes_only_the_client(subscriptions, clients):
    subscriptions.public_base = ""
    client, subscription, token = subscriptions.create_with_client("Alone", **CTX)
    assert subscription is None and token is None
    assert subscriptions.get(client.id) is None
```

Проверить, что `record` в `panel/subscriptions/service.py` вызывается с ключевыми аргументами (да: `record(db, actor=…, action=…)`), иначе обёртка не перехватит.

В `panel/tests/test_subscription_http.py` добавить:

```python
async def test_post_clients_hands_back_a_subscription_reveal(owner, public):
    created = await owner.post("/api/clients", json={"display_name": "Phone"})
    assert created.status_code == 201, created.text
    body = created.json()
    token = body["subscription_reveal_token"]
    assert token and "token" not in {k for k in body if k != "subscription_reveal_token"}
    payload = (await owner.get(f"/api/reveal/{token}")).json()
    assert payload["url"].startswith(f"https://{SUB_HOST}/s/") and payload["generation"] == 1
    assert (await public.get("/s/" + payload["url"].rsplit("/", 1)[1])).status_code == 200
    overview = (await owner.get(f"/api/clients/{body['id']}/subscription")).json()
    assert overview["subscription"]["generation"] == 1 and overview["escrowed"] is True

    without = await owner.post("/api/clients", json={"display_name": "Card only", "subscription": False})
    assert without.status_code == 201 and without.json()["subscription_reveal_token"] is None
```

- [ ] **Step 2: Запустить — падает**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_lifecycle.py tests/test_subscription_http.py -q -k "born_in_one or makes_only or hands_back"`
Expected: FAIL — нет `create_with_client`, нет `subscription_reveal_token`.

- [ ] **Step 3: Реализация**

`panel/schemas.py`:

```python
class ClientCreate(BaseModel):
    display_name: str = Field(min_length=1, max_length=128)
    # A subscription URL is issued with the client when a subscription domain is configured.
    subscription: bool = True
```

`panel/subscriptions/service.py`, после `create`:

```python
    def create_with_client(self, display_name: str, *, actor: dict, ip: str, request_id: str | None = None):
        """A client and its URL in one transaction: the operator sees the link on the same
        screen the client was made on. Without a subscription domain only the client is made."""
        from ..audit import digest

        now = int(self.clock.time())
        with self.database.transaction() as db:
            client = self.clients.new_client(db, display_name, now=now)
            record(
                db, actor=actor, action="client.create", target=client.id, ip=ip,
                request_id=request_id, after_digest=digest(client.model_dump()),
            )
            self.clients.notify(db, client.id)
            if not self.public_base:
                return client, None, None
            subscription, token = self._issue(db, client.id, generation=1)
            record(
                db, actor=actor, action="subscription.create", target=subscription.id,
                ip=ip, request_id=request_id, generation=subscription.generation,
                detail={"client_id": client.id},
            )
        return client, subscription, token
```

Импорт `digest` вынести наверх рядом с `record` (`from ..audit import digest, record`), а не внутрь метода.

`panel/client_routes.py`:

```python
    @app.post("/api/clients", status_code=201)
    async def create(body: ClientCreate, request: Request, user=Depends(context.roles("owner", "admin"))):
        subscriptions = app.state.subscriptions
        if body.subscription and subscriptions.public_base:
            client, subscription, token = await asyncio.to_thread(
                subscriptions.create_with_client, body.display_name, **_context(request, user)
            )
            reveal = context.create_reveal(subscription_reveal_payload(app, subscription, token), user)
            return {**client.model_dump(), "subscription_reveal_token": reveal}
        client = await asyncio.to_thread(
            app.state.clients.create_client, body.display_name, **_context(request, user)
        )
        return {**client.model_dump(), "subscription_reveal_token": None}
```

`reveal_payload` сейчас — замыкание внутри `register_subscription_admin_routes`. Вынести его на уровень модуля `panel/subscription_routes.py`:

```python
def subscription_reveal_payload(app, subscription, token: str) -> dict:
    """Everything a dialog shows: the URL, its variants and their QR codes."""
    url = app.state.subscriptions.public_url(token)
    return {
        "subscription_id": subscription.id,
        "generation": subscription.generation,
        "url": url,
        "qr": qr_data(url),
        "variants": {
            name: {"url": f"{url}{query}", "qr": qr_data(f"{url}{query}")}
            for name, query in SHARE_VARIANTS.items()
        },
    }
```

и внутри `register_subscription_admin_routes` заменить `reveal_payload(subscription, token)` на `subscription_reveal_payload(app, subscription, token)` (в `issue` и в `reveal` из Task 3). В `client_routes.py` импорт: `from .subscription_routes import subscription_reveal_payload` — проверить, что нет циклического импорта (`subscription_routes` не импортирует `client_routes`; да).

- [ ] **Step 4: Запустить группу и тесты клиентов**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_lifecycle.py tests/test_subscription_http.py tests/test_clients_domain.py tests/test_grant_lifecycle.py tests/test_client_import.py tests/test_rbac_audit.py -q`
Expected: PASS. Если `test_rbac_audit.py` или другие ожидают у `POST /api/clients` ровно поля `Client` — обновить ожидание на `subscription_reveal_token` в ответе.

- [ ] **Step 5: Коммит**

```bash
git add panel/schemas.py panel/subscriptions/service.py panel/subscription_routes.py panel/client_routes.py panel/tests/test_subscription_lifecycle.py panel/tests/test_subscription_http.py
printf '%s\n' 'клиенты: подписка выдаётся в транзакции создания клиента — POST /api/clients отдаёт subscription_reveal_token (subscription=false — только карточка; без домена подписки — null)' | git commit -q -F -
```

---

### Task 5: Аудит и документация модели (ADR 004, OPERATIONS)

**Files:**
- Modify: `panel/static/js/audit.js:58-63`
- Modify: `docs/adr/004-client-access-grant-subscription.md`
- Modify: `docs/OPERATIONS.ru.md:124-136`, `docs/OPERATIONS.en.md:118-130`
- Test: `panel/tests/test_subscription_ui_contract.py` (одна проверка подписи)

- [ ] **Step 1: Тест подписи аудита**

В `panel/tests/test_subscription_ui_contract.py` добавить:

```python
def test_the_audit_screen_names_the_reveal():
    audit = (ROOT / "static/js/audit.js").read_text()
    assert '"subscription.reveal": "Ссылка подписки показана"' in audit
```

- [ ] **Step 2: Запустить — падает**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_ui_contract.py::test_the_audit_screen_names_the_reveal -q`
Expected: FAIL.

- [ ] **Step 3: Правки**

`panel/static/js/audit.js` после строки `"subscription.revoke": …`:

```js
  "subscription.reveal": "Ссылка подписки показана",
```

`docs/adr/004-client-access-grant-subscription.md`, в `## Decision` после пункта про `ClientSubscription`:

```markdown
- **Since v0.10** the token is also kept encrypted under the panel keyring
  (`secret_versions`, purpose `subscription`). Showing the URL again is an
  owner/admin action that goes through a one-time reveal and writes an audit row;
  rotation and revocation retire the encrypted copy in the same transaction.
  Without a keyring nothing is escrowed and the URL is shown once, as before.
- A subscription is issued together with the client when a subscription domain is
  configured; grants without a stored credential do not block it — they render as
  `unsupported`, honestly, as every renderer already does.
```

Статус в шапке: `Status: accepted (v0.2), amended v0.10 (escrowed token)`.

`docs/OPERATIONS.ru.md`, абзац строк 126–130 заменить:

```markdown
Ссылка подписки клиента (`https://<домен подписки>/s/<token>`) — bearer-секрет: тот, у кого
она есть, получает все доступы этого клиента. По конструкции она не попадает в логи: панель
запускает uvicorn без access log, блок `server` Nginx для домена подписки имеет `access_log off`,
а в базе панели хранится хэш токена и (с v0.10) его копия, зашифрованная мастер-ключом панели —
без ключа копии нет. Показ ссылки в панели («Показать» в окне клиента) доступен owner/admin,
идёт через одноразовый reveal и пишет в аудит `subscription.reveal`. Не добавляйте логирование
запросов на этот путь и не вставляйте ссылку подписки в тикеты.
```

`docs/OPERATIONS.en.md`, строки 120–125 заменить:

```markdown
A client subscription URL (`https://<subscription domain>/s/<token>`) is a bearer
credential: whoever holds it receives every access of that client. By design it
never reaches a log — the panel runs uvicorn without an access log, the Nginx
`server` block for the subscription domain has `access_log off`, and the panel
stores the token's hash plus (since v0.10) a copy encrypted under the panel master
key — no key, no copy. Showing the URL in the panel («Показать» in the client
window) is an owner/admin action through a one-time reveal, audited as
`subscription.reveal`. Do not add request logging to that path, and do not paste
a subscription URL into a ticket.
```

- [ ] **Step 4: Запустить контракт**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_ui_contract.py -q`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add panel/static/js/audit.js docs/adr/004-client-access-grant-subscription.md docs/OPERATIONS.ru.md docs/OPERATIONS.en.md panel/tests/test_subscription_ui_contract.py
printf '%s\n' 'docs: ADR 004 и OPERATIONS ru/en — токен подписки в escrow под мастер-ключом, показ через reveal с аудитом subscription.reveal; подпись действия в журнале' | git commit -q -F -
```

---

### Task 6: Модуль матрицы `placement.js` — рендер, чтение, диф

**Files:**
- Create: `panel/static/js/placement.js`
- Create: `panel/tests/test_placement_ui_contract.py`
- Modify: `panel/static/style.css` (блок `.placement-*`)

**Interfaces:**
- Produces (ES-модуль, чистые функции, без DOM-состояния):
  - `PROTOCOLS = ["mtproxy", "naive", "mieru"]`
  - `placementRows(nodes, grants)` → `[{node_id, label, offered: bool, cells: {[protocol]: grant | null}}]` — строки: `local` + связанные панели с `link.enabled === true`, плюс узлы, у которых есть гранты (не `deleted`), но которые не предлагаются (`offered=false`).
  - `renderPlacement(rows, {canWrite})` → HTML-строка таблицы `table.placement` с `input[type=checkbox][data-node][data-protocol]`, состояние в `small`, кнопка `button[data-placement-delete][data-grant-id]` у клетки с грантом.
  - `readPlacement(root)` → `Set` строк `"node_id:protocol"` отмеченных чекбоксов.
  - `placementDiff(rows, checked, username)` → `{create: [{protocol, node_id, runtime_username, options: {}}], enable: [grant_id], disable: [grant_id]}`.
  - `cellStatus(grant)` → текст состояния клетки.

- [ ] **Step 1: Контракт-тест поверх Node**

Создать `panel/tests/test_placement_ui_contract.py`:

```python
"""placement.js is pure: rows from nodes+grants, a diff from ticks. Node runs the module
as shipped, so the test exercises the real code, not a re-implementation."""

from __future__ import annotations

import json
import shutil
import subprocess
from pathlib import Path

import pytest

ROOT = Path(__file__).resolve().parents[1]
NODE = shutil.which("node")

NODES = [
    {"node_id": "local", "display_name": "self", "transport": "local"},
    {"node_id": "fra", "display_name": "Frankfurt", "transport": "panel", "link": {"enabled": True}},
    {"node_id": "paused", "display_name": "Paused", "transport": "panel", "link": {"enabled": False}},
    {"node_id": "agent", "display_name": "Agent v1", "transport": "agent"},
]
GRANTS = [
    {"id": "g-naive-local", "protocol": "naive", "node_id": "local", "desired_state": "enabled", "observed_state": "enabled", "secret_ref": {"secret_id": "s", "version": 1}, "runtime_username": "alice"},
    {"id": "g-mieru-fra", "protocol": "mieru", "node_id": "fra", "desired_state": "disabled", "observed_state": "disabled", "secret_ref": {"secret_id": "s", "version": 1}, "runtime_username": "alice"},
    {"id": "g-mt-paused", "protocol": "mtproxy", "node_id": "paused", "desired_state": "enabled", "observed_state": "pending", "secret_ref": None, "runtime_username": "alice"},
    {"id": "g-deleted", "protocol": "mtproxy", "node_id": "local", "desired_state": "deleted", "observed_state": "missing", "secret_ref": None, "runtime_username": "alice"},
]


def run(script: str) -> dict:
    module = (ROOT / "static/js/placement.js").as_uri()
    code = f"import * as p from {json.dumps(module)}; const NODES={json.dumps(NODES)}; const GRANTS={json.dumps(GRANTS)}; {script}"
    result = subprocess.run([NODE, "--input-type=module", "-e", code], capture_output=True, text=True, check=True)
    return json.loads(result.stdout)


@pytest.mark.skipif(NODE is None, reason="node is not installed")
def test_rows_offer_local_and_linked_panels_and_keep_nodes_that_already_hold_grants():
    rows = run("console.log(JSON.stringify(p.placementRows(NODES, GRANTS)))")
    by_id = {row["node_id"]: row for row in rows}
    assert [row["node_id"] for row in rows] == ["local", "fra", "paused"]
    assert by_id["local"]["offered"] and by_id["fra"]["offered"] and not by_id["paused"]["offered"]
    assert by_id["local"]["label"] == "Этот сервер" and by_id["paused"]["label"] == "Paused"
    assert by_id["local"]["cells"]["naive"]["id"] == "g-naive-local"
    assert by_id["local"]["cells"]["mtproxy"] is None  # deleted grants are empty cells
    assert by_id["paused"]["cells"]["mtproxy"]["id"] == "g-mt-paused"


@pytest.mark.skipif(NODE is None, reason="node is not installed")
def test_diff_creates_enables_and_disables_from_ticks():
    diff = run("""
      const rows = p.placementRows(NODES, GRANTS);
      const checked = new Set(["fra:naive", "fra:mieru", "local:mieru"]);  // local:naive unticked
      console.log(JSON.stringify(p.placementDiff(rows, checked, "alice")));
    """)
    assert sorted((g["node_id"], g["protocol"]) for g in diff["create"]) == [("fra", "naive"), ("local", "mieru")]
    assert all(g["runtime_username"] == "alice" and g["options"] == {} for g in diff["create"])
    assert diff["enable"] == ["g-mieru-fra"] and diff["disable"] == ["g-naive-local"]


@pytest.mark.skipif(NODE is None, reason="node is not installed")
def test_unoffered_rows_are_never_part_of_the_diff():
    diff = run("""
      const rows = p.placementRows(NODES, GRANTS);
      console.log(JSON.stringify(p.placementDiff(rows, new Set(["paused:naive"]), "alice")));
    """)
    assert diff == {"create": [], "enable": [], "disable": ["g-naive-local"]}


@pytest.mark.skipif(NODE is None, reason="node is not installed")
def test_render_marks_cells_and_disables_what_cannot_change():
    html = run("""
      const rows = p.placementRows(NODES, GRANTS);
      console.log(JSON.stringify({rw: p.renderPlacement(rows, {canWrite: true}), ro: p.renderPlacement(rows, {canWrite: false})}));
    """)
    rw, ro = html["rw"], html["ro"]
    assert 'data-node="local" data-protocol="naive"' in rw and "checked" in rw
    assert 'data-node="paused"' in rw and "панель на паузе" in rw
    assert rw.count("data-placement-delete") == 3  # naive local, mieru fra, mtproxy paused
    assert "выключен" in rw and "ожидает узел" in rw and "без секрета" in rw
    assert "data-placement-delete" not in ro and ro.count("disabled") >= 9


def test_status_words_match_the_card():
    javascript = (ROOT / "static/js/placement.js").read_text()
    for word in ("ожидает узел", "выключен", "без секрета", "ошибка", "панель на паузе"):
        assert word in javascript, word
```

- [ ] **Step 2: Запустить — модуля нет**

Run: `cd panel && ../.venv/bin/pytest tests/test_placement_ui_contract.py -q`
Expected: FAIL (`ERR_MODULE_NOT_FOUND`). Если `node` на машине нет — тесты skip; на стенде `node` есть (память о стенде), там они обязаны пройти.

- [ ] **Step 3: Модуль `panel/static/js/placement.js`**

```js
// Where a client lives: one row per node, one column per protocol, one grant per cell.
// Pure functions — the dialogs render the HTML and hand the ticks back here for the diff,
// so «Новый клиент» and the client window share one definition of what a tick means.
import { esc } from "./common.js";

export const PROTOCOLS = ["mtproxy", "naive", "mieru"];
export const PROTOCOL_NAMES = { mtproxy: "MTProxy", naive: "NaiveProxy", mieru: "Mieru" };

const OBSERVED = { pending: "ожидает узел", failed: "ошибка", drifted: "расхождение", missing: "удалён" };

function offered(node) {
  return node.node_id === "local" || (node.transport === "panel" && node.link?.enabled === true);
}

function label(node) {
  return node.node_id === "local" ? "Этот сервер" : node.display_name || node.node_id;
}

// A grant's state in one phrase: what the card says, shortened to fit a cell.
export function cellStatus(grant) {
  if (!grant) return "";
  if (grant.observed_state === "failed") return grant.last_error ? `ошибка: ${grant.last_error}` : "ошибка";
  if (OBSERVED[grant.observed_state]) return OBSERVED[grant.observed_state];
  if (grant.desired_state === "disabled") return "выключен";
  if (grant.secret_ref === null || grant.secret_ref === undefined) return "без секрета";
  return "включён";
}

export function placementRows(nodes, grants) {
  const live = grants.filter((grant) => grant.desired_state !== "deleted");
  const known = new Map();
  for (const node of nodes) known.set(node.node_id, node);
  const ids = [];
  for (const node of nodes) if (offered(node)) ids.push(node.node_id);
  for (const grant of live) if (!ids.includes(grant.node_id)) ids.push(grant.node_id);
  if (!ids.includes("local")) ids.unshift("local");
  return ids.map((id) => {
    const node = known.get(id) || { node_id: id, display_name: id };
    const cells = {};
    for (const protocol of PROTOCOLS) cells[protocol] = live.find((g) => g.node_id === id && g.protocol === protocol) || null;
    return { node_id: id, label: label(node), offered: offered(node), cells };
  });
}

function cell(row, protocol, canWrite) {
  const grant = row.cells[protocol];
  const ticked = grant !== null && grant.desired_state === "enabled";
  const locked = !canWrite || !row.offered;
  const remove = canWrite && grant && row.offered
    ? `<button type="button" class="ghost danger-text" data-placement-delete data-grant-id="${esc(grant.id)}" title="Удалить доступ">×</button>`
    : "";
  return `<td class="placement-cell">
    <label><input type="checkbox" data-node="${esc(row.node_id)}" data-protocol="${esc(protocol)}"${ticked ? " checked" : ""}${locked ? " disabled" : ""}>
    <small>${esc(cellStatus(grant))}</small></label>${remove}
  </td>`;
}

export function renderPlacement(rows, { canWrite }) {
  const head = PROTOCOLS.map((protocol) => `<th>${esc(PROTOCOL_NAMES[protocol])}</th>`).join("");
  const body = rows.map((row) => `<tr data-placement-node="${esc(row.node_id)}"${row.offered ? "" : ' class="placement-row-locked"'}>
    <th scope="row">${esc(row.label)}${row.offered ? "" : " <small>панель на паузе</small>"}</th>
    ${PROTOCOLS.map((protocol) => cell(row, protocol, canWrite)).join("")}
  </tr>`).join("");
  return `<div class="placement-scroll"><table class="placement"><thead><tr><th>Узел</th>${head}</tr></thead><tbody>${body}</tbody></table></div>`;
}

export function readPlacement(root) {
  const checked = new Set();
  root.querySelectorAll("input[data-node][data-protocol]:checked").forEach((box) => {
    checked.add(`${box.dataset.node}:${box.dataset.protocol}`);
  });
  return checked;
}

// Ticks against rows: a tick without a grant creates, a tick on a disabled grant enables,
// a missing tick on an enabled grant disables. Rows that are not offered never change.
export function placementDiff(rows, checked, username) {
  const diff = { create: [], enable: [], disable: [] };
  for (const row of rows) {
    if (!row.offered) continue;
    for (const protocol of PROTOCOLS) {
      const grant = row.cells[protocol];
      const ticked = checked.has(`${row.node_id}:${protocol}`);
      if (ticked && grant === null) diff.create.push({ protocol, node_id: row.node_id, runtime_username: username, options: {} });
      else if (ticked && grant.desired_state === "disabled") diff.enable.push(grant.id);
      else if (!ticked && grant !== null && grant.desired_state === "enabled") diff.disable.push(grant.id);
    }
  }
  return diff;
}
```

Проверить, что `common.js` экспортирует `esc` (да — `clients.js` импортирует его оттуда) и что импорт `./common.js` не тянет DOM при загрузке в Node (если `common.js` обращается к `document` на верхнем уровне, тест упадёт при импорте — тогда в `placement.js` объявить свой `esc`: `const esc = (v) => String(v ?? "").replace(/[&<>"']/g, (c) => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));` и не импортировать `common.js`).

`panel/static/style.css`, добавить в конец блока клиентов (рядом со строками 107–110):

```css
.placement-scroll{overflow-x:auto;max-width:100%;margin:10px 0;-webkit-overflow-scrolling:touch}
.placement{border-collapse:collapse;min-width:100%;font-size:12px}
.placement th,.placement td{border:1px solid var(--line);padding:6px 8px;text-align:left;vertical-align:top;white-space:nowrap}
.placement th[scope=row]{font-weight:560}.placement th[scope=row] small{color:var(--muted);font-weight:400;margin-left:4px}
.placement-cell label{display:inline-flex;align-items:center;gap:6px;margin:0}.placement-cell small{color:var(--muted);font-size:10px}
.placement-cell button{margin-left:4px;min-height:24px;padding:0 6px}
.placement-row-locked th,.placement-row-locked td{color:var(--muted)}
```

- [ ] **Step 4: Запустить контракт**

Run: `cd panel && ../.venv/bin/pytest tests/test_placement_ui_contract.py -q`
Expected: PASS (5 тестов; при отсутствии `node` — 4 skip + 1 pass, тогда прогнать на стенде).

- [ ] **Step 5: Коммит**

```bash
git add panel/static/js/placement.js panel/static/style.css panel/tests/test_placement_ui_contract.py
printf '%s\n' 'panel: placement.js — матрица узел × протокол (строки: этот сервер, связанные панели, узлы с грантами на паузе), состояние клетки, диф отметок в create/enable/disable; контракт под node' | git commit -q -F -
```

---

### Task 7: Окно клиента: показ ссылки по кнопке, матрица, «Применить»

**Files:**
- Modify: `panel/static/index.html:257-277` (диалог `#subscription-modal`)
- Modify: `panel/static/js/subscriptions.js`
- Modify: `panel/static/js/clients.js` (клик по карточке, кнопка «Открыть», удаление ветки `grant`)
- Test: `panel/tests/test_subscription_ui_contract.py`

**Interfaces:**
- Consumes: `placementRows/renderPlacement/readPlacement/placementDiff` (Task 6); `GET …/subscription` с `escrowed/secret_store/grants[].node_id` (Task 3); `POST …/subscription/reveal` (Task 3); `GET /api/clients/{id}` (гранты с `desired_state`, `observed_state`, `secret_ref`); `GET /api/nodes`.
- Produces: `context.subscriptions.open(clientId, name)` — как раньше; окно открывается по `data-client-action="open"` и по клику на `.client-identity`.

- [ ] **Step 1: Обновить контракт-тест**

`panel/tests/test_subscription_ui_contract.py` — заменить содержимое трёх существующих тестов:

```python
def test_the_client_window_shows_the_url_on_request_and_never_keeps_it():
    javascript = (ROOT / "static/js/subscriptions.js").read_text()
    html = (ROOT / "static/index.html").read_text()
    assert 'id="subscription-modal"' in html and 'id="placement-body"' in html and 'id="placement-username"' in html
    assert "не обновляют подписку автоматически" in html
    assert "показывается один раз" not in html[html.index('id="subscription-modal"'):html.index('id="client-import-modal"')]
    # Shown through a reveal, consumed once, cleared on close; rotate/revoke confirmed, reveal not.
    assert "subscription/reveal" in javascript and "/api/reveal/" in javascript
    assert "subscription/rotate" in javascript and "subscription/revoke" in javascript
    assert "localStorage" not in javascript and "sessionStorage" not in javascript
    assert javascript.count("ui.confirmed(") == 3  # rotate, revoke, delete a cell — never show/apply
    assert 'data-subscription-action="show"' in javascript and "state.reveal = null" in javascript
    assert "ротируйте" in javascript and "показывает ссылку один раз" in javascript
    # The matrix and its diff come from placement.js; the diff goes to the existing endpoints.
    assert 'from "./placement.js"' in javascript and "placementDiff(" in javascript
    assert "/grants`" in javascript and "/enable`" in javascript and "/disable`" in javascript and "/delete`" in javascript
    for name in ("singbox", "singbox-official", "clash", "raw"):
        assert re.search(rf'format: "{name}"', javascript), name
    assert "/api/subscriptions/compatibility" in javascript and "data-auto-refresh" in javascript


def test_the_client_card_opens_the_window_and_main_wires_it():
    clients = (ROOT / "static/js/clients.js").read_text()
    main = (ROOT / "static/js/main.js").read_text()
    assert 'data-client-action="open"' in clients and "context.subscriptions.open(" in clients
    assert 'data-client-action="grant"' not in clients and "openGrantModal" not in clients
    assert "createSubscriptionDialog" in main and "context.subscriptions.bind()" in main


def test_the_window_is_reachable_by_viewers_but_only_writers_get_the_buttons():
    javascript = (ROOT / "static/js/subscriptions.js").read_text()
    clients = (ROOT / "static/js/clients.js").read_text()
    assert clients.index('data-client-action="open"') < clients.index('role === "viewer" || client.state === "archived"')
    assert 'role !== "viewer"' in javascript and "canWrite" in javascript
    assert "renderPlacement(rows, { canWrite" in javascript
```

- [ ] **Step 2: Запустить — падает**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_ui_contract.py -q`
Expected: FAIL.

- [ ] **Step 3: Разметка `#subscription-modal` в `index.html`**

Заменить диалог (строки 257–277) на:

```html
  <dialog id="subscription-modal" class="access-dialog">
    <form method="dialog">
      <div class="modal-head"><div><span class="eyebrow">КЛИЕНТ</span><h2 id="subscription-title">Клиент</h2></div><button class="icon-button close" value="cancel" formnovalidate aria-label="Закрыть">×</button></div>
      <p class="modal-copy">Одна ссылка на все доступы клиента на всех узлах. Панель рендерит её из собственного хранилища; доступ без сохранённого секрета попадает в неё как unsupported.</p>
      <p class="form-hint" id="subscription-status"></p>
      <fieldset class="subscription-formats">
        <legend>Вариант ссылки под клиента</legend>
        <div id="subscription-variants"></div>
      </fieldset>
      <div id="subscription-reveal" class="subscription-reveal" hidden>
        <label>URL подписки
          <span class="copy-field"><input id="subscription-url" readonly><button type="button" id="copy-subscription-url">Копировать</button></span>
        </label>
        <img id="subscription-qr" alt="QR-код подписки" class="subscription-qr">
      </div>
      <p class="form-hint subscription-warning">Telegram и часть native-клиентов не обновляют подписку автоматически — после изменений отправьте клиенту ссылку заново.</p>
      <div class="client-actions" id="subscription-actions"></div>
      <fieldset class="placement-box">
        <legend>Узлы и протоколы</legend>
        <label id="placement-username-row">Имя учётной записи<input id="placement-username" pattern="[A-Za-z0-9_.\-]{1,64}" maxlength="64" autocomplete="off" placeholder="например, laptop"><small>Одно имя для новых доступов; латиница, цифры, точка, дефис, подчёркивание</small></label>
        <div id="placement-body"></div>
        <ul class="client-grants subscription-grants" id="subscription-grants"></ul>
        <p class="form-hint">Галочка — выдать или включить, снятая — выключить (аккаунт остаётся). Удалить — крестиком в клетке.</p>
        <div class="client-actions" id="placement-actions"></div>
      </fieldset>
      <div class="form-error" id="subscription-error" role="alert"></div>
      <footer><button class="secondary" value="cancel" formnovalidate>Закрыть</button></footer>
    </form>
  </dialog>
```

`#grant-modal` (строки 279–295) **удалить**. В `style.css` добавить: `.placement-box{border:1px solid var(--line);border-radius:var(--radius);padding:10px 12px;margin:12px 0;display:grid;gap:8px}`.

- [ ] **Step 4: `subscriptions.js`**

Переписать модуль (сохранив `VARIANTS`, `AUTO_REFRESH`, `formatDate`, `grantRow`, `variantOption` как есть):

```js
import { esc, query, queryAll } from "./common.js";
import { placementDiff, placementRows, readPlacement, renderPlacement } from "./placement.js";
import { proposeUsername } from "./clients.js";

// … VARIANTS, AUTO_REFRESH, formatDate, grantRow, variantOption без изменений …

const OPERATION_MESSAGE = {
  succeeded: "Доступы выданы",
  compensated: "Операция отменена: созданное удалено, ничего лишнего не тронуто",
  manual_intervention_required: "Требуется вмешательство: часть изменений не удалось откатить",
};

export function createSubscriptionDialog(context) {
  const { api, root, ui } = context;
  const state = { clientId: null, name: "", client: null, grants: [], rows: [], reveal: null, format: "singbox", matrix: {} };

  function canWrite() {
    return context.state.me?.role !== "viewer" && state.client?.state !== "archived";
  }

  function renderSubscription(overview) {
    const current = overview.subscription;
    const status = query("#subscription-status", root);
    const actions = query("#subscription-actions", root);
    if (!overview.configured) {
      status.innerHTML = "<b>Домен подписки не настроен.</b> Задайте <code>domains.subscription</code> в install.toml (переменная <code>PANEL_SUBSCRIPTION_URL</code>) — без него URL выдать нечего.";
      actions.innerHTML = "";
      return;
    }
    if (!current) {
      status.textContent = "Подписка не создана.";
      actions.innerHTML = canWrite() ? '<button type="button" class="primary" data-subscription-action="create">Создать URL</button>' : "";
      return;
    }
    status.innerHTML = `URL выдан ${esc(formatDate(current.created_at))}, поколение <b>${esc(String(current.generation))}</b>.
      Последнее обновление клиентом: ${esc(formatDate(current.last_fetched_at))}. Интервал автообновления: ${esc(String(current.update_interval_hours))} ч.`;
    if (!canWrite()) {
      actions.innerHTML = "";
      return;
    }
    let show = '<button type="button" class="primary" data-subscription-action="show">Показать</button>';
    if (!overview.secret_store) {
      show = '<small class="form-hint">Панель без хранилища секретов показывает ссылку один раз — при создании и ротации.</small>';
    } else if (!overview.escrowed) {
      show = '<small class="form-hint">Ссылка выдана до включения хранилища — ротируйте, новая будет доступна для показа.</small>';
    }
    actions.innerHTML = `${show}
      <button type="button" class="secondary" data-subscription-action="rotate">Ротировать URL</button>
      <button type="button" class="danger ghost" data-subscription-action="revoke">Отозвать</button>`;
  }

  function renderPlacementBox(overview) {
    state.rows = placementRows(context.state.nodes || [], state.grants);
    query("#placement-body", root).innerHTML = renderPlacement(state.rows, { canWrite: canWrite() });
    const username = query("#placement-username", root);
    const names = new Set(state.grants.filter((g) => g.desired_state !== "deleted").map((g) => g.runtime_username));
    if (!username.dataset.typed) username.value = names.size === 1 ? [...names][0] : proposeUsername(state.name);
    query("#placement-username-row", root).hidden = !canWrite();
    query("#subscription-grants", root).innerHTML = overview.grants.length
      ? overview.grants.map((grant) => grantRow(grant, state.matrix)).join("")
      : '<li class="grant-chip empty"><small>У клиента нет доступов — подписке нечего отдавать</small></li>';
    query("#placement-actions", root).innerHTML = canWrite()
      ? '<button type="button" class="primary" data-placement-action="apply">Применить</button>'
      : "";
  }

  function render(overview) {
    query("#subscription-title", root).textContent = `${state.name}${state.client?.state === "archived" ? " · в архиве" : ""}`;
    query("#subscription-variants", root).innerHTML = VARIANTS.map((variant) => variantOption(variant, state.format)).join("");
    renderSubscription(overview);
    renderPlacementBox(overview);
    showReveal();
  }

  function showReveal() {
    const box = query("#subscription-reveal", root);
    if (!state.reveal) {
      box.hidden = true;
      query("#subscription-url", root).value = "";
      query("#subscription-qr", root).removeAttribute("src");
      return;
    }
    const variant = state.reveal.variants?.[state.format] || { url: state.reveal.url, qr: state.reveal.qr };
    query("#subscription-url", root).value = variant.url;
    query("#subscription-qr", root).src = variant.qr;
    box.hidden = false;
  }

  async function load() {
    const id = encodeURIComponent(state.clientId);
    const [overview, detail, nodes, compatibility] = await Promise.all([
      api(`/api/clients/${id}/subscription`),
      api(`/api/clients/${id}`),
      api("/api/nodes"),
      api("/api/subscriptions/compatibility"),
    ]);
    state.client = detail.client;
    state.grants = detail.grants || [];
    state.name = detail.client.display_name;
    context.state.nodes = nodes.items || [];
    state.matrix = compatibility.matrix || {};
    render(overview);
  }

  // `reveal` may be a payload already in hand (the client was just created): shown at once.
  async function open(clientId, name, reveal = null) {
    state.clientId = clientId;
    state.name = name;
    state.reveal = reveal;
    query("#subscription-error", root).textContent = "";
    query("#placement-username", root).dataset.typed = "";
    ui.openModal("#subscription-modal");
    try {
      await load();
    } catch (exception) {
      query("#subscription-error", root).textContent = exception.message;
    }
  }

  async function issue(path, button, busy) {
    const error = query("#subscription-error", root);
    error.textContent = "";
    try {
      ui.setBusy(button, true, busy);
      const { reveal_token: token } = await api(path, { method: "POST" });
      // One reveal, consumed on first read: the dialog keeps what it received until it closes.
      state.reveal = await api(`/api/reveal/${encodeURIComponent(token)}`);
      await load();
      ui.toast("Ссылка подписки на экране");
    } catch (exception) {
      error.textContent = exception.message;
    } finally {
      ui.setBusy(button, false);
    }
  }

  async function act(button) {
    const action = button.dataset.subscriptionAction;
    const base = `/api/clients/${encodeURIComponent(state.clientId)}`;
    if (action === "create") return issue(`${base}/subscription`, button, "Создаём…");
    if (action === "show") return issue(`${base}/subscription/reveal`, button, "Показываем…");
    if (action === "rotate") {
      const confirmed = await ui.confirmed(
        "Ротировать URL подписки?",
        "Старая ссылка перестанет открываться в тот же момент. Новую придётся отправить клиенту заново.",
        "Ротировать",
      );
      if (!confirmed) return undefined;
      return issue(`${base}/subscription/rotate`, button, "Ротируем…");
    }
    if (action === "revoke") {
      const confirmed = await ui.confirmed(
        "Отозвать подписку?",
        "Ссылка перестанет открываться. Доступы клиента остаются, но обновляться по подписке больше не будут.",
        "Отозвать",
      );
      if (!confirmed) return undefined;
      try {
        ui.setBusy(button, true, "Отзываем…");
        await api(`${base}/subscription/revoke`, { method: "POST" });
        state.reveal = null;
        await load();
        ui.toast("Подписка отозвана");
      } catch (exception) {
        query("#subscription-error", root).textContent = exception.message;
      } finally {
        ui.setBusy(button, false);
      }
    }
    return undefined;
  }

  // The matrix diff goes to the endpoints the card already uses: one saga for every new
  // cell, one call per enable/disable. What actually happened is re-read afterwards.
  async function apply(button) {
    const error = query("#subscription-error", root);
    error.textContent = "";
    const username = query("#placement-username", root);
    const diff = placementDiff(state.rows, readPlacement(query("#placement-body", root)), username.value.trim());
    if (diff.create.length && !username.reportValidity()) return;
    if (diff.create.length && !username.value.trim()) {
      error.textContent = "Для новых доступов нужно имя учётной записи";
      return;
    }
    if (!diff.create.length && !diff.enable.length && !diff.disable.length) {
      ui.toast("Изменений нет");
      return;
    }
    const base = `/api/clients/${encodeURIComponent(state.clientId)}`;
    try {
      ui.setBusy(button, true, "Применяем…");
      for (const id of diff.disable) await api(`/api/clients/grants/${encodeURIComponent(id)}/disable`, { method: "POST" });
      for (const id of diff.enable) await api(`/api/clients/grants/${encodeURIComponent(id)}/enable`, { method: "POST" });
      let result = null;
      if (diff.create.length) {
        result = await api(`${base}/grants`, { method: "POST", body: JSON.stringify({ grants: diff.create }) });
        ui.toast(OPERATION_MESSAGE[result.status] || result.status, result.status === "succeeded" ? "" : "error");
        if (result.status === "manual_intervention_required") {
          ui.toast(`Операция ${result.operation_id}: продолжить можно командой operations-resume`, "error");
        }
      } else {
        ui.toast("Состав клиента обновлён");
      }
      await load();
      if (result?.status === "succeeded") await context.access.openOperationBundle(result.operation_id);
    } catch (exception) {
      error.textContent = exception.message;
      await load().catch(() => {});
    } finally {
      ui.setBusy(button, false);
    }
  }

  async function removeGrant(button) {
    const grant = state.grants.find((g) => g.id === button.dataset.grantId);
    if (!grant) return;
    const label = `${grant.protocol} · ${grant.runtime_username}`;
    const confirmed = await ui.confirmed("Удалить доступ?", `${label} будет удалён из протокола; на связанной панели — после доставки узлу.`, "Удалить");
    if (!confirmed) return;
    try {
      ui.setBusy(button, true);
      await api(`/api/clients/grants/${encodeURIComponent(grant.id)}/delete`, { method: "POST" });
      ui.toast("Доступ удалён");
      await load();
    } catch (exception) {
      query("#subscription-error", root).textContent = exception.message;
    } finally {
      ui.setBusy(button, false);
    }
  }

  function bind() {
    const dialog = query("#subscription-modal", root);
    if (!dialog) return;
    dialog.addEventListener("click", (event) => {
      const subscription = event.target.closest("button[data-subscription-action]");
      if (subscription) return void act(subscription);
      const placement = event.target.closest("button[data-placement-action]");
      if (placement) return void apply(placement);
      const remove = event.target.closest("button[data-placement-delete]");
      if (remove) void removeGrant(remove);
    });
    dialog.addEventListener("change", (event) => {
      if (event.target.name !== "subscription-format") return;
      state.format = event.target.value;
      showReveal();
    });
    query("#placement-username", root)?.addEventListener("input", ({ currentTarget: input }) => {
      input.dataset.typed = input.value ? "1" : "";
    });
    query("#copy-subscription-url", root)?.addEventListener("click", async () => {
      await ui.copyText(query("#subscription-url", root));
      ui.toast("Ссылка подписки скопирована");
    });
    dialog.addEventListener("close", async () => {
      // Nothing of the URL survives the dialog; the list behind it shows what changed.
      state.reveal = null;
      showReveal();
      queryAll("#subscription-grants li", root).forEach((node) => node.remove());
      query("#placement-body", root).innerHTML = "";
      if (context.state.view === "clients") await context.navigate("clients");
    });
  }

  return { bind, open };
}
```

Проверить, что `clients.js` не импортирует `subscriptions.js` (циклический импорт ES-модулей с `proposeUsername` сработает, но лучше избежать): если импортирует — перенести `proposeUsername` и `TRANSLIT` в `common.js` и импортировать оттуда в обоих модулях.

- [ ] **Step 5: `clients.js` — карточка открывает окно**

В `actions(context, client)`:

```js
function actions(context, client) {
  // The client window is read-only for viewers, so it is offered to everyone; the buttons
  // that change anything appear inside it by role.
  const open = '<button class="secondary" data-client-action="open">Открыть</button>';
  if (context.state.me?.role === "viewer" || client.state === "archived") {
    return `<div class="client-actions">${open}</div>`;
  }
  const toggle = client.state === "suspended"
    ? '<button class="secondary" data-client-action="resume">Возобновить</button>'
    : '<button class="secondary" data-client-action="suspend">Приостановить</button>';
  return `<div class="client-actions">
    ${open}
    ${toggle}
    <button class="danger ghost" data-client-action="archive">Архивировать</button>
  </div>`;
}
```

В `clientCard` обернуть идентичность в кнопку-ссылку, чтобы «тыкнуть на клиента» работало:

```js
    <button type="button" class="client-identity" data-client-action="open">
      <b>${esc(client.display_name)}</b>
      <small>Доступов: ${grants.length}</small>
    </button>
```

и в `style.css` добавить `.client-identity{background:none;border:0;padding:0;text-align:left;cursor:pointer;color:inherit;font:inherit;display:grid;gap:2px}`.

В `handleClientsClick` заменить ветки `grant` и `subscription` одной:

```js
  if (action === "open") {
    const card = button.closest("[data-client-id]");
    const entry = context.state.clients.find((item) => item.client.id === card?.dataset.clientId);
    if (entry) void context.subscriptions.open(entry.client.id, entry.client.display_name);
    return true;
  }
```

Удалить `openGrantModal` и обработчик `#create-grants` в `bindClients` (строки 364–400). `loadNodeOptions` и `nodeOptions` пока оставить: их использует `openClientModal` до Task 8.

- [ ] **Step 6: Запустить контракты**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_ui_contract.py tests/test_fleet_v2_ui_contract.py tests/test_users_adapter_ui.py tests/test_ui_browser_findings.py -q`
Expected: PASS. `test_fleet_v2_ui_contract` требует `name="node_id"` в `index.html` — селекты `#user-node`, `#naive-node`, `#mieru-node` остаются, так что проходит. Если `test_ui_browser_findings.py` ссылается на `#grant-modal`/`grant-node` — обновить ожидание на `#placement-body`.

- [ ] **Step 7: Коммит**

```bash
git add panel/static/index.html panel/static/js/subscriptions.js panel/static/js/clients.js panel/static/style.css panel/tests/test_subscription_ui_contract.py
printf '%s\n' 'panel: окно клиента — ссылка подписки по кнопке «Показать» (reveal), матрица узел × протокол с «Применить» (диф в существующие ручки), удаление доступа крестиком; диалог «Выдать доступ» убран, карточка открывает окно' | git commit -q -F -
```

---

### Task 8: «Новый клиент» с матрицей и блок подписки в «Доступы выданы»

**Files:**
- Modify: `panel/static/index.html:230-255`
- Modify: `panel/static/js/clients.js` (`openClientModal`, обработчик `#create-client`; удалить `loadNodeOptions`, `nodeOptions`, `issueOnNode` оставить)
- Modify: `panel/static/js/access.js:478-493`
- Test: `panel/tests/test_subscription_ui_contract.py`

**Interfaces:**
- Consumes: `placementRows/renderPlacement/readPlacement/placementDiff` (Task 6); `POST /api/clients` → `subscription_reveal_token` (Task 4); `context.subscriptions.open(id, name, reveal)` (Task 7).
- Produces: `context.access.openOperationBundle(operationId, { subscription })` — второй аргумент необязателен, `subscription` — payload reveal подписки для блока сверху.

- [ ] **Step 1: Контракт**

Добавить в `panel/tests/test_subscription_ui_contract.py`:

```python
def test_new_client_places_grants_by_matrix_and_hands_the_subscription_over():
    clients = (ROOT / "static/js/clients.js").read_text()
    access = (ROOT / "static/js/access.js").read_text()
    html = (ROOT / "static/index.html").read_text()
    dialog = html[html.index('id="client-modal"'):html.index('id="bundle-modal"')]
    assert 'id="client-placement"' in dialog and 'id="client-node"' not in dialog
    assert 'id="grant-modal"' not in html
    assert 'from "./placement.js"' in clients and "placementDiff(" in clients
    assert "subscription_reveal_token" in clients and "openOperationBundle(" in clients
    # The bundle dialog carries the subscription block when a reveal came with the client.
    assert 'id="bundle-subscription"' in html and "bundle-subscription" in access and "subscription.qr" in access
```

- [ ] **Step 2: Запустить — падает**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_ui_contract.py::test_new_client_places_grants_by_matrix_and_hands_the_subscription_over -q`
Expected: FAIL.

- [ ] **Step 3: Разметка**

`#client-modal`: заменить `<label>Узел<select id="client-node"…></label>` и `<fieldset class="grant-protocols">…</fieldset>` на:

```html
      <p class="modal-copy">Клиент — это человек или устройство. Отметьте узлы и протоколы — доступы будут выданы сразу; без отметок создаётся карточка и ссылка подписки.</p>
      <label>Имя<input id="client-name" maxlength="128" autocomplete="off" placeholder="например, Ноутбук Сергея" required></label>
      <div id="client-placement"></div>
      <label id="client-username-row" hidden>Имя учётной записи<input id="client-username" pattern="[A-Za-z0-9_.\-]{1,64}" maxlength="64" autocomplete="off" placeholder="например, laptop"><small>Одно имя во всех отмеченных клетках; латиница, цифры, точка, дефис, подчёркивание</small></label>
```

(первую строку `modal-copy` заменить, вторую `label` оставить как есть.)

`#bundle-modal`: после `<p class="modal-copy">…</p>` добавить:

```html
      <section id="bundle-subscription" class="subscription-reveal" hidden>
        <h3>Подписка</h3>
        <label>URL подписки<span class="copy-field"><input id="bundle-subscription-url" readonly><button type="button" class="copy" data-copy="">Копировать</button></span></label>
        <img id="bundle-subscription-qr" alt="QR-код подписки" class="subscription-qr">
        <small>Одна ссылка на все доступы клиента; потом она снова доступна в окне клиента по кнопке «Показать».</small>
      </section>
```

Текст `modal-copy` бандла: «Ссылки доступов показываются один раз. Скопируйте всё нужное сейчас: панель хранит секреты в шифрованном виде, но эта страница больше не откроется по той же ссылке. Ссылку подписки можно показать снова из окна клиента.»

- [ ] **Step 4: `clients.js`**

Импорт: `import { placementDiff, placementRows, readPlacement, renderPlacement } from "./placement.js";`

`openClientModal`:

```js
export async function openClientModal(context) {
  const form = query("#client-form", context.root);
  form.reset();
  query("#client-error", context.root).textContent = "";
  query("#client-username-row", context.root).hidden = true;
  query("#client-placement", context.root).innerHTML = '<p class="form-hint">Читаем узлы…</p>';
  context.ui.openModal("#client-modal", "#client-name");
  try {
    const nodes = await context.api("/api/nodes");
    context.state.nodes = nodes.items || [];
  } catch (exception) {
    context.ui.toast(`Список узлов не загружен: ${exception.message}`, "error");
    context.state.nodes = context.state.nodes || [];
  }
  if (!query("#client-modal", context.root).open) return;
  query("#client-placement", context.root).innerHTML = renderPlacement(placementRows(context.state.nodes, []), { canWrite: true });
}
```

В `bindClients` заменить обработчик `change` формы:

```js
  query("#client-form", root)?.addEventListener("change", (event) => {
    if (!event.target.matches?.("#client-placement input[type=checkbox]")) return;
    const row = query("#client-username-row", root);
    const input = query("#client-username", root);
    const any = readPlacement(query("#client-placement", root)).size > 0;
    row.hidden = !any;
    if (any && !input.dataset.typed) input.value = proposeUsername(query("#client-name", root).value);
  });
```

Обработчик `#create-client`:

```js
  query("#create-client", root)?.addEventListener("click", async ({ currentTarget: button }) => {
    const form = query("#client-form", root);
    const error = query("#client-error", root);
    const usernameInput = query("#client-username", root);
    const rows = placementRows(context.state.nodes || [], []);
    const diff = placementDiff(rows, readPlacement(query("#client-placement", root)), usernameInput.value.trim());
    usernameInput.required = diff.create.length > 0;
    if (!form.reportValidity()) return;
    error.textContent = "";
    const displayName = query("#client-name", root).value.trim();
    let client = null;
    let subscription = null;
    try {
      ui.setBusy(button, true, "Создаём…");
      client = await api("/api/clients", { method: "POST", body: JSON.stringify({ display_name: displayName }) });
      if (client.subscription_reveal_token) {
        subscription = await api(`/api/reveal/${encodeURIComponent(client.subscription_reveal_token)}`);
      }
      if (!diff.create.length) {
        query("#client-modal", root).close();
        ui.toast("Клиент создан");
        await context.navigate("clients");
        if (subscription) await context.subscriptions.open(client.id, displayName, subscription);
        return;
      }
      ui.setBusy(button, true, "Выдаём доступы…");
      const result = await api(`/api/clients/${encodeURIComponent(client.id)}/grants`, {
        method: "POST",
        body: JSON.stringify({ grants: diff.create }),
      });
      query("#client-modal", root).close();
      ui.toast(result.status === "succeeded" ? "Клиент создан, доступы выданы" : OPERATION_MESSAGE[result.status] || result.status, result.status === "succeeded" ? "" : "error");
      if (result.status === "manual_intervention_required") {
        ui.toast(`Операция ${result.operation_id}: продолжить можно командой operations-resume`, "error");
      }
      if (result.status === "succeeded") await context.access.openOperationBundle(result.operation_id, { subscription });
      else if (subscription) await context.subscriptions.open(client.id, displayName, subscription);
      await context.navigate("clients");
    } catch (exception) {
      error.textContent = client ? `Клиент «${displayName}» создан, но доступы не выданы: ${exception.message}. Выдайте их из окна клиента.` : exception.message;
      if (client) await context.navigate("clients");
    } finally {
      ui.setBusy(button, false);
    }
  });
```

Удалить `nodeOptions` и `loadNodeOptions` из `clients.js`, если их больше никто не импортирует (проверить `ctx_search "loadNodeOptions" panel/static/js` — `access.js`/`main.js` могут использовать для `#user-node`/`#naive-node`/`#mieru-node`; если используют — оставить и не трогать).

- [ ] **Step 5: `access.js` — блок подписки в бандле**

```js
  async function openOperationBundle(operationId, { subscription = null } = {}) {
    // One reveal for the whole operation: the token is consumed on first read, so the
    // dialog renders everything it received and never re-fetches.
    const { reveal_token: token } = await api(
      `/api/operations/${encodeURIComponent(operationId)}/bundle`, { method: "POST" },
    );
    const bundle = await api(`/api/reveal/${encodeURIComponent(token)}`);
    const body = query("#bundle-body", root);
    body.innerHTML = (bundle.grants || []).map((grant) => `<section class="bundle-grant">
      <h3>${esc(grant.protocol)} · ${esc(grant.runtime_username)}</h3>
      ${(grant.artifacts || []).map((artifact) => `<label>${esc(artifact.label)}
        <span class="copy-field"><input readonly value="${esc(artifact.value)}"><button type="button" class="copy" data-copy="${esc(artifact.value)}">Копировать</button></span>
      </label>`).join("")}
    </section>`).join("") || "<p class=\"form-hint\">Артефактов нет.</p>";
    const block = query("#bundle-subscription", root);
    if (subscription) {
      const variant = subscription.variants?.singbox || { url: subscription.url, qr: subscription.qr };
      query("#bundle-subscription-url", root).value = variant.url;
      query("#bundle-subscription-qr", root).src = variant.qr;
      query("#bundle-subscription button.copy", root).dataset.copy = variant.url;
      block.hidden = false;
    } else {
      block.hidden = true;
      query("#bundle-subscription-url", root).value = "";
      query("#bundle-subscription-qr", root).removeAttribute("src");
    }
    ui.openModal("#bundle-modal");
  }
```

Проверить в `access.js`, что закрытие `#bundle-modal` чистит `#bundle-body` (найти обработчик `close` бандла; если он есть — добавить туда очистку `#bundle-subscription-url` и `src` QR; если нет — добавить `query("#bundle-modal", root)?.addEventListener("close", …)` в `bind`).

- [ ] **Step 6: Контракты и синтаксис**

Run: `cd panel && ../.venv/bin/pytest tests/test_subscription_ui_contract.py tests/test_placement_ui_contract.py tests/test_fleet_v2_ui_contract.py tests/test_users_adapter_ui.py tests/test_ui_browser_findings.py -q && for f in static/js/*.js; do node --check "$f" || exit 1; done`
Expected: PASS, `node --check` без ошибок.

- [ ] **Step 7: Коммит**

```bash
git add panel/static/index.html panel/static/js/clients.js panel/static/js/access.js panel/tests/test_subscription_ui_contract.py
printf '%s\n' 'panel: «Новый клиент» — матрица узлов и протоколов вместо одного узла; подписка выдаётся сразу и показывается в «Доступы выданы» (URL и QR) или в окне клиента' | git commit -q -F -
```

---

### Task 9: Мобильный аудит окна клиента

**Files:**
- Modify: `panel/tests/test_mobile_layout.py:578-640` и `client_card()` на строке 327

- [ ] **Step 1: Обновить тест окна**

В `test_subscription_dialog_fits_the_phone_viewport_when_open` после заполнения `#subscription-variants` добавить заполнение матрицы реальной разметкой из `placement.js` (Node недоступен внутри страницы — вставить строку, которую рендерит `renderPlacement` для двух узлов; взять её из вывода `run(...)` в Task 6 или собрать вручную по тому же шаблону):

```python
    placement = (
        '<div class="placement-scroll"><table class="placement"><thead><tr><th>Узел</th><th>MTProxy</th><th>NaiveProxy</th><th>Mieru</th></tr></thead><tbody>'
        '<tr data-placement-node="local"><th scope="row">Этот сервер</th>'
        '<td class="placement-cell"><label><input type="checkbox" data-node="local" data-protocol="mtproxy" checked><small>включён</small></label><button type="button" class="ghost danger-text" data-placement-delete data-grant-id="g1" title="Удалить доступ">×</button></td>'
        '<td class="placement-cell"><label><input type="checkbox" data-node="local" data-protocol="naive"><small></small></label></td>'
        '<td class="placement-cell"><label><input type="checkbox" data-node="local" data-protocol="mieru" checked><small>ожидает узел</small></label><button type="button" class="ghost danger-text" data-placement-delete data-grant-id="g2" title="Удалить доступ">×</button></td></tr>'
        '<tr data-placement-node="fra" class="placement-row-locked"><th scope="row">Frankfurt panel with a long name <small>панель на паузе</small></th>'
        '<td class="placement-cell"><label><input type="checkbox" data-node="fra" data-protocol="mtproxy" disabled><small></small></label></td>'
        '<td class="placement-cell"><label><input type="checkbox" data-node="fra" data-protocol="naive" checked disabled><small>без секрета</small></label></td>'
        '<td class="placement-cell"><label><input type="checkbox" data-node="fra" data-protocol="mieru" disabled><small></small></label></td></tr>'
        '</tbody></table></div>'
    )
```

и в скрипт:

```python
        document.querySelector("#placement-body").innerHTML = {json.dumps(placement)};
        document.querySelector("#placement-actions").innerHTML = '<button type="button" class="primary">Применить</button>';
        document.querySelector("#subscription-actions").innerHTML = '<button type="button" class="primary">Показать</button><button type="button" class="secondary">Ротировать URL</button><button type="button" class="danger ghost">Отозвать</button>';
```

В список проверяемых узлов добавить `.placement-box, .placement-scroll, #placement-actions button`; отдельно проверить, что прокручивается блок, а не диалог:

```js
        const scroll = document.querySelector(".placement-scroll");
        if (scroll.scrollWidth > scroll.clientWidth && form.scrollWidth > form.clientWidth + tolerance) errors.push("matrix scrolls the dialog instead of itself");
```

`client_card()` на строке 345: заменить `<button class="secondary">Выдать доступ</button><button class="secondary">Подписка</button>` на `<button class="secondary">Открыть</button>`.

- [ ] **Step 2: Запустить на стенде**

Run (на ams-test, после rsync): `cd /root/dev/proxy-control/panel && ../.venv/bin/pytest tests/test_mobile_layout.py -q`
Expected: PASS. При «matrix scrolls the dialog» — проверить, что `.placement-scroll{max-width:100%}` и у `.placement-box` есть `min-width:0`, добавить при необходимости.

- [ ] **Step 3: Коммит**

```bash
git add panel/tests/test_mobile_layout.py panel/static/style.css
printf '%s\n' 'мобильный аудит: окно клиента с матрицей узлов на ширине телефона — матрица прокручивается внутри блока, кнопки и QR в кадре' | git commit -q -F -
```

---

### Task 9b: Браузерный сценарий `ui-acceptance.py` под окно клиента и матрицу

Добавлена по итогам ревью задач 7–8: сценарий тира `ui` ещё ищет `#client-node`, `#client-form .grant-protocol`, `#grant-modal`, `#create-grants`, `[data-client-action=grant|subscription]` — всего этого больше нет.

**Files:**
- Modify: `scripts/lab/ui-acceptance.py:610-712` (`view_clients`), `:1135-1149` (`view_central`, грант на узел с центра)

**Interfaces:**
- Consumes: разметка задач 7–8: `#client-placement` (матрица в «Новом клиенте»), `#client-username-row`/`#client-username`, `#create-client`; окно клиента `#subscription-modal` с `#placement-body`, `#placement-username`, `#placement-actions button[data-placement-action=apply]`, `button[data-placement-delete][data-grant-id]`, `#subscription-actions button[data-subscription-action=create|show|rotate|revoke]`, `#subscription-url`, `#bundle-modal` с `#bundle-subscription`; карточка `[data-client-id] [data-client-action=open]`; чипы `.grant-chip[data-grant-id]` с `grant-disable|grant-enable|grant-rotate|grant-delete` остаются.
- Produces: тот же набор `self.check(...)` имён, где смысл сохранился, плюс новые: `clients.create_dialog_offers_matrix`, `clients.matrix_tick_proposes_username`, `clients.create_reveals_subscription_in_bundle`, `clients.window_opens_from_card`, `clients.window_show_reveals_url_again`, `clients.matrix_apply_issues_grants`, `clients.matrix_untick_disables`, `central.matrix_offers_the_node`.

- [ ] **Step 1: `view_clients`**

Заменить шаги строк 616–641 так:

```python
        self.check("clients.create_dialog_opens", self.open_add("#client-modal"))
        # One dialog: the node × protocol matrix; the account name appears once a cell is ticked.
        self.check("clients.create_dialog_offers_matrix", b.wait("!!document.querySelector('#client-placement input[data-node=local][data-protocol=naive]') && document.querySelector('#client-username-row')?.hidden === true", 10))
        b.type("#client-name", client_name)
        b.js("(() => { const i = document.querySelector('#client-placement input[data-node=local][data-protocol=naive]'); i.checked = true; i.dispatchEvent(new Event('change', {bubbles: true})); return true; })()")
        self.check("clients.matrix_tick_proposes_username", b.wait(f"document.querySelector('#client-username-row')?.hidden === false && document.querySelector('#client-username')?.value === {json.dumps(client_name.lower())}", 5), b.value("#client-username"))
        b.js("(() => { const i = document.querySelector('#client-placement input[data-node=local][data-protocol=naive]'); i.checked = false; i.dispatchEvent(new Event('change', {bubbles: true})); return true; })()")
        self.check("clients.untick_hides_username", b.wait("document.querySelector('#client-username-row')?.hidden === true", 5))
        b.click("#create-client")
        # No cells: the card and (with a subscription domain) the client window with the URL at once.
        self.check("clients.card_listed", b.wait(f"[...document.querySelectorAll('[data-client-id]')].some(c => c.querySelector('.client-identity b')?.textContent === {json.dumps(client_name)})", 20))
        client = next(e for e in self.api.json("/api/clients")["items"] if e["client"]["display_name"] == client_name)
        client_id = client["client"]["id"]
        self.created["clients"].append(client_id)
        card = f"[data-client-id={json.dumps(client_id)}]"
        if b.wait("document.querySelector('#subscription-modal')?.open === true", 10):
            b.close_dialog("#subscription-modal")
        # Grants from the client window: tick three cells on this server, apply, get the bundle.
        b.click(f"{card} [data-client-action=open]")
        self.check("clients.window_opens_from_card", b.wait("document.querySelector('#subscription-modal')?.open === true && !!document.querySelector('#placement-body input[data-node=local]')", 20))
        b.type("#placement-username", grant_user)
        b.js("[...document.querySelectorAll('#placement-body input[data-node=local]')].forEach(i => { i.checked = true; }); true")
        b.click("#placement-actions button[data-placement-action=apply]")
        self.check("clients.matrix_apply_issues_grants", b.wait("document.querySelector('#bundle-modal')?.open === true && (document.querySelector('#bundle-body')?.textContent || '').length > 0", 90),
                   f"subscription-error: {b.text('#subscription-error')!r}")
        bundle = b.text("#bundle-body")
        self.check("clients.bundle_names_three_protocols", all(word in bundle for word in ("mtproxy", "naive", "mieru")), bundle[:200])
        b.close_dialog("#bundle-modal")
        b.close_dialog("#subscription-modal")
        self.check("clients.three_grant_chips", b.wait(f"document.querySelectorAll('{card} .grant-chip[data-grant-id]').length === 3", 30))
```

Блок подписки (строки 651–670) заменить:

```python
        # The subscription: issued with the client, shown again on request, rotated, revoked.
        b.click(f"{card} [data-client-action=open]")
        self.check("clients.subscription_window_opens", b.wait("document.querySelector('#subscription-modal')?.open === true && !!document.querySelector('#subscription-actions button')", 20))
        configured = "не настроен" not in b.text("#subscription-status")
        self.check("clients.subscription_domain_configured", configured, b.text("#subscription-status")[:120])
        if configured:
            if b.exists("[data-subscription-action=create]"):
                b.click("[data-subscription-action=create]")
                b.wait("document.querySelector('#subscription-reveal')?.hidden === false", 20)
                b.close_dialog("#subscription-modal")
                b.click(f"{card} [data-client-action=open]")
                b.wait("!!document.querySelector('#subscription-actions button')", 20)
            b.click("[data-subscription-action=show]")
            self.check("clients.window_show_reveals_url_again", b.wait("document.querySelector('#subscription-reveal')?.hidden === false && (document.querySelector('#subscription-url')?.value || '').startsWith('https://')", 20), b.text("#subscription-error"))
            first_url = b.value("#subscription-url")
            self.report["facts"]["subscription_host"] = urllib.parse.urlsplit(first_url).hostname
            self.check("clients.subscription_url_serves", self.fetch(first_url) == 200)
            # Untick one cell: the grant is disabled and leaves the subscription.
            b.js("(() => { const i = document.querySelector('#placement-body input[data-node=local][data-protocol=naive]'); i.checked = false; return true; })()")
            b.click("#placement-actions button[data-placement-action=apply]")
            self.check("clients.matrix_untick_disables", b.wait("(document.querySelector('#placement-body input[data-node=local][data-protocol=naive]')?.checked === false) && (document.querySelector('#placement-body td.placement-cell:has(input[data-protocol=naive][data-node=local]) small')?.textContent || '').includes('выключен')", 30), b.text("#subscription-error"))
            b.js("(() => { const i = document.querySelector('#placement-body input[data-node=local][data-protocol=naive]'); i.checked = true; return true; })()")
            b.click("#placement-actions button[data-placement-action=apply]")
            self.check("clients.matrix_tick_enables", b.wait("(document.querySelector('#placement-body td.placement-cell:has(input[data-protocol=naive][data-node=local]) small')?.textContent || '').includes('включён')", 30), b.text("#subscription-error"))
            b.click("[data-subscription-action=rotate]")
            self.check("clients.subscription_rotate_moves_url", b.confirm() and b.wait(f"(document.querySelector('#subscription-url')?.value || '').startsWith('https://') && document.querySelector('#subscription-url').value !== {json.dumps(first_url)}", 20))
            second_url = b.value("#subscription-url")
            self.check("clients.old_subscription_url_is_404", self.fetch(first_url) == 404)
            self.check("clients.new_subscription_url_serves", self.fetch(second_url) == 200)
            b.click("[data-subscription-action=revoke]")
            self.check("clients.subscription_revoke_offers_create_again", b.confirm() and b.wait("!!document.querySelector('[data-subscription-action=create]')", 20))
            self.check("clients.revoked_subscription_url_is_404", self.fetch(second_url) == 404)
        b.close_dialog("#subscription-modal")
```

Если `:has()` в headless Chrome стенда не поддержан (Chrome ≥ 105 поддерживает), заменить на `[...document.querySelectorAll('#placement-body td.placement-cell')].find(td => td.querySelector('input[data-protocol=naive][data-node=local]'))?.querySelector('small')`.

- [ ] **Step 2: `view_central`**

Строки 1137–1149 заменить: создать клиента без клеток, открыть окно и отметить клетку узла:

```python
            self.open_add("#client-modal")
            b.type("#client-name", remote_user)
            b.click("#create-client")
            b.wait(f"[...document.querySelectorAll('[data-client-id]')].some(c => c.querySelector('.client-identity b')?.textContent === {json.dumps(remote_user)})", 20)
            if b.wait("document.querySelector('#subscription-modal')?.open === true", 10):
                b.close_dialog("#subscription-modal")
            client = next(e for e in central.json("/api/clients")["items"] if e["client"]["display_name"] == remote_user)
            ccard = f"[data-client-id={json.dumps(client['client']['id'])}]"
            b.click(f"{ccard} [data-client-action=open]")
            self.check("central.matrix_offers_the_node", b.wait(f"document.querySelector('#subscription-modal')?.open === true && !!document.querySelector('#placement-body input[data-node={json.dumps(node_id)}][data-protocol=naive]')", 20))
            b.type("#placement-username", remote_user)
            b.js(f"(() => {{ const i = document.querySelector('#placement-body input[data-node={json.dumps(node_id)}][data-protocol=naive]'); i.checked = true; return true; }})()")
            b.click("#placement-actions button[data-placement-action=apply]")
            self.check("central.remote_grant_accepted", b.wait("document.querySelector('#bundle-modal')?.open === true || (document.querySelector('#subscription-error')?.textContent || '') !== ''", 30) and not b.text("#subscription-error"), b.text("#subscription-error"))
            if b.exists("#bundle-modal[open]"):
                b.close_dialog("#bundle-modal")
            b.close_dialog("#subscription-modal")
```

- [ ] **Step 3: Проверка**

`python3 -m py_compile scripts/lab/ui-acceptance.py`; `ruff check scripts/lab/ui-acceptance.py` (через `remote-gate.sh quick tests/test_verification_matrix.py` — он гоняет ruff по всему дереву); статическая сверка: каждый `id`/`data-*` селектор из правок присутствует в `panel/static/index.html`, `placement.js`, `subscriptions.js`, `clients.js` (`grep`). Прогон самого сценария — в задаче 11 против реальной установки стенда (`--views clients` и, при связанном центре, `central`).

- [ ] **Step 4: Коммит**

```bash
git add scripts/lab/ui-acceptance.py
printf '%s\n' 'lab: браузерный сценарий «Клиенты» и «Центр» — матрица узлов в «Новом клиенте» и в окне клиента вместо диалога выдачи, подписка по «Показать», снятая клетка выключает доступ' | git commit -q -F -
```

---

### Task 10: Документация выпуска и VERSION

**Files:**
- Modify: `PANEL.ru.md:205-222`
- Modify: `CHANGELOG.ru.md`, `CHANGELOG.md` (раздел Unreleased → 0.10.0-beta.1)
- Modify: `docs/UPGRADING.ru.md`, `docs/UPGRADING.en.md` (раздел «до v0.10»)
- Modify: `VERSION` → `0.10.0-beta.1`
- Test: `panel/tests/test_versions.py` (если проверяет согласованность VERSION/CHANGELOG — прогнать)

- [ ] **Step 1: PANEL.ru.md**

После абзаца про `PANEL_SUBSCRIPTION_URL` (строка 213) добавить:

```markdown
С v0.10 подписка выдаётся вместе с клиентом (окно «Новый клиент»), а её ссылка и QR доступны
снова в окне клиента по кнопке «Показать»: токен хранится зашифрованным под мастер-ключом
панели, показ доступен owner/admin и записывается в аудит как `subscription.reveal`. Без
мастер-ключа панель показывает ссылку один раз, как раньше. Там же, в окне клиента, — матрица
«узел × протокол»: галочка выдаёт или включает доступ, снятая галочка выключает его (аккаунт
остаётся), крестик удаляет; строки — этот сервер и связанные панели не на паузе, а узел на паузе
с доступами клиента показывается только для чтения. Доступ без сохранённого секрета подписку
не блокирует — он попадает в неё как `unsupported`.
```

- [ ] **Step 2: CHANGELOG ru/en**

`CHANGELOG.ru.md`, под `## [Unreleased]` → `## [0.10.0-beta.1] - 2026-09-20`:

```markdown
## [0.10.0-beta.1] - 2026-09-20

Клиент как в 3x-ui: создал — сразу получил ссылку подписки и QR; открыл — видишь ссылку снова и
правишь узлы галочками.

### Добавлено

- **Клиент на нескольких узлах.** В «Новом клиенте» и в окне клиента — матрица «узел × протокол»:
  этот сервер и связанные панели строками, MTProxy/NaiveProxy/Mieru столбцами. Отметки уходят одной
  операцией выдачи; снятая галочка выключает доступ, крестик удаляет (в CHANGELOG — markdown-ссылка с текстом PANEL на файл PANEL.ru.md в корне).
- **Подписка сразу и снова.** Подписка выдаётся вместе с клиентом, ссылка и QR показываются в окне
  «Доступы выданы»; в окне клиента кнопка «Показать» открывает её снова. Токен хранится зашифрованным
  под мастер-ключом панели, каждый показ — в аудите (`subscription.reveal`). Без мастер-ключа — как
  раньше, один раз (в CHANGELOG — markdown-ссылка с текстом OPERATIONS на docs/OPERATIONS.ru.md, и упоминание ADR 004).
- `POST /api/clients` принимает `subscription` (по умолчанию `true`) и отдаёт `subscription_reveal_token`;
  `POST /api/clients/{id}/subscription/reveal`; обзор подписки отдаёт `escrowed`, `secret_store`, `node_id`.

### Изменено

- Доступ без сохранённого секрета больше не мешает выдать подписку: он попадает в неё как `unsupported`.
- Диалог «Выдать доступ» убран — его роль выполняет матрица в окне клиента. Кнопка «Подписка» на карточке
  стала «Открыть», по имени клиента тоже открывается окно.
- Миграция БД 20 (`subscription-escrow`): две колонки у `client_subscriptions`.
```

`CHANGELOG.md` — тот же раздел по-английски:

```markdown
## [0.10.0-beta.1] - 2026-09-20

Clients the 3x-ui way: create one and get its subscription link and QR at once; open it and see the link again, editing nodes with ticks.

### Added

- **A client on several nodes.** «Новый клиент» and the client window show a node × protocol matrix: this server and linked panels as rows, MTProxy/NaiveProxy/Mieru as columns. Ticks go out as one provisioning operation; an untick disables the grant, the cross deletes it.
- **The subscription at once, and again.** A subscription is issued with the client and its URL and QR appear in the «Доступы выданы» dialog; «Показать» in the client window opens it again. The token is stored encrypted under the panel master key and every reveal is audited (`subscription.reveal`). Without a master key the URL is shown once, as before (OPERATIONS, ADR 004).
- `POST /api/clients` takes `subscription` (default `true`) and returns `subscription_reveal_token`; `POST /api/clients/{id}/subscription/reveal`; the subscription overview carries `escrowed`, `secret_store` and each grant's `node_id`.

### Changed

- A grant without a stored credential no longer blocks a subscription: it renders as `unsupported`.
- The «Выдать доступ» dialog is gone; the matrix in the client window takes its place. The card's «Подписка» button is now «Открыть», and the client's name opens the window too.
- Database migration 20 (`subscription-escrow`): two columns on `client_subscriptions`.
```

- [ ] **Step 3: UPGRADING ru/en**

`docs/UPGRADING.ru.md`, после последнего раздела «Обновление до v0.9» (если его нет — после v0.8):

```markdown
## Обновление до v0.10: подписка под рукой

Только панель, миграция БД 20 (`subscription-escrow`) применяется при старте. Подписка, выданная до
v0.10, показать себя не может (у неё нет зашифрованной копии) — кнопки «Показать» у неё не будет,
ротируйте её из окна клиента. Мастер-ключ панели (`secrets/panel-master-key`) обязателен для
показа: без него панель ведёт себя как раньше. Откат — образ панели предыдущей версии; колонки
миграции 20 старому коду не мешают.
```

`docs/UPGRADING.en.md`:

```markdown
## Upgrading to v0.10: the subscription at hand

Panel only; database migration 20 (`subscription-escrow`) runs at start-up. A subscription issued before v0.10 has no encrypted copy and cannot be shown — it gets no «Показать» button; rotate it from the client window. The panel master key (`secrets/panel-master-key`) is required for showing; without it the panel behaves as before. Rollback: the previous panel image; migration 20's columns do not get in the old code's way.
```

- [ ] **Step 4: VERSION и проверка**

```bash
printf '0.10.0-beta.1\n' > VERSION
cd panel && ../.venv/bin/pytest tests/test_versions.py -q
```

Expected: PASS (если тест сверяет VERSION с CHANGELOG — оба уже 0.10.0-beta.1).

- [ ] **Step 5: Коммит**

```bash
git add PANEL.ru.md CHANGELOG.ru.md CHANGELOG.md docs/UPGRADING.ru.md docs/UPGRADING.en.md VERSION
printf '%s\n' 'v0.10.0-beta.1: VERSION, CHANGELOG (ru/en), UPGRADING «до v0.10» (миграция 20, подписки до v0.10 — ротировать), PANEL.ru.md — окно клиента, матрица узлов, показ подписки' | git commit -q -F -
```

---

### Task 11: Гейт на ams-test и живая проверка AMS_Z → ams-test

**Files:** нет правок кода, кроме починки того, что найдёт гейт (каждая починка — свой коммит).

Состояние стенда (проверено 2026-09-21): на `ams-test` стоит **настоящая** установка (панель `https://aurora.sky.dubr1kkk.uk`, статус `active`, связана с AMS_Z как узел); лабораторной `panel.lab.test` нет, поэтому тир `ui` (`remote-gate.sh ui`) не запускается без `LAB_RESET=1`, который снёс бы настоящую установку. Как для v0.9: гейт `full` + прогон браузерного сценария против настоящей установки стенда.

- [ ] **Step 1: Гейт `full` на стенде**

```bash
scripts/dev/remote-gate.sh full 2>&1 | tail -20
```

Он сам делает rsync (`.superpowers` исключён), `ruff`, весь `pytest` (в том числе корневой `tests/`), `unittest tests.test_deploy`, `route-coverage.py`, `check-doc-links.py`, `check-js-syntax.sh`, shellcheck, `systemd-analyze verify`, `git diff --check`; конец — `REMOTE_GATE_FULL_OK`. Сессия SSH на стенде живёт ~10 мин, `full` укладывается (~8 мин в v0.9); если рвётся — запустить ту же цепочку отсоединённо: `ssh ams-test 'cd /root/dev/proxy-control && setsid nohup bash -c "<команда из ветки full remote-gate.sh>; echo GATE_V10_EXIT=\$?" > /root/gate-v10-full.log 2>&1 < /dev/null &'` и опрашивать `tail -3 /root/gate-v10-full.log`. Ожидание: всё зелёное (в v0.9 было 2342 passed / 2 skipped; теперь на ~30 больше).

- [ ] **Step 2: Раскатать панель v0.10 на стенд и прогнать браузерный сценарий**

Панель настоящей установки стенда собирается из `/opt/mtproxy-shared443/panel/` (см. память «ams-test lab host», 2026-09-18): резервная копия `cp -a /opt/mtproxy-shared443/panel /root/panel-backup-$(date -u +%Y%m%dT%H%M%SZ)`, затем `rsync -a --delete /root/dev/proxy-control/panel/ /opt/mtproxy-shared443/panel/` (без `tests/`) и пересборка **только полной командой установщика**:

```bash
cd /opt/mtproxy-shared443 && docker compose --project-directory /opt/mtproxy-shared443 --env-file .env --env-file .env.mieru --env-file .env.xray-router --env-file .env.naive -f compose.yaml -f compose.mieru.yaml -f compose.xray-router.yaml -f compose.naive.yaml up -d --build --no-deps --wait panel
```

Проверить `curl -sk https://aurora.sky.dubr1kkk.uk/api/version` (или `/static/js/placement.js` отдаётся 200). Пароль owner — в `/root/install-v07.credentials` (TOML, значения в кавычках): извлечь в файл `/root/.ui-owner-password` с правами 0600 **не печатая** значение (`python3 - <<'EOF' … EOF` с записью в файл). Прогон:

```bash
ssh ams-test 'cd /root/dev/proxy-control && .venv/bin/python scripts/lab/ui-acceptance.py --node-url https://aurora.sky.dubr1kkk.uk --password-file /root/.ui-owner-password --output lab-results/ui-v10 --views login,clients --no-shots 2>&1 | tail -40'
```

(`--views` — список из `VIEWS` в `ui-acceptance.py`; `login` нужен, если сценарий логинится там; смотреть `run()`.) Ожидание: все `clients.*` проверки `ok`, созданные клиенты заархивированы, тестовые пользователи удалены (сценарий чистит за собой; проверить `GET /api/naive/users`, `/api/mieru/users`, `/api/users` на остатки `<prefix>-*`). Скриншоты не нужны; `lab-results/ui-v10/report.json` — в отчёт. Откат панели: `rsync -a --delete /root/panel-backup-<ts>/ /opt/mtproxy-shared443/panel/` и та же команда пересборки.

- [ ] **Step 3: Живая проверка на AMS_Z (панель) → ams-test (узел)**

Раскатать панель на AMS_Z по процедуре из памяти «Deployment targets» (rsync `panel/` в checkout хоста, резервная копия, пересборка полной командой установщика). Затем в браузере (или `scripts/lab/ui-acceptance.py` `Browser`):

1. «Новый клиент» → имя, отметить `local:naive` и `ams-test:mieru` → «Создать». Ожидание: «Доступы выданы» с двумя грантами и блоком «Подписка» (URL + QR).
2. Открыть `?format=singbox` по ссылке с телефона/curl: два outbound'а, `proxy_control.generation` = 2 (клиент → 1, гранты → 2).
3. Закрыть, кликнуть по имени клиента → окно: «Показать» → тот же URL; в аудите `subscription.reveal`.
4. Снять галочку `ams-test:mieru` → «Применить» → грант «выключен»; подписка при повторном запросе отдаёт только naive, ETag изменился.
5. Поставить галочку обратно → «Применить» → «включён» после heartbeat узла.
6. «Ротировать URL» → старая ссылка 404, новая открывается, «Показать» отдаёт новую.
7. Viewer-пользователь: окно открывается, ни «Показать», ни «Применить» нет.

Каждый пункт — строка в заметке для `docs/releases/v0.10.0-beta.1.md` (Task 12 владелец делает по слову: тег и заметка — только после его «да»). Найденные дефекты чинить по `superpowers:systematic-debugging`, коммит на каждый.

- [ ] **Step 4: Сохранить состояние**

Обновить `docs/superpowers/plans/CONTINUE-HERE-v0.10.md` по образцу `CONTINUE-HERE-v0.9.md`: что сделано, числа гейта, что проверено вживую, что дальше (тег по слову владельца). Коммит:

```bash
git add docs/superpowers/plans/CONTINUE-HERE-v0.10.md
printf '%s\n' 'docs: CONTINUE-HERE v0.10 — escrow подписки, матрица узлов, гейт на ams-test, живая проверка AMS_Z→ams-test; что дальше — тег по слову владельца' | git commit -q -F -
```

---

## Self-review

**Покрытие спеки.** §2 escrow → Tasks 1–2; §3 API (reveal, обзор, `POST /clients`, удаление `_renderable`) → Tasks 2–4; §4 аудит → Task 5; §5.1 окно клиента → Task 7; §5.2 «Новый клиент» и бандл → Task 8; §5.3 удаление «Выдать доступ» → Task 7; §5.4 экраны протоколов не трогаются → нигде не меняются; §6 крайние случаи: нет домена (Task 4, 7), нет keyring (Task 2, 7), reveal одноразовый (Task 3), отказ саги (Task 7 `apply` перечитывает), узел на паузе (Task 6 `offered=false`), отозвана (Task 7 «Создать URL»), архивный (Task 7 `canWrite`); §7 тесты → Tasks 1–9; §8 документация → Tasks 5, 10; гейт и живая проверка → Task 11.

**Имена.** `SUBSCRIPTION_PURPOSE`, `escrow_enabled`, `reveal_token`, `create_with_client`, `_retire_escrow`, `subscription_reveal_payload` — одинаковы во всех задачах; `placementRows/renderPlacement/readPlacement/placementDiff/cellStatus` — Task 6 определяет, Tasks 7–8 используют; `openOperationBundle(id, { subscription })` — Task 8 определяет и вызывает; `context.subscriptions.open(id, name, reveal)` — Task 7 определяет, Task 8 вызывает.

**Риск.** `test_subscription_ui_contract.py` в Task 5 добавляет тест, а Task 7 переписывает три существующих — порядок задач это учитывает. `proposeUsername` импортируется в `subscriptions.js` из `clients.js`; если возникнет цикл, перенос в `common.js` описан в Task 7.
