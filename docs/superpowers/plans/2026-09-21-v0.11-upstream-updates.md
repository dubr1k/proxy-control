# v0.11 — обновления из upstream и обзор в одну строку: план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** version-agent опрашивает upstream (GitHub Releases, ghcr.io, Docker Hub) по кнопке, ставит `xray`, `mita`, `telemt` из релизов и пересобирает Caddy для `naive`; обзор панели — ресурсы в одну строку, три карточки протоколов, счётчик клиентов, мобильная вёрстка.

**Architecture:** новый модуль `version_agent/upstream.py` (опрос и разбор хэшей, кэш в `state.json`), `version_agent/artifacts.py` (извлечение member из zip/tar.gz), расширение `VersionAgent` четвёртым компонентом `xray`, обновлением закреплённого потребителя `mita` и сборкой Caddy. Панель добавляет `POST /api/versions/check` и relay для узлов. Установщик принимает версии из `state.json` агента при `verify`.

**Tech Stack:** Python 3.11 stdlib (urllib, zipfile, tarfile, json), FastAPI/pydantic (панель), vanilla JS/CSS (фронт), pytest, headless Chrome через CDP (`scripts/lab/ui-acceptance.py`).

**Spec:** `docs/superpowers/specs/2026-09-21-v0.11-upstream-updates-design.md`

## Global Constraints

- Все тесты и гейты — на стенде `ams-test` по SSH (`scripts/dev/remote-gate.sh`), не локально; локально допустимы только `python -m pytest tests/test_version_agent*.py` и `panel/tests/test_*contract*.py` как быстрая проверка перед отправкой на стенд.
- Сообщения коммитов на русском, о сути изменения, без трейлеров об инструменте (`Co-Authored-By`, `Generated with` запрещены), через `git commit -F -`.
- Ни одного секрета в выводе, тестах, фикстурах, документации.
- Хосты опроса фиксированы в коде: `api.github.com`, `github.com`, `objects.githubusercontent.com` (редирект ассетов), `ghcr.io`, `registry-1.docker.io`, `auth.docker.io`, `index.docker.io`. Любой другой хост — `UpdateError`.
- Лимиты тел ответов: JSON релизов ≤ 1 МБ, файл хэша ≤ 4 КБ, архив ≤ 256 МБ, манифест ≤ 64 КБ.
- Версия из тега проходит `_VERSION` каталога (`^[A-Za-z0-9][A-Za-z0-9._+:-]{0,63}$`); тег без ведущей `v` хранится как `version`, тег целиком — как `tag`.
- Тексты UI на русском; пометки источника: «каталог» и «upstream»; подпись риска: «Версия из upstream: хэш из релиза подтверждает целостность скачивания, но проектом она не проверялась».
- Компонент `xray` показывается и принимается только при `PROXY_CONTROL_XRAY_ROUTER=on`.

---

## Карта файлов

| Файл | Ответственность |
|---|---|
| `panel/static/js/dashboard.js` | обзор: без «Application bytes», класс `host-row`, счётчик клиентов |
| `panel/static/js/common.js` | `paintClientsCount(context, total)` |
| `panel/static/js/clients.js` | обновление бейджа после мутаций |
| `panel/static/style.css` | сетка обзора, мобильные точки |
| `panel/tests/test_dashboard_ui_contract.py` | новый: контракт обзора |
| `scripts/lab/ui-acceptance.py` | шаг «обзор на 390px» |
| `version_agent/catalog.py` | `xray`, `source`, `archive`, `kind: build` |
| `version_agent/artifacts.py` | новый: `extract_member(payload, archive, member) -> bytes` |
| `version_agent/upstream.py` | новый: источники, опрос, разбор хэшей, кэш |
| `version_agent/service.py` | `check_upstream`, `xray`, `mita` с потребителем, `naive` build |
| `version_agent/server.py` | `POST /v1/upstream/check` |
| `deploy/version-agent.env.example`, `deploy/version-agent.service` | новые переменные, `ReadWritePaths` |
| `panel/versions.py`, `panel/version_routes.py`, `panel/schemas.py` | `check()`, `xray`, `/api/versions/check` |
| `panel/fleet_v2/node_routes.py`, `client.py`, `central_routes.py` | relay проверки, capability `versions.check` |
| `panel/static/js/management.js` | кнопка проверки, пометка источника, карточка Xray |
| `panel/tests/test_versions.py`, `test_fleet_v2_central_routes.py`, `test_management_ui_contract.py` | панель |
| `installer/runtime_state.py` | новый: чтение `state.json` агента |
| `installer/adapters/{xray_router,mieru,naive}.py` | `verify` принимает версию агента |
| `docs/UPGRADING*.md`, `CHANGELOG*.md`, `docs/releases/v0.11.0-beta.1.md` | документация |

---

### Task 1: Обзор — без «Application bytes», ресурсы одной строкой, три карточки

**Files:**
- Modify: `panel/static/js/dashboard.js:47-75` (hostCard), `:118-121` (Mieru card)
- Modify: `panel/static/style.css:2-4,9`
- Create: `panel/tests/test_dashboard_ui_contract.py`

**Interfaces:**
- Produces: DOM `.protocol-overview > article.host-card.host-row` + три `article.protocol-card`.

- [ ] **Step 1: Контрактный тест**

```python
"""Static contract of the overview (v0.11): one host row, three protocol cards, no Mieru
application-bytes tile, and a mobile layout without horizontal scroll."""
from __future__ import annotations

from pathlib import Path

STATIC = Path(__file__).resolve().parent.parent / "static"
DASHBOARD = (STATIC / "js/dashboard.js").read_text()
CSS = (STATIC / "style.css").read_text()


def test_mieru_card_has_no_application_bytes_tile():
    assert "Application bytes" not in DASHBOARD
    assert "rolling admission" not in DASHBOARD


def test_host_card_is_one_full_width_row_and_protocols_are_three_columns():
    assert 'class="protocol-card host-card host-row' in DASHBOARD
    assert ".protocol-overview{display:grid;grid-template-columns:repeat(3,minmax(0,1fr))" in CSS
    assert ".host-row{grid-column:1/-1" in CSS


def test_overview_has_phone_breakpoints():
    assert "@media(max-width:1040px){.protocol-overview{grid-template-columns:repeat(2,minmax(0,1fr))}" in CSS
    assert "@media(max-width:900px){.protocol-overview{grid-template-columns:1fr}" in CSS
    assert "@media(max-width:560px){.host-metrics{grid-template-columns:1fr}" in CSS
```

- [ ] **Step 2: Запустить, убедиться, что падает**

Run: `python -m pytest panel/tests/test_dashboard_ui_contract.py -v`
Expected: FAIL (три теста — «Application bytes» есть, `host-row` нет).

- [ ] **Step 3: dashboard.js**

В `hostCard` обе ветки `article` получают класс `host-row`:
`<article class="protocol-card host-card host-row degraded">` и
`<article class="protocol-card host-card host-row ${strained ? "degraded" : ""}">`.
Из карточки Mieru удалить строку `<div class="protocol-metrics"><span><small>Application bytes</small>…</div>` целиком.

- [ ] **Step 4: style.css**

Строка 2: заменить `.protocol-overview{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-bottom:12px}` на
`.protocol-overview{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;margin-bottom:12px}.host-row{grid-column:1/-1;min-height:0}.host-row .host-metrics{grid-template-columns:repeat(3,minmax(0,1fr))}`.
Строка 3: в `@media(max-width:1040px){` заменить `.protocol-overview{grid-template-columns:1fr}` на `.protocol-overview{grid-template-columns:repeat(2,minmax(0,1fr))}.protocol-overview>article:last-child{grid-column:1/-1}`.
Строка 4: в начало `@media(max-width:900px){` добавить `.protocol-overview{grid-template-columns:1fr}.protocol-overview>article:last-child{grid-column:auto}`.
Строка 9: оставить как есть (`≤560px` метрики по одной уже там); добавить в конец строки 9: `@media(max-width:560px){.protocol-access,.protocol-metrics{grid-template-columns:1fr 1fr}.protocol-metrics>span:last-child{grid-column:1/3}.protocol-card{min-width:0}}`.

- [ ] **Step 5: Запустить тест, PASS; затем `python -m pytest tests/test_deploy.py -q`**

- [ ] **Step 6: Commit**

```bash
git add panel/static/js/dashboard.js panel/static/style.css panel/tests/test_dashboard_ui_contract.py
printf '%s\n' 'panel: обзор — ресурсы сервера одной строкой, три карточки протоколов, у Mieru нет «Application bytes», сетка для телефона' | git commit -F -
```

---

### Task 2: Счётчик клиентов в навигации

**Files:**
- Modify: `panel/static/js/common.js` (экспорт `paintClientsCount`), `panel/static/js/dashboard.js:13-24`, `panel/static/js/clients.js:130-143` (`renderClients`)
- Modify: `panel/tests/test_dashboard_ui_contract.py`

**Interfaces:**
- Produces: `export function paintClientsCount(context, total)` в `common.js`: `total` — число или `null` → «—».

- [ ] **Step 1: Тест**

```python
def test_clients_badge_is_painted_from_the_clients_list():
    common = (STATIC / "js/common.js").read_text()
    clients = (STATIC / "js/clients.js").read_text()
    assert "export function paintClientsCount(context, total)" in common
    assert '"/api/clients"' in DASHBOARD and "paintClientsCount(context" in DASHBOARD
    assert "paintClientsCount(context, context.state.clients.length)" in clients
    assert 'id="clients-count"' in (STATIC / "index.html").read_text()
```

- [ ] **Step 2: Запустить — FAIL**

- [ ] **Step 3: common.js**

```js
export function paintClientsCount(context, total) {
  const badge = query("#clients-count", context.root);
  if (badge) badge.textContent = typeof total === "number" ? String(total) : "—";
}
```

- [ ] **Step 4: dashboard.js**

```js
async function clientsCount(context) {
  try {
    return ((await context.api("/api/clients")).items || []).length;
  } catch {
    return null;
  }
}
```
В `renderDashboard` расширить `Promise.all` четвёртым элементом `clientsCount(context)` → `[data, users, nodes, clients]`, после `paintNavCounts(...)` вызвать `paintClientsCount(context, clients);` (импорт из `./common.js`).

- [ ] **Step 5: clients.js**

В `renderClients` сразу после того, как `context.state.clients` присвоен список, добавить `paintClientsCount(context, context.state.clients.length);` (импорт из `./common.js`). Функции `lifecycle`/`adopt` уже перерисовывают список через `renderClients`, отдельных вызовов не нужно.

- [ ] **Step 6: Тест PASS, commit**

```bash
git add panel/static/js/common.js panel/static/js/dashboard.js panel/static/js/clients.js panel/tests/test_dashboard_ui_contract.py
printf '%s\n' 'panel: бейдж «Клиенты» в навигации показывает общее число клиентов' | git commit -F -
```

---

### Task 3: Браузерный шаг «обзор на телефоне» в lab

**Files:**
- Modify: `scripts/lab/ui-acceptance.py:191-193` (Browser: `phone()`), `:476` (`view_dashboard`)

- [ ] **Step 1: В `Browser` рядом с `desktop()` добавить**

```python
    def phone(self) -> None:
        self.cdp.call("Emulation.setDeviceMetricsOverride", {"width": 390, "height": 844, "deviceScaleFactor": 2, "mobile": True})
```

- [ ] **Step 2: В конце `view_dashboard` добавить**

```python
        # v0.11: the overview on a phone — one column, no horizontal scroll, the host row first.
        b = self.browser
        b.phone()
        try:
            self.check("dashboard.phone.no_horizontal_scroll",
                       b.js("document.documentElement.scrollWidth <= window.innerWidth + 1"),
                       b.js("JSON.stringify([document.documentElement.scrollWidth, window.innerWidth])"))
            self.check("dashboard.phone.host_row_first",
                       b.js("document.querySelector('.protocol-overview > article')?.classList.contains('host-row') === true"))
            self.check("dashboard.phone.one_column",
                       b.js("(() => { const a = [...document.querySelectorAll('.protocol-overview > article')]; return a.length >= 4 && a.every(e => Math.abs(e.getBoundingClientRect().left - a[0].getBoundingClientRect().left) < 2); })()"))
            self.cells_do_not_overlap("dashboard.phone.no_overlap", ".protocol-card", ".protocol-access > span, .protocol-metrics > span")
            self.check("dashboard.no_application_bytes", not b.js("document.body.innerText.includes('Application bytes')"))
            self.check("dashboard.clients_badge_is_a_number",
                       b.js("/^\\d+$/.test(document.querySelector('#clients-count')?.textContent || '')"))
            self.shot("dashboard-phone", 390)
        finally:
            b.desktop()
```

- [ ] **Step 3: `python -m py_compile scripts/lab/ui-acceptance.py`; commit**

```bash
git add scripts/lab/ui-acceptance.py
printf '%s\n' 'lab: браузерный сценарий проверяет обзор на ширине телефона и бейдж клиентов' | git commit -F -
```

---

### Task 4: Каталог — `xray`, `source`, `archive`, `kind: build`

**Files:**
- Modify: `version_agent/catalog.py`
- Test: `tests/test_version_agent.py`

**Interfaces:**
- Produces: `CatalogEntry(component, version, kind, image=None, url=None, sha256=None, runtime_version=None, archive=None, source="catalog", build=None)`; `archive: dict | None` = `{"format": "zip"|"tar.gz", "member": str}` или для `xray` `{"format": "zip", "members": {"xray": "xray", "geoip.dat": "geoip.dat", "geosite.dat": "geosite.dat"}}`; `build: dict | None` = `{"caddy_version": "2.12.0", "builder_image": "caddy:2.12.0-builder@sha256:…", "forwardproxy_commit": "<40 hex>"}`; `_COMPONENTS = ("telemt", "naive", "mita", "xray")`; `entry.public()` включает `source` и `archive`.

- [ ] **Step 1: Тесты**

```python
def test_catalog_accepts_archive_members_and_the_xray_component(tmp_path: Path):
    catalog = tmp_path / "catalog.json"
    catalog.write_text(json.dumps({"schema": 1, "components": {
        "xray": [{"version": "26.4.1", "kind": "binary",
                  "url": "https://github.com/XTLS/Xray-core/releases/download/v26.4.1/Xray-linux-64.zip",
                  "sha256": "a" * 64,
                  "archive": {"format": "zip", "members": {"xray": "xray", "geoip.dat": "geoip.dat", "geosite.dat": "geosite.dat"}}}],
        "mita": [{"version": "3.37.0", "kind": "binary",
                  "url": "https://github.com/enfein/mieru/releases/download/v3.37.0/mita_3.37.0_linux_amd64.tar.gz",
                  "sha256": "b" * 64, "archive": {"format": "tar.gz", "member": "mita"}}]}}))
    loaded = load_catalog(catalog)
    entry = loaded.entry("xray", "26.4.1")
    assert entry.archive["members"]["geosite.dat"] == "geosite.dat" and entry.source == "catalog"
    assert loaded.entry("mita", "3.37.0").public()["archive"] == {"format": "tar.gz", "member": "mita"}


def test_catalog_rejects_archive_members_that_escape(tmp_path: Path):
    catalog = tmp_path / "catalog.json"
    catalog.write_text(json.dumps({"schema": 1, "components": {"mita": [{
        "version": "3.37.0", "kind": "binary", "url": "https://github.com/enfein/mieru/releases/download/v3.37.0/x.tar.gz",
        "sha256": "b" * 64, "archive": {"format": "tar.gz", "member": "../mita"}}]}}))
    with pytest.raises(CatalogError, match="archive member"):
        load_catalog(catalog)


def test_catalog_accepts_a_caddy_build_entry(tmp_path: Path):
    catalog = tmp_path / "catalog.json"
    catalog.write_text(json.dumps({"schema": 1, "components": {"naive": [{
        "version": "2.12.0", "kind": "build",
        "build": {"caddy_version": "2.12.0", "builder_image": "caddy:2.12.0-builder@sha256:" + "c" * 64,
                  "forwardproxy_commit": "d" * 40}}]}}))
    entry = load_catalog(catalog).entry("naive", "2.12.0")
    assert entry.kind == "build" and entry.build["caddy_version"] == "2.12.0"
```

- [ ] **Step 2: FAIL** — `python -m pytest tests/test_version_agent.py -k catalog -v`

- [ ] **Step 3: catalog.py**

```python
_COMPONENTS = ("telemt", "naive", "mita", "xray")
_ARCHIVE_FORMATS = ("zip", "tar.gz")
_MEMBER = re.compile(r"^[A-Za-z0-9][A-Za-z0-9._-]*(/[A-Za-z0-9][A-Za-z0-9._-]*)*$")
_HEX40 = re.compile(r"^[0-9a-f]{40}$")
_SOURCES = ("catalog", "upstream")


@dataclass(frozen=True)
class CatalogEntry:
    component: str
    version: str
    kind: str
    image: str | None = None
    url: str | None = None
    sha256: str | None = None
    runtime_version: str | None = None
    archive: dict | None = None
    source: str = "catalog"
    build: dict | None = None

    def public(self) -> dict:
        result = {"version": self.version, "kind": self.kind, "source": self.source}
        if self.image: result["image"] = self.image
        if self.url: result["url"] = self.url
        if self.sha256: result["sha256"] = self.sha256
        if self.archive: result["archive"] = dict(self.archive)
        if self.build: result["build"] = dict(self.build)
        return result


def _archive(raw: object) -> dict | None:
    if raw is None:
        return None
    if not isinstance(raw, dict) or raw.get("format") not in _ARCHIVE_FORMATS:
        raise CatalogError("unsupported archive format")
    _reject_unknown(raw, {"format", "member", "members"}, "archive")
    members = raw.get("members")
    member = raw.get("member")
    if (member is None) == (members is None):
        raise CatalogError("archive needs member or members")
    names = [member] if member is not None else list(members.values()) if isinstance(members, dict) else None
    if names is None or not names or any(type(n) is not str or not _MEMBER.fullmatch(n) or ".." in n.split("/") for n in names):
        raise CatalogError("invalid archive member")
    if members is not None and set(members) != {"xray", "geoip.dat", "geosite.dat"}:
        raise CatalogError("invalid archive member set")
    return {"format": raw["format"], **({"member": member} if member is not None else {"members": dict(members)})}


def _build(raw: object) -> dict:
    if not isinstance(raw, dict):
        raise CatalogError("build entry must be an object")
    _reject_unknown(raw, {"caddy_version", "builder_image", "forwardproxy_commit"}, "build")
    version, image, commit = raw.get("caddy_version"), raw.get("builder_image"), raw.get("forwardproxy_commit")
    if type(version) is not str or not _VERSION.fullmatch(version) or type(image) is not str or not _IMAGE.fullmatch(image) \
            or not image.startswith("caddy:") or type(commit) is not str or not _HEX40.fullmatch(commit):
        raise CatalogError("build entry must pin caddy_version, a caddy builder image digest and a forwardproxy commit")
    return {"caddy_version": version, "builder_image": image, "forwardproxy_commit": commit}
```

В `_entry`: множество разрешённых полей корня расширить `{"archive", "source", "build"}`; `source = raw.get("source", "catalog")`, если не в `_SOURCES` — `CatalogError("invalid source")`; для `kind == "binary"` разрешённые поля + `archive`, `source`, передавать `archive=_archive(raw.get("archive"))`; для `kind == "image"` + `source`; новый `kind == "build"`: только для `component == "naive"`, поля `{"version","kind","build","source","runtime_version"}`, `build=_build(raw["build"])`. Для `component == "xray"` с `archive` требовать `members`, для остальных — `member`.

- [ ] **Step 4: PASS; весь `tests/test_version_agent.py` зелёный; commit**

```bash
git add version_agent/catalog.py tests/test_version_agent.py
printf '%s\n' 'version-agent: каталог знает компонент xray, member внутри архива, источник записи и сборку Caddy' | git commit -F -
```

---

### Task 5: Извлечение member из архива

**Files:**
- Create: `version_agent/artifacts.py`
- Create: `tests/test_version_agent_artifacts.py`

**Interfaces:**
- Produces: `extract_member(payload: bytes, archive: dict, member: str, *, maximum: int = 256 * 1024 * 1024) -> bytes`; `ArtifactError(ValueError)`.

- [ ] **Step 1: Тесты**

```python
from __future__ import annotations

import io
import tarfile
import zipfile

import pytest

from version_agent.artifacts import ArtifactError, extract_member


def _zip(entries: dict[str, bytes]) -> bytes:
    buffer = io.BytesIO()
    with zipfile.ZipFile(buffer, "w") as archive:
        for name, data in entries.items():
            archive.writestr(name, data)
    return buffer.getvalue()


def _targz(entries: dict[str, bytes], *, symlink: str | None = None) -> bytes:
    buffer = io.BytesIO()
    with tarfile.open(fileobj=buffer, mode="w:gz") as archive:
        for name, data in entries.items():
            info = tarfile.TarInfo(name); info.size = len(data)
            archive.addfile(info, io.BytesIO(data))
        if symlink:
            info = tarfile.TarInfo(symlink); info.type = tarfile.SYMTYPE; info.linkname = "/etc/passwd"
            archive.addfile(info)
    return buffer.getvalue()


def test_zip_member_is_returned_by_exact_name():
    assert extract_member(_zip({"xray": b"bin", "geoip.dat": b"geo"}), {"format": "zip"}, "xray") == b"bin"


def test_tar_member_may_sit_under_a_directory():
    payload = _targz({"mita_3.37.0_linux_amd64/mita": b"bin"})
    assert extract_member(payload, {"format": "tar.gz"}, "mita_3.37.0_linux_amd64/mita") == b"bin"


def test_missing_member_symlink_and_oversize_are_refused():
    with pytest.raises(ArtifactError, match="member not found"):
        extract_member(_zip({"xray": b"bin"}), {"format": "zip"}, "geoip.dat")
    with pytest.raises(ArtifactError, match="not a regular file"):
        extract_member(_targz({}, symlink="mita"), {"format": "tar.gz"}, "mita")
    with pytest.raises(ArtifactError, match="too large"):
        extract_member(_zip({"xray": b"x" * 32}), {"format": "zip"}, "xray", maximum=16)
```

- [ ] **Step 2: FAIL (ModuleNotFoundError)**

- [ ] **Step 3: artifacts.py**

```python
"""Pull one regular file out of a release archive the agent already verified by SHA-256."""
from __future__ import annotations

import io
import tarfile
import zipfile


class ArtifactError(ValueError):
    """The archive does not hold the member the catalog names, or holds it unsafely."""


def extract_member(payload: bytes, archive: dict, member: str, *, maximum: int = 256 * 1024 * 1024) -> bytes:
    if ".." in member.split("/") or member.startswith("/"):
        raise ArtifactError("invalid archive member")
    fmt = archive.get("format")
    try:
        if fmt == "zip":
            with zipfile.ZipFile(io.BytesIO(payload)) as bundle:
                try:
                    info = bundle.getinfo(member)
                except KeyError as exc:
                    raise ArtifactError("archive member not found") from exc
                if info.is_dir() or (info.external_attr >> 16) & 0o170000 == 0o120000:
                    raise ArtifactError("archive member is not a regular file")
                if info.file_size > maximum:
                    raise ArtifactError("archive member is too large")
                return bundle.read(info)
        if fmt == "tar.gz":
            with tarfile.open(fileobj=io.BytesIO(payload), mode="r:gz") as bundle:
                for info in bundle:
                    if info.name.lstrip("./") != member:
                        continue
                    if not info.isreg():
                        raise ArtifactError("archive member is not a regular file")
                    if info.size > maximum:
                        raise ArtifactError("archive member is too large")
                    handle = bundle.extractfile(info)
                    return handle.read() if handle else b""
                raise ArtifactError("archive member not found")
    except (zipfile.BadZipFile, tarfile.TarError, EOFError, OSError) as exc:
        raise ArtifactError("archive is unreadable") from exc
    raise ArtifactError("unsupported archive format")
```

- [ ] **Step 4: PASS; commit**

```bash
git add version_agent/artifacts.py tests/test_version_agent_artifacts.py
printf '%s\n' 'version-agent: извлечение одного файла из zip/tar.gz релиза без symlink и выхода из архива' | git commit -F -
```

---

### Task 6: Опрос upstream — GitHub Releases, ghcr.io, Docker Hub

**Files:**
- Create: `version_agent/upstream.py`
- Create: `tests/test_version_agent_upstream.py`

**Interfaces:**
- Produces:
  - `Fetcher = Callable[[str, dict[str, str] | None, int], tuple[int, dict[str, str], bytes]]` — `(url, headers, maximum) -> (status, headers, body)`; реализация по умолчанию `fetch_https` (urllib, редирект только внутри `ALLOWED_HOSTS`).
  - `ALLOWED_HOSTS = frozenset({"api.github.com", "github.com", "objects.githubusercontent.com", "ghcr.io", "registry-1.docker.io", "auth.docker.io", "index.docker.io"})`
  - `check_component(component: str, current: str | None, *, fetcher: Fetcher, router_enabled: bool) -> dict` → `{"latest": str|None, "installable": bool, "reason": str|None, "candidates": [entry-dict…]}` где entry-dict — то, что примет `catalog._entry` (`version`, `kind`, `url`, `sha256`, `archive`, `source: "upstream"`, `published_at`, `tag`; для `telemt` — `image`; для `naive` — `kind: "build"`, `build`).
  - `check_all(current: dict[str, str | None], *, fetcher, router_enabled) -> dict[str, dict]`.
  - `parse_dgst(text: str) -> str` (строка `SHA2-256= <hex>` или `SHA256= <hex>`), `parse_sha256_txt(text: str) -> str` (первое 64-hex слово).
  - `compare_versions(a: str, b: str) -> int` — числовые точки, суффикс после `-` младше.

- [ ] **Step 1: Тесты (фикстура-fetcher по словарю URL → ответ)**

```python
from __future__ import annotations

import json

import pytest

from version_agent.upstream import (ALLOWED_HOSTS, UpstreamError, check_component, compare_versions,
                                    parse_dgst, parse_sha256_txt)

XRAY_RELEASES = [{"tag_name": "v26.4.1", "prerelease": False, "draft": False, "published_at": "2026-04-01T00:00:00Z",
                  "assets": [{"name": "Xray-linux-64.zip", "browser_download_url": "https://github.com/XTLS/Xray-core/releases/download/v26.4.1/Xray-linux-64.zip"},
                             {"name": "Xray-linux-64.zip.dgst", "browser_download_url": "https://github.com/XTLS/Xray-core/releases/download/v26.4.1/Xray-linux-64.zip.dgst"}]},
                 {"tag_name": "v26.5.0-rc1", "prerelease": True, "draft": False, "published_at": "2026-05-01T00:00:00Z", "assets": []}]
MIERU_RELEASES = [{"tag_name": "v3.37.0", "prerelease": False, "draft": False, "published_at": "2026-03-01T00:00:00Z",
                   "assets": [{"name": "mita_3.37.0_linux_amd64.tar.gz", "browser_download_url": "https://github.com/enfein/mieru/releases/download/v3.37.0/mita_3.37.0_linux_amd64.tar.gz"},
                              {"name": "mita_3.37.0_linux_amd64.tar.gz.sha256.txt", "browser_download_url": "https://github.com/enfein/mieru/releases/download/v3.37.0/mita_3.37.0_linux_amd64.tar.gz.sha256.txt"}]}]


def fetcher_from(table: dict[str, tuple[int, dict, bytes]]):
    seen: list[str] = []
    def fetch(url, headers=None, maximum=0):
        seen.append(url)
        if url not in table:
            raise UpstreamError(f"unexpected url {url}")
        return table[url]
    fetch.seen = seen
    return fetch


def test_parsers_read_the_published_digests():
    assert parse_dgst("MD5= x\nSHA1= y\nSHA2-256= " + "a" * 64 + "\nSHA2-512= z\n") == "a" * 64
    assert parse_sha256_txt("b" * 64 + "  mita_3.37.0_linux_amd64.tar.gz\n") == "b" * 64
    with pytest.raises(UpstreamError):
        parse_dgst("nothing here")


def test_compare_versions_orders_numerically_and_prereleases_lower():
    assert compare_versions("26.4.1", "26.3.27") > 0
    assert compare_versions("3.37.0", "3.37.0") == 0
    assert compare_versions("2.12.0-beta.1", "2.12.0") < 0


def test_xray_candidate_carries_the_dgst_hash_and_skips_prereleases():
    fetch = fetcher_from({
        "https://api.github.com/repos/XTLS/Xray-core/releases?per_page=10": (200, {}, json.dumps(XRAY_RELEASES).encode()),
        "https://github.com/XTLS/Xray-core/releases/download/v26.4.1/Xray-linux-64.zip.dgst": (200, {}, b"SHA2-256= " + b"a" * 64 + b"\n"),
    })
    result = check_component("xray", "26.3.27", fetcher=fetch, router_enabled=True)
    assert result["latest"] == "26.4.1" and result["installable"] is True
    [candidate] = result["candidates"]
    assert candidate["sha256"] == "a" * 64 and candidate["source"] == "upstream"
    assert candidate["archive"] == {"format": "zip", "members": {"xray": "xray", "geoip.dat": "geoip.dat", "geosite.dat": "geosite.dat"}}
    assert candidate["url"].endswith("/v26.4.1/Xray-linux-64.zip")


def test_mita_candidate_reads_sha256_txt_and_names_the_tar_member():
    fetch = fetcher_from({
        "https://api.github.com/repos/enfein/mieru/releases?per_page=10": (200, {}, json.dumps(MIERU_RELEASES).encode()),
        "https://github.com/enfein/mieru/releases/download/v3.37.0/mita_3.37.0_linux_amd64.tar.gz.sha256.txt": (200, {}, b"b" * 64 + b"  mita_3.37.0_linux_amd64.tar.gz\n"),
    })
    result = check_component("mita", "3.36.0", fetcher=fetch, router_enabled=False)
    [candidate] = result["candidates"]
    assert candidate["archive"] == {"format": "tar.gz", "member": "mita"} and candidate["sha256"] == "b" * 64


def test_release_without_a_digest_file_is_visible_but_not_installable():
    releases = [{**MIERU_RELEASES[0], "assets": MIERU_RELEASES[0]["assets"][:1]}]
    fetch = fetcher_from({"https://api.github.com/repos/enfein/mieru/releases?per_page=10": (200, {}, json.dumps(releases).encode())})
    result = check_component("mita", "3.36.0", fetcher=fetch, router_enabled=False)
    assert result == {"latest": "3.37.0", "installable": False, "reason": "no_published_digest", "candidates": []}


def test_telemt_candidate_is_the_registry_manifest_digest():
    fetch = fetcher_from({
        "https://ghcr.io/token?scope=repository:samnet-dev/mtproxymax-telemt:pull": (200, {}, b'{"token": "t"}'),
        "https://ghcr.io/v2/samnet-dev/mtproxymax-telemt/tags/list?n=100": (200, {}, b'{"tags": ["3.4.24", "3.4.25", "latest"]}'),
        "https://ghcr.io/v2/samnet-dev/mtproxymax-telemt/manifests/3.4.25": (200, {"Docker-Content-Digest": "sha256:" + "c" * 64}, b"{}"),
    })
    result = check_component("telemt", "3.4.24", fetcher=fetch, router_enabled=False)
    [candidate] = result["candidates"]
    assert candidate == {"version": "3.4.25", "kind": "image", "source": "upstream", "tag": "3.4.25",
                         "image": "ghcr.io/samnet-dev/mtproxymax-telemt@sha256:" + "c" * 64, "published_at": None}


def test_naive_candidate_is_a_build_with_the_builder_digest_and_forwardproxy_commit():
    fetch = fetcher_from({
        "https://api.github.com/repos/caddyserver/caddy/releases?per_page=10": (200, {}, json.dumps([{"tag_name": "v2.12.0", "prerelease": False, "draft": False, "published_at": "2026-02-01T00:00:00Z", "assets": []}]).encode()),
        "https://api.github.com/repos/klzgrad/forwardproxy/commits/caddy2": (200, {}, json.dumps({"sha": "d" * 40}).encode()),
        "https://auth.docker.io/token?service=registry.docker.io&scope=repository:library/caddy:pull": (200, {}, b'{"token": "t"}'),
        "https://registry-1.docker.io/v2/library/caddy/manifests/2.12.0-builder": (200, {"Docker-Content-Digest": "sha256:" + "e" * 64}, b"{}"),
    })
    result = check_component("naive", "2.11.4", fetcher=fetch, router_enabled=False)
    [candidate] = result["candidates"]
    assert candidate["kind"] == "build" and candidate["build"] == {
        "caddy_version": "2.12.0", "builder_image": "caddy:2.12.0-builder@sha256:" + "e" * 64, "forwardproxy_commit": "d" * 40}


def test_only_newer_than_current_are_candidates_and_xray_is_off_without_the_router():
    fetch = fetcher_from({"https://api.github.com/repos/XTLS/Xray-core/releases?per_page=10": (200, {}, json.dumps(XRAY_RELEASES).encode())})
    assert check_component("xray", "26.4.1", fetcher=fetch, router_enabled=True)["candidates"] == []
    assert check_component("xray", "26.3.27", fetcher=fetch, router_enabled=False) == {
        "latest": None, "installable": False, "reason": "router_not_installed", "candidates": []}


def test_fetch_https_refuses_hosts_outside_the_allowlist():
    from version_agent.upstream import fetch_https
    with pytest.raises(UpstreamError, match="host"):
        fetch_https("https://example.com/x", None, 10)
    assert "api.github.com" in ALLOWED_HOSTS
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: upstream.py**

```python
"""Ask the upstream projects what they published (v0.11): GitHub Releases for Xray-core,
mieru and Caddy, the ghcr.io and Docker Hub registries for image digests. Fixed hosts,
bounded bodies, and a candidate only when the publisher also published a digest.
"""
from __future__ import annotations

import json
import re
from typing import Callable
from urllib.error import HTTPError, URLError
from urllib.parse import urlsplit
from urllib.request import Request, urlopen

ALLOWED_HOSTS = frozenset({"api.github.com", "github.com", "objects.githubusercontent.com", "ghcr.io",
                           "registry-1.docker.io", "auth.docker.io", "index.docker.io"})
MAX_JSON = 1024 * 1024
MAX_DIGEST_FILE = 4096
MAX_MANIFEST = 64 * 1024
USER_AGENT = "proxy-control-version-agent/1"
_HEX64 = re.compile(r"\b([0-9a-f]{64})\b")
_VERSION = re.compile(r"^[A-Za-z0-9][A-Za-z0-9._+:-]{0,63}$")
XRAY_MEMBERS = {"xray": "xray", "geoip.dat": "geoip.dat", "geosite.dat": "geosite.dat"}

Fetcher = Callable[[str, dict | None, int], tuple[int, dict, bytes]]


class UpstreamError(RuntimeError):
    """The upstream answered something the agent will not act on."""


def fetch_https(url: str, headers: dict | None, maximum: int) -> tuple[int, dict, bytes]:
    parts = urlsplit(url)
    if parts.scheme != "https" or parts.hostname not in ALLOWED_HOSTS or parts.username or parts.password:
        raise UpstreamError("upstream host is not allowed")
    request = Request(url, headers={"User-Agent": USER_AGENT, **(headers or {})})
    try:
        with urlopen(request, timeout=30) as response:  # noqa: S310 - host checked above
            final = urlsplit(response.geturl())
            if final.scheme != "https" or final.hostname not in ALLOWED_HOSTS:
                raise UpstreamError("upstream redirect left the allowed hosts")
            body = response.read(maximum + 1)
            if len(body) > maximum:
                raise UpstreamError("upstream answer is too large")
            return response.status, {k.title(): v for k, v in response.headers.items()}, body
    except HTTPError as exc:
        return exc.code, {k.title(): v for k, v in exc.headers.items()}, b""
    except (URLError, OSError, ValueError) as exc:
        raise UpstreamError(f"upstream unreachable: {exc}") from exc


def _json(fetcher: Fetcher, url: str, maximum: int = MAX_JSON, headers: dict | None = None):
    status, _, body = fetcher(url, headers, maximum)
    if status != 200:
        raise UpstreamError(f"upstream answered {status} for {urlsplit(url).path}")
    try:
        return json.loads(body)
    except ValueError as exc:
        raise UpstreamError("upstream answer is not JSON") from exc


def parse_dgst(text: str) -> str:
    for line in text.splitlines():
        key, _, value = line.partition("=")
        if key.strip() in ("SHA2-256", "SHA256") and _HEX64.fullmatch(value.strip().lower()):
            return value.strip().lower()
    raise UpstreamError("no SHA2-256 line in .dgst")


def parse_sha256_txt(text: str) -> str:
    match = _HEX64.search(text.lower())
    if not match:
        raise UpstreamError("no SHA-256 in .sha256.txt")
    return match.group(1)


def _parts(version: str) -> tuple[tuple[int, ...], str]:
    core, _, suffix = version.partition("-")
    numbers = tuple(int(piece) if piece.isdigit() else 0 for piece in core.split("."))
    return numbers, suffix


def compare_versions(a: str, b: str) -> int:
    (na, sa), (nb, sb) = _parts(a), _parts(b)
    width = max(len(na), len(nb))
    na, nb = na + (0,) * (width - len(na)), nb + (0,) * (width - len(nb))
    if na != nb:
        return 1 if na > nb else -1
    if sa == sb:
        return 0
    if not sa:
        return 1
    if not sb:
        return -1
    return 1 if sa > sb else -1


def _releases(fetcher: Fetcher, repository: str) -> list[dict]:
    items = _json(fetcher, f"https://api.github.com/repos/{repository}/releases?per_page=10")
    if not isinstance(items, list):
        raise UpstreamError("releases answer is not a list")
    result = []
    for item in items:
        if not isinstance(item, dict) or item.get("prerelease") or item.get("draft"):
            continue
        tag = str(item.get("tag_name") or "")
        version = tag[1:] if tag.startswith("v") else tag
        if not _VERSION.fullmatch(version):
            continue
        assets = {a.get("name"): a.get("browser_download_url") for a in item.get("assets", []) if isinstance(a, dict)}
        result.append({"tag": tag, "version": version, "published_at": item.get("published_at"), "assets": assets})
    return result


def _asset_url(assets: dict, name: str, repository: str) -> str | None:
    url = assets.get(name)
    if not isinstance(url, str):
        return None
    parts = urlsplit(url)
    if parts.scheme != "https" or parts.hostname != "github.com" or not parts.path.startswith(f"/{repository}/releases/download/"):
        raise UpstreamError("asset URL is not the release download of its repository")
    return url


def _github_binary(fetcher: Fetcher, current: str | None, repository: str, asset_for: Callable[[str], str],
                   digest_suffix: str, parse, archive_for: Callable[[str], dict]) -> dict:
    releases = _releases(fetcher, repository)
    if not releases:
        return {"latest": None, "installable": False, "reason": "no_releases", "candidates": []}
    latest = releases[0]["version"]
    candidates, reason = [], None
    for release in releases:
        if current is not None and compare_versions(release["version"], current) <= 0:
            continue
        name = asset_for(release["version"])
        url = _asset_url(release["assets"], name, repository)
        digest_url = _asset_url(release["assets"], name + digest_suffix, repository)
        if not url or not digest_url:
            reason = reason or "no_published_digest"
            continue
        status, _, body = fetcher(digest_url, None, MAX_DIGEST_FILE)
        if status != 200:
            reason = reason or "no_published_digest"
            continue
        candidates.append({"version": release["version"], "tag": release["tag"], "kind": "binary", "source": "upstream",
                           "url": url, "sha256": parse(body.decode("utf-8", "replace")),
                           "archive": archive_for(release["version"]), "published_at": release["published_at"]})
    return {"latest": latest, "installable": bool(candidates), "reason": None if candidates else reason, "candidates": candidates}


def _registry_digest(fetcher: Fetcher, registry: str, repository: str, tag: str, token_url: str) -> str:
    token = _json(fetcher, token_url, 16 * 1024).get("token")
    headers = {"Authorization": f"Bearer {token}",
               "Accept": "application/vnd.oci.image.index.v1+json, application/vnd.docker.distribution.manifest.list.v2+json, "
                         "application/vnd.oci.image.manifest.v1+json, application/vnd.docker.distribution.manifest.v2+json"}
    status, response_headers, _ = fetcher(f"https://{registry}/v2/{repository}/manifests/{tag}", headers, MAX_MANIFEST)
    digest = response_headers.get("Docker-Content-Digest", "")
    if status != 200 or not re.fullmatch(r"sha256:[0-9a-f]{64}", digest):
        raise UpstreamError("registry did not answer with a content digest")
    return digest


def _telemt(fetcher: Fetcher, current: str | None) -> dict:
    repository = "samnet-dev/mtproxymax-telemt"
    token_url = f"https://ghcr.io/token?scope=repository:{repository}:pull"
    token = _json(fetcher, token_url, 16 * 1024).get("token")
    tags = _json(fetcher, f"https://ghcr.io/v2/{repository}/tags/list?n=100", headers={"Authorization": f"Bearer {token}"}).get("tags") or []
    versions = sorted((t for t in tags if isinstance(t, str) and re.fullmatch(r"\d+(\.\d+){1,3}", t)),
                      key=lambda v: _parts(v)[0], reverse=True)
    if not versions:
        return {"latest": None, "installable": False, "reason": "no_releases", "candidates": []}
    candidates = []
    for version in versions[:5]:
        if current is not None and compare_versions(version, current) <= 0:
            continue
        digest = _registry_digest(fetcher, "ghcr.io", repository, version, token_url)
        candidates.append({"version": version, "tag": version, "kind": "image", "source": "upstream",
                           "image": f"ghcr.io/{repository}@{digest}", "published_at": None})
    return {"latest": versions[0], "installable": bool(candidates), "reason": None, "candidates": candidates}


def _naive(fetcher: Fetcher, current: str | None) -> dict:
    releases = _releases(fetcher, "caddyserver/caddy")
    if not releases:
        return {"latest": None, "installable": False, "reason": "no_releases", "candidates": []}
    latest = releases[0]
    if current is not None and compare_versions(latest["version"], current) <= 0:
        return {"latest": latest["version"], "installable": False, "reason": None, "candidates": []}
    commit = _json(fetcher, "https://api.github.com/repos/klzgrad/forwardproxy/commits/caddy2").get("sha")
    if not isinstance(commit, str) or not re.fullmatch(r"[0-9a-f]{40}", commit):
        raise UpstreamError("forwardproxy commit is missing")
    digest = _registry_digest(fetcher, "registry-1.docker.io", "library/caddy", f"{latest['version']}-builder",
                              "https://auth.docker.io/token?service=registry.docker.io&scope=repository:library/caddy:pull")
    candidate = {"version": latest["version"], "tag": latest["tag"], "kind": "build", "source": "upstream",
                 "published_at": latest["published_at"],
                 "build": {"caddy_version": latest["version"], "builder_image": f"caddy:{latest['version']}-builder@{digest}",
                           "forwardproxy_commit": commit}}
    return {"latest": latest["version"], "installable": True, "reason": None, "candidates": [candidate]}


def check_component(component: str, current: str | None, *, fetcher: Fetcher, router_enabled: bool) -> dict:
    if component == "xray":
        if not router_enabled:
            return {"latest": None, "installable": False, "reason": "router_not_installed", "candidates": []}
        return _github_binary(fetcher, current, "XTLS/Xray-core", lambda _v: "Xray-linux-64.zip", ".dgst", parse_dgst,
                              lambda _v: {"format": "zip", "members": dict(XRAY_MEMBERS)})
    if component == "mita":
        return _github_binary(fetcher, current, "enfein/mieru", lambda v: f"mita_{v}_linux_amd64.tar.gz", ".sha256.txt",
                              parse_sha256_txt, lambda _v: {"format": "tar.gz", "member": "mita"})
    if component == "telemt":
        return _telemt(fetcher, current)
    if component == "naive":
        return _naive(fetcher, current)
    raise UpstreamError("unknown component")


def check_all(current: dict[str, str | None], *, fetcher: Fetcher, router_enabled: bool) -> dict[str, dict]:
    result = {}
    for component in ("telemt", "naive", "mita", "xray"):
        try:
            result[component] = check_component(component, current.get(component), fetcher=fetcher, router_enabled=router_enabled)
        except UpstreamError as exc:
            result[component] = {"latest": None, "installable": False, "reason": None, "candidates": [], "last_error": str(exc)}
    return result
```

Примечание к member `mita`: релизный `tar.gz` может класть бинарник в корень или в каталог — во время живой проверки (Task 16) уточнить и при необходимости поправить `archive_for` на путь с каталогом; тест Task 5 уже покрывает оба варианта.

- [ ] **Step 4: PASS; commit**

```bash
git add version_agent/upstream.py tests/test_version_agent_upstream.py
printf '%s\n' 'version-agent: опрос upstream — релизы GitHub с опубликованными хэшами, digest образов из ghcr и Docker Hub, только разрешённые хосты' | git commit -F -
```

---

### Task 7: Агент — `check_upstream`, кэш в `state.json`, выбор версии из upstream

**Files:**
- Modify: `version_agent/service.py:118-230` (`__init__`, `list_versions`, `update`), `:477-505` (`agent_from_env`)
- Test: `tests/test_version_agent.py`

**Interfaces:**
- Consumes: `check_all` из Task 6, `CatalogEntry`/`_entry` из Task 4 (сделать `_entry` публичным как `entry_from_dict(component, raw) -> CatalogEntry`, старое имя оставить алиасом).
- Produces: `VersionAgent(..., fetcher: Fetcher | None = None, upstream_enabled: bool = True, router_enabled: bool = False, clock=time.time)`; `check_upstream(force: bool = False) -> dict` (тот же формат, что `list_versions`); `list_versions()` отдаёт `components[c]["upstream"] = {"checked_at", "latest", "installable", "reason", "last_error"}` и `available` = каталог + кандидаты upstream (без дублей версий, каталог приоритетнее); `_resolve(component, version) -> CatalogEntry` ищет в каталоге, затем в `state["upstream"]["components"][c]["candidates"]`; `state.json` `schema: 2` (читается и 1).

- [ ] **Step 1: Тесты**

```python
def _agent(tmp_path, **extra):
    catalog = tmp_path / "catalog.json"
    write_catalog(catalog)
    return VersionAgent(catalog_path=catalog, state_path=tmp_path / "state.json",
                        runner=lambda command, *, env=None, cwd=None, timeout=None: "active\n", **extra)


def test_check_upstream_caches_candidates_and_lists_them_after_the_catalog(tmp_path: Path):
    calls = []
    def fake_check_all(current, *, fetcher, router_enabled):
        calls.append(dict(current))
        return {"mita": {"latest": "3.37.0", "installable": True, "reason": None, "candidates": [
                    {"version": "3.37.0", "tag": "v3.37.0", "kind": "binary", "source": "upstream",
                     "url": "https://github.com/enfein/mieru/releases/download/v3.37.0/mita_3.37.0_linux_amd64.tar.gz",
                     "sha256": "b" * 64, "archive": {"format": "tar.gz", "member": "mita"}, "published_at": None}]},
                "telemt": {"latest": None, "installable": False, "reason": "no_releases", "candidates": []},
                "naive": {"latest": None, "installable": False, "reason": None, "candidates": [], "last_error": "upstream unreachable"},
                "xray": {"latest": None, "installable": False, "reason": "router_not_installed", "candidates": []}}
    now = [1_000_000]
    agent = _agent(tmp_path, clock=lambda: now[0])
    agent.check_all = fake_check_all
    listed = agent.check_upstream()
    mita = listed["components"]["mita"]
    assert [e["version"] for e in mita["available"]] == ["3.35.0", "3.37.0"]
    assert mita["available"][1]["source"] == "upstream" and mita["upstream"]["checked_at"] == 1_000_000
    assert listed["components"]["naive"]["upstream"]["last_error"] == "upstream unreachable"
    assert "xray" not in listed["components"]  # router off → the component is not shown
    assert json.loads((tmp_path / "state.json").read_text())["schema"] == 2
    now[0] += 30
    agent.check_upstream()
    assert len(calls) == 1  # inside the 60 s window the cache answers
    now[0] += 31
    agent.check_upstream()
    assert len(calls) == 2


def test_update_installs_a_cached_upstream_candidate_by_version(tmp_path: Path):
    target = tmp_path / "mita"; target.write_bytes(b"old"); target.chmod(0o755)
    payload = _targz({"mita": BINARY})  # helper from tests/test_version_agent_artifacts.py, import it
    digest = hashlib.sha256(payload).hexdigest()
    commands = []
    def run(command, *, env=None, cwd=None, timeout=None):
        commands.append(command); return "active\n"
    agent = _agent(tmp_path, binary_paths={"mita": target}, service_names={"mita": "mita"}, downloader=lambda url: payload, runner=run)
    agent.check_all = lambda current, *, fetcher, router_enabled: {"mita": {"latest": "3.37.0", "installable": True, "reason": None, "candidates": [
        {"version": "3.37.0", "tag": "v3.37.0", "kind": "binary", "source": "upstream",
         "url": "https://github.com/enfein/mieru/releases/download/v3.37.0/mita_3.37.0_linux_amd64.tar.gz",
         "sha256": digest, "archive": {"format": "tar.gz", "member": "mita"}, "published_at": None}]}}
    agent.check_upstream()
    result = agent.update("mita", "3.37.0", expected_current=None)
    assert result["changed"] is True and target.read_bytes() == BINARY
    state = json.loads((tmp_path / "state.json").read_text())["components"]["mita"]
    assert state["version"] == "3.37.0" and state["source"] == "upstream" and state["sha256"] == hashlib.sha256(BINARY).hexdigest()


def test_upstream_disabled_lists_only_the_catalog_and_refuses_to_check(tmp_path: Path):
    agent = _agent(tmp_path, upstream_enabled=False)
    assert agent.list_versions()["upstream_enabled"] is False
    with pytest.raises(UpdateError, match="disabled"):
        agent.check_upstream()
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: service.py**

В `__init__` добавить параметры `fetcher=None, upstream_enabled=True, router_enabled=False, clock=time.time` и сохранить; `from .upstream import check_all as _check_all, fetch_https, UpstreamError`; атрибут `self.check_all = _check_all` (тест подменяет); `from .artifacts import ArtifactError, extract_member`; `from .catalog import entry_from_dict`.

`_load_state`: принимать `schema in (1, 2)`; при сохранении писать `schema: 2`.

```python
    UPSTREAM_MIN_INTERVAL = 60

    def _components(self) -> tuple[str, ...]:
        return ("telemt", "naive", "mita", "xray") if self.router_enabled else ("telemt", "naive", "mita")

    def _current_versions(self, state: dict) -> dict[str, str | None]:
        return {c: (state.get("components", {}).get(c) or {}).get("version") for c in self._components()}

    def list_versions(self) -> dict:
        catalog = load_catalog(self.catalog_path)
        state = self._state()
        cached = state.get("upstream", {})
        components = {}
        for component in self._components():
            current = state.get("components", {}).get(component, {})
            current = current if isinstance(current, dict) else {}
            entries = [entry.public() for entry in catalog.components.get(component, ())]
            seen = {entry["version"] for entry in entries}
            probe = cached.get("components", {}).get(component, {})
            for candidate in probe.get("candidates", []):
                if candidate["version"] not in seen:
                    entries.append(dict(candidate)); seen.add(candidate["version"])
            components[component] = {
                "current": current.get("version"), "status": current.get("status", "ready"), "available": entries,
                "upstream": {"checked_at": cached.get("checked_at"), "latest": probe.get("latest"),
                             "installable": probe.get("installable", False), "reason": probe.get("reason"),
                             "last_error": probe.get("last_error")}}
        return {"enabled": True, "upstream_enabled": self.upstream_enabled, "checked_at": cached.get("checked_at"), "components": components}

    def check_upstream(self, force: bool = False) -> dict:
        if not self.upstream_enabled:
            raise UpdateError("upstream check is disabled on this host")
        with self._locked():
            state = self._state()
            cached = state.get("upstream", {})
            now = int(self.clock())
            if not force and cached.get("checked_at") and now - int(cached["checked_at"]) < self.UPSTREAM_MIN_INTERVAL:
                return self.list_versions()
            probes = self.check_all(self._current_versions(state), fetcher=self.fetcher or fetch_https, router_enabled=self.router_enabled)
            for component, probe in probes.items():
                # A source that failed keeps its previous candidates; the error rides alongside.
                if probe.get("last_error") and component in cached.get("components", {}):
                    previous = cached["components"][component]
                    probes[component] = {**previous, "last_error": probe["last_error"]}
            state["upstream"] = {"checked_at": now, "components": probes}
            self._save_state(state)
        return self.list_versions()

    def _resolve(self, component: str, version: str, state: dict) -> CatalogEntry:
        catalog = load_catalog(self.catalog_path)
        try:
            return catalog.entry(component, version)
        except CatalogError:
            pass
        for candidate in state.get("upstream", {}).get("components", {}).get(component, {}).get("candidates", []):
            if candidate.get("version") == version:
                raw = {k: v for k, v in candidate.items() if k not in ("tag", "published_at")}
                return entry_from_dict(component, raw)
        raise CatalogError(f"version is not approved for {component}")
```

В `update`: `if component not in self._components(): raise CatalogError("unsupported component")`; внутри lock заменить `catalog = load_catalog(...); entry = catalog.entry(component, version)` на `state = self._state(); entry = self._resolve(component, version, state)` (state читается один раз). В сохранённое состояние компонента добавить `"source": entry.source, "archive": entry.archive`, а для `binary` — `"sha256"` = хэш **установленного файла** (для архивов он отличается от хэша архива): `_update_binary` возвращает `installed_sha256`, `update` пишет его.

В `_update_binary`: после `payload = self.downloader(entry.url)` и проверки `sha256_bytes(payload) != entry.sha256` добавить

```python
        if entry.archive:
            try:
                payload = extract_member(payload, entry.archive, entry.archive["member"])
            except ArtifactError as exc:
                raise UpdateError(f"{component} archive: {exc}") from exc
        installed_sha256 = sha256_bytes(payload)
```
и везде ниже, где `entry.sha256` сверяется с файлом (`_assert_binary_generation(target, entry.sha256)`), использовать `installed_sha256`; вернуть его.

`agent_from_env`: `upstream_enabled=os.getenv("PROXY_CONTROL_UPSTREAM_CHECK", "on").lower() != "off"`, `router_enabled=os.getenv("PROXY_CONTROL_XRAY_ROUTER", "off").lower() == "on"`.

`catalog.py`: `def entry_from_dict(component, raw): return _entry(component, raw)`.

- [ ] **Step 4: PASS (`tests/test_version_agent*.py`); commit**

```bash
git add version_agent/service.py version_agent/catalog.py tests/test_version_agent.py
printf '%s\n' 'version-agent: проверка upstream по запросу с кэшем в state.json, установка версии из кэша upstream, хэш установленного файла отдельно от хэша архива' | git commit -F -
```

---

### Task 8: `mita` — обновление закреплённого потребителя вместо отказа

**Files:**
- Modify: `version_agent/service.py` (`_update_binary`, `_assert_no_pinned_consumer` → `_consumer_overlay`), `agent_from_env`
- Modify: `deploy/version-agent.env.example`
- Test: `tests/test_version_agent.py:215-280` (переписать два теста про potребителя)

**Interfaces:**
- Produces: `VersionAgent(..., consumer_overlays: dict[str, tuple[Path, str, str]] | None)` — `component → (overlay_path, env_key, compose_service)`; для `mita`: `(Path("/opt/mtproxy-shared443/.env.mieru"), "MIERU_MITA_SHA256", "mieru-manager")`. Env: `PROXY_CONTROL_CONSUMER_OVERLAYS=mita=/opt/mtproxy-shared443/.env.mieru:MIERU_MITA_SHA256:mieru-manager`. `pinned_consumers` удаляется (и из env example).
- Helper: `_rewrite_env_line(path: Path, key: str, value: str) -> bytes | None` — возвращает прежнее содержимое; `_compose_up(service, *, cwd_overlay_env: Path)` — `docker compose --project-name mtproxy -f <compose files> --env-file <overlay> up -d --wait <service>`.

- [ ] **Step 1: Тесты (заменить `test_update_is_refused_while_a_container_pins_the_binary` и `test_update_is_blocked_when_pinned_consumer_inspect_has_daemon_failure`)**

```python
def test_mita_update_rewrites_the_consumer_pin_restarts_slots_and_recreates_the_manager(tmp_path: Path):
    catalog = tmp_path / "catalog.json"; write_catalog(catalog)
    target = tmp_path / "mita"; target.write_bytes(b"old"); target.chmod(0o755)
    overlay = tmp_path / ".env.mieru"
    overlay.write_text("MIERU_PUBLIC_HOST=m.example.com\nMIERU_MITA_SHA256=" + "0" * 64 + "\nMIERU_LANE_SLOTS=1:46101:/run/mita1/s:/var/lib/mita1\n")
    commands = []
    def run(command, *, env=None, cwd=None, timeout=None):
        commands.append(command)
        if command[:3] == ["systemctl", "list-units", "--plain"]:
            return "mita@1.service loaded active running\n"
        return "active\n"
    agent = VersionAgent(catalog_path=catalog, state_path=tmp_path / "state.json", compose_dir=tmp_path, compose_files=("compose.yaml", "compose.mieru.yaml"),
                         binary_paths={"mita": target}, service_names={"mita": "mita"},
                         consumer_overlays={"mita": (overlay, "MIERU_MITA_SHA256", "mieru-manager")},
                         downloader=lambda url: BINARY, runner=run)
    agent.update("mita", "3.35.0", expected_current=None)
    assert f"MIERU_MITA_SHA256={BINARY_SHA256}" in overlay.read_text() and "MIERU_PUBLIC_HOST=m.example.com" in overlay.read_text()
    assert ["systemctl", "restart", "mita"] in commands and ["systemctl", "restart", "mita@1"] in commands
    assert any(c[:4] == ["docker", "compose", "--project-name", "mtproxy"] and c[-4:] == ["up", "-d", "--wait", "mieru-manager"] for c in commands)


def test_mita_update_restores_the_consumer_pin_when_the_manager_does_not_come_back(tmp_path: Path):
    catalog = tmp_path / "catalog.json"; write_catalog(catalog)
    target = tmp_path / "mita"; target.write_bytes(b"old"); target.chmod(0o755)
    overlay = tmp_path / ".env.mieru"; overlay.write_text("MIERU_MITA_SHA256=" + "0" * 64 + "\n")
    ups = []
    def run(command, *, env=None, cwd=None, timeout=None):
        if command[-4:-1] == ["up", "-d", "--wait"]:
            ups.append(overlay.read_text())
            if len(ups) == 1:
                raise RuntimeError("unhealthy")
        return "" if command[:3] == ["systemctl", "list-units", "--plain"] else "active\n"
    agent = VersionAgent(catalog_path=catalog, state_path=tmp_path / "state.json", compose_dir=tmp_path, compose_files=("compose.yaml",),
                         binary_paths={"mita": target}, service_names={"mita": "mita"},
                         consumer_overlays={"mita": (overlay, "MIERU_MITA_SHA256", "mieru-manager")},
                         downloader=lambda url: BINARY, runner=run)
    with pytest.raises(RolledBackError):
        agent.update("mita", "3.35.0", expected_current=None)
    assert target.read_bytes() == b"old" and overlay.read_text() == "MIERU_MITA_SHA256=" + "0" * 64 + "\n"
    assert ups == [f"MIERU_MITA_SHA256={BINARY_SHA256}\n", "MIERU_MITA_SHA256=" + "0" * 64 + "\n"]
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: service.py**

```python
    @staticmethod
    def _rewrite_env_line(path: Path, key: str, value: str) -> bytes | None:
        previous = path.read_bytes() if path.exists() else None
        lines = (previous or b"").decode().splitlines()
        lines = [line for line in lines if not line.startswith(key + "=")] + [f"{key}={value}"]
        _atomic_write(path, ("\n".join(lines) + "\n").encode(), 0o600)
        return previous

    def _compose_up(self, service: str) -> None:
        self.runner(self._compose_command("up", "-d", "--wait", service, include_override=False), cwd=self.compose_dir, timeout=300)

    def _slot_units(self, service: str) -> list[str]:
        listed = self.runner(["systemctl", "list-units", "--plain", "--no-legend", f"{service}@*.service"], timeout=30)
        return [line.split()[0].removesuffix(".service") for line in listed.splitlines() if line.strip()]
```

В `_update_binary`: убрать `self._assert_no_pinned_consumer(component)`. После `_assert_pin` и **перед** `systemctl restart <service>`: `consumer = self.consumer_overlays.get(component)`; `previous_overlay = self._rewrite_env_line(path, key, installed_sha256) if consumer else None`. После `systemctl is-active` добавить рестарт слотов `for unit in self._slot_units(service): restart + is-active` и `if consumer: self._compose_up(consumer[2])`. В блоке отката: после восстановления бинарника — `if consumer: (path.write_bytes(previous_overlay) if previous_overlay is not None else path.unlink(missing_ok=True))`, затем рестарты `service` и слотов, `if consumer: self._compose_up(consumer[2])`. Удалить `_assert_no_pinned_consumer`, параметр `pinned_consumers`, парсинг `PROXY_CONTROL_PINNED_CONSUMERS`; добавить парсинг `PROXY_CONTROL_CONSUMER_OVERLAYS` (`component=path:key:service`, разделитель между компонентами `,`) с default `mita=/opt/mtproxy-shared443/.env.mieru:MIERU_MITA_SHA256:mieru-manager`.

`_compose_command` вызывается без override для mieru-manager, но compose-файлы уже включают `compose.mieru.yaml`, а env-overlay `.env.mieru` подключается как в установщике: в `_compose_up` добавить перед `up` аргументы `--env-file <compose_dir>/.env`, `--env-file <overlay>` для каждого overlay из `consumer_overlays`, чей файл существует (см. `installer/adapters/mieru.py:2072` `_compose_argv` — повторить тот же порядок: `.env`, `.env.naive`, `.env.mieru`, `.env.xray-router`, если существуют).

- [ ] **Step 4: env example** — заменить строки про `PROXY_CONTROL_PINNED_CONSUMERS` на

```
# Containers that pin a host binary by digest through their env overlay. The agent
# rewrites the key and recreates the service after the binary is replaced.
PROXY_CONTROL_CONSUMER_OVERLAYS=mita=/opt/mtproxy-shared443/.env.mieru:MIERU_MITA_SHA256:mieru-manager
```

- [ ] **Step 5: PASS; commit**

```bash
git add version_agent/service.py deploy/version-agent.env.example tests/test_version_agent.py
printf '%s\n' 'version-agent: обновление mita переписывает пин mieru-manager в .env.mieru, перезапускает слоты и пересоздаёт менеджер вместо отказа' | git commit -F -
```

---

### Task 9: Компонент `xray`

**Files:**
- Modify: `version_agent/service.py` (`_update_xray`), `agent_from_env`
- Modify: `deploy/version-agent.env.example`, `deploy/version-agent.service`
- Test: `tests/test_version_agent.py`

**Interfaces:**
- Produces: `VersionAgent(..., xray_bin_dir: Path = Path("/usr/local/lib/proxy-control/xray-router"), xray_overlay: Path = Path("/opt/mtproxy-shared443/.env.xray-router"), xray_container: str = "proxy-control-xray-router")`; env `PROXY_CONTROL_XRAY_BIN_DIR`, `PROXY_CONTROL_XRAY_OVERLAY`, `PROXY_CONTROL_XRAY_CONTAINER`, `PROXY_CONTROL_XRAY_ROUTER=on|off`.
- Состояние компонента: `{"version", "kind": "binary", "source", "url", "sha256" (архив), "members": {"xray": hex, "geoip.dat": hex, "geosite.dat": hex}, "updated_at"}`.
- Проверка версии после запуска: `docker exec proxy-control-xray-router /opt/xray/xray version` → первая строка начинается с `Xray <version>`.

- [ ] **Step 1: Тесты**

```python
def _xray_zip(version_line: bytes = b"") -> bytes:
    return _zip({"xray": b"xray-" + version_line, "geoip.dat": b"geoip", "geosite.dat": b"geosite", "README.md": b"x"})


def test_xray_update_replaces_members_rewrites_overlay_and_verifies_the_running_version(tmp_path: Path):
    bin_dir = tmp_path / "xray-router"; bin_dir.mkdir()
    for name in ("xray", "geoip.dat", "geosite.dat"): (bin_dir / name).write_bytes(b"old-" + name.encode())
    overlay = tmp_path / ".env.xray-router"
    overlay.write_text("XRAY_ROUTER_BIN_DIR=" + str(bin_dir) + "\nXRAY_ROUTER_XRAY_SHA256=" + "0" * 64 + "\nXRAY_ROUTER_GEOIP_SHA256=" + "1" * 64 + "\nXRAY_ROUTER_GEOSITE_SHA256=" + "2" * 64 + "\nXRAY_ROUTER_EGRESS_WARP=\n")
    payload = _xray_zip()
    catalog = tmp_path / "catalog.json"
    catalog.write_text(json.dumps({"schema": 1, "components": {"xray": [{"version": "26.4.1", "kind": "binary",
        "url": "https://github.com/XTLS/Xray-core/releases/download/v26.4.1/Xray-linux-64.zip", "sha256": hashlib.sha256(payload).hexdigest(),
        "archive": {"format": "zip", "members": {"xray": "xray", "geoip.dat": "geoip.dat", "geosite.dat": "geosite.dat"}}}]}}))
    commands = []
    def run(command, *, env=None, cwd=None, timeout=None):
        commands.append(command)
        if command[:2] == ["docker", "exec"]:
            return "Xray 26.4.1 (Xray, Penetrates Everything.) Custom (go1.24 linux/amd64)\n"
        return ""
    agent = VersionAgent(catalog_path=catalog, state_path=tmp_path / "state.json", compose_dir=tmp_path, compose_files=("compose.yaml", "compose.xray-router.yaml"),
                         router_enabled=True, xray_bin_dir=bin_dir, xray_overlay=overlay, downloader=lambda url: payload, runner=run)
    result = agent.update("xray", "26.4.1", expected_current=None)
    assert result["changed"] is True
    assert (bin_dir / "xray").read_bytes() == b"xray-" and (bin_dir / "geosite.dat").read_bytes() == b"geosite"
    assert (bin_dir / "xray").stat().st_mode & 0o777 == 0o755 and (bin_dir / "geoip.dat").stat().st_mode & 0o777 == 0o644
    text = overlay.read_text()
    assert f"XRAY_ROUTER_XRAY_SHA256={hashlib.sha256(b'xray-').hexdigest()}" in text and "XRAY_ROUTER_EGRESS_WARP=" in text
    assert any(c[-4:] == ["up", "-d", "--wait", "xray-router"] for c in commands)
    assert ["docker", "exec", "proxy-control-xray-router", "/opt/xray/xray", "version"] in commands
    state = json.loads((tmp_path / "state.json").read_text())["components"]["xray"]
    assert state["members"]["geoip.dat"] == hashlib.sha256(b"geoip").hexdigest() and state["version"] == "26.4.1"


def test_xray_update_rolls_back_all_three_members_when_the_router_reports_another_version(tmp_path: Path):
    bin_dir = tmp_path / "xray-router"; bin_dir.mkdir()
    for name in ("xray", "geoip.dat", "geosite.dat"): (bin_dir / name).write_bytes(b"old-" + name.encode())
    overlay = tmp_path / ".env.xray-router"; overlay.write_text("XRAY_ROUTER_XRAY_SHA256=" + "0" * 64 + "\n")
    payload = _xray_zip()
    catalog = tmp_path / "catalog.json"
    catalog.write_text(json.dumps({"schema": 1, "components": {"xray": [{"version": "26.4.1", "kind": "binary",
        "url": "https://github.com/XTLS/Xray-core/releases/download/v26.4.1/Xray-linux-64.zip", "sha256": hashlib.sha256(payload).hexdigest(),
        "archive": {"format": "zip", "members": {"xray": "xray", "geoip.dat": "geoip.dat", "geosite.dat": "geosite.dat"}}}]}}))
    def run(command, *, env=None, cwd=None, timeout=None):
        return "Xray 26.3.27 (...)\n" if command[:2] == ["docker", "exec"] else ""
    agent = VersionAgent(catalog_path=catalog, state_path=tmp_path / "state.json", compose_dir=tmp_path, compose_files=("compose.yaml",),
                         router_enabled=True, xray_bin_dir=bin_dir, xray_overlay=overlay, downloader=lambda url: payload, runner=run)
    with pytest.raises(RolledBackError):
        agent.update("xray", "26.4.1", expected_current=None)
    assert {n: (bin_dir / n).read_bytes() for n in ("xray", "geoip.dat", "geosite.dat")} == {n: b"old-" + n.encode() for n in ("xray", "geoip.dat", "geosite.dat")}
    assert overlay.read_text() == "XRAY_ROUTER_XRAY_SHA256=" + "0" * 64 + "\n"
```

(Откат в этом тесте «успешен», потому что после восстановления `docker exec` снова отвечает 26.3.27 — версия, которую агент ждёт после отката, берётся из `state.json` компонента, а при его отсутствии проверка версии при откате пропускается.)

- [ ] **Step 2: FAIL**

- [ ] **Step 3: service.py**

```python
    XRAY_MODES = {"xray": 0o755, "geoip.dat": 0o644, "geosite.dat": 0o644}
    XRAY_KEYS = {"xray": "XRAY_ROUTER_XRAY_SHA256", "geoip.dat": "XRAY_ROUTER_GEOIP_SHA256", "geosite.dat": "XRAY_ROUTER_GEOSITE_SHA256"}

    def _xray_version_running(self) -> str:
        first = self.runner(["docker", "exec", self.xray_container, "/opt/xray/xray", "version"], timeout=30).splitlines()[:1]
        return first[0].split()[1] if first and first[0].startswith("Xray ") else ""

    def _update_xray(self, entry: CatalogEntry, previous_version: str | None) -> dict[str, str]:
        if entry.kind != "binary" or not entry.url or not entry.sha256 or not entry.archive or "members" not in entry.archive:
            raise UpdateError("xray entry must name the archive members")
        if self.xray_bin_dir.is_symlink() or not self.xray_bin_dir.is_dir():
            raise UpdateError("xray-router binary directory is missing")
        payload = self.downloader(entry.url)
        if sha256_bytes(payload) != entry.sha256:
            raise UpdateError("xray archive SHA-256 mismatch")
        try:
            members = {name: extract_member(payload, entry.archive, path) for name, path in entry.archive["members"].items()}
        except ArtifactError as exc:
            raise UpdateError(f"xray archive: {exc}") from exc
        digests = {name: sha256_bytes(data) for name, data in members.items()}
        backup_dir = self.state_path.parent / "backups" / "xray.previous"
        backup_dir.mkdir(parents=True, exist_ok=True)
        previous = {}
        for name in members:
            path = self.xray_bin_dir / name
            if path.is_symlink():
                raise UpdateError(f"refusing to replace symlink {path}")
            if path.exists():
                shutil.copyfile(path, backup_dir / name); os.chmod(backup_dir / name, self.XRAY_MODES[name]); previous[name] = True
        previous_overlay = self.xray_overlay.read_bytes() if self.xray_overlay.exists() else None
        try:
            for name, data in members.items():
                _atomic_write(self.xray_bin_dir / name, data, self.XRAY_MODES[name])
            self._fsync_directory(self.xray_bin_dir)
            for name, digest in digests.items():
                self._rewrite_env_line(self.xray_overlay, self.XRAY_KEYS[name], digest)
            self._compose_up("xray-router")
            running = self._xray_version_running()
            if running != entry.version:
                raise UpdateError(f"xray-router runs {running or 'unknown'}, expected {entry.version}")
        except Exception as exc:
            try:
                for name in members:
                    if previous.get(name):
                        os.replace(backup_dir / name, self.xray_bin_dir / name)
                    else:
                        (self.xray_bin_dir / name).unlink(missing_ok=True)
                self._fsync_directory(self.xray_bin_dir)
                if previous_overlay is None:
                    self.xray_overlay.unlink(missing_ok=True)
                else:
                    _atomic_write(self.xray_overlay, previous_overlay, 0o600)
                self._compose_up("xray-router")
                if previous_version and self._xray_version_running() != previous_version:
                    raise UpdateError("restored xray-router does not run the previous version")
            except Exception as rollback_exc:
                raise RollbackFailedError("xray update failed; restored generation could not be verified") from rollback_exc
            raise RolledBackError("xray update failed; previous generation was restored and verified") from exc
        return digests
```

В `update`: ветка `elif component == "xray": members = self._update_xray(entry, current)`; в сохранённое состояние для `xray` добавить `"members": members`. `_compose_up` из Task 8 подключает `.env.xray-router` как overlay, если файл существует (там же перечислены все четыре overlay).

`agent_from_env`: `xray_bin_dir=Path(os.getenv("PROXY_CONTROL_XRAY_BIN_DIR", "/usr/local/lib/proxy-control/xray-router"))`, `xray_overlay=Path(os.getenv("PROXY_CONTROL_XRAY_OVERLAY", "/opt/mtproxy-shared443/.env.xray-router"))`, `xray_container=os.getenv("PROXY_CONTROL_XRAY_CONTAINER", "proxy-control-xray-router")`.

- [ ] **Step 4: deploy** — в `version-agent.service` добавить `/usr/local/lib/proxy-control` в `ReadWritePaths`; в env example:

```
# Xray-router (v0.11): on = the xray component is offered and updated from the pinned bin-dir.
# The installer writes `on` when it installs the router.
PROXY_CONTROL_XRAY_ROUTER=off
PROXY_CONTROL_XRAY_BIN_DIR=/usr/local/lib/proxy-control/xray-router
PROXY_CONTROL_XRAY_OVERLAY=/opt/mtproxy-shared443/.env.xray-router
# Upstream check (v0.11): off = only the local catalog is offered.
PROXY_CONTROL_UPSTREAM_CHECK=on
```
Установщик Xray-router (`installer/adapters/xray_router.py` `apply`) дописывает `PROXY_CONTROL_XRAY_ROUTER=on` в `/etc/proxy-control/version-agent.env` через `_rewrite_env_line`-подобный helper установщика (`_set_env_line(path, key, value)` рядом с `env_text`) и `systemctl restart version-agent` если unit активен; `rollback` возвращает `off`. Тест в `tests/test_installer_xray_router.py`: после `apply` в `/etc/proxy-control/version-agent.env` есть строка `PROXY_CONTROL_XRAY_ROUTER=on`.

- [ ] **Step 5: PASS (`tests/test_version_agent*.py`, `tests/test_installer_xray_router.py`, `tests/test_deploy.py`); commit**

```bash
git add version_agent/service.py deploy/version-agent.env.example deploy/version-agent.service installer/adapters/xray_router.py tests/
printf '%s\n' 'version-agent: компонент xray — замена трёх файлов роутера из релиза Xray-core, пины в .env.xray-router, проверка версии в контейнере, откат' | git commit -F -
```

---

### Task 10: `naive` — пересборка Caddy

**Files:**
- Modify: `version_agent/service.py` (`_update_naive_build`), `agent_from_env`
- Test: `tests/test_version_agent.py`

**Interfaces:**
- Produces: `VersionAgent(..., caddy_build_dir: Path = Path("/var/lib/proxy-control/version-agent/caddy-build"), caddy_image: str = "proxy-control/caddy-naive:agent")`; для `kind == "build"` агент пишет Dockerfile, собирает, извлекает `/caddy`, вычисляет `runtime_version` (`caddy version` собранного бинарника), затем идёт обычный `_update_binary` с готовым payload.
- Рефакторинг: `_update_binary(component, entry, payload: bytes | None = None, runtime_version: str | None = None)` — если `payload` передан, скачивание пропускается.

- [ ] **Step 1: Тест**

```python
def test_naive_build_writes_a_pinned_dockerfile_builds_extracts_and_installs(tmp_path: Path):
    catalog = tmp_path / "catalog.json"
    catalog.write_text(json.dumps({"schema": 1, "components": {"naive": [{"version": "2.12.0", "kind": "build", "build": {
        "caddy_version": "2.12.0", "builder_image": "caddy:2.12.0-builder@sha256:" + "e" * 64, "forwardproxy_commit": "d" * 40}}]}}))
    target = tmp_path / "caddy"; target.write_bytes(b"old"); target.chmod(0o755)
    pin = tmp_path / "caddy-naive.pin"; pin.write_text(PINNED_CADDY + "\n")
    build_dir = tmp_path / "caddy-build"
    commands = []
    def run(command, *, env=None, cwd=None, timeout=None):
        commands.append((command, env))
        if command[:2] == ["docker", "create"]:
            return "0123456789abcdef\n"
        if command[:2] == ["docker", "cp"]:
            Path(command[3]).write_bytes(BINARY); return ""
        if command[1:2] == ["version"] or (len(command) == 2 and command[1] == "version"):
            return "v2.12.0 h1:newbuildhash=\n"
        return "active\n"
    agent = VersionAgent(catalog_path=catalog, state_path=tmp_path / "state.json", binary_paths={"naive": target}, service_names={"naive": "caddy-naive"},
                         checkers={"naive": "/usr/local/libexec/check-naive-caddy-build"}, version_pins={"naive": pin},
                         caddy_build_dir=build_dir, downloader=lambda url: (_ for _ in ()).throw(AssertionError("no download for a build")), runner=run)
    agent.update("naive", "2.12.0", expected_current=None)
    dockerfile = (build_dir / "Dockerfile").read_text()
    assert "FROM caddy:2.12.0-builder@sha256:" + "e" * 64 + " AS builder" in dockerfile
    assert "xcaddy build v2.12.0" in dockerfile and "github.com/klzgrad/forwardproxy@" + "d" * 40 in dockerfile
    assert any(c[:3] == ["docker", "build", "-f"] for c, _ in commands)
    assert target.read_bytes() == BINARY and pin.read_text().strip() == "v2.12.0 h1:newbuildhash="
    assert any(env and env.get("EXPECTED_CADDY_VERSION") == "v2.12.0 h1:newbuildhash=" for _, env in commands)
    state = json.loads((tmp_path / "state.json").read_text())["components"]["naive"]
    assert state["version"] == "2.12.0" and state["kind"] == "build" and state["runtime_version"] == "v2.12.0 h1:newbuildhash="
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: service.py**

```python
    def _build_caddy(self, entry: CatalogEntry) -> tuple[bytes, str]:
        build = entry.build or {}
        self.caddy_build_dir.mkdir(parents=True, exist_ok=True)
        os.chmod(self.caddy_build_dir, 0o700)
        dockerfile = self.caddy_build_dir / "Dockerfile"
        _atomic_write(dockerfile, (
            f"FROM {build['builder_image']} AS builder\n"
            f"RUN xcaddy build v{build['caddy_version']} \\\n"
            f"    --with github.com/caddyserver/forwardproxy@caddy2=github.com/klzgrad/forwardproxy@{build['forwardproxy_commit']}\n"
            "FROM scratch\nCOPY --from=builder /usr/bin/caddy /caddy\n").encode(), 0o600)
        self.runner(["docker", "build", "-f", str(dockerfile), "-t", self.caddy_image, str(self.caddy_build_dir)], timeout=900)
        container = self.runner(["docker", "create", "--entrypoint", "/caddy", self.caddy_image, "version"], timeout=60).strip()
        if not re.fullmatch(r"[0-9a-f]{12,64}", container):
            raise UpdateError("caddy build produced no container")
        extracted = self.caddy_build_dir / "caddy"
        try:
            self.runner(["docker", "cp", f"{container}:/caddy", str(extracted)], timeout=120)
        finally:
            self.runner(["docker", "rm", container], timeout=60)
        payload = extracted.read_bytes()
        os.chmod(extracted, 0o755)
        runtime_version = self.runner([str(extracted), "version"], timeout=30).strip().splitlines()[0]
        extracted.unlink(missing_ok=True)
        if not runtime_version.startswith(f"v{build['caddy_version']} "):
            raise UpdateError(f"built Caddy reports {runtime_version!r}, expected v{build['caddy_version']}")
        return payload, runtime_version
```

В `update`: `elif component == "naive" and entry.kind == "build": payload, runtime_version = self._build_caddy(entry); installed = self._update_binary(component, entry, payload=payload, runtime_version=runtime_version)`; в состояние писать `runtime_version` вычисленный. `_update_binary`: параметры `payload=None, runtime_version=None`; `runtime_version = runtime_version or entry.runtime_version`; если `payload is None` — скачать и сверить хэш (как сейчас), иначе пропустить скачивание и проверку `entry.sha256`; проверку «entry.kind != binary» ослабить до `entry.kind not in ("binary", "build")`. `import re` уже нужен. `agent_from_env`: `caddy_build_dir=Path(os.getenv("PROXY_CONTROL_CADDY_BUILD_DIR", "/var/lib/proxy-control/version-agent/caddy-build"))`.

- [ ] **Step 4: PASS; commit**

```bash
git add version_agent/service.py tests/test_version_agent.py
printf '%s\n' 'version-agent: naive обновляется пересборкой Caddy с forwardproxy по digest builder-образа, пин берётся из собранного бинарника' | git commit -F -
```

---

### Task 11: Сервер агента — `POST /v1/upstream/check`, долгий таймаут для сборки

**Files:**
- Modify: `version_agent/server.py:77-110`
- Test: `tests/test_version_agent_server.py`

- [ ] **Step 1: Тест (по образцу `test_unix_socket_server_preserves_update_contract`)**

```python
def test_unix_socket_server_checks_upstream_on_post(tmp_path: Path):
    class Agent:
        def __init__(self): self.checked = 0
        def list_versions(self): return {"enabled": True, "components": {}}
        def check_upstream(self, force=False):
            self.checked += 1
            return {"enabled": True, "components": {}, "checked_at": 1}
    server, agent = _serve(tmp_path, Agent())  # reuse the module's helper that starts UnixHTTPServer in a thread
    try:
        status, body = _request(server.socket_path, "POST", "/v1/upstream/check", b"")
        assert status == 200 and body["checked_at"] == 1 and agent.checked == 1
        status, body = _request(server.socket_path, "POST", "/v1/upstream/check", b"{}")
        assert status == 200
    finally:
        server.shutdown(); server.server_close()
```
(если в модуле нет `_serve`/`_request`, вынести их из существующих тестов в helpers в начале файла).

- [ ] **Step 2: FAIL**

- [ ] **Step 3: server.py** — в `do_POST` перед проверкой `/v1/update`:

```python
        if self.path == "/v1/upstream/check":
            try:
                self._send(200, self.server.agent.check_upstream())
            except (CatalogError, UpdateError) as exc:
                self._send(502, {"detail": str(exc), "state": getattr(exc, "state", "update_failed")})
            return
```
(тело не читается; `Content-Length` может быть 0.)

- [ ] **Step 4: PASS; commit**

```bash
git add version_agent/server.py tests/test_version_agent_server.py
printf '%s\n' 'version-agent: POST /v1/upstream/check на сокете агента' | git commit -F -
```

---

### Task 12: Панель — `check()`, компонент `xray`, `/api/versions/check`, relay для узлов

**Files:**
- Modify: `panel/versions.py`, `panel/version_routes.py`, `panel/schemas.py:100,253`, `panel/fleet_v2/node_routes.py:38,86-88,393-397`, `panel/fleet_v2/client.py:201-204`, `panel/fleet_v2/central_routes.py:209-216`
- Test: `panel/tests/test_versions.py`, `panel/tests/test_fleet_v2_central_routes.py`

**Interfaces:**
- `VersionClient.check()` → `POST /v1/upstream/check` (timeout 90); `VersionClient.update(...)` timeout 1200 (сборка Caddy).
- `MemoryVersions.check()` → помечает `self.checks += 1`, добавляет в `mita.available` `{"version": "3.37.0", "kind": "binary", "source": "upstream"}` и ставит `checked_at`; `MemoryVersions` включает `xray` компонент `{"current": "26.3.27", "available": [...]}` только если `MemoryVersions(router=True)`.
- Роуты: `POST /api/versions/check` (owner, audit `runtime.version.check`); `Literal["telemt", "naive", "mita", "xray"]` в `version_routes.py`, `node_routes.py:88`, `central_routes.py:210`; `POST /api/fleet/v2/versions/check` (fleet key, audit `runtime.version.check`); `NodeClient.check_versions()` → `POST /api/fleet/v2/versions/check`; `POST /api/nodes/{id}/versions/check` (owner, audit `node.version.check`); `CAPABILITIES += ("versions.check",)`.

- [ ] **Step 1: Тесты**

```python
@pytest.mark.anyio
async def test_owner_checks_upstream_and_sees_the_candidate(tmp_path, telemt, naive, mieru, login_user):
    from httpx import ASGITransport, AsyncClient
    from panel.app import Settings, create_app
    versions = MemoryVersions(router=True)
    settings = Settings(database_path=tmp_path / "panel.sqlite3", session_cookie_secure=False, allowed_hosts=("testserver",),
                        naive_public_host="naive.example.com", naive_enabled=True, mieru_enabled=True)
    app = create_app(settings, telemt=telemt, naive=naive, mieru=mieru, version_client=versions)
    app.state.store.create_admin("owner", "correct horse battery staple", "owner")
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://testserver") as client:
        await login_user(client)
        csrf = client.cookies["panel_csrf"]
        checked = await client.post("/api/versions/check", headers={"X-CSRF-Token": csrf})
        assert checked.status_code == 200 and versions.checks == 1
        assert {"version": "3.37.0", "kind": "binary", "source": "upstream"} in checked.json()["components"]["mita"]["available"]
        assert checked.json()["components"]["xray"]["current"] == "26.3.27"
        updated = await client.post("/api/versions/xray/update", json={"version": "26.4.1", "expected_current": "26.3.27"}, headers={"X-CSRF-Token": csrf})
        assert updated.status_code == 200 and versions.calls[-1] == ("xray", "26.4.1", "26.3.27")
```

В `test_fleet_v2_central_routes.py` в `test_node_version_update_is_relayed_to_the_node_and_audited_on_both_sides` добавить после `unknown`:
```python
        checked = await http.post(f"/api/nodes/{node_id}/versions/check")
        assert checked.status_code == 200 and node.state.versions.checks == 1
        xray = await http.post(f"/api/nodes/{node_id}/versions/xray", json={"version": "26.4.1", "expected_current": None})
        assert xray.status_code in (200, 409)  # accepted by the Literal; the memory node decides
```
и заменить ожидание для `unknown` (`versions/xray` больше не 422) на компонент `versions/panel`.

- [ ] **Step 2: FAIL**

- [ ] **Step 3: Реализация**

`panel/versions.py`:
```python
    async def check(self):
        return await self._request("POST", "/v1/upstream/check", timeout=90.0)
```
`update`: `timeout=1200.0`. `MemoryVersions.__init__(self, router: bool = False)`: `self.checks = 0`; при `router` добавить `"xray": {"current": "26.3.27", "available": [{"version": "26.3.27", "kind": "binary"}, {"version": "26.4.1", "kind": "binary", "source": "upstream"}]}`;
```python
    async def check(self):
        self.checks += 1
        self.components["mita"]["available"].append({"version": "3.37.0", "kind": "binary", "source": "upstream"})
        return {"enabled": True, "upstream_enabled": True, "checked_at": 1, "components": self.components}
```
`version_routes.py`:
```python
    @app.post("/api/versions/check")
    async def check_versions(request: Request, user=Depends(context.roles("owner"))):
        result = await app.state.versions.check()
        await context.audit(user, "runtime.version.check", "upstream", request, {"checked_at": result.get("checked_at")})
        return result
```
`Literal` + `"xray"` в трёх местах. `node_routes.py`: `CAPABILITIES = (..., "versions.check")`, роут `@app.post("/api/fleet/v2/versions/check")` с `key=Depends(context.fleet_key)`. `client.py`: `async def check_versions(self) -> dict: return (await self._call("POST", "/api/fleet/v2/versions/check"))[1]`. `central_routes.py`: `@app.post("/api/nodes/{node_id}/versions/check")` → `_linked(node_id)`, `client_for(node_id).check_versions()`, audit `node.version.check`. Ошибки агента (`VersionAgentError`) в `/api/versions/check` пробрасываются как сейчас у `update` (глобальный обработчик).

- [ ] **Step 4: PASS `panel/tests/test_versions.py panel/tests/test_fleet_v2_central_routes.py`; commit**

```bash
git add panel/versions.py panel/version_routes.py panel/schemas.py panel/fleet_v2/ panel/tests/
printf '%s\n' 'panel: проверка обновлений через агент, компонент xray, relay проверки на управляемый узел' | git commit -F -
```

---

### Task 13: Экран версий — кнопка проверки, пометка источника, карточка Xray

**Files:**
- Modify: `panel/static/js/management.js:9-45,127-139`, `panel/static/style.css:3`
- Create: `panel/tests/test_management_ui_contract.py`
- Modify: `tests/test_deploy.py:50-56`

- [ ] **Step 1: Контракт**

```python
from pathlib import Path

STATIC = Path(__file__).resolve().parent.parent / "static"
JS = (STATIC / "js/management.js").read_text()


def test_versions_screen_checks_upstream_and_labels_the_source():
    assert '"/api/versions/check"' in JS and 'data-version-check' in JS
    for text in ("Проверить обновления", "Проверено ", "каталог", "upstream",
                 "Версия из upstream: хэш из релиза подтверждает целостность скачивания, но проектом она не проверялась",
                 "Xray-router / Xray-core", "Собираем…"):
        assert text in JS, text
    assert "entry.version !== current" in JS
    assert 'reason === "no_published_digest"' in JS and 'reason === "router_not_installed"' not in JS
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: management.js**

`names` + `xray: "Xray-router / Xray-core"`. Верх экрана:
```js
  const checkedAt = context.state.versions.checked_at ? `Проверено ${date(context.state.versions.checked_at)}` : "Upstream ещё не проверялся";
  const checkBar = context.state.versions.upstream_enabled === false ? "" : `<div class="version-check"><span>${esc(checkedAt)}</span><button class="ghost" data-version-check="1">Проверить обновления</button></div>`;
```
В карточке: option — `${esc(entry.version)} · ${entry.source === "upstream" ? "upstream" : "каталог"}`; под select для upstream-кандидата в списке — `<small class="version-risk">Версия из upstream: хэш из релиза подтверждает целостность скачивания, но проектом она не проверялась</small>` (показывать, если среди `offered` есть `source === "upstream"`). Если `item.upstream?.latest && !offered.length && item.upstream.reason === "no_published_digest"` — `<p class="version-empty"><b>Вышла ${esc(item.upstream.latest)}</b>, но релиз без опубликованного хэша — установка невозможна</p>`; если `item.upstream?.last_error` — строка `<small class="version-note">Проверка не удалась: ${esc(item.upstream.last_error)}</small>`. Кнопка обновления для `naive` с `kind === "build"` в `setBusy` показывает «Собираем…» (в `versionAction`: `const building = component === "naive" && select.selectedOptions[0]?.dataset.kind === "build"; setBusy(button, true, building ? "Собираем…" : "Обновляем…")`; option получает `data-kind="${esc(entry.kind)}"`).

```js
async function checkVersions(context, button) {
  try {
    context.ui.setBusy(button, true, "Проверяем…");
    await context.api("/api/versions/check", { method: "POST" });
    context.ui.toast("Upstream проверен");
    await context.navigate("versions");
  } catch (error) {
    context.ui.toast(`Не удалось проверить: ${error.message}`, "error");
  } finally {
    context.ui.setBusy(button, false);
  }
}
```
`handleManagementClick`: `if (button.dataset.versionCheck) { void checkVersions(context, button); return true; }`.
CSS (строка 3, рядом с `.version-grid`): `.version-check{display:flex;align-items:center;justify-content:space-between;gap:12px;margin:0 0 12px;color:var(--muted);font-size:12px}.version-risk{display:block;color:var(--amber);font-size:10px;line-height:1.35}@media(max-width:560px){.version-check{flex-direction:column;align-items:stretch}}`.

`tests/test_deploy.py:56`: оставить, текст «в каталоге нет версий для этого компонента» сохранить в ветке без кандидатов.

- [ ] **Step 4: PASS; commit**

```bash
git add panel/static/js/management.js panel/static/style.css panel/tests/test_management_ui_contract.py
printf '%s\n' 'panel: экран версий — «Проверить обновления», пометка источника и риск upstream, карточка Xray-router, «Собираем…» для Caddy' | git commit -F -
```

---

### Task 14: Установщик принимает версии из `state.json` агента

**Files:**
- Create: `installer/runtime_state.py`
- Modify: `installer/adapters/xray_router.py:560-568` (`verify`), `installer/adapters/naive.py:1642-1648` (`_assert_pinned_caddy`), `installer/adapters/mieru.py` (проверка `executable_digest` в acceptance/verify — найти `verify_artifact(binary, expected_executable)` для `/usr/bin/mita` и обернуть)
- Test: `tests/test_installer_runtime_state.py`, дополнения в `tests/test_installer_xray_router.py`

**Interfaces:**
- `installer/runtime_state.py`: `agent_component(root: Path, component: str, state_path: str = "/var/lib/proxy-control/version-agent/state.json") -> dict | None` — запись `components[component]` или `None`, если файла нет/невалиден/`status == "rollback_failed"`.
- `accepted_sha256(root, component, member: str | None, pinned: str) -> set[str]` — `{pinned}` ∪ хэш из состояния (`sha256` или `members[member]`).
- `accepted_caddy_pins(root, pinned: str) -> set[str]` — `{pinned}` ∪ `runtime_version` из состояния `naive`.

- [ ] **Step 1: Тесты**

```python
import json
from pathlib import Path

from installer.runtime_state import accepted_caddy_pins, accepted_sha256, agent_component


def _state(root: Path, components: dict) -> None:
    path = root / "var/lib/proxy-control/version-agent/state.json"
    path.parent.mkdir(parents=True); path.write_text(json.dumps({"schema": 2, "components": components}))


def test_missing_or_failed_state_yields_only_the_pin(tmp_path: Path):
    assert agent_component(tmp_path, "xray") is None
    assert accepted_sha256(tmp_path, "xray", "xray", "p" * 64) == {"p" * 64}
    _state(tmp_path, {"mita": {"version": "3.37.0", "sha256": "a" * 64, "status": "rollback_failed"}})
    assert accepted_sha256(tmp_path, "mita", None, "p" * 64) == {"p" * 64}


def test_state_adds_the_agent_installed_hashes(tmp_path: Path):
    _state(tmp_path, {"xray": {"version": "26.4.1", "members": {"xray": "x" * 64, "geoip.dat": "g" * 64, "geosite.dat": "s" * 64}},
                      "mita": {"version": "3.37.0", "sha256": "m" * 64},
                      "naive": {"version": "2.12.0", "runtime_version": "v2.12.0 h1:abc="}})
    assert accepted_sha256(tmp_path, "xray", "geoip.dat", "p" * 64) == {"p" * 64, "g" * 64}
    assert accepted_sha256(tmp_path, "mita", None, "p" * 64) == {"p" * 64, "m" * 64}
    assert accepted_caddy_pins(tmp_path, "v2.11.4 h1:old=") == {"v2.11.4 h1:old=", "v2.12.0 h1:abc="}
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: runtime_state.py**

```python
"""What the version-agent installed after the installer's pin (v0.11): `verify`, `repair`
and the acceptance checks accept either the installer's pinned artifact or the one the
agent recorded in its state, never anything else."""
from __future__ import annotations

import json
from pathlib import Path

STATE_PATH = "/var/lib/proxy-control/version-agent/state.json"


def agent_component(root: Path, component: str, state_path: str = STATE_PATH) -> dict | None:
    path = root / state_path.lstrip("/")
    try:
        raw = json.loads(path.read_text(encoding="utf-8"))
    except (OSError, ValueError):
        return None
    entry = raw.get("components", {}).get(component) if isinstance(raw, dict) else None
    if not isinstance(entry, dict) or entry.get("status") == "rollback_failed":
        return None
    return entry


def accepted_sha256(root: Path, component: str, member: str | None, pinned: str) -> set[str]:
    entry = agent_component(root, component) or {}
    recorded = (entry.get("members") or {}).get(member) if member else entry.get("sha256")
    return {pinned} | ({recorded} if isinstance(recorded, str) and len(recorded) == 64 else set())


def accepted_caddy_pins(root: Path, pinned: str) -> set[str]:
    entry = agent_component(root, "naive") or {}
    recorded = entry.get("runtime_version")
    return {pinned} | ({recorded} if isinstance(recorded, str) and recorded else set())
```

- [ ] **Step 4: Адаптеры**

`xray_router.py` `verify`: `if path.is_symlink() or not path.is_file() or _file_sha256(path) not in accepted_sha256(self.root, "xray", member, members[member].sha256): raise ArtifactError(...)`. `naive.py` `_assert_pinned_caddy`: `if version.strip() not in accepted_caddy_pins(self.root, _CADDY_VERSION) or not forward_proxy:`. `mieru.py`: там, где `verify`/acceptance сверяет `/usr/bin/mita` с `executable_digest` (поиск `_file_sha256(self._host(self.paths.binary))` и `executable_digest_ok`), заменить на `in accepted_sha256(self.root, "mita", None, expected_executable)`. `self.root` — атрибут адаптеров (есть у naive `self.root == Path("/")`; у остальных использовать `self._host(Path("/"))`-эквивалент, т.е. корень, к которому `_host` прибавляет пути).

Тест в `tests/test_installer_xray_router.py`: после `apply` заменить в bin-dir `xray` другим содержимым, записать `state.json` с `members.xray` = его хэш → `verify` проходит; без state — `verify` падает `does not match its pin`.

- [ ] **Step 5: PASS `tests/test_installer_*.py`; commit**

```bash
git add installer/runtime_state.py installer/adapters/ tests/
printf '%s\n' 'installer: verify принимает версии, установленные version-agent (state.json), рядом с пином установщика' | git commit -F -
```

---

### Task 15: Документация и changelog

**Files:**
- Modify: `docs/UPGRADING.md:355-380`, `docs/UPGRADING.ru.md:353-378`, `CHANGELOG.md`, `CHANGELOG.ru.md`, `VERSION`
- Create: `docs/releases/v0.11.0-beta.1.md`

- [ ] **Step 1: UPGRADING (обе версии)** — в раздел про version-agent добавить подраздел «Обновления из upstream (v0.11)»: кнопка «Проверить обновления», откуда берутся версии (таблица из спека §3), что подтверждает хэш из релиза и чего не подтверждает, `PROXY_CONTROL_UPSTREAM_CHECK=off`, `PROXY_CONTROL_XRAY_ROUTER=on`, новый `PROXY_CONTROL_CONSUMER_OVERLAYS` вместо `PROXY_CONTROL_PINNED_CONSUMERS` (миграция env-файла при обновлении агента), `ReadWritePaths` unit'а, пересборка Caddy (Docker, до 15 минут).
- [ ] **Step 2: CHANGELOG** — секция `v0.11.0-beta.1` (EN и RU): обзор одной строкой, счётчик клиентов, upstream-проверка, компонент xray, mita без отказа из-за менеджера, Caddy из UI, установщик и state.json.
- [ ] **Step 3: docs/releases/v0.11.0-beta.1.md** — для пользователей (правило владельца: без таблиц гейта и внутренностей раскатки): что нового, как включить, что означает пометка upstream, как выключить опрос.
- [ ] **Step 4: `VERSION` → `0.11.0-beta.1`; commit**

```bash
git add docs/ CHANGELOG.md CHANGELOG.ru.md VERSION
printf '%s\n' 'docs: v0.11 — обновления из upstream, компонент xray, пересборка Caddy, обзор одной строкой' | git commit -F -
```

---

### Task 16: Гейт на ams-test и живая проверка на AMS_Z

**Files:** без изменений кода, кроме правок по итогам.

- [ ] **Step 1: Гейт full на ams-test** — `scripts/dev/remote-gate.sh` (отсоединённо: `setsid nohup … &`, опрос лога), дождаться `gate: passed`. Правки по падениям — отдельными коммитами.
- [ ] **Step 2: Раскатка агента на ams-test** — обновить checkout, `systemctl restart version-agent` с новым env (`PROXY_CONTROL_XRAY_ROUTER=on`, `PROXY_CONTROL_CONSUMER_OVERLAYS`), пересобрать панель; в UI нажать «Проверить обновления», убедиться в кандидатах для `xray`, `mita`, `telemt`, `naive`.
- [ ] **Step 3: Проверить путь member `mita` в реальном `tar.gz`** (`tar tzf`); если бинарник в подкаталоге — поправить `archive_for` в `upstream.py` и тест Task 6.
- [ ] **Step 4: Живое обновление на ams-test:** `xray` до последнего релиза, `mita`, затем `installer verify` (Xray-router, Mieru) — зелёный; `naive` пересборка (замерить время).
- [ ] **Step 5: AMS_Z (после зелёного гейта, разрешение владельца):** точка отката `/root/v11-rollout/<ts>` (копии bin-dir роутера, `/usr/bin/mita`, `/usr/local/bin/caddy`, три overlay, `state.json`), обновить `xray` через UI, проверить `routing`/`geodata` и NaiveProxy через роутер; при проблеме вернуть копии и `docker compose up -d`.
- [ ] **Step 6: `docs/superpowers/plans/CONTINUE-HERE-v0.11.md`** — что сделано, номера коммитов, точки отката; commit. Тег — только по слову владельца.

---

## Self-review

- **Spec coverage:** §2 → Tasks 1–3; §3 → Tasks 4–7; §3.1 → Task 5; §4 → Tasks 11–13; §5 → Task 9 + 14; §6 → Task 8; §7 → Task 10; §8 → Tasks 11–12; §9 (ошибки) → покрыты тестами Tasks 6–10 и UI-ветками Task 13; §10 → Tasks 15–16.
- **Type consistency:** `consumer_overlays: dict[str, tuple[Path, str, str]]` (Task 8) используется только там; `installed_sha256` из `_update_binary` (Task 7) нужен Task 8 и 9; `entry_from_dict` (Task 7) используется `_resolve`; `_compose_up` определён в Task 8, используется в Task 9; `_rewrite_env_line` — Task 8, используется Task 9; `MemoryVersions(router=True)` — Task 12, используется тестами центральной панели через `node.state.versions = MemoryVersions()` (без роутера — `xray` там даст 409/422 от памяти, тест это допускает).
- **Placeholders:** нет; единственная отложенная проверка — реальный путь member `mita`, закреплена как шаг Task 16.3 с указанием, что править.
