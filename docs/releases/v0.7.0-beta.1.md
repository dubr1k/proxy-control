# Proxy Control v0.7.0-beta.1

> **Статус / Status:** Beta — цепи и полосы: трафик отдельного клиента по правилам geosite/geoip уходит через выбранный узел парка, остальное — через WARP узла; настраивается на каждого клиента из центральной панели. Проверено на стенде `ams-test` (tier'ы `chains`, `fleet`, `ui` и все остальные) и живьём: AMS_Z как центр с роутером → ams-test как связанный узел-выход. См. таблицу гейта и «Живая проверка» ниже. / Beta — chains and lanes: one client's traffic routed by geosite/geoip rules through a chosen node of the fleet, the rest through the node's WARP, set up per client from the central panel. Proven on the lab host `ams-test` (the `chains`, `fleet`, `ui` and every other tier) and live: AMS_Z as the central with a router → ams-test as the linked exit node. See the gate table and «Live check» below.

> **Руководство / Guide:** [руководство оператора](v0.6-operator-guide.ru.md) — §8.8 «Цепи и полосы» (пошагово для владельца); протокол для ИИ-агентов — [AGENTS.md](../../AGENTS.md) (алгоритм E6). / The operator guide (Russian, §8.8) and the AI-agent protocol (algorithm E6).
>
> **Архив / Archive:** _(заполняется при публикации / filled at publication)_

Журнал изменений / Changelog: [CHANGELOG.ru.md](../../CHANGELOG.ru.md) · [CHANGELOG.md](../../CHANGELOG.md). Решение / Decision: [ADR 009](../adr/009-lanes-and-chains.md). Документация / Documentation: [ROUTING.ru.md](../ROUTING.ru.md) · [XRAY_ROUTER.ru.md](../XRAY_ROUTER.ru.md) · [INSTALLER_REFERENCE.ru.md](../INSTALLER_REFERENCE.ru.md) · [UPGRADING.ru.md](../UPGRADING.ru.md) · [UPGRADING.md](../UPGRADING.md) · [VERIFICATION_MATRIX.md](../VERIFICATION_MATRIX.md).

## Русский

### Что это за выпуск

Поручение владельца: *«клиент — это я; я отправляю трафик через AMS_Z, который отправляет его на ams-test; хочу, чтобы мой трафик по geosite/geoip шёл на выбранный сервер, а остальное — через внутренний WARP; удобно настраивать цепи; и для каждого созданного клиента»*. Три вещи, которых не было в модели v0.4–v0.6, появились: **полоса** (политика одного доступа, а не всего сервиса), **выход через другой узел парка** (relay + учётки, которые выдаёт центр) и **цепь** (до трёх узлов). Всё это работает только на сервисе, подключённом к Xray-router узла, и не трогает нативные backend'ы.

Как это устроено — [ADR 009](../adr/009-lanes-and-chains.md) и [ROUTING](../ROUTING.ru.md) «Цепи и полосы»; что именно добавилось — [CHANGELOG.ru.md](../../CHANGELOG.ru.md).

### Что нашла проверка

- **Роутер падал в цикле при старте** после снятия полосы: `lanes.json` терял учётку раньше, чем intent — нашёл tier `chains`; теперь снятие атомарно, старт и откат вычищают полосы без учётки.
- **Узел не сходился после снятия полосы** (секция сервиса продолжала нести цепь к остановленному узлу B) — следующее поколение идёт без полосы.
- **Слот `mita@4` не успевал открыть RPC** к проверке установщика — ожидание с повторами; **`metrics.pb` делился** между демонами mita — у слота свой `/var/lib/mita`.
- **Правило `geoip:cloudflare → напрямую` ловило `api.ipify.org`** (он за Cloudflare) — цель сценария заменена; **гонка unlink ↔ heartbeat** давала traceback; **предпросмотр отправлял недописанное правило** — исправлено.
- **Настоящая установка ams-test по TOML владельца нашла три дефекта установщика**: `plan` правил `nginx.conf` при планировании (digest расходился с хостом, `install --accept-plan` отказывал собственному плану), пробное продление сертификата падало на гонке `orderNotReady` (certbot 2.9 ↔ Boulder), и общий роутер 443 не нёс ни одного маршрута 3x-ui (панель, подписка и оба входа VLESS отвечали только на loopback) — все три закрыты с тестами. Заодно системные события журнала получили подписи (шли кодами на кадре tier'а `ui`).
- **Проба лаборатории через Caddy была однократной** — на финальном дереве две пробы tier'а `chains` (`c10`, `x06`) попали на смену поколения роутера (снятие полосы перезапускает Xray) и оборвались (curl 55/35); повтор tier'а и tier `router` того же прогона зелёные, логи Caddy и роутера без ошибок. Проба повторяет оборванное соединение, как уже делала проба входа роутера; отказ не повторяется (тест-страж).
- **Цепь через узел без WARP не сходилась никогда** — нашла живая проверка AMS_Z → ams-test (узел поставлен с `[egress] warp = false`): политика полосы ждала `relay_credential_pending` 120 с, а узел отвечал центру `relay failed: egress_invalid` — его роутер отказывал всему набору учёток из-за warp-учётки без провайдера. Роутер несёт direct-учётки и называет, что несёт, отчёт узла подтверждает ровно их; тесты менеджера и узла. Лаборатория этого не видела — у её узла B есть stub-WARP.

### Обновление

`docs/UPGRADING.ru.md` → «Обновление до v0.7»: обычная замена кода и `docker compose up -d --build`, миграция 16 при старте; relay и слоты — повторным `install` того же TOML (по умолчанию `relay_port = 45443`, `lane_slots = 4`) или руками; порядок в парке — узлы, затем центр; откат — предыдущее поколение с базой, полосы снять до отката.

### Установка

Как в v0.6 ([INSTALL.ru.md](../../INSTALL.ru.md), `scripts/install-release.sh --version 0.7.0-beta.1 --sha256 <lab-sha256>`); с `[egress] router = true` установщик ставит relay (45443/tcp) и четыре слота Mieru (46101–46104/tcp) и открывает их в UFW.

### Чек-лист гейта (стенд `ams-test`)

| Гейт | Результат |
| --- | --- |
| `scripts/dev/remote-gate.sh full` (с `VERIFICATION_STRICT=1`) | `REMOTE_GATE_FULL_OK` 2026-09-18 на финальном дереве `644bea8` (pytest **2293 passed / 2 skipped**, 529,7 с; матрица без `gap`) и на `0371c69` (2291 passed, 598,5 с): образы panel/mieru-manager/xray-router, ruff, unittest `test_deploy` (34), `route-coverage.py` (0 проблем), doc-links, JS-синтаксис, `bash -n`/shellcheck, `systemd-analyze verify`, `git diff --check`. Раньше — на `bbcab08` (2283 passed) и `8fbf13d` (2285 passed) |
| `scripts/dev/remote-gate.sh compose` | `REMOTE_GATE_COMPOSE_OK` на `0371c69` (и раньше на `bbcab08`, `41ec41e`) |
| `scripts/dev/remote-gate.sh lab-container` | `REMOTE_GATE_LAB_CONTAINER_OK` на `0371c69`, 9,7 с (9 контейнерных сценариев до установки: preflight, целостность артефакта, audit, plan, nginx-multi-map, coexist-existing-xui, uninstall-foreign-identity, dns-tls, secrets-scan) |
| `LAB_RESET=1 LAB_KEEP_INSTALL=0 scripts/dev/remote-gate.sh lab-host` (с uninstall и coexistence) | `REMOTE_GATE_LAB_HOST_OK` на `0371c69`: 20 сценариев `passed` — install 119,9 с, repair 43,8 с, idempotence 4,2 с, **backup-restore 64,5 с**, reboot-recovery 67,1 с, crash-every-phase 1,6 с, fleet 204,7 с, secrets-scan, **uninstall 32,2 с, coexistence** (раньше — на `bbcab08` и `41ec41e`) |
| `LAB_RESET=1 LAB_KEEP_INSTALL=1 scripts/dev/remote-gate.sh lab-host` | `REMOTE_GATE_LAB_HOST_OK` на `644bea8`: 18 `passed` — install 112,8 с, fleet 201,1 с; и на `0371c69`: 18 `passed` — install 121,9 с, repair 43,2 с, backup-restore 62,4 с, reboot-recovery 66,1 с, fleet 196,8 с; uninstall/coexistence `skipped` (узел оставлен tier'ам ниже) |
| `scripts/dev/remote-gate.sh fleet` | `REMOTE_GATE_FLEET_OK` на `0371c69`: 84/84, 195,3 с |
| `scripts/dev/remote-gate.sh routing` | `REMOTE_GATE_ROUTING_OK` на `0371c69`: 154/154, 302,6 с |
| `scripts/dev/remote-gate.sh router` | `REMOTE_GATE_ROUTER_OK` на `0371c69`: 242/242 (88 router-01…14), 474,0 с (шаг роутера 173,7 с) |
| `scripts/dev/remote-gate.sh chains` | `REMOTE_GATE_CHAINS_OK` на `0371c69`: **277/277** (35 c01…c10 рядом с routing/router; узел B из дерева на стенде, relay через поколение, полоса с цепью, `geoip → напрямую`, слот Mieru, ротация, откат, снятие), 546,2 с (шаг цепей 65,0 с) — отсоединённый прогон на хосте. Первый прогон на том же дереве дал 275/277: две пробы через Caddy (`c10`, `x06`) оборвались (curl 55/35) в момент смены поколения роутера — повтор и tier `router` того же прогона зелёные; проба получила повтор оборванного соединения (см. «Что нашла проверка») — с ним **277/277** ещё раз на `bed7f44` (578,2 с) и **277/277** на финальном `644bea8` (567,0 с, шаг цепей 63,9 с) после исправления relay-учёток узла без WARP |
| `scripts/dev/remote-gate.sh ui` | `REMOTE_GATE_UI_OK` на `0371c69`: **197/197 за 151,1 с** в headless Chrome — 11 экранов узла (вход 5,1 с, обзор 2,9, MTProxy 4,2, Naive 7,4, Mieru 8,9, клиенты 18,3, версии 1,2, узлы 1,8, маршрутизация 39,8 — включая полосу клиента в браузере, администраторы 8,7, журнал 2,0) и центр 40,2 с; без ошибок консоли, без исключений, без секретов в кадрах; runtime-пользователи узла = исходным; кадры ниже — с этого прогона (знак бренда обновлён) |
| `scripts/dev/remote-gate.sh managed-xui` | `REMOTE_GATE_MANAGED_XUI_OK` на `0371c69`: настоящий 3x-ui 3.7.0 поставлен, настроен через API, панель на loopback, все обещанные inbound слушают — 12 с |
| `lab-sha256` архива гейта | `9bb30461536fc02ba2d93a37159d9da18878a2d81989380f38148053752309c2` — дерево `644bea8` (full, lab-host с KEEP, chains; настоящая установка ams-test для живой проверки — из этого архива); `ba5e969056751abf68f4363e0d9b18782a8c986ccd429e3fef97331507328a1d` — дерево `0371c69`, все tier'ы целиком (между ними — повтор пробы лаборатории, relay-учётки узла без WARP, заметка и кадры); раньше `a2dc9379…` (`bbcab08`) и `920be8f8…` (`41ec41e`). Digest тегированного дерева — в аннотации тега и в блоке «Архив» |

### Скриншоты

Сняты 2026-09-18 tier'ом `ui` на стенде по политике v0.4–v0.6: только стендовые данные (установка `lab-host` `https://panel.lab.test`, центр — вторая панель из дерева на `127.0.0.1`, провайдер — SOCKS5-stub на loopback, имена — с префиксом прогона), headless Chrome по CDP, 1440 px и 390 px, PNG без метаданных, каждый ≤ 150 КБ; ни секрета, ни боевого имени в кадре нет.

- **новое в v0.7** — маршрутизация сервиса на Xray-router с «Выходами узла» и relay: [`routing-naive-router-applied.png`](assets/v0.7.0-beta.1/routing-naive-router-applied.png); вкладка **полосы клиента** с её политикой, применённой на роутер, и «Куда пойдёт…»: [`routing-lane-applied.png`](assets/v0.7.0-beta.1/routing-lane-applied.png); клиенты с переключателем «маршрут: как у сервиса / своя полоса» на каждом доступе: [`clients.png`](assets/v0.7.0-beta.1/clients.png); центр — клиент с доступом на связанном узле и его полосой: [`central-clients.png`](assets/v0.7.0-beta.1/central-clients.png);
- обзор: [`dashboard.png`](assets/v0.7.0-beta.1/dashboard.png); MTProxy, NaiveProxy, Mieru: [`users.png`](assets/v0.7.0-beta.1/users.png) · [`naive.png`](assets/v0.7.0-beta.1/naive.png) · [`mieru.png`](assets/v0.7.0-beta.1/mieru.png); версии: [`versions.png`](assets/v0.7.0-beta.1/versions.png); администраторы и API-ключи: [`admins.png`](assets/v0.7.0-beta.1/admins.png); журнал прогона: [`audit.png`](assets/v0.7.0-beta.1/audit.png);
- центр: «Узлы» со связанным узлом стенда: [`central-nodes.png`](assets/v0.7.0-beta.1/central-nodes.png); узел, пока им управляет центр (relay включён, полосы центру): [`node-managed.png`](assets/v0.7.0-beta.1/node-managed.png); маршрутизация на 390 px: [`routing-mobile.png`](assets/v0.7.0-beta.1/routing-mobile.png).

![Полоса клиента на Xray-router: своя политика, «Куда пойдёт…»](assets/v0.7.0-beta.1/routing-lane-applied.png)

### Живая проверка (AMS_Z → ams-test)

2026-09-18, дерево `644bea8`. Два хоста параллельно: **ams-test** — узел-выход, поставлен по-настоящему из архива гейта `9bb30461…` по TOML владельца (`fresh`, `full`, десять настоящих доменов с сертификатами Let's Encrypt, `[egress] warp = false, router = true`, `naive`/`mieru` через роутер, `[three_xui] managed-new` с доменом подписки, UFW); **AMS_Z** — боевой узел владельца, обновлён на месте v0.6 → v0.7 (точки отката `/root/v07-live/20260918T101858Z`, миграция 16, `docker compose up -d --build --no-deps panel naive-manager mieru-manager`), затем стал центром с роутером.

- **Узел** (`plan` → digest → `install --accept-plan`, `active`, 14 действий, 3,6 мин; проверка **26/26** через API и снаружи): relay от установщика слушает 45443 с именем панели и ключом; четыре слота `mita@1…4` активны, UFW открыл 45443 и 46101–46104; naive и mieru подключены к роутеру, relay известен панели; подписка клиента отдаётся **только на десятом домене** (naive+mieru; панельный домен, чужой токен и корень домена подписки — 404); своя полоса Mieru переводит ссылку и подписку на порт слота 46101 (слот `RUNNING`), возврат — обратно на 46001 (слот `IDLE`), черновик политики полосы скопирован с сервиса; сервер подписок 3x-ui отвечает на своём домене с валидным TLS (200); отозванная подписка — 404. Свой клиент проверка убрала; ключ `node-sync` выдан центру.
- **Центр** (проверка **41/41**; первый прогон 38/42 нашёл дефект relay-учёток узла без WARP — см. выше): роутер поставлен адаптером установщика (8,5 с; `relay_port = 0` — центр здесь источник цепей, не выход; входы только на loopback), два слота Mieru руками по `docs/UPGRADING`; naive и mieru подключены к роутеру с политикой сервиса «через WARP» (как было: Caddyfile ведёт на вход роутера, mita — на прокси `router`); ams-test связан (identity с `egress.lanes.v1` и `relay.v1`, online), его relay включён через поколение и отчитан как выход за 16,1 с; временный клиент с доступами naive+mieru и своими полосами (блок handler'ов в Caddyfile, шаблон Mieru на порт слота); политика полосы «по умолчанию WARP, `api.ipify.org` → `node:<ams-test>`»: первый apply честно `relay_credential_pending`, учётка подтверждена узлом за 15,6 с, политика `applied`, «Куда пойдёт…» называет цепь; **выходной IP по правилу — адрес ams-test, по умолчанию — WARP** (не адрес центра); история маскирует UUID учётки, журнал несёт `grant.lane.enable`/`routing.relay.enable` и ни одного секрета; после уборки — runtime-пользователи трёх протоколов равны исходным, Caddyfile без handler'ов полосы, удалённый доступ отказан, сервисы по-прежнему через WARP. Владельцу оставлены роутер, слоты, связь с ams-test и его relay.

## English

### What this release is

The owner's requirement: *«I am the client; I send my traffic through AMS_Z, which sends it on to ams-test; I want my traffic routed by geosite/geoip to a chosen server and the rest through the built-in WARP; chains set up conveniently; and for every client I create»*. Three things the v0.4–v0.6 model lacked arrived: a **lane** (one grant's policy rather than the whole service's), an **exit through another node of the fleet** (a relay with accounts the central issues) and a **chain** (up to three nodes). All of it lives on a service attached to the node's Xray-router and leaves the native backends alone.

How it works — [ADR 009](../adr/009-lanes-and-chains.md) and [ROUTING](../ROUTING.en.md) «Chains and lanes»; what exactly was added — [CHANGELOG.md](../../CHANGELOG.md).

### What the verification found

- **The router crash-looped at start** after a lane was withdrawn: `lanes.json` lost the account before the intent did — found by the `chains` tier; the withdrawal is atomic now, a start or a rollback strips account-less lanes.
- **A node stopped converging after a lane was withdrawn** (the service's section still carried the chain to a stopped node B) — the next generation goes without the lane.
- **Slot `mita@4` had not opened its RPC** by the installer's check — waited for with retries; **`metrics.pb` was shared** between mita daemons — a slot gets its own `/var/lib/mita`.
- **The rule `geoip:cloudflare → direct` caught `api.ipify.org`** (it sits behind Cloudflare) — the scenario's target changed; **an unlink racing a heartbeat** left a traceback; **the preview sent a half-typed rule** — all fixed.
- **The real install of ams-test from the owner's TOML found three installer defects**: `plan` edited `nginx.conf` while planning (the digest no longer matched the host and `install --accept-plan` refused its own plan), the certificate renewal dry run failed on the `orderNotReady` race (certbot 2.9 ↔ Boulder), and the shared 443 router carried no 3x-ui route at all (the panel, the subscription server and both VLESS inbounds answered only on loopback) — all three closed with tests. The journal's system events got labels along the way (they showed as codes in the `ui` tier's frame).
- **The lab's probe through Caddy was a single shot** — on the final tree two probes of the `chains` tier (`c10`, `x06`) landed on the router's generation swap (a lane withdrawal restarts Xray) and died (curl 55/35); the tier's rerun and the `router` tier of the same run were green, the Caddy and router logs clean. The probe now retries a cut connection the way the ingress probe already did; a refusal is never retried (guarded by a test).
- **A chain through a node without WARP never converged** — found by the live check AMS_Z → ams-test (the node installed with `[egress] warp = false`): the lane's policy waited 120 s in `relay_credential_pending` while the node answered the central `relay failed: egress_invalid` — its router refused the whole account set over the warp account without a provider. The router carries the direct accounts and names what it carries, the node's report confirms exactly those; tests for the manager and the node. The lab never saw it — its node B has a stub WARP.

### Upgrading

`docs/UPGRADING.md` → «Upgrading to v0.7»: the usual code swap and `docker compose up -d --build`, migration 16 on start; the relay and the slots through a repeated `install` of the same TOML (defaults `relay_port = 45443`, `lane_slots = 4`) or by hand; nodes before the central; rollback — the previous generation with its database, lanes withdrawn first.

### Installing

As in v0.6 ([INSTALL.en.md](../../INSTALL.en.md), `scripts/install-release.sh --version 0.7.0-beta.1 --sha256 <lab-sha256>`); with `[egress] router = true` the installer brings the relay (45443/tcp) and four Mieru slots (46101–46104/tcp) and opens them in UFW.

### Gate checklist (lab host `ams-test`)

_(the Russian table above holds the figures)_

### Screenshots

Captured on 2026-09-18 by the `ui` tier on the lab host under the v0.4–v0.6 policy: lab data only (the `lab-host` install `https://panel.lab.test`, the central a second panel from the tree on `127.0.0.1`, the provider a SOCKS5 stub on loopback, names with the run's prefix), headless Chrome over CDP, 1440 px and 390 px, PNG without metadata, each ≤ 150 KB; no secret and no production name in any frame.

- **new in v0.7** — a service's routing on the Xray-router with «Node exits» and the relay: [`routing-naive-router-applied.png`](assets/v0.7.0-beta.1/routing-naive-router-applied.png); the **client's lane** tab with its policy applied to the router and «Where will it go…»: [`routing-lane-applied.png`](assets/v0.7.0-beta.1/routing-lane-applied.png); clients with the «route: the service's / own lane» switch on every grant: [`clients.png`](assets/v0.7.0-beta.1/clients.png); the central — a client with a grant on the linked node and its lane: [`central-clients.png`](assets/v0.7.0-beta.1/central-clients.png);
- the overview: [`dashboard.png`](assets/v0.7.0-beta.1/dashboard.png); MTProxy, NaiveProxy, Mieru: [`users.png`](assets/v0.7.0-beta.1/users.png) · [`naive.png`](assets/v0.7.0-beta.1/naive.png) · [`mieru.png`](assets/v0.7.0-beta.1/mieru.png); versions: [`versions.png`](assets/v0.7.0-beta.1/versions.png); administrators and API keys: [`admins.png`](assets/v0.7.0-beta.1/admins.png); the run's journal: [`audit.png`](assets/v0.7.0-beta.1/audit.png);
- the central: «Nodes» with the lab node linked: [`central-nodes.png`](assets/v0.7.0-beta.1/central-nodes.png); the node while the central manages it (relay on, lanes to the central): [`node-managed.png`](assets/v0.7.0-beta.1/node-managed.png); routing at 390 px: [`routing-mobile.png`](assets/v0.7.0-beta.1/routing-mobile.png).

### Live check (AMS_Z → ams-test)

2026-09-18, tree `644bea8`. Two hosts in parallel: **ams-test** — the exit node, installed for real from the gate's archive `9bb30461…` with the owner's TOML (`fresh`, `full`, ten real domains with Let's Encrypt certificates, `[egress] warp = false, router = true`, `naive`/`mieru` through the router, `[three_xui] managed-new` with a subscription domain, UFW); **AMS_Z** — the owner's production node, upgraded in place v0.6 → v0.7 (rollback points `/root/v07-live/20260918T101858Z`, migration 16, `docker compose up -d --build --no-deps panel naive-manager mieru-manager`), then made the central with a router.

- **The node** (`plan` → digest → `install --accept-plan`, `active`, 14 actions, 3.6 min; **26/26** checks through the API and from outside): the installer's relay listens on 45443 with the panel's name and a key; the four slots `mita@1…4` are active, UFW opened 45443 and 46101–46104; naive and mieru are attached to the router, the relay is known to the panel; a client's subscription is served **on the tenth domain only** (naive+mieru; the panel domain, a foreign token and the subscription domain's root answer 404); a Mieru grant's own lane moves the link and the subscription to the slot's port 46101 (slot `RUNNING`), the way back returns them to 46001 (slot `IDLE`), the lane's draft policy is copied from the service; the 3x-ui subscription server answers on its own domain with a valid TLS certificate (200); a revoked subscription is 404. The check removed its own client; a `node-sync` key was handed to the central.
- **The central** (**41/41** checks; the first run, 38/42, found the relay-account defect of a node without WARP — see above): the router installed by the installer's adapter (8.5 s; `relay_port = 0` — the central is a source of chains here, not an exit; ingresses on loopback only), two Mieru slots by hand per `docs/UPGRADING`; naive and mieru attached to the router with the service policy «through WARP» (as before: the Caddyfile points at the router's ingress, mita at the `router` proxy); ams-test linked (identity with `egress.lanes.v1` and `relay.v1`, online), its relay enabled through a generation and reported as an exit in 16.1 s; a throw-away client with naive+mieru grants and their own lanes (a handler block in the Caddyfile, the Mieru template on the slot's port); the lane's policy «WARP by default, `api.ipify.org` → `node:<ams-test>`»: the first apply honestly `relay_credential_pending`, the account confirmed by the node in 15.6 s, the policy `applied`, «Where will it go…» names the chain; **the exit IP by the rule is ams-test's address, by default WARP's** (not the central's); the history masks the account's UUID, the journal carries `grant.lane.enable`/`routing.relay.enable` and no secret; after the cleanup — the runtime users of the three protocols equal the initial ones, the Caddyfile has no lane handler, the deleted grant is refused, the services still go through WARP. Left to the owner: the router, the slots, the link to ams-test and its relay.
