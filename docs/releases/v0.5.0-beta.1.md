# Proxy Control v0.5.0-beta.1

> **Статус / Status:** Beta — гейт v0.5 на стенде `ams-test` и живая проверка на боевом узле `AMS_Z`: см. таблицу гейта и раздел «Живая проверка» ниже (заполнены по прогону 2026-09-16). Xray-router: выделенный закреплённый Xray как egress-роутер для NaiveProxy и Mieru — подключение сервиса явным действием, политики с geosite/geoip/портами и блокировкой рядом с WARP, транзакционные поколения с откатом; узел без роутера работает ровно как в v0.4. Это последняя стадия vNext. / Beta — the v0.5 gate on the lab host `ams-test` and the live check on the production node `AMS_Z`: see the gate table and «Live check» below (filled in from the run of 2026-09-16). The Xray-router: one dedicated, pinned Xray as the egress router for NaiveProxy and Mieru — a service is attached by an explicit action, policies with geosite/geoip/ports and a block beside WARP, transactional generations with a rollback; a node without it behaves exactly as in v0.4. This is the last vNext stage.

> **Архив / Archive:** `proxy-control-v0.5.0-beta.1.tar.gz` — digest и дерево тега появятся здесь после сборки на стенде и публикации (путь v0.4: архив дважды на `ams-test` из чистого клона, `lab-sha256` в аннотации тега, workflow `Release`, attestation). Установка: `scripts/install-release.sh --version 0.5.0-beta.1 --sha256 <lab-sha256>` или четыре шага из раздела «Установка». / The digest and the tagged tree land here after the build on the lab host and the publication (the v0.4 path: the archive built twice on `ams-test` from a clean clone, `lab-sha256` in the tag annotation, the `Release` workflow, the attestation). Install: `scripts/install-release.sh --version 0.5.0-beta.1 --sha256 <lab-sha256>` or the four steps under «Installing».

Журнал изменений / Changelog: [CHANGELOG.ru.md](../../CHANGELOG.ru.md) · [CHANGELOG.md](../../CHANGELOG.md). Документация / Documentation: [XRAY_ROUTER.ru.md](../XRAY_ROUTER.ru.md) · [XRAY_ROUTER.en.md](../XRAY_ROUTER.en.md) · [ROUTING.ru.md](../ROUTING.ru.md) · [ROUTING.en.md](../ROUTING.en.md) · [UPGRADING.ru.md](../UPGRADING.ru.md) · [UPGRADING.md](../UPGRADING.md) · [SECURITY_TEST_MATRIX.md](../SECURITY_TEST_MATRIX.md).

## Русский

### Что нового

- **Xray-router.** Один выделенный процесс Xray-core 26.3.27 в контейнере `proxy-control-xray-router` (сеть хоста, identity 10006, read-only, `cap_drop: ALL`) с двумя аутентифицированными SOCKS5-ingress на loopback — `naive` 45101, `mieru` 45102 — и менеджером-супервизором: поколения, посервисный журнал, типизированный egress API на своём UDS. Применение = рендер из intent'ов обоих сервисов → `xray run -test` → swap (≈ 50 мс) → readback → commit; сбой возвращает последнее хорошее поколение, умерший процесс поднимает watchdog, чужой бинарь или geodata держат роутер выключенным (`artifact_mismatch`). `geoip:private → block` первым правилом каждого ingress и `IPOnDemand`: перепривязанное имя не достаёт хост. UDP не ретранслируется. ADR 007 принят для роутера; каждая ячейка возможностей доказана spike'ом на стенде.
- **Подключение и отключение** сервиса — явные действия owner (`POST …/targets/{node}/{protocol}/attach | detach`): сначала секция роутера pass-through, затем `upstream socks5://…@127.0.0.1:45101` у Caddy / `egress` с `socks5Authentication` у mita; отключение — в обратном порядке. Политика никогда не переключает сервис сама; ключ ingress — идентичность сервиса, и он не появляется ни в одном API, аудите, поколении или отчёте.
- **Политика с новыми селекторами.** `geosites`, `geoips`, порты и блокировка рядом с WARP по умолчанию применяются роутером; на нативном backend'е предпросмотр честно называет роутер (`rule_kind_unsupported`). Backend `xray_router` в модели (миграция 15), `compiler_version` 2, новые причины отказа (`router_unavailable`, `not_attached`, `node_lacks_router`, `artifact_mismatch`, `geosite_unknown`, `geoip_unknown`) и предупреждение `router_credential_stale`.
- **Fleet v2.** Узел объявляет `egress.router.v1` и `identity.router`; секция `egress` поколения может нести `backend: xray_router`, `companion` и `passthrough` — всё опускается при отсутствии, узел и центр v0.4 сохраняют свои дайджесты.
- **Установщик.** `[egress] router = true` и выбор `router` для `naive`/`mieru`; адаптер `xray_router` между `warp` и сервисами: архив `Xray-linux-64.zip` в `/var/lib/proxy-control/` (скачивается сам по закреплённому URL, если не положен; SHA-256 сверяется до использования), три члена по своим дайджестам, identity 10006, `/var/lib/xray-router`, секреты `secrets/xray-router-*` (root:10006 0440), `.env.xray-router`; проверка — статус менеджера, loopback-only ingress, аутентифицированный CONNECT. Ротация ключей — `rotate-xray-router-ingress`.
- **Экран «Маршрутизация».** Бейдж backend (Caddy / mita / Xray-router), строка роутера с «Подключить к Xray-router» / «Отключить от Xray-router», поля geosite / geoip / порты.
- **Лаборатория.** Tier `router` (`remote-gate.sh router`, `fleet-acceptance.py --router`): router-01…14 — подключение, WARP и блокировка рядом через роутер, порт / geosite / geoip, неизвестный код geodata, Mieru с выборочным правилом, отказ ingress без ключа и с чужим, откат при нетронутом Caddyfile, watchdog после SIGKILL, fail-closed без провайдера, secret-scan API/аудита/базы/логов, ротация, отключение и нетронутый хост. `lab-host` ставит роутер по умолчанию.
- **Фаза 8.** `docs/SECURITY_TEST_MATRIX.md` (каждая строка матрицы Task 39 → тест или сценарий), `BACKUP_RESTORE` с состоянием роутера, замороженные идентификаторы в `tests/test_deploy.py` и `docs/COMPATIBILITY.md`.
- **Фикс-волна v0.4.** Userinfo рукописного `upstream` маскируется в egress-представлениях naive-manager; мёртвый `profile_environment` удалён.

### Обновление

Обычное обновление, роутер оно **не ставит**: `panel/`, `naive_manager/`, `mieru_manager/`, `xray_router_manager/`, `compose*.yaml`, `scripts/`, `VERSION` → `docker compose up -d --build --wait panel naive-manager mieru-manager`; миграция 15 сохраняет каждую строку v0.4; узлы раньше центра. Роутер добавляется установщиком (`[egress] router = true` + архив в `/var/lib/proxy-control/`) или шагами из `INSTALLER_REFERENCE` на хосте, собранном вручную; сервисы подключаются потом, на экране маршрутизации. Откат — предыдущее поколение вместе с базой (образ v0.4 не стартует на схеме 15); перед откатом отключите сервисы от роутера. Подробно: `docs/UPGRADING.ru.md`.

### Установка

**Что нужно.** VPS **x86-64** на **Ubuntu 24.04** с `systemd`, вход обычным пользователем с `sudo`; публичный IPv4 без NAT; свободные TCP/443 и TCP/80 (режим `fresh`) либо свой Nginx `stream` на 443 с одной картой `$ssl_preread_server_name` (режим `coexist`); домены панели и MTProxy (всегда), NaiveProxy и Mieru (по профилю), необязательный домен подписки — с A-записями на этот сервер, без чужих AAAA и без CDN перед MTProxy (сертификаты — Let's Encrypt HTTP-01); закреплённые внешние артефакты (`mita_3.36.0_amd64.deb` и `mieru_3.36.0_amd64.deb` для профилей с Mieru, `Xray-linux-64.zip` для роутера) класть не нужно — установщик скачивает их сам в `/var/lib/proxy-control/` и сверяет SHA-256, а хост без интернета кладёт их туда заранее (URL и SHA-256 — `release/external-artifacts.json`); Python 3.11+ (в Ubuntu 24.04 — 3.12), `curl`, `tar`, `sha256sum`. WARP и роутер необязательны: секция `[egress]` в `install.toml` или вопросы мастера (мастер предлагает роутер каждому профилю с NaiveProxy или Mieru).

**Что устанавливается.** Недостающие пакеты Ubuntu (`ca-certificates certbot curl docker-compose-v2 docker.io nginx-full openssl python3` — только они попадают под владение установщика); сертификаты Let's Encrypt с проверкой продления сразу; блок SNI-маршрутов Nginx на общем 443 и TLS-vhost панели; Compose-проект `mtproxy` в `/opt/mtproxy-shared443` (контейнеры `proxy-control-panel`, `-mtproxy` (Telemt), `-mask`, по профилю `-naive-manager`, `-mieru-manager` и `-xray-router`; тома, `secrets/`, мастер-ключ панели); host-службы `caddy-naive.service` (NaiveProxy) и `mita.service` (Mieru) по профилю; бинари роутера в `/usr/local/lib/proxy-control/xray-router` и состояние в `/var/lib/xray-router`; правила UFW — только с вашего разрешения и только на свежем хосте; журнал транзакции, владение и отчёты — `/var/lib/proxy-control/`. Каждый шаг журналируется и откатывается; ничего не меняется до подтверждения digest плана. Полный список — `scripts/install-release.sh --requirements` и [INSTALLER_REFERENCE.ru.md](../INSTALLER_REFERENCE.ru.md).

**Как.** `install-bootstrap` отказывает предрелизным версиям, поэтому бета ставится четырьмя шагами без него (см. [INSTALL.ru.md](../../INSTALL.ru.md)); скрипт [`scripts/install-release.sh`](../../scripts/install-release.sh) делает их сам — без root до проверки байтов, без «скачать и выполнить одной командой»:

```bash
# вручную: четыре файла со страницы выпуска, затем
sha256sum --check SHA256SUMS
tar -xzf proxy-control-v0.5.0-beta.1.tar.gz
cd proxy-control
sudo python3 -m installer.cli wizard

# или скриптом (из клона репозитория или скачанным и прочитанным отдельно):
scripts/install-release.sh --requirements                      # что нужно и что устанавливается
scripts/install-release.sh --version 0.5.0-beta.1 --sha256 <lab-sha256 из таблицы гейта>
# --check-only — только скачать и проверить; --no-wizard — распаковать без запуска; -- plan --config … --json — план без изменений
```

### Чек-лист гейта (стенд `ams-test`)

| Гейт | Результат |
| --- | --- |
| `scripts/dev/remote-gate.sh full` | `REMOTE_GATE_FULL_OK` 2026-09-16 на `6622208` (образы panel/mieru-manager/xray-router, ruff, pytest — см. число ниже, unittest `test_deploy`, doc-links, JS-синтаксис, `bash -n`/shellcheck, `systemd-analyze verify`, `git diff --check`); pytest 2145 passed / 2 skipped, 485,7 с |
| `scripts/dev/remote-gate.sh compose` | `REMOTE_GATE_COMPOSE_OK` на `ef400ff` (config всех оверлеев, включая `compose.xray-router.yaml`; образ роутера uid 10006) |
| `LAB_RESET=1 LAB_KEEP_INSTALL=1 scripts/dev/remote-gate.sh lab-host` | `REMOTE_GATE_LAB_HOST_OK` на `ef400ff`: 17 сценариев `passed` (preflight 18,3 с, install 115,2 с с `[egress] router = true`, repair 41,1 с, idempotence 3,8 с, reboot-recovery 65,0 с, crash-every-phase 1,6 с, fleet 175,0 с, secrets-scan), uninstall/coexistence `skipped` (узел оставлен для tier'ов) |
| `scripts/dev/remote-gate.sh fleet` | `REMOTE_GATE_FLEET_OK`: 84/84, 184,5 с |
| `scripts/dev/remote-gate.sh routing` | `REMOTE_GATE_ROUTING_OK`: 154/154 (70 routing), 296,6 с — узел с установленным роутером, политики v0.4 на нативных бэкендах |
| `scripts/dev/remote-gate.sh router` | `REMOTE_GATE_ROUTER_OK`: 242/242 (88 router-01…14 + 70 routing), 471,2 с (шаг роутера 168,5 с; attach naive ≈ 8 с, mieru ≈ 10 с, применения 3–6 с, Xray 26.3.27) |
| `lab-sha256` архива гейта | `5de24c49d322d42a2e3972dfa9f9b12a8b0a3792c31ea69e540e7188cc9dfbb5` (дерево `ef400ff`; digest тегируемого дерева — в аннотации тега) |

### Живая проверка (AMS_Z)

Проведена 2026-09-16 15:08–15:31 UTC по разрешению владельца («тесты на тех же стендах ssh AMS_Z и ssh ams-test»). **Узел** — боевой хост `AMS_Z` (`https://api.dubr1k-kawai.uk`, реальные пользователи 5/4/3, WARP proxy mode `warp-svc` на `127.0.0.1:45000`, рукописный `upstream http://127.0.0.1:8118` (privoxy) в Caddyfile NaiveProxy, у mita — секция `egress` с `warp`), обновлён rsync'ом с дерева `ef400ff` (адаптер роутера — с исправлением `2f06770`, см. наблюдение 1); всё — через публичный API панели узла как owner, роутер — шагами адаптера установщика (`XrayRouterAdapter.plan → prepare → apply → verify → rollback(purge)`); секреты в отчёт не попали (`/root/v05-live/router-live-report.json`, root, единственный URL в нём — loopback-провайдер WARP из мутаций действия, без учётных данных).

**Обновление v0.4 → v0.5 на месте.** Точки отката в `/root/v05-live/` (`20260916T150806Z`: онлайн-бэкап `panel.sqlite3` (14 миграций), tar кода без секретов, теги образов `*:rollback-20260916T150806Z`, копия Caddyfile, `mita describe config`, `.env`, `docker ps`) → rsync `panel/ naive_manager/ mieru_manager/ xray_router_manager/ installer/ compose*.yaml VERSION` и четыре скрипта v0.5 (`prepare-xray-router-state.sh`, `rotate-xray-router-ingress.sh`, `prepare-naive-state.py`, `install-release.sh`) → `docker compose build panel naive-manager mieru-manager` → `up -d --wait --no-deps panel naive-manager mieru-manager` — 28 с до `healthy`; `db-status`: 15 applied, `checksum_ok` у всех (последняя — `routing-xray-router`), `/app/VERSION` = `0.5.0-beta.1`, `mtproxy` и `mask` не пересозданы, `caddy-naive`/`mita` активны, в журнале панели ошибок нет. **Без роутера v0.4-поведение цело:** `targets` — naive `naive_native`, `mode=custom`, mieru `mieru_native`, `mode=proxy`, у обоих `providers = {warp: reachable}` и `router = null`; ни URL, ни `8118` в ответе.

**Роутер.** Архив `Xray-linux-64.zip` (sha256 `23cd9af9…`) выложен в `/var/lib/proxy-control/`; адаптер: члены `xray`/`geoip.dat`/`geosite.dat` с закреплёнными digest в `/usr/local/lib/proxy-control/xray-router`, identity `xray-router` 10006, state `/var/lib/xray-router`, секреты `root:10006 0440`, `.env.xray-router` с `XRAY_ROUTER_EGRESS_WARP=socks5://127.0.0.1:45000`, `compose up -d --build --wait xray-router` — 7,4 с (образ уже был собран первым прогоном); `verify`: digest сходятся, статус `idle`/поколение 1/артефакты verified, Xray `26.3.27`, оба ingress только на loopback, аутентифицированный CONNECT через 45101 доходит до сети. Затем как у установщика: копии ключей в state менеджеров (`10002:101` / `10005:10005`, 0400), `NAIVE_EGRESS_ROUTER*`/`MIERU_EGRESS_ROUTER*` и `compose.xray-router.yaml` в `.env` (рукописный хост), `up -d --wait --no-deps panel naive-manager mieru-manager` — 24 с; панель в группе 10006; `targets`: у naive и mieru `router = {available: true, attached: false, xray_version: 26.3.27}`, `providers.router.reachable = true`, секретов нет.

**Attach → WARP через роутер → откат → detach.** Пробный пользователь NaiveProxy `live-router-*` (создан и удалён через API узла): до изменений exit `104.28.219.140` (privoxy уже ведёт в WARP), прямой адрес хоста `194.87.220.7`. `POST …/targets/local/naive/attach` → `backend=xray_router`, `attached=true`; в Caddyfile управляемый блок с `upstream socks5://naive-…@127.0.0.1:45101` (ключ только в файле: его нет ни в `targets`, ни в аудите, ни в egress-виде менеджера), строки privoxy нет; сквозной режим роутера — exit `194.87.220.7` (прямой). Политика naive «весь сервис через WARP + block `example.com`, `*.example.com`» с `backend=xray_router`: предпросмотр `supported` → apply `applied` (поколение роутера ≥ 2) → через прокси `api.ipify.org` = `104.28.219.140` ≠ прямой, `cdn-cgi/trace` → `warp=on`, `https://example.com/` отвергнут; Caddyfile при этом не изменился (правила живут в роутере). Откат политики → `rolled_back`, exit снова прямой → `detach` → `backend=naive_native`, `attached=false`, роутера в Caddyfile нет → два отката нативного журнала (direct → attach → пол) → Caddyfile байт-в-байт равен снимку с пробным пользователем, строка `upstream http://127.0.0.1:8118` на месте, exit `104.28.219.140` как до всего → политика удалена (`applied_digest` = пол журнала), `targets`: `mode=custom`, без политики.

**Удаление роутера.** `rollback(purge_data=True, rollback_target="uninstalled")`: контейнер, бинари, helpers, `.env.xray-router`, state, секреты, identity 10006 — удалены; копии ключей у менеджеров сняты, `.env` восстановлен байт-в-байт (sha256 `4566a8ad…`), `up -d --wait --no-deps panel naive-manager mieru-manager`, том `mtproxy_xray-router-run` удалён; `targets` снова с `router = null` и без `providers.router`. **Итог:** sha256 Caddyfile = снимку до обновления (`0e3a8ea0d18cdeb1…` — тот же, что и после живой проверки v0.4), `mita describe config` = снимку, пользователи 5/4/3 без изменений, `routing_policies` пуст, все пять сервисов `healthy`, `mita` active, аудит содержит `routing.target.attach|detach`, `routing.policy.update|apply|rollback|delete` без секретов. 53/53 проверок во втором прогоне.

**Наблюдения.** (1) Первый прогон (`20260916T152038Z`, дерево `ef400ff`) прошёл 52 из 53 проверок: `rollback(purge)` адаптера удалил всё, кроме самого контейнера — у реального runner'а не было `compose_service_present` (фейк в тестах его имел), поэтому `_compose_service_present()` всегда отвечал «нет»; лаборатория этого не видела, потому что `LAB_KEEP_INSTALL=1` пропускает uninstall. Исправлено в `2f06770` (метод у реального runner'а + тест на него), контейнер и том сняты вручную, второй прогон — 53/53 на исправленном адаптере. Отчёт первого прогона не записался (mappingproxy в `Evidence.details`) — журнал прогона сохранён как `router-live-run1.log`. (2) AMS_Z остаётся на v0.5 **без роутера** (архив Xray оставлен в `/var/lib/proxy-control/` — при желании владелец включит роутер по `docs/XRAY_ROUTER.ru.md`); менеджер NaiveProxy тумбстонит имена навсегда: `live-router-4f3e07`, `live-router-9d849c` сожжены. Точки отката — `/root/v05-live/`, только root.

## English

### What's new

- **The Xray-router.** One dedicated Xray-core 26.3.27 process in the container `proxy-control-xray-router` (host network, identity 10006, read-only, `cap_drop: ALL`) with two authenticated SOCKS5 ingresses on the loopback — `naive` 45101, `mieru` 45102 — and a supervising manager: generations, a per-service journal, a typed egress API on its own UDS. An apply is render from both services' intents → `xray run -test` → swap (≈ 50 ms) → readback → commit; a failure restores the last known good generation, a dead child is restarted by the watchdog, a foreign binary or geodata keeps the router down (`artifact_mismatch`). `geoip:private → block` first on every ingress and `IPOnDemand`: a rebinding name cannot reach the host. UDP is not relayed. ADR 007 is accepted for the router; every capability cell was proved by the spike on the stand.
- **Attach and detach** are explicit owner actions (`POST …/targets/{node}/{protocol}/attach | detach`): the router's pass-through section first, then Caddy's `upstream socks5://…@127.0.0.1:45101` / mita's `egress` with `socks5Authentication`; detach in the reverse order. A policy never moves a service by itself; the ingress credential is the service's identity and appears in no API, audit, generation or report.
- **Policies with new selectors.** `geosites`, `geoips`, ports and a block beside a WARP default are enforced by the router; on a native backend the preview names the router (`rule_kind_unsupported`). Backend `xray_router` in the model (migration 15), `compiler_version` 2, new reasons (`router_unavailable`, `not_attached`, `node_lacks_router`, `artifact_mismatch`, `geosite_unknown`, `geoip_unknown`) and the warning `router_credential_stale`.
- **Fleet v2.** The node declares `egress.router.v1` and `identity.router`; a generation's `egress` section may carry `backend: xray_router`, `companion` and `passthrough` — all omitted when absent; a v0.4 node and central keep their digests.
- **Installer.** `[egress] router = true` and the choice `router` for `naive`/`mieru`; the adapter `xray_router` between `warp` and the services: `Xray-linux-64.zip` in `/var/lib/proxy-control/` (fetched from its pinned URL unless staged; the SHA-256 is proven before use), three members against their own digests, identity 10006, `/var/lib/xray-router`, secrets `secrets/xray-router-*` (root:10006 0440), `.env.xray-router`; verify reads the manager's status, checks loopback-only ingresses and one authenticated CONNECT. Key rotation: `rotate-xray-router-ingress`.
- **The «Routing» screen.** The backend badge (Caddy / mita / Xray-router), the router line with attach/detach, the geosite / geoip / ports fields.
- **The lab.** Tier `router` (`remote-gate.sh router`, `fleet-acceptance.py --router`): router-01…14 — attach, WARP and a block beside it through the router, port / geosite / geoip, an unknown geodata code, Mieru with a selective rule, the ingress refusing a missing or a cross-service key, rollback with the Caddyfile untouched, the watchdog after a SIGKILL, fail-closed without the provider, the secret scan of API/audit/database/logs, rotation, detach and the untouched host. `lab-host` installs the router by default.
- **Phase 8.** `docs/SECURITY_TEST_MATRIX.md` (every row of the Task 39 matrix → a test or a scenario), `BACKUP_RESTORE` with the router's state, frozen identifiers in `tests/test_deploy.py` and `docs/COMPATIBILITY.md`.
- **The v0.4 fix-wave.** The userinfo of a hand-written `upstream` is redacted in the naive-manager's egress views; the dead `profile_environment` is gone.

### Upgrading

The ordinary upgrade, which **does not** install a router: `panel/`, `naive_manager/`, `mieru_manager/`, `xray_router_manager/`, `compose*.yaml`, `scripts/`, `VERSION` → `docker compose up -d --build --wait panel naive-manager mieru-manager`; migration 15 keeps every v0.4 row; nodes before the central. The router is added by the installer (`[egress] router = true` plus the archive in `/var/lib/proxy-control/`) or by the steps in `INSTALLER_REFERENCE` on a hand-assembled host; services are attached afterwards on the routing screen. Rollback: the previous generation together with the database (a v0.4 image does not start on schema 15); detach the services from the router first. Details: `docs/UPGRADING.md`.

### Installing

**Requirements.** An **x86-64** VPS on **Ubuntu 24.04** with `systemd`, a regular user with `sudo`; a public IPv4 without NAT; free TCP/443 and TCP/80 (`fresh`) or your own Nginx `stream` on 443 with one `$ssl_preread_server_name` map (`coexist`); the panel and MTProxy domains (always), NaiveProxy and Mieru (per profile), an optional subscription domain — with A records at this server, no foreign AAAA and no CDN in front of MTProxy (Let's Encrypt HTTP-01); the pinned external artifacts (`mita_3.36.0_amd64.deb` and `mieru_3.36.0_amd64.deb` for the Mieru profiles, `Xray-linux-64.zip` for the router) need no staging — the installer fetches them into `/var/lib/proxy-control/` and proves their SHA-256, an offline host stages them there in advance (URLs and SHA-256 in `release/external-artifacts.json`); Python 3.11+ (3.12 on Ubuntu 24.04), `curl`, `tar`, `sha256sum`. WARP and the router are optional: the `[egress]` section of `install.toml` or the wizard's questions (the wizard asks about the router only when the archive is staged).

**What gets installed.** Missing Ubuntu packages (`ca-certificates certbot curl docker-compose-v2 docker.io nginx-full openssl python3` — only those come under the installer's ownership); Let's Encrypt certificates with a renewal dry-run; the SNI route block on the shared 443 and the panel's TLS vhost; the Compose project `mtproxy` in `/opt/mtproxy-shared443` (containers `proxy-control-panel`, `-mtproxy` (Telemt), `-mask`, per profile `-naive-manager`, `-mieru-manager` and `-xray-router`; volumes, `secrets/`, the panel master key); the host services `caddy-naive.service` (NaiveProxy) and `mita.service` (Mieru) per profile; the router's binaries in `/usr/local/lib/proxy-control/xray-router` and its state in `/var/lib/xray-router`; UFW rules only with your consent and only on a fresh host; the transaction journal, ownership and reports in `/var/lib/proxy-control/`. Every step is journaled and reversible; nothing changes before the plan digest is confirmed. The full list: `scripts/install-release.sh --requirements` and [INSTALLER_REFERENCE.en.md](../INSTALLER_REFERENCE.en.md).

**How.** `install-bootstrap` refuses pre-releases, so a beta is installed in four steps without it (see [INSTALL.en.md](../../INSTALL.en.md)); [`scripts/install-release.sh`](../../scripts/install-release.sh) does them for you — no root before the bytes are verified, no «download and run in one line»:

```bash
# by hand: the four files from the release page, then
sha256sum --check SHA256SUMS
tar -xzf proxy-control-v0.5.0-beta.1.tar.gz
cd proxy-control
sudo python3 -m installer.cli wizard

# or with the script (from a clone, or downloaded and read separately):
scripts/install-release.sh --requirements                      # what is needed and what gets installed
scripts/install-release.sh --version 0.5.0-beta.1 --sha256 <lab-sha256 from the gate table>
# --check-only — download and verify only; --no-wizard — extract without running; -- plan --config … --json — a plan without changes
```

### Gate checklist (lab host `ams-test`)

| Gate | Result |
| --- | --- |
| `scripts/dev/remote-gate.sh full` | `REMOTE_GATE_FULL_OK` 2026-09-16 on `6622208` (panel/mieru-manager/xray-router images, ruff, pytest — see the number below, unittest `test_deploy`, doc-links, JS syntax, `bash -n`/shellcheck, `systemd-analyze verify`, `git diff --check`); pytest 2145 passed / 2 skipped, 485.7 s |
| `scripts/dev/remote-gate.sh compose` | `REMOTE_GATE_COMPOSE_OK` on `ef400ff` (config of every overlay including `compose.xray-router.yaml`; the router image runs as uid 10006) |
| `LAB_RESET=1 LAB_KEEP_INSTALL=1 scripts/dev/remote-gate.sh lab-host` | `REMOTE_GATE_LAB_HOST_OK` on `ef400ff`: 17 scenarios `passed` (preflight 18.3 s, install 115.2 s with `[egress] router = true`, repair 41.1 s, idempotence 3.8 s, reboot-recovery 65.0 s, crash-every-phase 1.6 s, fleet 175.0 s, secrets-scan), uninstall/coexistence `skipped` (the node is kept for the tiers) |
| `scripts/dev/remote-gate.sh fleet` | `REMOTE_GATE_FLEET_OK`: 84/84, 184.5 s |
| `scripts/dev/remote-gate.sh routing` | `REMOTE_GATE_ROUTING_OK`: 154/154 (70 routing), 296.6 s — a node with the router installed, v0.4 policies on the native backends |
| `scripts/dev/remote-gate.sh router` | `REMOTE_GATE_ROUTER_OK`: 242/242 (88 router-01…14 + 70 routing), 471.2 s (the router step 168.5 s; attach naive ≈ 8 s, mieru ≈ 10 s, applies 3–6 s, Xray 26.3.27) |
| `lab-sha256` of the gate archive | `5de24c49d322d42a2e3972dfa9f9b12a8b0a3792c31ea69e540e7188cc9dfbb5` (tree `ef400ff`; the tagged tree's digest goes into the tag annotation) |

### Live check (AMS_Z)

Run on 2026-09-16 15:08–15:31 UTC under the owner's permission («tests on the same stands, ssh AMS_Z and ssh ams-test»). **The node** — the production host `AMS_Z` (`https://api.dubr1k-kawai.uk`, real users 5/4/3, WARP proxy mode `warp-svc` on `127.0.0.1:45000`, a hand-written `upstream http://127.0.0.1:8118` (privoxy) in the NaiveProxy Caddyfile, mita with an `egress` section naming `warp`), updated by rsync from tree `ef400ff` (the router adapter with the fix `2f06770`, see observation 1); everything through the node panel's public API as the owner, the router by the installer adapter's own steps (`XrayRouterAdapter.plan → prepare → apply → verify → rollback(purge)`); no secret reached the report (`/root/v05-live/router-live-report.json`, root only; the one URL in it is the WARP loopback provider from the action's mutations, without credentials).

**In-place upgrade v0.4 → v0.5.** Rollback points in `/root/v05-live/` (`20260916T150806Z`: an online backup of `panel.sqlite3` (14 migrations), a code tar without secrets, image tags `*:rollback-20260916T150806Z`, a copy of the Caddyfile, `mita describe config`, `.env`, `docker ps`) → rsync `panel/ naive_manager/ mieru_manager/ xray_router_manager/ installer/ compose*.yaml VERSION` and the four v0.5 scripts (`prepare-xray-router-state.sh`, `rotate-xray-router-ingress.sh`, `prepare-naive-state.py`, `install-release.sh`) → `docker compose build panel naive-manager mieru-manager` → `up -d --wait --no-deps panel naive-manager mieru-manager` — 28 s to `healthy`; `db-status`: 15 applied, every `checksum_ok` (the last one `routing-xray-router`), `/app/VERSION` = `0.5.0-beta.1`, `mtproxy` and `mask` not recreated, `caddy-naive`/`mita` active, no error in the panel log. **Without a router the v0.4 behaviour is intact:** `targets` — naive `naive_native`, `mode=custom`, mieru `mieru_native`, `mode=proxy`, both with `providers = {warp: reachable}` and `router = null`; neither a URL nor `8118` in the response.

**The router.** The archive `Xray-linux-64.zip` (sha256 `23cd9af9…`) staged in `/var/lib/proxy-control/`; the adapter: the members `xray`/`geoip.dat`/`geosite.dat` with their pinned digests in `/usr/local/lib/proxy-control/xray-router`, the identity `xray-router` 10006, the state `/var/lib/xray-router`, secrets `root:10006 0440`, `.env.xray-router` with `XRAY_ROUTER_EGRESS_WARP=socks5://127.0.0.1:45000`, `compose up -d --build --wait xray-router` — 7.4 s (the image had been built by the first run); `verify`: digests match, status `idle`/generation 1/artifacts verified, Xray `26.3.27`, both ingresses on the loopback only, one authenticated CONNECT through 45101 reaches the Internet. Then as the installer does: credential copies in the managers' state (`10002:101` / `10005:10005`, 0400), `NAIVE_EGRESS_ROUTER*`/`MIERU_EGRESS_ROUTER*` and `compose.xray-router.yaml` in `.env` (a hand-maintained host), `up -d --wait --no-deps panel naive-manager mieru-manager` — 24 s; the panel in group 10006; `targets`: naive and mieru carry `router = {available: true, attached: false, xray_version: 26.3.27}`, `providers.router.reachable = true`, no secret.

**Attach → WARP through the router → rollback → detach.** A throw-away NaiveProxy user `live-router-*` (created and deleted through the node's API): the exit before any change `104.28.219.140` (privoxy already leads into WARP), the host's direct address `194.87.220.7`. `POST …/targets/local/naive/attach` → `backend=xray_router`, `attached=true`; the Caddyfile carries the managed block with `upstream socks5://naive-…@127.0.0.1:45101` (the credential lives in the file only: absent from `targets`, the audit and the manager's egress view), no privoxy line; the router in pass-through — exit `194.87.220.7` (direct). The naive policy «whole service through WARP + block `example.com`, `*.example.com`» with `backend=xray_router`: preview `supported` → apply `applied` (router generation ≥ 2) → through the proxy `api.ipify.org` = `104.28.219.140` ≠ direct, `cdn-cgi/trace` → `warp=on`, `https://example.com/` refused; the Caddyfile unchanged meanwhile (the rules live in the router). Policy rollback → `rolled_back`, the exit direct again → `detach` → `backend=naive_native`, `attached=false`, no router in the Caddyfile → two rollbacks of the native journal (direct → attach → floor) → the Caddyfile byte for byte equal to the snapshot with the throw-away user, the line `upstream http://127.0.0.1:8118` in its place, exit `104.28.219.140` as before everything → the policy deleted (`applied_digest` = the journal's floor), `targets`: `mode=custom`, no policy.

**Removing the router.** `rollback(purge_data=True, rollback_target="uninstalled")`: the container, binaries, helpers, `.env.xray-router`, state, secrets, identity 10006 — gone; the managers' credential copies removed, `.env` restored byte for byte (sha256 `4566a8ad…`), `up -d --wait --no-deps panel naive-manager mieru-manager`, the volume `mtproxy_xray-router-run` removed; `targets` again with `router = null` and no `providers.router`. **Outcome:** the Caddyfile's sha256 equals the pre-upgrade snapshot (`0e3a8ea0d18cdeb1…` — the same as after the v0.4 live check), `mita describe config` equals the snapshot, users 5/4/3 unchanged, `routing_policies` empty, all five services `healthy`, `mita` active, the audit carries `routing.target.attach|detach`, `routing.policy.update|apply|rollback|delete` without a secret. 53/53 checks on the second run.

**Observations.** (1) The first run (`20260916T152038Z`, tree `ef400ff`) passed 52 of 53 checks: the adapter's `rollback(purge)` removed everything but the container itself — the real runner had no `compose_service_present` (the test fake had one), so `_compose_service_present()` always answered «no»; the lab never saw it because `LAB_KEEP_INSTALL=1` skips uninstall. Fixed in `2f06770` (the method on the real runner + a test for it), the container and the volume removed by hand, the second run — 53/53 on the fixed adapter. The first run's report was not written (a mappingproxy in `Evidence.details`) — its log is kept as `router-live-run1.log`. (2) AMS_Z stays on v0.5 **without a router** (the Xray archive is left in `/var/lib/proxy-control/` — the owner can enable the router per `docs/XRAY_ROUTER.en.md`); the NaiveProxy manager tombstones names for good: `live-router-4f3e07`, `live-router-9d849c` are burnt. Rollback points: `/root/v05-live/`, root only.
