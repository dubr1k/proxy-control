# Proxy Control v0.4.0-beta.1

> **Статус / Status:** Beta — гейт v0.4 пройден на стенде `ams-test` 2026-09-14 (дерево `841a086`, `lab-sha256` `4d1e96d4dbf638fd…`) и проверен живьём на боевом узле `AMS_Z`. Маршрутизация: egress-политики по узлам и сервисам с честным предпросмотром под backend узла — только то, что spike доказал на стенде. Xray-роутер (v0.5) остаётся дорожной картой. / Beta — the v0.4 gate passed on the lab host `ams-test` on 2026-09-14 (tree `841a086`, `lab-sha256` `4d1e96d4dbf638fd…`) and was checked live on the production node `AMS_Z`. Routing: per-node, per-service egress policies with a preview that tells the truth about the node's backend — only what the spike proved on the stand. The Xray router (v0.5) stays roadmap.

## Русский

### Что нового

- **Политика egress на (узел, сервис)** для NaiveProxy и Mieru: «по умолчанию — напрямую | через WARP», «при недоступности WARP — отказать | напрямую», упорядоченные first-match правила (домены, CIDR, порты; блокировать / напрямую / через WARP), оптимистичные ревизии. MTProxy — вне области (`protocol_out_of_scope`). ADR 006 принят.
- **Компилятор и предпросмотр.** Политика компилируется под backend узла — `naive_native` (Caddy forwardproxy `upstream` + `acl`) или `mieru_native` (mita `egress`) — против возможностей, которые объявляет менеджер. Всё, что backend не может выполнить, — `unsupported` с именем правила: у NaiveProxy один upstream на сервис и нет выборочных правил, а forwardproxy не применяет ACL рядом с upstream, поэтому блокировка при WARP по умолчанию не держится; блокировка по порту — ни у кого; loopback и приватные сети можно только блокировать. Недоступный WARP — fail-closed, если политика не выбрала `approved_direct` (тогда это предупреждение в предпросмотре).
- **Egress API менеджеров** (`/v1/egress`, `plan | apply | rollback` на тех же UDS): naive-manager владеет размеченным блоком в `forward_proxy` (рукописный `upstream` перенимается как `custom` и возвращается откатом байт в байт), mieru-manager — секцией `egress` mita (применяется перезапуском — `mita reload` её не подхватывает). CAS по revision, проба доступности провайдера перед применением, readback после reload, журнал последних десяти записей. mieru-manager переходит в сеть хоста ради пробы (по-прежнему `read_only`, `cap_drop: ALL`, без слушателя). ADR 007 принят для нативных backend.
- **Установщик `[egress]`** (`warp`, `warp_port`, `naive`, `mieru`): WARP вынесен из `[three_xui]` (старые ключи читаются с одним предупреждением); `NAIVE_EGRESS_WARP` / `MIERU_EGRESS_WARP` доходят до менеджеров через `.env` и Compose; начальный egress сеется один раз, upgrade и repair его не трогают.
- **Маршрутизация через Fleet v2.** Узел объявляет `egress.v1` и свои egress-цели в `identity`; поколение несёт необязательную секцию `egress` (опускается из проводной формы и digest при отсутствии — узел и центр v0.3 и v0.4 сходятся на любом документе без неё); узел применяет её после ресурсов, идемпотентно по digest, и отчитывается `converged | failed | unsupported`; центр переводит политику в `applied` / `failed` по отчёту. Локальное применение отклоняется, пока узлом управляет центр (`managed_by_central`).
- **`/api/routing/*`** (targets, политика, preview черновика, apply, rollback, history) и аудит `routing.policy.update | apply | rollback | delete`; **экран «Маршрутизация»** с живым предпросмотром, «Применить» только для сохранённой поддерживаемой политики, историей и строкой «Маршрутизация: …» на карточке узла.
- **Стенд.** Tier `routing` (`remote-gate.sh routing`, `fleet-acceptance.py --routing`, `socks5-stub.py`): WARP на весь сервис через журналирующий SOCKS5-stub, блокировки по домену и CIDR при живом cover-сайте, выборочное правило Mieru, откат байт в байт, недоступный провайдер → fail-closed, nginx и nftables не тронуты.
- **Фикс-волна v0.3** (отложенные замечания post-merge): 429/5xx узла не переводят связь в `offline`, эскроу только из отчёта о текущем поколении, `capture` с `purpose: escrow | import`, `DELETE /api/nodes/{id}?force=1`, «применено, ожидает учётные данные», tmpfs `/tmp` менеджеров и агента, единые имена событий аудита.

### Обновление

Обычное обновление, но пересобираются панель **и оба менеджера** (`up -d --build --wait panel naive-manager mieru-manager`; `upgrade` установщика делает это сам); миграция 14 аддитивна; сначала узлы, затем центр; хост, собранный вручную, задаёт `NAIVE_EGRESS_WARP` / `MIERU_EGRESS_WARP` в `.env` сам. Откат — полная предыдущая генерация вместе с базой (образ v0.3 не стартует на схеме 14); откат панели не отменяет применённую политику — сначала «Сбросить» + «Применить». Подробно: `docs/UPGRADING.ru.md`.

### Установка

Скачайте `proxy-control-v0.4.0-beta.1.tar.gz`, `SHA256SUMS`, `release-manifest.json`, `sbom.spdx.json`. `install-bootstrap` отказывает предрелизным версиям — для беты распакуйте архив после проверки `sha256sum --check SHA256SUMS` и запустите `sudo python3 -m installer.cli wizard` из каталога `proxy-control` (см. `INSTALL.ru.md`). WARP: секция `[egress]` в `install.toml` или соответствующие вопросы мастера.

### Чек-лист гейта (стенд `ams-test`, дерево `841a086`, 2026-09-14)

| Гейт | Результат |
| --- | --- |
| `scripts/dev/remote-gate.sh full` | `REMOTE_GATE_FULL_OK` — 2026-09-14 17:03 UTC, 1978 passed / 2 skipped (427 с), `tests/test_deploy.py` OK, ruff, doc-links, JS, shellcheck, systemd-analyze, `git diff --check` |
| `scripts/dev/remote-gate.sh compose` | `REMOTE_GATE_COMPOSE_OK` — 2026-09-14 (после Task 5 и повторно на `841a086`): пять compose-моделей, mieru-manager в `network_mode: host`, образы agent/ingress, uid ingress 10001 |
| `LAB_RESET=1 LAB_KEEP_INSTALL=1 scripts/dev/remote-gate.sh lab-host` | `REMOTE_GATE_LAB_HOST_OK` — 2026-09-14 17:21 UTC, `LAB_HOST_EXIT=0`, 17 сценариев passed (preflight 11,5 с, install 104 с, repair 38 с, reboot-recovery 62 с, `fleet` 169 с, secrets-scan), `uninstall`/`coexistence` skipped (узел оставлен). Первый прогон дня (дерево `4373edb`) упал в сценарии `fleet` на s07/s09 — центр после фикс-волны T9 считал голый 502 nginx перед остановленной панелью «отказом узла» → исправлено `1b2eeed`; preflight ронял `dpkg configure nginx` на сертификате прошлого прогона → `d93b598` |
| `scripts/dev/remote-gate.sh fleet` | `FLEET_ACCEPTANCE_OK` + `REMOTE_GATE_FLEET_OK` — 2026-09-14 17:27 UTC, 84 проверки, все пройдены, 173 с (link online 4,1 с, доступы enabled 4,0 с, offline-конвергенция 1,0 с, 20 доступов при рестарте 11,2 с, purge 11,2 с) |
| `scripts/dev/remote-gate.sh routing` | `ROUTING_ACCEPTANCE_OK` + `REMOTE_GATE_ROUTING_OK` — 2026-09-14 17:36 UTC, 154 проверки (70 routing), все пройдены, 262 с (routing-шаг 89 с; применение 3–6 с на протокол через поколение): targets с `egress.v1`, WARP на весь сервис через журналирующий SOCKS5-stub (naive/mieru), блокировки по домену и CIDR при живом cover-сайте, выборочное правило Mieru (direct-исключение мимо stub, остальное — через), честный 422 у naive на выборочное правило, откат байт-в-байт (sha256 Caddyfile, секция egress mita), провайдер остановлен → `provider_unreachable`, Caddyfile не тронут, `nginx -T` и `nft list ruleset` без изменений, сброс и удаление политик. Первый прогон дня вскрыл счётчики nft в digest и сид mita с DIRECT-правилом (`fcf452b`) и перекрытие `.env.naive` (`e7c180c`) |
| `lab-sha256` архива гейта | `4d1e96d4dbf638fd846c8b43a86c78223571b25ebfcceb41df8a6034c27e6d24` — `/root/lab-host.sha` = `dist/SHA256SUMS` на стенде, архив `proxy-control-v0.4.0-beta.1.tar.gz` из дерева `841a086`. Коммиты после него — только `scripts/dev/remote-gate.sh` (env-файлы tier `routing`), документация и план; аннотация тега должна нести digest архива **тегируемого** дерева |

### Живая проверка (AMS_Z)

Проведена 2026-09-14 17:20–17:41 UTC по разрешению владельца от 2026-09-14. **Узел** — боевой хост `AMS_Z` (`https://api.dubr1k-kawai.uk`, реальные пользователи 5/4/3, WARP proxy mode `warp-svc` на `127.0.0.1:45000`, рукописный `upstream http://127.0.0.1:8118` (privoxy) в Caddyfile NaiveProxy, у mita — секция `egress` с `warp` и правилом `* → PROXY`), обновлён rsync'ом с дерева `841a086`; всё — через публичный API панели узла как owner; секреты и ссылки в отчёт не попали (`/root/v04-live/routing-live-report.json`).

**Обновление v0.3 → v0.4 на месте.** Точки отката в `/root/v04-live/` (`20260914T172034Z`: онлайн-бэкап `panel.sqlite3`, tar кода без секретов, теги образов `*:rollback-20260914T172034Z`, копия Caddyfile, `mita describe config`, `.env`, `docker ps`) → rsync `panel/ naive_manager/ mieru_manager/ compose*.yaml VERSION` → в `.env` добавлены `NAIVE_EGRESS_WARP=socks5://127.0.0.1:45000` и `MIERU_EGRESS_WARP=socks5://127.0.0.1:45000` (хост обновляется вручную — путь из `docs/UPGRADING`) → `docker compose build panel naive-manager mieru-manager` → `up -d --wait --no-deps panel naive-manager mieru-manager` — 25 с до `healthy`; миграция 14 (`db-status`: 14 applied), `/app/VERSION` = `0.4.0-beta.1`, mieru-manager пересоздан в `network_mode: host`, `mtproxy` и `mask` не пересозданы, `caddy-naive`/`mita` активны, оба менеджера видят провайдера, `/api/routing/targets` без сессии → 401.

**Маршрутизация.** `targets`: naive `naive_native`, `mode=custom` (рукописный upstream), mieru `mieru_native`, `mode=proxy`, у обоих `providers.warp.reachable=true`, mtproxy — `protocol_out_of_scope`; ни URL провайдера, ни `8118` в ответе нет. Пробный пользователь NaiveProxy `live-routing-*` (создан и удалён через API узла): исходящий адрес до изменений `104.28.219.140` (privoxy уже ведёт в WARP), прямой адрес хоста `194.87.220.7`. Политика naive «весь сервис через WARP»: предпросмотр `supported` с предупреждением `adopts_unmanaged_upstream` → apply (перезагрузка Caddy, без перезапуска) → в Caddyfile управляемый блок с `upstream socks5://127.0.0.1:45000`, строки privoxy нет → через прокси: `api.ipify.org` = `104.28.219.140` ≠ прямой, `cdn-cgi/trace` → `warp=on` → откат → строка `upstream http://127.0.0.1:8118` вернулась на своё место, прокси работает, политика удалена (`applied_digest` = пол журнала). Политика mieru «через WARP + блокировать `example.com`, `*.example.com`»: предпросмотр `supported`, `restart_required=true` → apply (перезапуск mita ≈ 3 с, сессии Mieru переподключились) → в конфиге mita правила `REJECT` и `PROXY`, mita `active` → откат → секция `egress` равна снимку, mita `active` → политика удалена. Итог: sha256 Caddyfile = снимку до обновления (`0e3a8ea0d18cdeb1…`), `mita describe config` = снимку, списки пользователей 5/4/3 без изменений, `routing_policies` пуст, аудит содержит `routing.policy.update|apply|rollback|delete` без секретов, в журнале панели ошибок нет.

**Наблюдения.** (1) Первый прогон скрипта сравнил Caddyfile после отката со снимком, снятым до создания пробного пользователя (блок пользователей менеджера отличался на одну строку) — финальное равенство после удаления пользователя это опровергло; во втором прогоне у mieru не было предупреждения `adopts_unmanaged_egress` — журнал mieru-manager уже помнил первое применение, конфигурация при этом восстановлена байт в байт (у naive предупреждение идёт от файла, у mieru — от журнала; расхождение косметическое). Оба прогона: все содержательные проверки пройдены. (2) AMS_Z остаётся на v0.4 с переменными провайдера в `.env` — честное состояние хоста с WARP; менеджер NaiveProxy тумбстонит имена навсегда: `live-routing-b46c5d`, `live-routing-ce3602` сожжены. Точки отката — `/root/v04-live/`, только root.

## English

### What's new

- **An egress policy per (node, service)** for NaiveProxy and Mieru: «default — direct | through WARP», «when WARP is down — refuse | direct», ordered first-match rules (domains, CIDRs, ports; block / direct / through WARP), optimistic revisions. MTProxy is out of scope (`protocol_out_of_scope`). ADR 006 accepted.
- **Compiler and preview.** The policy is compiled for the node's backend — `naive_native` (Caddy forwardproxy `upstream` + `acl`) or `mieru_native` (mita `egress`) — against the capabilities the manager declares. Whatever a backend cannot enforce is `unsupported` with the rule named: NaiveProxy has one upstream per service and no selective rules, and forwardproxy skips its ACL beside an upstream, so a block cannot hold with a WARP default; block by port — nowhere; loopback and private networks may only be blocked. A WARP that is down fails closed unless the policy chose `approved_direct` (then a preview warning).
- **Egress API of the managers** (`/v1/egress`, `plan | apply | rollback` on the same UDS): the naive-manager owns a marked block inside `forward_proxy` (a hand-written `upstream` is adopted as `custom` and restored verbatim on rollback), the mieru-manager owns mita's `egress` section (applied by a restart — `mita reload` does not pick it up). Revision CAS, a provider reachability probe before apply, a readback after reload, a journal of the last ten entries. The mieru-manager moves to the host network for the probe (still `read_only`, `cap_drop: ALL`, no listener). ADR 007 accepted for the native backends.
- **Installer `[egress]`** (`warp`, `warp_port`, `naive`, `mieru`): WARP moves out of `[three_xui]` (the old keys are still read, with one warning); `NAIVE_EGRESS_WARP` / `MIERU_EGRESS_WARP` reach the managers through `.env` and Compose; the initial egress is seeded once, upgrade and repair leave it alone.
- **Routing over Fleet v2.** A node declares `egress.v1` and its egress targets in `identity`; a generation carries an optional `egress` section (left out of the wire form and the digest when absent — a v0.3 and a v0.4 panel agree on every document without one); the node applies it after the resources, idempotently by digest, and reports `converged | failed | unsupported`; the central moves the policy to `applied` / `failed` from the report. A local apply is refused while a central manages the node (`managed_by_central`).
- **`/api/routing/*`** (targets, the policy, preview of a draft, apply, rollback, history) and the audit events `routing.policy.update | apply | rollback | delete`; **the «Маршрутизация» screen** with a live preview, «Применить» only for a saved supported policy, history, and the line «Маршрутизация: …» on the node card.
- **Lab.** The tier `routing` (`remote-gate.sh routing`, `fleet-acceptance.py --routing`, `socks5-stub.py`): whole-service WARP through a logging SOCKS5 stub, block by domain and CIDR with the cover site alive, Mieru's selective rule, rollback byte for byte, a provider down → fail-closed, nginx and nftables untouched.
- **v0.3 fix wave** (the deferred post-merge findings): a node's 429/5xx no longer turns the link `offline`, escrow only from a report about the current generation, `capture` with `purpose: escrow | import`, `DELETE /api/nodes/{id}?force=1`, «применено, ожидает учётные данные», tmpfs `/tmp` for the managers and the agent, unified audit event names.

### Upgrading

The ordinary upgrade, but the panel **and both managers** are rebuilt (`up -d --build --wait panel naive-manager mieru-manager`; the installer's `upgrade` does it); migration 14 is additive; nodes first, then the central; a host assembled by hand sets `NAIVE_EGRESS_WARP` / `MIERU_EGRESS_WARP` in `.env` itself. Rollback — the complete previous generation, database included (a v0.3 image refuses schema 14); rolling the panel back does not undo an applied policy — «Сбросить» + «Применить» first. Details: `docs/UPGRADING.md`.

### Installing

Download `proxy-control-v0.4.0-beta.1.tar.gz`, `SHA256SUMS`, `release-manifest.json`, `sbom.spdx.json`. `install-bootstrap` refuses pre-release versions — for a beta, extract the archive after `sha256sum --check SHA256SUMS` and run `sudo python3 -m installer.cli wizard` from the `proxy-control` directory (see `INSTALL.en.md`). WARP: the `[egress]` section of `install.toml` or the wizard's questions.

### Gate checklist (lab host `ams-test`, tree `841a086`, 2026-09-14)

| Gate | Result |
| --- | --- |
| `scripts/dev/remote-gate.sh full` | `REMOTE_GATE_FULL_OK` — 2026-09-14 17:03 UTC, 1978 passed / 2 skipped (427 s), `tests/test_deploy.py` OK, ruff, doc links, JS, shellcheck, systemd-analyze, `git diff --check` |
| `scripts/dev/remote-gate.sh compose` | `REMOTE_GATE_COMPOSE_OK` — 2026-09-14 (after Task 5 and again on `841a086`): five compose models, mieru-manager on `network_mode: host`, agent/ingress images, ingress uid 10001 |
| `LAB_RESET=1 LAB_KEEP_INSTALL=1 scripts/dev/remote-gate.sh lab-host` | `REMOTE_GATE_LAB_HOST_OK` — 2026-09-14 17:21 UTC, `LAB_HOST_EXIT=0`, 17 scenarios passed (preflight 11.5 s, install 104 s, repair 38 s, reboot-recovery 62 s, `fleet` 169 s, secrets-scan), `uninstall`/`coexistence` skipped (the node is kept). The day's first run (tree `4373edb`) failed the `fleet` scenario on s07/s09 — after the T9 fix wave the central took nginx's bare 502 in front of a stopped panel for «the node refuses» → fixed in `1b2eeed`; the preflight tripped `dpkg configure nginx` over the previous run's certificate → `d93b598` |
| `scripts/dev/remote-gate.sh fleet` | `FLEET_ACCEPTANCE_OK` + `REMOTE_GATE_FLEET_OK` — 2026-09-14 17:27 UTC, 84 checks, all passed, 173 s (link online 4.1 s, grants enabled 4.0 s, offline convergence 1.0 s, 20 grants across a restart 11.2 s, purge 11.2 s) |
| `scripts/dev/remote-gate.sh routing` | `ROUTING_ACCEPTANCE_OK` + `REMOTE_GATE_ROUTING_OK` — 2026-09-14 17:36 UTC, 154 checks (70 routing), all passed, 262 s (routing step 89 s; 3–6 s per apply through a generation): targets with `egress.v1`, whole-service WARP through a logging SOCKS5 stub (naive/mieru), block by domain and CIDR with the cover site alive, Mieru's selective rule (the direct exception bypasses the stub, the rest goes through it), an honest 422 on NaiveProxy for a selective rule, rollback byte for byte (Caddyfile sha256, mita's egress section), the provider stopped → `provider_unreachable` with the Caddyfile untouched, `nginx -T` and `nft list ruleset` unchanged, reset and deletion of the policies. The day's first run exposed the nft counters in the digest and the installer's mita seed with a DIRECT rule (`fcf452b`) and the `.env.naive` override (`e7c180c`) |
| `lab-sha256` of the gated archive | `4d1e96d4dbf638fd846c8b43a86c78223571b25ebfcceb41df8a6034c27e6d24` — `/root/lab-host.sha` = `dist/SHA256SUMS` on the stand, archive `proxy-control-v0.4.0-beta.1.tar.gz` from tree `841a086`. The commits after it touch only `scripts/dev/remote-gate.sh` (the `routing` tier's env files), the documentation and the plan; the tag annotation must carry the digest of the archive of the **tagged** tree |

### Live check (AMS_Z)

Run on 2026-09-14 17:20–17:41 UTC under the owner's permission of 2026-09-14. **The node** — the production host `AMS_Z` (`https://api.dubr1k-kawai.uk`, real users 5/4/3, WARP proxy mode `warp-svc` on `127.0.0.1:45000`, a hand-written `upstream http://127.0.0.1:8118` (privoxy) in the NaiveProxy Caddyfile, mita with an `egress` section naming `warp` and the rule `* → PROXY`), updated by rsync from tree `841a086`; everything through the node panel's public API as the owner; no secret or link reached the report (`/root/v04-live/routing-live-report.json`).

**In-place upgrade v0.3 → v0.4.** Rollback points in `/root/v04-live/` (`20260914T172034Z`: an online backup of `panel.sqlite3`, a code tar without secrets, image tags `*:rollback-20260914T172034Z`, a copy of the Caddyfile, `mita describe config`, `.env`, `docker ps`) → rsync `panel/ naive_manager/ mieru_manager/ compose*.yaml VERSION` → `.env` gained `NAIVE_EGRESS_WARP=socks5://127.0.0.1:45000` and `MIERU_EGRESS_WARP=socks5://127.0.0.1:45000` (a hand-updated host, the path of `docs/UPGRADING`) → `docker compose build panel naive-manager mieru-manager` → `up -d --wait --no-deps panel naive-manager mieru-manager` — 25 s to `healthy`; migration 14 (`db-status`: 14 applied), `/app/VERSION` = `0.4.0-beta.1`, the mieru-manager recreated on `network_mode: host`, `mtproxy` and `mask` not recreated, `caddy-naive`/`mita` active, both managers see the provider, `/api/routing/targets` without a session → 401.

**Routing.** `targets`: naive `naive_native`, `mode=custom` (the hand-written upstream), mieru `mieru_native`, `mode=proxy`, both `providers.warp.reachable=true`, mtproxy `protocol_out_of_scope`; neither the provider's URL nor `8118` appears. A throw-away NaiveProxy user `live-routing-*` (created and deleted through the node's API): the exit address before any change `104.28.219.140` (privoxy already leads into WARP), the host's direct address `194.87.220.7`. The naive policy «whole service through WARP»: preview `supported` with the warning `adopts_unmanaged_upstream` → apply (a Caddy reload, no restart) → the Caddyfile carries the managed block with `upstream socks5://127.0.0.1:45000` and no privoxy line → through the proxy: `api.ipify.org` = `104.28.219.140` ≠ direct, `cdn-cgi/trace` → `warp=on` → rollback → the line `upstream http://127.0.0.1:8118` is back in its place, the proxy works, the policy deleted (`applied_digest` = the journal's floor). The mieru policy «through WARP + block `example.com`, `*.example.com`»: preview `supported`, `restart_required=true` → apply (mita restarted in ≈ 3 s, Mieru sessions reconnected) → mita's config carries the `REJECT` and `PROXY` rules, mita `active` → rollback → the `egress` section equals the snapshot, mita `active` → the policy deleted. Outcome: the Caddyfile's sha256 equals the pre-upgrade snapshot (`0e3a8ea0d18cdeb1…`), `mita describe config` equals the snapshot, user lists 5/4/3 unchanged, `routing_policies` empty, the audit carries `routing.policy.update|apply|rollback|delete` without a secret, the panel log has no error.

**Observations.** (1) The script's first run compared the Caddyfile after the rollback with a snapshot taken before the throw-away user existed (the manager's user block differed by one line) — the final equality after deleting the user refuted it; on the second run mieru's `adopts_unmanaged_egress` warning was absent because the mieru-manager's journal already remembered the first apply while the config was restored byte for byte (naive derives the warning from the file, mieru from its journal; a cosmetic difference). Every substantive check passed on both runs. (2) AMS_Z stays on v0.4 with the provider variables in `.env` — the honest state of a host with WARP; the NaiveProxy manager tombstones names for good: `live-routing-b46c5d`, `live-routing-ce3602` are burnt. Rollback points: `/root/v04-live/`, root only.
